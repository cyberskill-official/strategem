# COV-008 implementation notes

## Landed

- API: `POST /api/v1/scenario/compare` via STRAT-002 `compare_scenarios` + same cast path as COV-007
- Web: `apps/web/app/scenarios/page.tsx`
- Tests: `test_scenario_cross_cov008_012.py` + strat unit + page smoke

## Status

`ready_to_review` — **HITL required**. Agent will not set `done`.
