window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLAT-011"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR freezes the <strong>production topology</strong> for Tam Thuc Strategem, aligned with CyberOS multi-surface deploy practice and the operator preference:</p>\n<table><thead><tr><th>Surface</th><th>Platform</th><th>Artefact</th></tr></thead><tbody><tr><td>User web</td><td><strong>Vercel</strong></td><td>Next.js <code>apps/web</code> (standalone/static export as required by Vercel)</td></tr><tr><td>Database</td><td><strong>Supabase</strong> (hosted Postgres)</td><td>Forward-only SQL under <code>db/migrations/</code> + RLS (FR-PLAT-003)</td></tr><tr><td>Backend API (+ engine CLI/images)</td><td><strong>Custom VPS</strong></td><td>Docker Compose, GHCR images, Caddy TLS edge</td></tr></tbody></table>\n<p>The monorepo SHALL remain the single source of truth. A push to <code>main</code> SHALL be able to advance all three surfaces without three divergent repos. Web SHALL never hold DB credentials that can bypass RLS for other tenants; the API on the VPS is the only writer of user-scoped data. The web SHALL call the public API origin over HTTPS (<code>NEXT_PUBLIC_API_BASE</code> / server <code>API_URL</code>). Secrets SHALL live only in Vercel env, Supabase dashboard, and VPS env files — never in git.</p>\n<p>This FR owns the topology document and the cross-surface invariants. It does NOT implement migrate scripts (FR-PLAT-012), VPS compose (FR-PLAT-013), Vercel project config (FR-PLAT-014), or the CD workflow split (FR-PLAT-015).</p>\n<h2>§2 - Why this design</h2>\n<p>CyberOS already separates <strong>web surface</strong> from <strong>VPS services</strong> and uses <strong>Supabase as the Postgres of record</strong> with migrations applied on deploy. Strategem mirrors that split with one explicit change preferred by the operator: the <strong>user-facing Next app ships on Vercel</strong> (fast CDN, preview deploys, zero Node host on the VPS), while <strong>compute and secrets for cast/RAG stay on a VPS</strong> the team controls. Supabase keeps FR-PLAT-003 RLS and VN PDPD/GDPR erasure paths on managed Postgres without operating the DB host.</p>\n<h2>§3 - Contract</h2>\n<h3>Invariants</h3>\n<ol><li>Web → API only over HTTPS public origin; no direct Supabase service-role key in the browser.</li><li>API → Supabase via <code>DATABASE_URL</code> (pooler URL preferred for serverless-adjacent patterns; session mode when RLS session vars require it).</li><li>Engines run as VPS-side processes/containers; web never shells to <code>cast-cli</code>.</li><li>Migrations are forward-only SQL checked into <code>db/migrations/</code>.</li><li>Rollback: Vercel redeploy previous deployment; VPS re-pull prior image tag; DB via new reverse migration (not auto-down).</li></ol>\n<h3>Env map (names only)</h3>\n<table><thead><tr><th>Surface</th><th>Vars (examples)</th></tr></thead><tbody><tr><td>Vercel</td><td><code>NEXT_PUBLIC_API_BASE</code>, <code>API_URL</code> (SSR rewrite target if used)</td></tr><tr><td>VPS</td><td><code>DATABASE_URL</code>, <code>JWT_SECRET</code>, <code>TAMTHUC_AUTH_*</code>, <code>CAST_CLI</code>, image tags</td></tr><tr><td>Supabase</td><td>project URL, anon key (optional for future client auth), service role (API only, VPS)</td></tr></tbody></table>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>docs/deploy/topology.md</code> states the three-surface table and the five invariants.</li><li>No production path requires the VPS to serve the Next.js user UI.</li><li>No production path requires the browser to hold a Supabase service-role key.</li><li>Related FRs 012–015 depend on this document and cite it.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>Doc review against cyberos <code>docs/deploy/*</code> and <code>deploy/vps/*</code> patterns.</li><li>Grep: browser code has no <code>SERVICE_ROLE</code> / service-role env usage.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Author <code>docs/deploy/topology.md</code> + short contract pointer.</li><li>Cross-link from <code>deploy/README.md</code>.</li></ol>";
