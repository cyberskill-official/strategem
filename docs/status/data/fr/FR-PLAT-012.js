window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLAT-012"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR wires the existing FR-PLAT-003 migrations (<code>db/migrations/*.sql</code>) to a <strong>Supabase</strong> project as the production database. It SHALL provide:</p>\n<ol><li>Documented connection modes: <strong>direct</strong> (<code>db.&lt;ref&gt;.supabase.co:5432</code>) for migrations/admin; <strong>pooler</strong> (transaction/session) for the API runtime when appropriate.</li><li>An idempotent <code>deploy/vps/migrate.sh</code> that applies ordered SQL files once (tracked in a <code>_strategem_schema_migrations</code> table or equivalent).</li><li>Documentation of RLS session GUC names used by policies (<code>app.current_user_id</code>, <code>app.current_role</code> per FR-PLAT-003) and how the API sets them per request.</li><li>Optional <code>supabase/config.toml</code> for local <code>supabase start</code> parity — production remains hosted Supabase.</li></ol>\n<p>The FR does NOT redesign tables (FR-PLAT-003 owns schema). It does NOT deploy the API (FR-PLAT-013).</p>\n<h2>§2 - Why this design</h2>\n<p>CyberOS applies per-service migrations on the VPS against Supabase before container roll. Strategem has one product schema under <code>db/migrations/</code>; the same “migrate then start API” order prevents the API from talking to an unmigrated DB. Pooler vs session notes prevent the classic RLS + PgBouncer footgun.</p>\n<h2>§3 - Contract</h2>\n<pre><code class=\"language-bash\"># on VPS or CI with secrets\nexport DATABASE_URL='postgresql://...'   # prefer direct for migrate\nbash deploy/vps/migrate.sh</code></pre>\n<p>Migrate script SHALL:</p>\n<ul><li>require <code>DATABASE_URL</code></li><li>create migrations ledger table if missing</li><li>apply each <code>db/migrations/NNNN_*.sql</code> not yet recorded, in name order</li><li>exit non-zero on failure; never skip failed file</li></ul>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>migrate.sh</code> is executable and dry-run documented.</li><li><code>docs/deploy/supabase.md</code> lists env vars and RLS session setup.</li><li>Existing <code>db/migrations/0001</code>–<code>0009</code> are the apply set (no duplicate schema).</li><li>A local test can run migrations against ephemeral Postgres (CI already has Postgres service).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>Script shellcheck-clean enough for CI; unit: ledger inserts once (optional pytest with testcontainers/psql).</li><li>Docs link topology FR-PLAT-011.</li></ul>";
