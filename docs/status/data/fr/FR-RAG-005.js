window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-RAG-005"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR widens a query across the layered senses of the classical terms it contains, so retrieval does not miss văn bản cổ that uses a term in an extended, loaned, or allusive sense. It sits upstream of FR-RAG-002: it takes the query text (and system), expands the classical terms it recognizes, and hands FR-RAG-002 an enriched query to retrieve on. It is a recall aid over retrieval; it produces no interpretation and asserts nothing.</p>\n<p>A classical term SHALL be expandable across four sense layers (Claude-06 s4.2): bản nghĩa (本義, the base/original sense), nghĩa dẫn thân (引申, the extended sense), nghĩa giả tá (假借, the phonetic-loan sense - a borrowed sound with an unrelated meaning), and nghĩa điển tích (典故, the allusion sense - a reference into a classical story or text). Expansion SHALL be driven by a curated glossary that maps each term to its senses per layer, each sense carrying its own surface forms (Han + romanized + Vietnamese) and a weight. The expander SHALL detect glossary terms in the query, filtered to <code>system in {system, all}</code>, and SHALL add the weighted surface forms of their enabled senses, returning an <code>ExpandedQuery</code> for FR-RAG-002.</p>\n<p>Expansion SHALL be bounded and weighted so it widens recall without drowning the query's intent: the base sense SHALL weigh highest, the extended sense lower, and the phonetic-loan and allusion senses lowest, and the total number of added terms SHALL be capped. Because the giả tá (phonetic-loan) layer carries the highest noise risk - a borrowed sound with an unrelated meaning - it SHALL be off by default and enabled only per-term where the glossary marks it reliable. Expansion SHALL be deterministic given the query and the glossary, SHALL preserve the original query as the highest-weighted signal, and SHALL make no claim and touch no la so field - it operates on query text only.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Văn ngôn văn is compressed and polysemous in a way modern text is not: one term routinely carries a base meaning, an extended meaning grown from it, sometimes a meaning borrowed only for its sound, and sometimes a meaning that only makes sense as a reference to a classical passage (Claude-06 s4.2). A query embedded as-is retrieves passages that match the sense the user happened to phrase, and silently misses passages where the classics use the same term in one of its other senses. That is the exact recall gap this FR closes: by expanding the query through the glossary's sense layers, a question phrased in one sense can still surface a điều that speaks in another. It is the query-side complement to storing three parallel text layers (FR-KB-003) - the store keeps each layer's language, and this FR reaches across the senses within them.</p>\n<p>The weighting and the bound are what keep expansion from turning recall into noise. If every sense of every term were added at equal weight, the query would blur and precision would collapse, so the base sense dominates, the extended sense supports, and the loan and allusion senses only nudge. The giả tá layer is singled out because a phonetic loan is, by definition, an unrelated meaning riding on a shared sound; adding it blindly pulls in text about something else entirely, so it stays off unless a curator has vouched for a specific term. Keeping the expander strictly a query-side, claim-free step is deliberate: it changes what gets retrieved, never what gets asserted, so all of the anti-hallucination discipline still lives where it belongs, downstream in FR-RAG-003, and this FR can widen recall without ever being a place where meaning is invented. Its real payoff is measured, not assumed - FR-RAG-006 is where \"did expansion help recall without hurting faithfulness\" gets a number.</p>\n<h2>§3 - Contract (glossary, expander)</h2>\n<h3>Glossary (<code>tamthuc_rag/glossary.py</code>, <code>data/glossary/&lt;system&gt;_terms.json</code>)</h3>\n<pre><code class=\"language-python\">class SenseLayer(str, Enum):\n    ban_nghia = \"ban_nghia\"      # 本義 base / original\n    dan_than = \"dan_than\"        # 引申 extended\n    gia_ta = \"gia_ta\"            # 假借 phonetic loan (off by default)\n    dien_tich = \"dien_tich\"      # 典故 allusion\n\nclass TermSense(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    layer: SenseLayer\n    gloss: str                   # what this sense means\n    surface_forms: list[str]     # words/phrases that carry it (Han + romanized + Viet)\n    weight: float                # base &gt; extended &gt; loan/allusion\n    reliable: bool = True        # gia_ta senses default reliable=False until curated\n    citations: list[str] = []    # optional unit(s) documenting this sense (FR-KB-003)\n\nclass TermEntry(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    term: str                    # canonical term, e.g. \"值符\"\n    term_han: str | None\n    system: System               # qimen | liuren | taiyi | all\n    aliases: list[str] = []      # Han / romanized / slug forms that resolve to this term\n    senses: list[TermSense]\n\nclass TermGlossary:\n    def match(self, query: str, system: System) -&gt; list[TermEntry]: ...   # system in {system, all}\n    @classmethod\n    def load(cls, paths: list[Path]) -&gt; \"TermGlossary\": ...</code></pre>\n<h3>Expander (<code>tamthuc_rag/expand.py</code>)</h3>\n<pre><code class=\"language-python\">DEFAULT_LAYERS = {SenseLayer.ban_nghia, SenseLayer.dan_than, SenseLayer.dien_tich}   # gia_ta off\n\nclass ExpandConfig(BaseModel):\n    layers: set[SenseLayer] = DEFAULT_LAYERS\n    max_added_terms: int = 12        # hard cap\n    min_weight: float = 0.1\n\nclass ExpandedQuery(BaseModel):\n    original: str\n    matched_terms: list[str]                 # glossary terms that fired\n    added_terms: list[str]                   # surface forms added, capped and de-duped\n    weighted_terms: list[tuple[str, float]]  # original terms highest, expansions weighted down\n    layers_used: list[SenseLayer]\n    def enriched_text(self) -&gt; str: ...       # original + weighted added terms, for FR-RAG-002 to embed\n    def query_variants(self) -&gt; list[str]: ... # optional per-sense variants for multi-query retrieval\n\nclass TermExpander:\n    def __init__(self, glossary: TermGlossary, config: ExpandConfig = ExpandConfig()): ...\n    def expand(self, query: str, system: System) -&gt; ExpandedQuery:\n        # 1. glossary.match(query, system) -&gt; terms present (system-filtered)\n        # 2. for each term, take senses in config.layers; skip gia_ta senses with reliable=False\n        # 3. add weighted surface forms; base &gt; extended &gt; allusion; drop below min_weight\n        # 4. cap to max_added_terms by weight; keep the original query highest-weighted\n        # 5. return ExpandedQuery (deterministic; stable order by weight then form)</code></pre>\n<h3>The FR-RAG-002 seam</h3>\n<p>FR-RAG-002's <code>RetrievalRequest</code> carries <code>query: str</code>. The default integration is single-query: the caller sets <code>request.query = expanded.enriched_text()</code> (original plus weighted added terms) and FR-RAG-002 embeds it as one vector - FR-RAG-002 is unchanged. A recall-sensitive mode uses <code>expanded.query_variants()</code>: the caller retrieves for each variant and unions the hits into FR-RAG-002's fusion. The default is single-query (cheapest, keeps the interface intact); multi-query is a flag evaluated by FR-RAG-006.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A query containing a glossary term (e.g. \"值符\" / \"truc phu\") expands into <code>added_terms</code> drawn from that term's enabled senses, with the base sense weighted above the extended and allusion senses.</li><li>The per-system filter holds: a <code>qimen</code> query matches only <code>qimen</code> and <code>all</code> terms, never a <code>liuren</code>/<code>taiyi</code>-only term.</li><li>The giả tá layer is off by default: a <code>gia_ta</code> sense with <code>reliable = false</code> contributes nothing unless the layer is enabled and the sense is marked reliable.</li><li>Expansion is bounded: <code>added_terms</code> never exceeds <code>max_added_terms</code>, low-weight forms below <code>min_weight</code> are dropped, and the original query stays the highest-weighted signal.</li><li><code>enriched_text()</code> returns the original query plus the weighted expansions in a stable order, and feeding it to a stub FR-RAG-002 retrieves a passage that the bare query missed on the fixture (recall lift shown).</li><li>Expansion is deterministic and claim-free: identical query + glossary yield an identical <code>ExpandedQuery</code>; no la so field is read or written and no interpretation is produced.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_expand.py</code>: term detection across Han/romanized/alias forms; the per-system filter; the gia_ta-off-by-default and reliable-gate cases; the <code>max_added_terms</code> cap and <code>min_weight</code> drop; base &gt; extended &gt; allusion weighting; determinism (repeat runs equal); the recall-lift case with a stub FR-RAG-002 retriever over the FR-RAG-001 fixture index (expanded query retrieves a unit the bare query does not).</li><li>Glossary conformance: every <code>data/glossary/&lt;system&gt;_terms.json</code> row validates against <code>docs/contracts/term-glossary.schema.json</code>; any <code>gia_ta</code> sense defaults <code>reliable=false</code>; a lint asserts weights are ordered base &gt;= extended &gt;= loan/allusion within a term.</li><li>Claim-free guard: a test asserts <code>expand</code> never constructs an <code>Interpretation</code> and never accesses a la so envelope - it takes only <code>query</code> and <code>system</code>.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_rag</code>, <code>pytest packages/tamthuc_rag</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>glossary.py</code>: <code>SenseLayer</code>, <code>TermSense</code>, <code>TermEntry</code>, <code>TermGlossary</code> (load + match); author <code>docs/contracts/term-glossary.schema.json</code>.</li><li><code>expand.py</code>: <code>ExpandConfig</code>, <code>ExpandedQuery</code>, <code>TermExpander.expand</code> (detect, sense-select, weight, cap, deterministic order).</li><li>Author <code>data/glossary/qimen_terms.json</code> first (the P0/P1 flagship terms - truc phu, the luc nghi tam ky, the headline cách cục terms), then <code>liuren</code> and <code>taiyi</code> to representative coverage; <code>data/glossary/SENSES.md</code> documents the four layers and the weighting convention.</li><li>Wire the FR-RAG-002 seam: <code>enriched_text()</code> for single-query (default) and <code>query_variants()</code> for the multi-query flag.</li><li>Commit <code>fixtures/term_sense_sample.json</code> (a few terms across systems, with a deliberate gia_ta case) as the test exemplar.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-RAG-002 (this FR widens the query FR-RAG-002 retrieves on; the default single-query integration keeps FR-RAG-002's interface intact). Reads no la so - it operates on the query string and the system only. Related to FR-RAG-001 (the same three-layer chunks its expanded query retrieves over) and FR-KB-003 (the corpus a sense's optional <code>citations</code> resolve into, and a natural source for authoring the glossary's senses). Blocks nothing; it is an optional recall enhancement in front of retrieval. Its value is scored by FR-RAG-006, which measures whether expansion lifts recall without hurting faithfulness or precision. The RAG-branch invariant holds trivially here: this FR never produces an interpretation and never touches <code>ban</code>/<code>cach_cuc</code>/<code>lich_phap</code>/<code>co_truong_phai</code>; the citation and AIDisclosure discipline lives downstream in FR-RAG-003 and is untouched by widening retrieval.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// a glossary entry (abbreviated) for a QiMen term, with gia_ta off by default\n{ \"term\": \"值符\", \"term_han\": \"值符\", \"system\": \"qimen\", \"aliases\": [\"truc phu\", \"truc_phu\"],\n  \"senses\": [\n    { \"layer\": \"ban_nghia\", \"gloss\": \"the on-duty talisman star, the chief of the nine stars\",\n      \"surface_forms\": [\"值符\", \"truc phu\", \"chief star\"], \"weight\": 1.0, \"citations\": [\"yba_dieu_003\"] },\n    { \"layer\": \"dan_than\", \"gloss\": \"by extension, the acting/leading party in the chart\",\n      \"surface_forms\": [\"acting palace\", \"leading party\"], \"weight\": 0.6 },\n    { \"layer\": \"gia_ta\", \"gloss\": \"(loan) not used for this term\", \"surface_forms\": [],\n      \"weight\": 0.2, \"reliable\": false } ] }</code></pre>\n<pre><code class=\"language-json\">// ExpandedQuery for \"co nen tin nguoi dang truc phu khong\"\n{ \"original\": \"co nen tin nguoi dang truc phu khong\",\n  \"matched_terms\": [\"值符\"], \"layers_used\": [\"ban_nghia\", \"dan_than\"],\n  \"added_terms\": [\"值符\", \"chief star\", \"acting palace\", \"leading party\"],\n  \"weighted_terms\": [[\"co nen tin nguoi dang truc phu khong\", 1.0], [\"值符\", 1.0],\n    [\"chief star\", 1.0], [\"acting palace\", 0.6], [\"leading party\", 0.6]] }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Single enriched query vs multi-query retrieval. Default: single enriched query (keeps FR-RAG-002 unchanged, cheapest); multi-query behind a flag for recall-critical cases. Decide from FR-RAG-006 recall/precision numbers.</li><li>Whether the glossary is hand-authored or bootstrapped from a classical dictionary. Default: hand-author the flagship QiMen terms with a curator, cite senses into FR-KB-003 where a source documents them; bootstrap wider coverage later under FR-KB-004 review.</li><li>How aggressively to enable điển tích (allusion) expansion. Default: on but low-weighted; an allusion pulls in the referenced passage, which helps when the reference is apt and adds noise when it is not - tune the weight from eval, keep it below the base and extended senses.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Over-expansion drowns intent</td><td>every sense added at equal weight</td><td>base &gt; extended &gt; loan/allusion weighting; <code>max_added_terms</code> cap; original stays highest</td></tr><tr><td>Phonetic-loan noise</td><td>a gia_ta sense pulls unrelated text</td><td>gia_ta off by default; enabled only per-term with <code>reliable=true</code></td></tr><tr><td>Cross-system leak</td><td>a foreign-system term expands the query</td><td><code>system in {system, all}</code> filter on glossary match; isolation test</td></tr><tr><td>Non-deterministic expansion</td><td>unstable ordering of added terms</td><td>stable order by (weight desc, form); determinism test</td></tr><tr><td>Expansion becomes assertion</td><td>expander drifts into producing meaning</td><td>claim-free guard: <code>expand</code> takes only query+system, returns terms, never an interpretation</td></tr><tr><td>Recall lift unverified</td><td>expansion shipped on faith</td><td>FR-RAG-006 scores recall vs precision/faithfulness; expansion tuned from the number</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR closes a recall gap specific to văn ngôn văn: a query phrased in one sense of a term must still reach classical text that uses another sense. Its discipline is entirely in the weighting and the bound - widen recall, never blur intent - and its safety is that it is claim-free, so it changes what is retrieved and never what is asserted. Author the QiMen glossary first to match the P0/P1 flagship, keep giả tá off unless a curator vouches for a term, and let FR-RAG-006 decide the layer weights from measured recall and faithfulness rather than intuition. The package <code>tamthuc_rag</code> is shared with FR-RAG-001/002/003/004/006/007; this FR adds <code>expand.py</code> and <code>glossary.py</code>, so the RAG branch stays one installable, mypy-clean unit.</p>";
