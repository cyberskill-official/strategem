window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-RULE-001"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR defines the pattern-as-data model that the whole rule engine stands on: a cach cuc (格局) or khoa the (課體) is a row of data, never a branch of code. The module SHALL express every detectable pattern as a <code>Pattern</code> record whose match logic lives in a JSON condition tree (the <code>conditions</code> field, whose grammar FR-RULE-002 owns) and whose meaning lives in cited text. No pattern SHALL be hardcoded in Rust; adding, editing, or retiring a pattern SHALL be a data change, expert-reviewed and versioned, that ships without recompiling the engine (strategy 4.4, Grok-31).</p>\n<p>This FR owns three artifacts and one boundary. It owns (a) the canonical shape of the <code>knowledge_patterns</code> table - the column set, types, and enums; (b) the JSON Schema for a single pattern row at <code>docs/contracts/knowledge-pattern.schema.json</code>; and (c) a Rust <code>validate_pattern</code> function plus the seed-file format. It does not own the migration: FR-PLAT-003 creates the physical table and RLS from this shape, and FR-KB-002 fills the seed with the 150-200 real patterns. The boundary: this FR shape-checks <code>conditions</code> only structurally (is it a JSON object with a recognized top-level form); the deep DSL grammar and the evaluator belong to FR-RULE-002, which depends on this FR.</p>\n<p>Every pattern SHALL carry a monotonic <code>version</code> integer per <code>id</code>. A detected pattern SHALL stamp <code>(id, version)</code> so a downstream interpretation cites the exact pattern text that fired and is reproducible against it. A semantic edit (to <code>conditions</code>, <code>polarity</code>, or either meaning) SHALL increment <code>version</code>; retiring a pattern SHALL set <code>status = deprecated</code> and SHALL NOT delete the row. An <code>active</code> pattern SHALL carry at least one citation, because a pattern with no textual source cannot ground a claim under the anti-hallucination rule (strategy 4.4, FR-RAG-003).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Both source sets converge on the same instruction: keep the knowledge out of the code. The Grok source spells the rule engine out as pattern-as-data JSON (Grok-31); the Claude source keeps cach cuc detection deterministic and table-driven rather than buried in branches (Claude-06 s2.3). The reason is operational, not aesthetic. Tam Thuc has hundreds of named patterns across three systems, they are contested between schools, and they will be corrected as experts review them. If each one is an <code>if</code> in Rust, every correction is a code change, a deploy, and a regression risk, and no domain expert can read or audit the ruleset. As data, the ruleset is one reviewable table, an expert can propose an edit as JSON, and the change is versioned and traceable.</p>\n<p>The <code>version</code> stamp is what makes interpretation reproducible over time. An interpretation persisted last month cited a pattern whose meaning an expert has since sharpened; because the detected cach cuc recorded <code>(id, version)</code>, the old report still maps to the exact text it was written against, and the eval loop (FR-RAG-006) can tell a genuine regression from an intended revision. The citation-required rule on <code>active</code> patterns is the same anti-hallucination principle seen at the data layer: the chain source -&gt; pattern -&gt; detected cach cuc -&gt; cited interpretation has no gap where meaning appears without a source.</p>\n<h2>§3 - Contract (schema / types)</h2>\n<h3>knowledge_patterns columns (this FR fixes the shape; FR-PLAT-003 migrates it)</h3>\n<table><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>id</td><td>text PK</td><td>stable slug, e.g. <code>qimen_thanh_long_hoi_dau</code>; never renumbered</td></tr><tr><td>system</td><td>text</td><td>enum: <code>qimen</code> \\</td><td><code>liuren</code> \\</td><td><code>taiyi</code> \\</td><td><code>all</code></td></tr><tr><td>name</td><td>text</td><td>display name, e.g. Thanh Long Hoi Dau</td></tr><tr><td>name_han</td><td>text</td><td>青龍返首 (nullable)</td></tr><tr><td>conditions</td><td>jsonb</td><td>condition DSL tree (FR-RULE-002 grammar); shape-checked here only</td></tr><tr><td>polarity</td><td>text</td><td>enum: <code>cat</code> \\</td><td><code>hung</code> \\</td><td><code>trung</code></td></tr><tr><td>meaning_classical</td><td>text</td><td>classical / bạch thoại gloss</td></tr><tr><td>meaning_modern</td><td>text</td><td>decision-support framing (no medical/legal/financial claim)</td></tr><tr><td>citations</td><td>jsonb</td><td>array of citation ids into the classical corpus (FR-KB-003)</td></tr><tr><td>version</td><td>int</td><td>monotonic per <code>id</code>, &gt;= 1; bumped on any semantic edit</td></tr><tr><td>confidence</td><td>real</td><td>0..1 expert prior; the base match score emitted by FR-RULE-002</td></tr><tr><td>status</td><td>text</td><td>enum: <code>active</code> \\</td><td><code>draft</code> \\</td><td><code>deprecated</code> (FR-RULE-003 loads <code>active</code>)</td></tr><tr><td>reviewed_by</td><td>text</td><td>expert sign-off handle (FR-KB-004); nullable while <code>draft</code></td></tr><tr><td>created_at</td><td>timestamptz</td><td>audit</td></tr><tr><td>updated_at</td><td>timestamptz</td><td>audit</td></tr></tbody></table>\n<p>Uniqueness is on <code>id</code> (the current row). Version history is retained per the FR-KB-004 curation workflow; at MVP a single-current-row table plus an append-only <code>pattern_audit</code> row (FR-PLAT-003) is sufficient, and <code>(id, version)</code> is the reproducibility key.</p>\n<h3>Pattern row JSON (one seed entry / one table row)</h3>\n<pre><code class=\"language-json\">{\n  \"id\": \"qimen_thanh_long_hoi_dau\",\n  \"system\": \"qimen\",\n  \"name\": \"Thanh Long Hoi Dau\",\n  \"name_han\": \"青龍返首\",\n  \"conditions\": {\n    \"type\": \"and\",\n    \"rules\": [\n      { \"field\": \"truc_phu.cung\", \"operator\": \"eq\", \"value\": \"ban.thien_ban.cung_of.丙\" },\n      { \"field\": \"door\", \"operator\": \"in\", \"value\": [\"Sinh\", \"Khai\", \"Tu\"] }\n    ]\n  },\n  \"polarity\": \"cat\",\n  \"meaning_classical\": \"丙加值符, cat khi tu tap, loi cho khoi su va cau kien.\",\n  \"meaning_modern\": \"A strong-timing configuration for initiating or requesting; frame as favorable window, not a guarantee.\",\n  \"citations\": [\"yba_thien_can_khac_ung_12\", \"kmdg_cach_cuc_thanh_long\"],\n  \"version\": 1,\n  \"confidence\": 0.9,\n  \"status\": \"active\"\n}</code></pre>\n<h3>Rust types (<code>crates/cyberos-rule/src/pattern.rs</code>)</h3>\n<pre><code class=\"language-rust\">#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]\n#[serde(deny_unknown_fields)]\npub struct Pattern {\n    pub id: String,\n    pub system: System,\n    pub name: String,\n    #[serde(default)] pub name_han: Option&lt;String&gt;,\n    pub conditions: serde_json::Value,   // parsed + deep-validated by FR-RULE-002\n    pub polarity: Polarity,\n    pub meaning_classical: String,\n    pub meaning_modern: String,\n    #[serde(default)] pub citations: Vec&lt;String&gt;,\n    pub version: u32,                    // &gt;= 1\n    pub confidence: f32,                 // 0.0..=1.0\n    #[serde(default = \"Status::active\")] pub status: Status,\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum System { Qimen, Liuren, Taiyi, All }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum Polarity { Cat, Hung, Trung }\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum Status { Active, Draft, Deprecated }\n\nimpl Pattern { pub fn stamp(&amp;self) -&gt; (String, u32) { (self.id.clone(), self.version) } }</code></pre>\n<p><code>conditions</code> is <code>serde_json::Value</code> at this layer on purpose: the shared crate must not depend on the DSL evaluator (FR-RULE-002 depends on this FR, not the reverse). This FR validates the pattern envelope and does a shallow structural check of <code>conditions</code>; FR-RULE-002 parses and deep-validates it.</p>\n<h3>Validator (<code>crates/cyberos-rule/src/validate.rs</code>)</h3>\n<pre><code class=\"language-rust\">pub fn validate_pattern(v: &amp;serde_json::Value) -&gt; Result&lt;Pattern, Vec&lt;ValidationError&gt;&gt;;</code></pre>\n<p>Checks, accumulating all failures (not fail-on-first):</p>\n<ol><li>Deserializes into <code>Pattern</code> with <code>deny_unknown_fields</code>; unknown/missing fields are errors.</li><li><code>system</code>, <code>polarity</code>, <code>status</code> parse to their enums.</li><li><code>confidence</code> in <code>[0.0, 1.0]</code>; <code>version &gt;= 1</code>.</li><li><code>conditions</code> is a non-empty JSON object whose top-level form is recognized: either a connective (<code>type</code> in {<code>and</code>, <code>or</code>, <code>not</code>}) or a leaf (has a <code>field</code> key). Depth and operator validity are deferred to FR-RULE-002.</li><li>If <code>status == active</code>: <code>citations</code> is non-empty and <code>reviewed_by</code> is present at the DB layer (the seed loader warns; the DB enforces).</li></ol>\n<h3>Seed-file format (<code>crates/cyberos-rule/seed/&lt;system&gt;.json</code>)</h3>\n<p>One JSON file per system (<code>qimen.json</code>, <code>liuren.json</code>, <code>taiyi.json</code>, <code>all.json</code>), each a JSON array of pattern rows in the shape above minus <code>created_at</code>/<code>updated_at</code>. <code>load_seed(dir) -&gt; Result&lt;Vec&lt;Pattern&gt;, SeedError&gt;</code> validates every entry with <code>validate_pattern</code> and rejects the whole file on any error (so a malformed pattern never reaches the DB). This seed is the source FR-KB-002 fills with 150-200 patterns, the fixture FR-RULE-002 tests against, and the offline repository FR-RULE-003 reads when no DB is present.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>docs/contracts/knowledge-pattern.schema.json</code> validates the sample row and rejects a row with an unknown field, a bad <code>system</code>, a <code>confidence</code> of 1.5, or <code>version</code> 0.</li><li><code>validate_pattern</code> returns <code>Ok(Pattern)</code> for a valid row and an error vector listing every violation for an invalid one (multiple violations reported at once).</li><li>An <code>active</code> pattern with an empty <code>citations</code> array fails validation with a citation-required error.</li><li><code>conditions</code> that is a JSON array, a scalar, an empty object, or an object lacking both <code>type</code> and <code>field</code> fails the shallow structural check.</li><li><code>load_seed</code> loads <code>seed/qimen.json</code>, validates every entry, and returns them; injecting one malformed entry fails the whole load with the offending <code>id</code> named.</li><li><code>Pattern::stamp()</code> returns <code>(id, version)</code>; the Rust struct and the JSON Schema expose the identical field set (a test asserts parity).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/pattern_schema.rs</code>: round-trips the sample fixture (<code>fixtures/patterns_seed_sample.json</code>) Rust -&gt; JSON -&gt; Rust byte-stable; asserts each acceptance case above with a table of good/bad rows.</li><li>Schema parity: a test loads <code>knowledge-pattern.schema.json</code> and asserts every <code>required</code> property maps to a <code>Pattern</code> field and vice versa, so the two cannot drift.</li><li>Seed validation: <code>load_seed</code> over the committed <code>seed/qimen.json</code> sample must pass; a second fixture with a deliberately broken entry must fail with the entry <code>id</code> in the error.</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-rule -- -D warnings</code>, <code>cargo test -p cyberos-rule</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Create the <code>cyberos-rule</code> crate (this FR owns its birth; FR-RULE-002/003 add modules to it, mirroring how <code>cyberos-lichphap</code> is born in FR-CORE-001).</li><li>Author <code>docs/contracts/knowledge-pattern.schema.json</code> from §3 (source of truth for the row shape).</li><li><code>pattern.rs</code>: the <code>Pattern</code> struct, <code>System</code> / <code>Polarity</code> / <code>Status</code> enums, <code>stamp()</code>.</li><li><code>validate.rs</code>: <code>validate_pattern</code> with accumulating <code>ValidationError</code>; the shallow <code>conditions</code> structural check.</li><li><code>version.rs</code>: helpers for the <code>(id, version)</code> stamp and a <code>bump()</code> guard used by FR-KB-004.</li><li><code>seed.rs</code>: <code>load_seed(dir)</code>; commit a small <code>seed/qimen.json</code> (a handful of patterns) as the format exemplar and test fixture. FR-KB-002 grows it.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-PLAT-003, which creates the physical <code>knowledge_patterns</code> table, indexes, and RLS from the shape this FR defines. Blocks FR-RULE-002 (the DSL parses the <code>conditions</code> field this FR carries) and FR-KB-002 (pattern seeding fills the seed-file format and the table this FR shapes). FR-QMDG-005 consumes detected patterns downstream through FR-RULE-003.</p>\n<h2>§8 - Example payloads</h2>\n<p>A LiuRen khoa the row and a cross-system row:</p>\n<pre><code class=\"language-json\">[\n  { \"id\": \"liuren_nguyen_thai\", \"system\": \"liuren\", \"name\": \"Nguyen Thai\",\n    \"name_han\": \"元胎\", \"conditions\": { \"type\": \"and\", \"rules\": [\n      { \"field\": \"khoa_the\", \"operator\": \"eq\", \"value\": \"nguyen_thai\" } ] },\n    \"polarity\": \"trung\", \"meaning_classical\": \"Khoa the co ban, chi su sinh khoi.\",\n    \"meaning_modern\": \"Baseline lesson-type; read the tam truyen for direction.\",\n    \"citations\": [\"dllr_khoa_the_01\"], \"version\": 1, \"confidence\": 0.7, \"status\": \"active\" },\n\n  { \"id\": \"chung_khong_vong\", \"system\": \"all\", \"name\": \"Khong Vong\",\n    \"name_han\": \"空亡\", \"conditions\": { \"type\": \"and\", \"rules\": [\n      { \"field\": \"dung_than.tuan_khong\", \"operator\": \"eq\", \"value\": true } ] },\n    \"polarity\": \"hung\", \"meaning_classical\": \"Dung than lac tuan khong, su kho thanh.\",\n    \"meaning_modern\": \"Key significator falls void; treat timing as unripe, not doomed.\",\n    \"citations\": [\"chung_tuan_khong_01\"], \"version\": 2, \"confidence\": 0.6, \"status\": \"active\" }\n]</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Full version history table vs single-current-row + audit. Default for MVP: one current row per <code>id</code> plus a <code>pattern_audit</code> append row in FR-PLAT-003; a dedicated <code>pattern_versions</code> table is a FR-KB-004 concern if experts need diffs across many revisions.</li><li>Should <code>citations</code> be typed objects rather than string ids at this layer? Default: string ids that resolve against the FR-KB-003 corpus, so this crate stays free of corpus types; the citation card is assembled in FR-RAG-003. Revisit if the rule engine needs locator ranges.</li><li><code>confidence</code> provenance: expert-set constant now; whether FR-RAG-006 evals ever feed a learned prior back into it is deferred, and would itself be a versioned edit.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Hardcoded pattern</td><td>a cach cuc encoded as Rust <code>if</code> instead of a row</td><td>code review rejects; no detection path exists outside the data ruleset</td></tr><tr><td>Silent pattern edit</td><td><code>conditions</code>/meaning changed without version bump</td><td>FR-KB-004 guard + reproducibility test (old stamp no longer matches text) fails</td></tr><tr><td>Uncited active pattern</td><td><code>status = active</code>, <code>citations = []</code></td><td><code>validate_pattern</code> fails; DB check constraint (FR-PLAT-003) rejects insert</td></tr><tr><td>Schema drift</td><td>Rust <code>Pattern</code> and JSON Schema diverge</td><td>parity test fails in CI before ship</td></tr><tr><td>Malformed seed</td><td>one bad entry in a seed file</td><td><code>load_seed</code> fails the whole file with the offending <code>id</code>; nothing partial loads</td></tr><tr><td>Bad enum from DB</td><td>a legacy <code>system</code> string not in the enum</td><td>typed deserialize error, surfaced by FR-RULE-003 loader, not silently skipped</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the smallest P0 RULE FR but it sets a hard rule for the entire product: patterns are data. Everything else in the module (FR-RULE-002 evaluator, FR-RULE-003 loader) and the KB seeding (FR-KB-002) and curation (FR-KB-004) assume this shape. The crate name <code>cyberos-rule</code> is shared with FR-RULE-002/003; they extend this crate rather than create new ones, so the rule engine is one deterministic, cargo-testable unit shared by all three casting systems. Keep the schema under <code>docs/contracts/</code> beside <code>laso-envelope.schema.json</code> so the contract is reviewable independently of the build.</p>";
