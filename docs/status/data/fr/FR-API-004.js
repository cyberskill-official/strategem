window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-API-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR implements step 9 of the query flow: persisting the query, the cast chart(s), the detected patterns, and the generated report, and writing an audit row for every sensitive action. It extends the <code>tamthuc_api</code> package. It owns the write path and the repository layer over the FR-PLAT-003 tables; it does NOT own the table migrations or RLS (FR-PLAT-003) nor the human-review queue (FR-RAG-004), though its audit rows feed both.</p>\n<p>Persistence SHALL write into the tables FR-PLAT-003 provisions (Grok-46 schema): <code>queries(id, user_id, query_type, input_data, systems_used, created_at)</code>, <code>charts(id, query_id, system, chart_data, patterns_detected, created_at)</code>, and <code>reports(id, query_id, report_data, pdf_path, generated_at)</code>. The <code>charts.chart_data</code> column SHALL store the engine's la so JSON envelope (FR-PLAT-002) exactly as cast, and <code>charts.patterns_detected</code> SHALL store the FR-RULE-003 output; this FR SHALL NOT re-derive or normalize any chart field, preserving the read-only invariant end to end (strategy 4.3, 4.4). A single query with multiple systems SHALL produce one <code>queries</code> row and one <code>charts</code> row per system, all sharing the <code>query_id</code>.</p>\n<p>Auditing SHALL write an <code>audit_logs(id, user_id, action, details, created_at)</code> row for every sensitive action: any read or write of <code>birth_data</code>, a chart cast on behalf of a user, a report generation or download, a tier change, an authentication event of note (login, refresh, social link), a DSAR export or erasure (FR-AUTH-004), and any abuse action (FR-API-003). Audit rows SHALL be append-only and SHALL NOT contain plaintext <code>birth_data</code> or full question text - they record that an action occurred, by whom, and enough context to investigate, not the sensitive payload itself. Writes SHALL be transactional with the request outcome: a failed persistence SHALL NOT silently drop a served response, and a served response SHALL have its audit row (the audit write is part of completing the request, not a fire-and-forget afterthought).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Step 9 is where the platform becomes accountable. Persisting the query, chart, patterns, and report gives the user their history, gives the product its reproducibility (a stored la so envelope plus its flags re-casts to the same chart), and gives the eval loop and human review their raw material. Storing the chart as the exact engine envelope, never a re-serialized copy, is the same read-only discipline the gateway enforces in flight, now enforced at rest: the database holds what the engine produced, so a stored chart is auditable against the deterministic oracle (RISK-8).</p>\n<p>Audit is a legal and safety obligation, not a log nicety. The product handles birth data and question text, which are sensitive personal data under VN PDPD and GDPR (strategy 4.4, RISK-5), and the platform's whole positioning depends on being able to show who did what with that data (strategy 7). The two rules that make audit trustworthy are that it is append-only (you cannot quietly rewrite the record) and that it records the fact of an action without copying the sensitive payload into a second, less-protected place - an audit table full of plaintext birth data would be a new breach surface, not a control. Making the audit write part of request completion, rather than a best-effort side effect, is what keeps the trail complete when it matters.</p>\n<h2>§3 - Contract (schema / repositories / audit)</h2>\n<h3>Tables written (FR-PLAT-003 owns the DDL; this FR writes them)</h3>\n<table><thead><tr><th>Table</th><th>Columns (Grok-46)</th><th>This FR writes</th></tr></thead><tbody><tr><td>queries</td><td>id, user_id, query_type, input_data jsonb, systems_used text[], created_at</td><td>one row per query</td></tr><tr><td>charts</td><td>id, query_id, system, chart_data jsonb, patterns_detected jsonb, created_at</td><td>one row per system</td></tr><tr><td>reports</td><td>id, query_id, report_data jsonb, pdf_path, generated_at</td><td>one row per generated report</td></tr><tr><td>audit_logs</td><td>id, user_id, action, details jsonb, created_at</td><td>one row per sensitive action</td></tr></tbody></table>\n<p><code>charts.chart_data</code> is the FR-PLAT-002 envelope verbatim; <code>input_data</code> holds the <code>QueryRequest</code> minus any secret; <code>systems_used</code> mirrors the requested systems.</p>\n<h3>Repositories (<code>tamthuc_api/repositories.py</code>, <code>persistence.py</code>)</h3>\n<pre><code class=\"language-python\">class QueryRepo(Protocol):\n    async def create(self, user_id: str, req: QueryRequest) -&gt; str: ...          # -&gt; query_id\nclass ChartRepo(Protocol):\n    async def create(self, query_id: str, system: str,\n                     chart: LaSo, patterns: list[CachCuc]) -&gt; str: ...           # stores envelope verbatim\nclass ReportRepo(Protocol):\n    async def create(self, query_id: str, report_data: dict, pdf_path: str | None) -&gt; str: ...\n    async def get(self, report_id: str, user_id: str) -&gt; Report | None: ...      # RLS-scoped read\n\nasync def persist_query_result(user_id: str, req: QueryRequest,\n                               charts: list[LaSo], patterns: list[CachCuc],\n                               report: dict | None) -&gt; PersistResult:\n    # one queries row + one charts row per system (+ report row) in a single transaction</code></pre>\n<h3>Audit (<code>tamthuc_api/audit.py</code>)</h3>\n<pre><code class=\"language-python\">class AuditAction(str, Enum):\n    birth_data_read = \"birth_data_read\"; birth_data_write = \"birth_data_write\"\n    chart_cast = \"chart_cast\"; report_generate = \"report_generate\"; report_download = \"report_download\"\n    tier_change = \"tier_change\"; auth_login = \"auth_login\"; auth_refresh = \"auth_refresh\"\n    dsar_export = \"dsar_export\"; dsar_erase = \"dsar_erase\"; abuse_action = \"abuse_action\"\n\nasync def audit(user_id: str | None, action: AuditAction, details: dict) -&gt; None:\n    # append-only insert into audit_logs; details carries context, NEVER plaintext birth_data or full question text</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A single-system query persists one <code>queries</code> row and one <code>charts</code> row; a multi-system <code>/calculate/all</code> query persists one <code>queries</code> row and one <code>charts</code> row per system, all sharing the <code>query_id</code>.</li><li><code>charts.chart_data</code> equals the engine's la so envelope byte-for-byte (no re-derivation); a round-trip test re-reads it and re-casts to an identical chart from <code>dau_vao</code> plus flags.</li><li>Every sensitive action writes exactly one <code>audit_logs</code> row with the correct <code>AuditAction</code>, the acting <code>user_id</code> (or null for anonymous/abuse), and a <code>details</code> object that contains no plaintext <code>birth_data</code> and no full question text.</li><li>Persistence and the served response are transactional: an injected DB failure on the write does not return a 200 with lost data; the request fails with the FR-API-001 error envelope and no partial rows remain.</li><li><code>ReportRepo.get</code> honors row-level security (FR-PLAT-003): a user cannot read another user's report; a cross-user read returns 404, not another user's data.</li><li>Audit rows are append-only: an attempt to update or delete an audit row is rejected (enforced by FR-PLAT-003 grants); a test asserts the write-only contract at the repository layer.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_persistence.py</code>: single- and multi-system row counts and shared <code>query_id</code>; the byte-identical <code>chart_data</code> and the re-cast round-trip; the transactional failure case (no partial persistence, no silent 200); RLS-scoped report read.</li><li><code>tests/test_audit.py</code>: one row per sensitive action with the right <code>AuditAction</code>; the no-plaintext-sensitive-data assertion on <code>details</code>; the append-only contract; audit emitted for abuse actions (FR-API-003) and auth events (FR-AUTH-001).</li><li>Integration: the orchestrator's step 9 calls <code>persist_query_result</code> and <code>audit</code>; a served query is followed by its persisted rows and its <code>chart_cast</code> audit row.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_api</code>, <code>pytest packages/tamthuc_api</code> (the DB path runs behind a marker when a test Postgres is present; a fake repo covers the default suite).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>repositories.py</code>: <code>QueryRepo</code> / <code>ChartRepo</code> / <code>ReportRepo</code> protocols with a Postgres implementation over the FR-PLAT-003 tables and an in-memory fake for tests.</li><li><code>persistence.py</code>: <code>persist_query_result</code> writing the query, per-system charts, and report in one transaction; the re-cast round-trip helper.</li><li><code>audit.py</code>: the <code>AuditAction</code> enum and the append-only <code>audit</code> writer with the sensitive-data redaction rule.</li><li>Wire step 9 of the FR-API-001 orchestrator to <code>persist_query_result</code> + <code>audit</code>; wire abuse and auth audit calls from FR-API-003 / FR-AUTH-001.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-API-001 (the orchestrator whose step 9 this completes, and the error envelope a failed write returns) and FR-PLAT-003 (the physical <code>queries</code> / <code>charts</code> / <code>reports</code> / <code>audit_logs</code> tables, their indexes, and the RLS this write path relies on). Stores the FR-PLAT-002 la so envelope verbatim and the FR-RULE-003 patterns. Its audit rows feed FR-RAG-004 (human-review context), FR-AUTH-004 (DSAR export reads the user's persisted history; erasure removes it), and FR-LEGAL-002 (retention and disclosure operate on exactly these tables). Auth and abuse events audited here originate in FR-AUTH-001 and FR-API-003.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// queries row (input_data mirrors the QueryRequest, minus secrets)\n{ \"id\": \"q_...\", \"user_id\": \"u_...\", \"query_type\": \"trach_thoi\",\n  \"input_data\": { \"datetime\": \"2004-01-01T10:30:00\", \"tz\": \"+07:00\", \"kinh_do\": 105.85, \"systems\": [\"qimen\"] },\n  \"systems_used\": [\"qimen\"], \"created_at\": \"2026-07-08T12:00:00Z\" }</code></pre>\n<pre><code class=\"language-json\">// audit_logs row - records the fact, not the sensitive payload\n{ \"id\": \"a_...\", \"user_id\": \"u_...\", \"action\": \"birth_data_read\",\n  \"details\": { \"purpose\": \"chart_cast\", \"query_id\": \"q_...\", \"fields\": [\"date\",\"time\",\"place\"] },\n  \"created_at\": \"2026-07-08T12:00:00Z\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether <code>charts.patterns_detected</code> duplicates the envelope's <code>cach_cuc</code> or is stored separately. Default: store the FR-RULE-003 match output in <code>patterns_detected</code> (it carries match scores and the <code>(id, version)</code> stamps), while <code>chart_data.cach_cuc</code> holds the engine's own list; a test asserts they reconcile. Revisit if the duplication proves noisy.</li><li>Retention windows per table. Default: this FR persists; FR-LEGAL-002 sets the retention and the erasure contract over these tables, and FR-AUTH-004 executes DSAR against them. Keep the windows in the LEGAL config, not hardcoded here.</li><li>Audit sink: same Postgres vs a separate append-only store. Default: the <code>audit_logs</code> table in the shared Postgres with delete/update revoked (FR-PLAT-003 grants); a dedicated immutable sink is a later hardening step (FR-PLAT-007) that keeps the same <code>audit</code> interface.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Chart re-derived at rest</td><td><code>chart_data</code> re-serialized/normalized instead of stored verbatim</td><td>forbidden; store the envelope byte-for-byte; the re-cast round-trip test fails on any change</td></tr><tr><td>Sensitive data in audit</td><td>plaintext birth_data or full question text in <code>audit_logs.details</code></td><td>redaction rule strips it; a test asserts <code>details</code> never carries the sensitive payload</td></tr><tr><td>Silent data loss</td><td>write fails but a 200 is returned</td><td>persistence is transactional with the response; a failed write yields the error envelope, no partial rows</td></tr><tr><td>Mutable audit trail</td><td>an audit row updated or deleted</td><td>append-only; DB grants revoke update/delete (FR-PLAT-003); repository exposes no mutator</td></tr><tr><td>Cross-user read</td><td>one user reads another's chart/report</td><td>RLS on the tables; report read is user-scoped; a cross-user read returns 404</td></tr><tr><td>Missing audit on served action</td><td>a sensitive action completes without an audit row</td><td>audit is part of request completion, not fire-and-forget; integration test pairs each action with its row</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR closes the query flow and makes the platform accountable: it stores what the engine produced (never a re-derivation) and records who touched sensitive data (never by copying it). The two disciplines to hold are verbatim chart storage and append-only, payload-free audit. It extends the same <code>tamthuc_api</code> app as FR-API-001/002/003, writing into the FR-PLAT-003 schema, so the gateway remains one installable, mypy-clean unit. Retention, export, and erasure over these tables belong to FR-LEGAL-002 and FR-AUTH-004; this FR gives them a clean, audited store to operate on.</p>";
