window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-API-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR adds rate limiting and abuse detection to the gateway: it enforces the per-tier request quotas that FR-AUTH-002 configures, and it detects and blunts abusive traffic patterns. It extends the <code>tamthuc_api</code> package. It owns the counting and the enforcement; it does NOT own the quota numbers (those are FR-AUTH-002's single-source config) nor the tier resolution (FR-AUTH-001/002).</p>\n<p>The limiter SHALL enforce the quotas from <code>docs/contracts/rbac-tiers.json</code> via FR-AUTH-002's <code>quota_for(principal)</code>: Free 100 requests/day, Premium 5000 requests/day, Enterprise the account's custom number, Admin unmetered. Counting SHALL be per principal (user id or Enterprise API-key account) over a rolling or calendar-day window, backed by Redis (FR-PLAT-006) so the count is shared across gateway instances. When a principal exceeds its quota the gateway SHALL return HTTP 429 with the FR-API-001 error envelope (<code>code = RATE_LIMITED</code>), a <code>Retry-After</code> header, and <code>details</code> carrying the limit and the reset time. Limiting SHALL be applied as middleware on the metered routes so no metered endpoint can be reached without passing the check, and Admin/unmetered principals SHALL bypass counting.</p>\n<p>Abuse detection SHALL run alongside quota enforcement and cover at least: request-velocity spikes (a burst well above a principal's normal rate), credential-stuffing signatures on the auth routes (many failed logins across accounts from one source), and repeated malformed or probing requests. On a detected abuse signal the gateway SHALL apply a graduated response - short-window throttle, temporary lockout, and an audited flag - rather than a silent drop, and SHALL record the event for FR-API-004 audit and FR-PLAT-005 alerting. Rate-limit and abuse state SHALL fail safe: if Redis is unavailable the limiter SHALL degrade to a conservative local limit rather than failing open to unlimited traffic.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The calculate endpoints call an LLM, so an unmetered request is a direct cost and a direct abuse surface (Grok-12). Rate limiting is therefore not just a plan-enforcement nicety; it is the first line of cost control and the first line of denial-of-service defence (Grok-21). Reading the quota from FR-AUTH-002's single config rather than hardcoding numbers here is what keeps the enforced limit equal to the advertised one - the same discipline stated from the enforcement side.</p>\n<p>Backing the counters with Redis matters because the gateway scales horizontally; a per-instance in-memory counter would let a principal multiply its quota by the number of instances. Failing safe rather than open is the non-obvious but critical choice: a limiter that disables itself when its datastore blinks turns a cache outage into an unlimited-spend incident, so the degraded mode is a conservative local cap, never \"allow everything\". Abuse detection is separate from quota because the shapes differ - a paying Premium user can hit their quota legitimately, while a burst of failed logins across many accounts from one IP is abuse regardless of any single account's quota. Graduated, audited responses (throttle, then lock, then flag) give operators signal and a paper trail instead of a silent black hole (RISK-5 touches this: the auth surface is where credential attacks against sensitive accounts land).</p>\n<h2>§3 - Contract (types / middleware / policy)</h2>\n<h3>Limiter (<code>tamthuc_api/ratelimit.py</code>)</h3>\n<pre><code class=\"language-python\">class RateDecision(BaseModel):\n    allowed: bool\n    limit: int | Literal[\"unmetered\"]\n    remaining: int\n    reset_at: int           # epoch seconds\n    retry_after: int | None # seconds, when not allowed\n\nclass RateLimiter(Protocol):\n    async def check_and_count(self, principal: Principal) -&gt; RateDecision: ...\n\nclass RedisRateLimiter:     # default (FR-PLAT-006 Redis); shared counter across instances\n    # key: rl:{principal}:{yyyymmdd}; INCR + EXPIRE; quota from AUTH-002 quota_for(principal)\n    ...\nclass LocalFallbackLimiter: # conservative per-instance cap when Redis is unreachable (fail safe)\n    ...</code></pre>\n<h3>Abuse detection (<code>tamthuc_api/abuse.py</code>)</h3>\n<pre><code class=\"language-python\">class AbuseSignal(str, Enum):\n    velocity_spike = \"velocity_spike\"\n    credential_stuffing = \"credential_stuffing\"\n    probing = \"probing\"                    # repeated malformed / 4xx-heavy traffic\n\nclass AbuseVerdict(BaseModel):\n    signal: AbuseSignal | None\n    action: Literal[\"allow\", \"throttle\", \"lockout\", \"flag\"]\n    window_s: int | None\n\nasync def evaluate(principal: Principal | None, source_ip: str, event: RequestEvent) -&gt; AbuseVerdict: ...</code></pre>\n<h3>Middleware (<code>tamthuc_api/middleware/ratelimit.py</code>)</h3>\n<pre><code class=\"language-python\"># ASGI middleware on the metered routes:\n#  1. resolve principal (FR-AUTH-001/002); Admin/unmetered -&gt; pass through, no count\n#  2. limiter.check_and_count(principal); if not allowed -&gt; 429 envelope + Retry-After\n#  3. abuse.evaluate(...); on throttle/lockout -&gt; 429/423 envelope; on flag -&gt; allow + audit event\n#  4. set X-RateLimit-Limit / X-RateLimit-Remaining / X-RateLimit-Reset response headers</code></pre>\n<h3>429 envelope (via FR-API-001 error contract)</h3>\n<pre><code class=\"language-json\">{ \"error\": { \"code\": \"RATE_LIMITED\",\n             \"message\": \"Daily request quota exceeded for the Free tier.\",\n             \"details\": { \"limit\": 100, \"remaining\": 0, \"reset_at\": 1700000000 },\n             \"request_id\": \"req_...\" } }</code></pre>\n<p>with header <code>Retry-After: &lt;seconds&gt;</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A Free principal is allowed for 100 requests in the window and receives 429 <code>RATE_LIMITED</code> with <code>Retry-After</code> on the 101st; a Premium principal's cutover is at 5000; the numbers come from <code>quota_for</code>, not from constants in this module.</li><li>Counting is shared across two limiter instances against one Redis (a second instance sees the first's count); an Admin/unmetered principal is never counted and never limited.</li><li>Response headers <code>X-RateLimit-Limit</code>, <code>X-RateLimit-Remaining</code>, and <code>X-RateLimit-Reset</code> are set on metered responses and reflect the true remaining count.</li><li>When Redis is unreachable the limiter degrades to the conservative <code>LocalFallbackLimiter</code> cap and never fails open to unlimited traffic; a test simulates the outage.</li><li>A burst well above a principal's baseline raises <code>velocity_spike</code> and a graduated <code>throttle</code>; many failed logins across accounts from one source raise <code>credential_stuffing</code> and a <code>lockout</code>; each produces an audited event for FR-API-004 and an alert signal for FR-PLAT-005.</li><li>The Enterprise custom quota from the account override is honored, and an Enterprise API-key principal is metered against the account, not per key.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_ratelimit.py</code>: per-tier cutovers using a fake/embedded Redis; cross-instance shared count; unmetered bypass; header correctness; the fail-safe degraded mode on Redis outage; Enterprise override.</li><li><code>tests/test_abuse.py</code>: velocity-spike, credential-stuffing, and probing detection with synthetic event streams; the graduated allow/throttle/lockout/flag actions; the audit-event emission and the alert signal.</li><li>Integration: the middleware rejects an over-quota request before it reaches the orchestrator (a metered route cannot be served without the check).</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_api</code>, <code>pytest packages/tamthuc_api</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>ratelimit.py</code>: <code>RateLimiter</code> protocol, <code>RedisRateLimiter</code> (INCR/EXPIRE keyed by principal + day, quota from <code>quota_for</code>), <code>LocalFallbackLimiter</code>.</li><li><code>abuse.py</code>: the signal enum, <code>evaluate</code> with velocity, credential-stuffing, and probing heuristics; the graduated action policy.</li><li><code>middleware/ratelimit.py</code>: the ASGI middleware wiring principal resolution, limiter, abuse, headers, and the 429/423 envelope.</li><li>Wire the middleware onto the metered routes in <code>app.py</code>; emit audit events to the FR-API-004 seam and alert signals to FR-PLAT-005.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-API-001 (the gateway, the metered routes, and the error envelope this returns) and FR-AUTH-002 (the tier resolution and <code>quota_for</code> single-source config). Uses FR-PLAT-006 Redis for the shared counters; audited events flow to FR-API-004 and alerts to FR-PLAT-005; the fail-safe posture aligns with FR-PLAT-008 resilience. This FR blocks nothing directly but is a gating control on the FR-API-001 endpoints.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// audited abuse event (to FR-API-004)\n{ \"action\": \"credential_stuffing_lockout\", \"source_ip\": \"...\", \"principal\": null,\n  \"details\": { \"failed_logins\": 42, \"window_s\": 300, \"accounts_touched\": 17 } }</code></pre>\n<pre><code class=\"language-json\">// response headers on an allowed metered request\n{ \"X-RateLimit-Limit\": \"5000\", \"X-RateLimit-Remaining\": \"4987\", \"X-RateLimit-Reset\": \"1700000000\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Window model: calendar-day vs rolling 24h. Default: calendar-day counters (simplest to reason about and to communicate as a plan limit), with a short burst window optionally layered on from the same AUTH-002 config. Revisit if users report day-boundary gaming.</li><li>Where abuse heuristics live long-term. Default: in-process heuristics at MVP reading Redis-held velocity keys; a dedicated WAF or an external anomaly service is a later PLAT-007 concern that this module can defer to without changing the middleware seam.</li><li>Whether Enterprise API keys get per-key sub-limits under the account quota. Default: metered at the account level now; per-key sub-limits are an Enterprise-management feature deferred until requested.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Fail open on datastore outage</td><td>Redis down, limiter disabled</td><td>forbidden; degrade to the conservative <code>LocalFallbackLimiter</code>, never unlimited</td></tr><tr><td>Per-instance count multiplies quota</td><td>in-memory counter under horizontal scaling</td><td>shared Redis counter keyed by principal; cross-instance test asserts one count</td></tr><tr><td>Advertised vs enforced drift</td><td>quota hardcoded here</td><td>numbers come only from <code>quota_for</code> / <code>rbac-tiers.json</code>; no constants in this module</td></tr><tr><td>Silent abuse drop</td><td>abusive traffic dropped with no record</td><td>graduated throttle/lockout/flag, each audited (FR-API-004) and alerted (FR-PLAT-005)</td></tr><tr><td>Metered route bypass</td><td>a calculate route served without the check</td><td>middleware applied to all metered routes; integration test asserts pre-orchestrator rejection</td></tr><tr><td>Unmetered principal throttled</td><td>Admin counted and limited</td><td>Admin/unmetered bypasses counting entirely</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR is the cost and abuse control on an LLM-backed API, so its two rules are: read the quota from one source (never hardcode), and fail safe (never fail open). Keep the split with FR-AUTH-002 clean - AUTH decides the number, API-003 does the counting and returns the 429. It extends the same <code>tamthuc_api</code> app as FR-API-001/002/004; the limiter and abuse controls are middleware on the existing routes, so the gateway stays one installable, mypy-clean unit.</p>";
