window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-KB-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR is the source-of-truth store for the classical text of Tam Thuc: per source work it holds three parallel layers - nguyen van chu Han (original), ban bạch thoại (modern-Chinese rendering), and ban dịch (Vietnamese/English translation) - segmented into the natural bibliographic units of the thư tịch, each unit carrying a stable citation id. It is the corpus that FR-RAG-001 ingests and embeds, and the corpus that every citation in a pattern (FR-KB-002) or an interpretation (FR-RAG-003) resolves into. This FR owns the canonical text and its unit segmentation; it does NOT own the embeddings or the vector index (FR-RAG-001) nor the knowledge graph (FR-KB-001).</p>\n<p>Text SHALL be segmented by the natural unit of the work - điều (條), pháp (法), khoá (課), or câu (句) - never by fixed token windows (Claude-06 s4.2). Every unit SHALL carry <code>unit_id</code>, <code>source</code>, <code>system</code> (qimen | liuren | taiyi | all), <code>unit_type</code>, <code>citation_id</code>, <code>ordinal</code>, and up to three layer texts (<code>han</code>, <code>bach_thoai</code>, <code>dich</code>), of which at least one SHALL be present. The three layers of a unit SHALL be aligned by construction (one unit row, three layer fields) so a retrieval can return the Han and its translation as the same passage. Segmentation SHALL keep a unit whole: a source is divided at natural-unit boundaries only, and a unit is never split mid-sentence to fit any downstream constraint (sub-unit splitting for embedding context is FR-RAG-001's concern, not this store's).</p>\n<p>The store SHALL be pluggable behind a <code>ClassicalStore</code> protocol whose MVP default is two relational tables - <code>classical_source</code> and <code>classical_unit</code> - in the shared Postgres (FR-PLAT-003), matching the cyberos house pattern of keeping stateful data in the one managed Postgres. It SHALL expose a read API that yields units for FR-RAG-001 to ingest and a <code>resolve_citation(citation_id)</code> that returns the unit(s) a citation points at, so FR-RAG-003 can render citation cards (Han + bạch thoại + dịch + locator). Every <code>citation_id</code> a source declares SHALL be resolvable; a citation referenced by an active pattern (FR-KB-002) that resolves to no unit is a dangling citation and SHALL be reported, because the chain source -&gt; pattern -&gt; cách cục -&gt; cited interpretation must have no link where a citation has no text behind it (strategy 4.4).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The interpretation branch can only be as honest as the text it cites, and the hard fact of this domain is that the primary sources are văn ngôn văn - classical Chinese, a language distinct from both modern Chinese and Vietnamese (Claude-06 s4.2). A single-layer store forces a bad choice: keep only the Han and no Vietnamese user can read the citation card, or keep only a translation and the original's precision is lost and the scholarship is unverifiable. Keeping three aligned layers per unit is what lets a citation card show the nguyen van, a bạch thoại gloss, and a dịch together, so a user sees exactly the passage a claim rests on and a reviewer can check the translation against the original. This is the cultural-respect rule made concrete: cite the classical text, and keep the Han alongside the transliteration and translation (strategy 7).</p>\n<p>Segmenting by natural unit rather than token window is a correctness choice that this store must get right because everything downstream inherits it. A điều or a khoá is a complete thought in these texts; a citation points at exactly such a unit, and FR-RAG-001 embeds exactly such a unit. If the store split a unit arbitrarily, citations would point at fragments and retrieval would return half a rule. The store is relational at MVP for the same reason FR-KB-001's graph is: any managed Postgres can host it, it needs no second stateful system to run and back up, and the queries this corpus needs (units of a source in order, a unit by id, a citation resolved to its unit) are trivial in SQL. The <code>citation_id</code> is deliberately a first-class field separate from <code>unit_id</code> so the citation-id prefixes that FR-KB-002 agrees in <code>SOURCES.md</code> resolve here directly, keeping the pattern seed and the corpus aligned on one naming scheme rather than two.</p>\n<h2>§3 - Contract (models, store, storage, read API)</h2>\n<h3>Unit and source models (<code>tamthuc_kb/corpus/models.py</code>)</h3>\n<pre><code class=\"language-python\">class Layer(str, Enum): han = \"han\"; bach_thoai = \"bach_thoai\"; dich = \"dich\"\nclass UnitType(str, Enum): dieu = \"dieu\"; phap = \"phap\"; khoa = \"khoa\"; cau = \"cau\"\n# System (qimen | liuren | taiyi | all) is the shared enum defined in FR-KB-001.\n\nclass ClassicalSource(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    id: str                     # e.g. \"yen_ba_dieu_tau_ca\"\n    system: System\n    title: str                  # display title\n    title_han: str | None       # 煙波釣叟歌\n    citation_prefix: str        # the citation-id prefix this source owns (agreed in KB-002 SOURCES.md)\n    layers_available: list[Layer]   # which of han/bach_thoai/dich this source provides\n    license_note: str | None    # provenance / license of the text used\n\nclass ClassicalUnit(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    unit_id: str                # stable id, e.g. \"yba_dieu_012\"\n    source: str                 # ClassicalSource.id\n    system: System\n    unit_type: UnitType\n    citation_id: str            # what a pattern/interpretation cites; often == unit_id\n    ordinal: int                # position within the source (stable ordering / display)\n    han: str | None             # nguyen van chu Han\n    bach_thoai: str | None      # ban bach thoai (modern-Chinese rendering)\n    dich: str | None            # ban dich (Vietnamese/English)</code></pre>\n<p>A unit with all three layer fields <code>None</code> is invalid; the model validator SHALL require at least one non-empty layer. The <code>Layer</code> / <code>UnitType</code> vocabulary is the canonical contract in <code>docs/contracts/classical-corpus.schema.json</code>; FR-RAG-001 mirrors it in <code>tamthuc_rag/models.py</code> and validates against the same schema, so the two packages cannot drift (reconciliation note in section 7).</p>\n<h3>ClassicalStore protocol (<code>tamthuc_kb/corpus/store.py</code>)</h3>\n<pre><code class=\"language-python\">class ClassicalStore(Protocol):\n    def upsert_sources(self, sources: list[ClassicalSource]) -&gt; None: ...   # idempotent by id\n    def upsert_units(self, units: list[ClassicalUnit]) -&gt; None: ...          # idempotent by unit_id\n    def units_of_source(self, source: str) -&gt; list[ClassicalUnit]: ...       # ordered by ordinal\n    def iter_units(self, system: System | None = None) -&gt; Iterator[ClassicalUnit]: ...  # FR-RAG-001 ingest feed\n    def resolve_citation(self, citation_id: str) -&gt; list[ClassicalUnit]: ...  # citation cards (FR-RAG-003)\n    def get_unit(self, unit_id: str) -&gt; ClassicalUnit | None: ...\n    def count(self, system: System | None = None) -&gt; int: ...\n\nclass RelationalClassicalStore:   # default (MVP): classical_source + classical_unit in the shared Postgres\n    ...\nclass InMemoryClassicalStore:     # tests\n    ...</code></pre>\n<p><code>iter_units</code> is the feed FR-RAG-001 consumes: it yields units as validated rows (dicts under the schema), which <code>ingest(source_rows: Iterable[dict], ...)</code> chunks per layer and embeds. The store emits JSON-shaped rows so no Python type crosses the package boundary; the contract is the schema, not an import.</p>\n<h3>Relational storage (<code>packages/tamthuc_kb/migrations/0002_classical_corpus.sql</code>)</h3>\n<pre><code class=\"language-sql\">create table classical_source (\n  id              text primary key,\n  system          text not null,               -- checked against System\n  title           text not null,\n  title_han       text,\n  citation_prefix text not null,\n  layers_available text[] not null,\n  license_note    text\n);\ncreate table classical_unit (\n  unit_id     text primary key,\n  source      text not null references classical_source(id),\n  system      text not null,                    -- checked against System\n  unit_type   text not null,                    -- checked against UnitType\n  citation_id text not null,\n  ordinal     int  not null,\n  han         text,\n  bach_thoai  text,\n  dich        text,\n  check (han is not null or bach_thoai is not null or dich is not null)\n);\ncreate index classical_unit_source   on classical_unit(source, ordinal);\ncreate index classical_unit_system   on classical_unit(system);\ncreate unique index classical_unit_citation on classical_unit(citation_id);</code></pre>\n<p><code>system</code> and <code>unit_type</code> carry SQL check constraints listing the closed enums so the database rejects a bad value even if a caller bypasses the app layer. The <code>check</code> on the three layers enforces at-least-one-layer at the storage tier too. Idempotency is by primary key on both tables. The DDL is owned here (KB is the natural owner of the corpus tables) and applied through the PLAT migration runner in the same shared Postgres (soft edge to FR-PLAT-003; the hard <code>depends_on</code> stays PLAT-001, mirroring FR-KB-001).</p>\n<h3>Segmentation (<code>tamthuc_kb/corpus/segment.py</code>)</h3>\n<pre><code class=\"language-python\">def segment(raw: RawSource) -&gt; list[ClassicalUnit]:\n    # split a committed source work into ClassicalUnits at its marked natural-unit\n    # boundaries (dieu/phap/khoa/cau); assign ordinal; derive citation_id from the\n    # source's citation_prefix + unit index; carry each present layer; keep units whole.</code></pre>\n<p>Raw source works live under <code>data/corpus/&lt;system&gt;/&lt;source&gt;.json</code> with the natural-unit boundaries already marked by the curator (a structured array per source, one entry per unit with its layer texts). <code>segment</code> validates the markers, assigns <code>ordinal</code>, and derives <code>citation_id</code> from <code>citation_prefix</code>. <code>load.py</code> runs <code>segment</code> over the committed sources and upserts sources + units into the store; it is idempotent and fails the whole load, naming the offending <code>unit_id</code>, if any unit is malformed or lacks a layer.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A committed source (<code>data/corpus/qimen/yen_ba_dieu_tau_ca.json</code>) segments into <code>ClassicalUnit</code> rows, each with a <code>unit_type</code>, a monotonic <code>ordinal</code>, a <code>citation_id</code> under the source's <code>citation_prefix</code>, and at least one non-empty layer; a unit spanning two natural units, or split mid-unit, fails segmentation.</li><li><code>units_of_source</code> returns a source's units in <code>ordinal</code> order; <code>get_unit</code> and <code>resolve_citation</code> return the aligned three-layer unit for a known <code>unit_id</code> / <code>citation_id</code>.</li><li>The at-least-one-layer rule is enforced at both the model validator and the SQL check constraint (two independent guards); a unit with all layers empty is rejected.</li><li><code>upsert_sources</code> / <code>upsert_units</code> are idempotent: re-loading the same corpus leaves source and unit counts unchanged; <code>citation_id</code> is unique (a duplicate citation id anywhere fails the load).</li><li><code>iter_units</code> yields rows valid under <code>docs/contracts/classical-corpus.schema.json</code>, and a round-trip through FR-RAG-001's <code>ingest</code> chunks one unit into one chunk per present layer sharing the <code>unit_id</code> (the KB-003 -&gt; RAG-001 seam works on the fixture).</li><li>A dangling-citation check reports any <code>citation_id</code> referenced by an active FR-KB-002 pattern that resolves to no unit here; on the aligned fixture the report is empty.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_corpus.py</code>: segmentation correctness per <code>unit_type</code>; the at-least-one-layer guard at both tiers; idempotent re-load; <code>citation_id</code> uniqueness; <code>units_of_source</code> ordering; <code>resolve_citation</code> returning aligned layers; the RAG-001 ingest seam on <code>fixtures/corpus_sample.json</code> (assert one chunk per present layer, shared <code>unit_id</code>).</li><li>Backend parity: run the fixture through the <code>RelationalClassicalStore</code> (against a test Postgres or a fake) and the <code>InMemoryClassicalStore</code>; assert identical <code>units_of_source</code> and <code>resolve_citation</code> results.</li><li>Schema conformance: every emitted row validates against <code>docs/contracts/classical-corpus.schema.json</code>, which is the same shape FR-RAG-001 validates its ingest input against.</li><li>Cross-FR integrity: the dangling-citation check runs against a small aligned FR-KB-002 pattern fixture and asserts every active pattern's <code>citations</code> resolve here.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_kb</code>, <code>pytest packages/tamthuc_kb</code> (default suite uses the in-memory store; the Postgres path runs behind a marker when a test DB is present).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>corpus/models.py</code>: <code>ClassicalSource</code>, <code>ClassicalUnit</code>, the <code>Layer</code> / <code>UnitType</code> enums, the at-least-one-layer validator; author <code>docs/contracts/classical-corpus.schema.json</code> as the source of truth.</li><li><code>corpus/store.py</code>: the <code>ClassicalStore</code> protocol, <code>RelationalClassicalStore</code> (default), <code>InMemoryClassicalStore</code> (tests).</li><li><code>migrations/0002_classical_corpus.sql</code>: the two tables, check constraints, indexes.</li><li><code>corpus/segment.py</code>: natural-unit segmentation from marked source files, ordinal + citation_id derivation, unit-whole guard.</li><li><code>corpus/load.py</code>: read <code>data/corpus/**</code>, segment, upsert idempotently, whole-file fail with the offending id, dangling-citation check against KB-002.</li><li>Author <code>data/corpus/qimen/yen_ba_dieu_tau_ca.json</code> (marked units, the P1 flagship source) and <code>data/corpus/SOURCES.md</code> aligned to the FR-KB-002 citation-id prefixes; commit <code>fixtures/corpus_sample.json</code>.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-PLAT-001 (the Python workspace and, for the relational store, the shared Postgres from FR-PLAT-003 - a soft edge like FR-KB-001's graph tables). Blocks FR-RAG-001 (which reads and embeds this corpus; its <code>Chunker</code> splits these units into per-layer chunks and its <code>citation_id</code>/<code>unit_id</code> come from here) and FR-KB-005 (whose graph query collects citations that resolve into this store). Aligns with FR-KB-002 on the citation-id prefixes: KB-002's <code>SOURCES.md</code> declares the prefixes, and this FR's <code>data/corpus/SOURCES.md</code> provides the units those prefixes resolve into, so a cited pattern always has text behind it. Read at runtime by FR-RAG-003 (citation cards) and FR-EDU-003 (the bilingual classical library). Reconciliation note: FR-RAG-001 declares <code>Layer</code> / <code>UnitType</code> enums in <code>tamthuc_rag</code>; both packages validate against <code>docs/contracts/classical-corpus.schema.json</code>, which this FR owns, so the vocabularies are one contract enforced in CI, not two that can drift.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// a marked source unit under data/corpus/qimen/yen_ba_dieu_tau_ca.json\n{ \"unit_id\": \"yba_dieu_012\", \"source\": \"yen_ba_dieu_tau_ca\", \"system\": \"qimen\",\n  \"unit_type\": \"dieu\", \"citation_id\": \"yba_dieu_012\", \"ordinal\": 12,\n  \"han\": \"丙加值符，青龍返首...\", \"bach_thoai\": \"丙加在值符上，称青龙返首...\",\n  \"dich\": \"Binh gia truc phu, goi la Thanh Long Hoi Dau...\" }</code></pre>\n<pre><code class=\"language-json\">// resolve_citation(\"yba_dieu_012\") -&gt; the aligned three-layer unit (for a citation card)\n[ { \"unit_id\": \"yba_dieu_012\", \"source\": \"yen_ba_dieu_tau_ca\", \"system\": \"qimen\",\n    \"unit_type\": \"dieu\", \"citation_id\": \"yba_dieu_012\", \"ordinal\": 12,\n    \"han\": \"丙加值符，青龍返首...\", \"bach_thoai\": \"丙加在值符上...\",\n    \"dich\": \"Binh gia truc phu, goi la Thanh Long Hoi Dau...\" } ]</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether a <code>citation_id</code> may span several units (a long điều split into câu). Default: one <code>citation_id</code> resolves to one unit at MVP; if a citation must cover a range, <code>resolve_citation</code> returns the ordered set and the citation card renders the range. Decide when the first multi-câu source is authored.</li><li>How the Han text is normalized (traditional vs simplified, variant characters). Default: store the source's own form verbatim in <code>han</code>, keep a normalized field only if retrieval quality (FR-RAG-006) shows variant-character misses; do not silently transcode the original.</li><li>How much of a source ships before P1 vs stays <code>draft</code>. Default: only cited, curator-checked units load; the citation-id prefixes are fixed early (with FR-KB-002) so patterns can reference units before every layer is translated. FR-KB-004 owns the sign-off.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Token-window segmentation</td><td>splitting mid-điều/câu to fit a size</td><td>forbidden; segment at natural-unit boundaries only; keep units whole</td></tr><tr><td>Layerless unit</td><td>a unit with all three layers empty</td><td>rejected by the model validator and the SQL check constraint (two guards)</td></tr><tr><td>Dangling citation</td><td>a pattern cites an id no unit resolves</td><td>dangling-citation check reports the id; the citation chain is not shippable with a gap</td></tr><tr><td>Duplicate citation id</td><td>same <code>citation_id</code> on two units</td><td>unique index / load check fails, naming the id</td></tr><tr><td>Layer misalignment</td><td>a translation stored against the wrong Han</td><td>aligned by construction (one row, three fields); a test asserts a unit's layers co-resolve</td></tr><tr><td>Silent Han transcode</td><td>original quietly simplified/normalized</td><td>store the source form verbatim; normalization is an explicit, flagged step, never silent</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR is the textual ground the whole anti-hallucination contract stands on: no citation card, no cited interpretation, and no dangling-citation check exists without a store that holds the classical text as cited, aligned, natural units. Keep the boundary clean - this FR owns the canonical three-layer text and its unit segmentation; FR-RAG-001 owns the embeddings and the vector index over it; FR-KB-001 owns the graph. The one contract that binds this FR to FR-RAG-001 is <code>docs/contracts/classical-corpus.schema.json</code>; author it first and let both packages validate against it. The package <code>tamthuc_kb</code> is shared with FR-KB-001/002/004/005; this FR adds the <code>corpus/</code> module to it, so the knowledge base stays one installable, mypy-clean unit.</p>";
