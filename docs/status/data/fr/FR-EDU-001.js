window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EDU-001"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR defines the training platform's spine: the four-level Tam Thuc curriculum and its measurable progression criteria (Tam-Thuc-07 s3.1-3.2). It is the P3 learning flow's structural foundation; FR-EDU-002 wires the auto-grader onto it and FR-EDU-003 supplies the bilingual library it links to.</p>\n<p>The module SHALL define four levels in the fixed knowledge-dependency order: L1 can chi / ngu hanh foundation, L2 LiuRen (the base system), L3 QiMen then TaiYi, L4 integration across the three. Each level SHALL declare its prerequisites, so the levels form a linear chain L1 -&gt; L2 -&gt; L3 -&gt; L4 with no cycles. Each transition SHALL carry a measurable level-up criterion drawn verbatim from the s3.2 table; criteria SHALL be tied to practical ability (build a correct chart) rather than theory recall.</p>\n<p>A learner's current level SHALL be a pure function of which criteria they have met: a level cannot be entered while a prerequisite's criterion is unmet. Criteria that Tam-Thuc-07 s3.3 grades with the deterministic engine SHALL be marked so (measure <code>auto_graded</code>, <code>gradedBy: \"engine\"</code>), so FR-EDU-002 knows exactly which criteria it grades by diffing the learner's chart against the engine's. The curriculum content SHALL render inside the FR-WEB-001 app shell using the existing Design System v1.3.0 components, introducing no new tokens, and SHALL be bilingual-ready (an optional <code>han</code> field on domain modules; stable ids on criteria so FR-WEB-006 can translate by id).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Tam Thuc is hard to learn: it needs a solid ganzhi foundation, many memorized tables, and repeated chart-casting practice (Tam-Thuc-07 s3). A flat pile of lessons would let a learner attempt QiMen dinh cuc before they can build four pillars, and they would fail without knowing why. Encoding the dependency order as prerequisites, and gating each level on a measurable criterion, makes the path honest: you advance when you can actually cast the charts of the level below, not when you have clicked through the pages.</p>\n<p>The criteria are the pedagogical version of the project's core discipline. The engine has to be exactly right for casting; that same determinism makes it an ideal marker (Tam-Thuc-07 s3.3) - one input yields one chart, so a learner's chart can be diffed against the engine's and the exact missed step pointed out. Marking which criteria are engine-graded (chart-casting ones) versus interpretive (the L4 \"choose the right system and explain\" criterion, which is subjective and stays quiz/manual) keeps the auto-grader inside the boundary it can be trusted in: it grades casting, not meaning (strategy 7).</p>\n<p>Keeping the criteria as machine-checkable records with stable ids, rather than prose in a lesson, is what lets FR-EDU-002 grade them, FR-WEB-006 translate them, and this FR test them against the source table. The curriculum is data, not copy.</p>\n<h2>§3 - Contract (types and data)</h2>\n<h3>Types (<code>apps/web/src/features/edu/types.ts</code>)</h3>\n<pre><code class=\"language-ts\">export type Level = 1 | 2 | 3 | 4;\n\nexport interface Module {\n  id: string;\n  title: string;\n  han?: string;            // original Han for domain modules (bilingual-ready)\n  summary: string;\n}\n\nexport type Measure = \"auto_graded\" | \"quiz\" | \"manual\";\n\nexport interface Criterion {\n  id: string;              // stable; WEB-006 translates by id\n  transition: \"1-&gt;2\" | \"2-&gt;3\" | \"3-&gt;4\" | \"4\";\n  description: string;     // verbatim from Tam-Thuc-07 s3.2\n  measure: Measure;\n  gradedBy?: \"engine\";     // set when EDU-002 grades this by chart diff\n  systems?: (\"luc_nham\" | \"ky_mon\" | \"thai_at\")[];  // which engine(s) grade it\n}\n\nexport interface CurriculumLevel {\n  level: Level;\n  title: string;\n  prerequisites: Level[];\n  modules: Module[];\n  levelUpCriteria: Criterion[];\n}</code></pre>\n<h3>The four levels (Tam-Thuc-07 s3.1)</h3>\n<ul><li>L1 - nen can chi + ngu hanh: thien can dia chi, ngu hanh sinh khac, chi hop/xung/hinh/hai, tu tru, tiet khi, chan thai duong thoi. The shared foundation of all three engines (the FR-CORE content).</li><li>L2 - one system in depth, LiuRen: thien dia ban, tu khoa, tam truyen, thien tuong, khoa the. Started first because it is the base system with the most shared concepts.</li><li>L3 - second and third systems: QiMen (dinh cuc, bat mon / cuu tinh / bat than, truc phu / truc su) then TaiYi (tich nien, muoi sau than, tam tuong, cac toan va cach).</li><li>L4 - integration: choose the system by question type, cross-check results across the three, apply the chu-khach / dung than frame across systems.</li></ul>\n<h3>Progression criteria (Tam-Thuc-07 s3.2, reproduced verbatim)</h3>\n<table><thead><tr><th>Cap</th><th>Tieu chi len cap</th></tr></thead><tbody><tr><td>1 -&gt; 2</td><td>Dựng đúng bốn trụ và xác định tiết khí cho một tập ngày giờ cho trước; thuộc ngũ hành sinh khắc và quan hệ chi</td></tr><tr><td>2 -&gt; 3</td><td>Lập đúng lá số Lục Nhâm gồm tứ khoá tam truyền và an thiên tướng cho một tập ca kiểm, khớp đáp án</td></tr><tr><td>3 -&gt; 4</td><td>Lập đúng lá số Kỳ Môn và Thái Ất cho tập ca kiểm; nhận diện được các cách cục cơ bản của từng hệ</td></tr><tr><td>4</td><td>Chọn đúng hệ cho một loại câu hỏi và giải thích được cách đọc; đối chiếu kết quả nhiều hệ cho cùng tình huống</td></tr></tbody></table>\n<p>The 1-&gt;2 (pillars + tiet khi) and 2-&gt;3 (LiuRen chart) criteria are <code>auto_graded</code> / <code>gradedBy: \"engine\"</code>: FR-EDU-002 casts the learner's answer and diffs it against the engine. The 3-&gt;4 criterion is <code>auto_graded</code> for the chart-casting half and <code>manual</code>/<code>quiz</code> for the \"nhan dien cach cuc co ban\" half. The L4 criterion (\"chon dung he ... giai thich duoc cach doc\") is <code>quiz</code> / <code>manual</code> - it is interpretive, so the engine does not grade it.</p>\n<h3>Progression rule (<code>apps/web/src/features/edu/progression.ts</code>)</h3>\n<pre><code class=\"language-ts\">// Given the set of met criterion ids, return the highest unlocked level.\n// A level unlocks only when every criterion of every prerequisite transition is met.\nexport function unlockedLevel(metCriterionIds: ReadonlySet&lt;string&gt;): Level;\nexport function isLevelUnlocked(level: Level, met: ReadonlySet&lt;string&gt;): boolean;</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Four <code>CurriculumLevel</code> records exist with prerequisites forming the linear chain L1 -&gt; L2 -&gt; L3 -&gt; L4; a test asserts the prerequisite graph is acyclic and linear.</li><li>The s3.2 table is represented as four <code>Criterion</code> records (transitions 1-&gt;2, 2-&gt;3, 3-&gt;4, and the L4 mastery criterion); each <code>description</code> matches the source table text verbatim.</li><li><code>unlockedLevel</code> is a pure function of the met-criteria set: a level is not unlocked while any prerequisite transition's criterion is unmet.</li><li>The 1-&gt;2 and 2-&gt;3 criteria are marked <code>measure: \"auto_graded\"</code>, <code>gradedBy: \"engine\"</code>, with <code>systems</code> set (<code>[\"luc_nham\"]</code> for 2-&gt;3); the L4 criterion is <code>quiz</code>/<code>manual</code> with no <code>gradedBy</code>.</li><li>Domain modules carry an optional <code>han</code> field and every <code>Criterion</code> has a stable <code>id</code>; the curriculum renders in the FR-WEB-001 shell with no new design tokens.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>Vitest: DAG test (prerequisite chain is linear + acyclic); criteria-completeness test (one criterion per transition + the L4 criterion); source-parity test asserting each <code>description</code> equals the s3.2 text.</li><li>Progression test: for representative met-criteria sets, <code>unlockedLevel</code> returns the correct level, and an unmet prerequisite blocks the next level.</li><li>Grader-hook test: every <code>gradedBy: \"engine\"</code> criterion has a non-empty <code>systems</code> list (so FR-EDU-002 knows what to cast); the L4 criterion has none.</li><li>Component/snapshot test: the curriculum renders in the app shell using DS v1.3.0 components only.</li><li>Gates: <code>pnpm test</code>, <code>tsc --noEmit</code>, <code>eslint</code> (the WEB toolchain).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>content/edu/curriculum.ts</code>: the four <code>CurriculumLevel</code> records with their modules.</li><li><code>content/edu/criteria.ts</code>: the four <code>Criterion</code> records with the s3.2 text verbatim and the <code>measure</code> / <code>gradedBy</code> / <code>systems</code> flags.</li><li><code>features/edu/types.ts</code>: the types above.</li><li><code>features/edu/progression.ts</code>: <code>unlockedLevel</code>, <code>isLevelUnlocked</code>.</li><li><code>features/edu/progression.test.ts</code>: DAG, completeness, source-parity, progression, and grader-hook tests.</li><li>Render the levels + criteria + lock state in the FR-WEB-001 learning flow (list view, no new tokens).</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-WEB-001 (the app shell and Design System v1.3.0 components; the curriculum renders in the learning flow, Tam-Thuc-07 s6). Blocks FR-EDU-002 (auto-graded practice consumes the criteria marked <code>gradedBy: \"engine\"</code> and needs FR-QMDG-006 + FR-LN-006 to cast the reference charts) and informs FR-EDU-004 (onboarding). Related to FR-KB-003 / FR-EDU-003 (the bilingual classical library the curriculum links out to) and FR-WEB-006 (i18n translates the criteria by id).</p>\n<h2>§8 - Example payloads</h2>\n<p>The L2 -&gt; L3 level record and its criterion (abridged):</p>\n<pre><code class=\"language-ts\">export const level2: CurriculumLevel = {\n  level: 2,\n  title: \"Luc Nham chuyen sau\",\n  prerequisites: [1],\n  modules: [\n    { id: \"ln-thien-dia-ban\", title: \"Thien dia ban\", han: \"天地盤\", summary: \"...\" },\n    { id: \"ln-tu-khoa\", title: \"Tu khoa\", han: \"四課\", summary: \"...\" },\n    { id: \"ln-tam-truyen\", title: \"Tam truyen\", han: \"三傳\", summary: \"...\" },\n  ],\n  levelUpCriteria: [\n    {\n      id: \"edu.crit.2to3\",\n      transition: \"2-&gt;3\",\n      description:\n        \"Lập đúng lá số Lục Nhâm gồm tứ khoá tam truyền và an thiên tướng cho một tập ca kiểm, khớp đáp án\",\n      measure: \"auto_graded\",\n      gradedBy: \"engine\",\n      systems: [\"luc_nham\"],\n    },\n  ],\n};</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>QiMen vs TaiYi ordering inside L3: default LiuRen (L2) then QiMen then TaiYi within L3, matching the build order (strategy 3.4) and s3.1.</li><li>Are L3/L4 criteria auto_graded? Default: the chart-casting half is engine-graded (diff); \"nhan dien cach cuc co ban\" and the L4 \"choose the system + explain\" are quiz/manual because they are interpretive - the engine grades casting, not meaning (strategy 7).</li><li>Localization: the s3.2 table is Vietnamese; the EN mirror is deferred to FR-WEB-006. This FR keeps the VN source text and a stable id per criterion so WEB-006 can translate by id without re-authoring.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Level enterable with unmet prerequisite</td><td>progression not gated on criteria</td><td>DAG + progression tests fail</td></tr><tr><td>Criterion not measurable</td><td>vague \"read the chapter\" text</td><td>schema requires a <code>measure</code>; a test asserts every criterion has one</td></tr><tr><td>s3.2 text drift</td><td>a criterion edited away from the source</td><td>source-parity test against the s3.2 table fails</td></tr><tr><td>auto_graded criterion with no engine hook</td><td>mislabeled <code>measure</code> / missing <code>systems</code></td><td>grader-hook test: every <code>gradedBy: \"engine\"</code> criterion has a non-empty <code>systems</code> list</td></tr><tr><td>Curriculum cycle</td><td>a prerequisite loop introduced</td><td>DAG acyclicity test fails</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the P3 training platform's spine - the four-level curriculum (can chi / ngu hanh -&gt; LiuRen -&gt; QiMen + TaiYi -&gt; integration) with measurable, mostly engine-graded level-up criteria (Tam-Thuc-07 s3.1-3.2). The deterministic engine doubles as the auto-grader (s3.3): because one input yields exactly one chart, the app diffs the learner's chart against the engine's and points to the exact step they missed. This FR owns the levels, the criteria, and the gating; FR-EDU-002 wires the grader; FR-EDU-003 the bilingual library. Language is TypeScript (DEC-2) - typed curriculum data plus a pure progression function - and the content renders in the WEB-001 shell with no new design tokens. refs Claude-07 s3.</p>";
