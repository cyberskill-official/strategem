window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-STRAT-001"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR builds the product's headline strategic tool: the Timing Optimizer. Given a date range and a question type, it scans candidate times, casts QiMen for each via the engine, scores each window by its cat / hung cach cuc, and returns the top recommended windows with their scores. It is the technical form of trach thoi / trach nhat (choosing a good time for an action, Claude-07 s1.1).</p>\n<p>The module SHALL accept a <code>TimingRequest</code> (start, end, granularity, question type, timezone, longitude, school flags, top_n) and enumerate candidate instants across the range at the chosen granularity. For each candidate it SHALL cast a QiMen chart by calling the FR-QMDG-006 engine and SHALL NOT re-implement any casting logic - the chart, its plates, and its detected <code>cach_cuc</code> come only from the engine (strategy 4.3). It SHALL score each window as a documented, deterministic function over the envelope's <code>cach_cuc</code> (polarity and score), using FR-RULE-003 to select and weight the patterns that matter for the question type; when a phuong vi (direction) component is present the score MAY include a directional-favorability term. It SHALL rank the windows and return the top_n with, for each, the score, the contributing cat and hung cach cuc (with citations), and a reference to the cast.</p>\n<p>The module SHALL be deterministic: identical request plus identical flags yields identical ranked windows. The result is a decision-support signal, never a verdict: every window SHALL carry its contributing patterns and citations so the score is explainable, and any interpretation prose attached downstream SHALL carry an AIDisclosure (strategy 7). Interpretation itself is the RAG branch's job; STRAT-001 returns the scored structure.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The product is named and positioned around strategic timing (strategy 3.4). The lookup flow answers \"what does this chart say\"; the Timing Optimizer answers the question a decision-maker actually asks: \"of all the times I could do this, which are best, and why\". That turn - from one chart to a ranked, explained set of windows over a real range - is the \"Strategem\" in the product name (Claude-07 s1.1, Grok-02).</p>\n<p>The optimizer must call the engine and never cast its own chart, for the same reason the whole platform splits deterministic casting from everything else: if STRAT re-implements dinh cuc or plate placement, it will drift from the oracle-gated engine and quietly recommend the wrong hour. Scanning through FR-QMDG-006 (with the FR-PLAT-006 chart cache making repeat casts cheap) keeps every recommendation traceable to an oracle-exact chart.</p>\n<p>The score is a pure, documented function of the chart's <code>cach_cuc</code> so it is explainable. A timing tool that returned a bare number would be a black box and, worse, would read as a fortune-telling verdict. Returning the contributing cat / hung patterns with citations makes the recommendation a transparent, auditable decision aid - the responsible-positioning rule (strategy 7) expressed as a feature.</p>\n<h2>§3 - Contract (models and process)</h2>\n<h3>Models (<code>packages/tamthuc_strat/models.py</code>)</h3>\n<pre><code class=\"language-python\">from pydantic import BaseModel, ConfigDict\nfrom datetime import datetime, timedelta\n\nclass TimingRequest(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    start: datetime\n    end: datetime\n    granularity: str                  # \"gio\" (2h) | \"ngay\" | ISO 8601 duration\n    loai_cau_hoi: str                 # question type -&gt; dung than palace filter\n    tz: str\n    kinh_do: float\n    co_truong_phai: dict              # QiMen school flags, passed through to the engine\n    top_n: int = 5\n\nclass ScoredWindow(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    start: datetime\n    end: datetime\n    score: float\n    cat: list[dict]                   # contributing cat cach cuc (id, name, score, citations)\n    hung: list[dict]                  # contributing hung cach cuc\n    cast_ref: str                     # cache key / id of the QMDG-006 envelope\n\nclass TimingResult(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    windows: list[ScoredWindow]       # ranked, length &lt;= top_n\n    request_echo: TimingRequest</code></pre>\n<h3>Process</h3>\n<ol><li>Enumerate candidate instants across <code>[start, end]</code> at <code>granularity</code> (QiMen changes per gio / per cuc; the default scan granularity is gio).</li><li>For each candidate, cast via the FR-QMDG-006 engine client (cache-keyed by FR-PLAT-002 rules) -&gt; la so envelope. Never compute plates in STRAT.</li><li>Select the relevant <code>cach_cuc</code> for <code>loai_cau_hoi</code> via FR-RULE-003 (per-system filter + match); default all-palace until FR-QMDG-007 (dung than by question type) narrows to the dung than palace.</li><li>Score = a documented aggregation over the selected <code>cach_cuc</code> (sum of cat scores minus sum of hung scores, each pattern's own <code>score</code> and <code>polarity</code> from the envelope), plus an optional phuong vi term.</li><li>Rank by score; break ties by earliest instant first; return the top_n with contributing patterns + citations + <code>cast_ref</code>.</li></ol>\n<p>Determinism: the scan reuses the chart cache and the score is pure, so the same request returns the same ranked windows. An inverted range (<code>end &lt; start</code>) returns a typed error; <code>start == end</code> returns exactly one window.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>For a fixed range + granularity + flags, the scan produces exactly one candidate per granularity step and one <code>ScoredWindow</code> each, in deterministic order.</li><li>Each candidate is cast via the FR-QMDG-006 engine (a repeat scan hits the cache); a test asserts STRAT calls the engine and does not compute plates itself.</li><li><code>score</code> is a pure function of the selected <code>cach_cuc</code> (polarity + score) for the question type; the same envelope yields the same score.</li><li>The returned windows are the <code>top_n</code> highest scores, ties broken by earliest instant first.</li><li>Each window lists its contributing cat and hung cach cuc with citations, and its <code>score</code> equals the documented aggregation of exactly those listed patterns (explainability).</li><li><code>start == end</code> returns one window; <code>end &lt; start</code> raises a typed error, not an empty result.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>pytest with a stub engine returning known envelopes over a small range: assert candidate count, per-window scoring, ranking, tie-break, and top_n selection.</li><li>Determinism test: the same request twice yields identical results; the second scan hits the cache (spy on the engine client).</li><li>Explainability test: each window's <code>score</code> equals the documented aggregation of its listed cat/hung patterns.</li><li>Property: adding a hung cach cuc to a candidate's envelope never raises its window score; adding a cat never lowers it (monotonicity).</li><li>Boundary: <code>start == end</code> (one window); <code>end &lt; start</code> (typed error); a long range at fine granularity trips the candidate cap with a typed error.</li><li>Gates: <code>python -m pytest packages/tamthuc_strat</code>, <code>ruff check</code>, <code>mypy packages/tamthuc_strat</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Create the <code>tamthuc_strat</code> package (<code>models.py</code>, <code>timing_optimizer.py</code>, <code>scoring.py</code>, <code>tests/</code>).</li><li><code>models.py</code>: <code>TimingRequest</code>, <code>ScoredWindow</code>, <code>TimingResult</code>.</li><li><code>timing_optimizer.py</code>: enumerate candidates by granularity; call the FR-QMDG-006 engine client per candidate (cache-keyed); collect envelopes.</li><li><code>scoring.py</code>: the documented weighting over <code>cach_cuc</code> polarity/score; FR-RULE-003 selection for the question type; the optional phuong vi term.</li><li>Rank + top_n + tie-break; attach contributing patterns + citations + <code>cast_ref</code>.</li><li>Expose the STRAT API endpoint; tests with a stub engine and golden windows.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-QMDG-006 (casts each candidate chart and emits the <code>cach_cuc</code>) and FR-RULE-003 (selects and weights the patterns that matter for the question type). Reads the FR-PLAT-002 envelope and benefits from FR-PLAT-006 (chart cache makes the scan cheap). Blocks FR-STRAT-002 (Scenario Comparison compares <code>TimingResult</code>s across options). When FR-QMDG-007 (dung than by question type) lands, the palace filter tightens from all-palace to the dung than palace.</p>\n<h2>§8 - Example payloads</h2>\n<p>Request over one day at gio granularity, and the ranked result (abridged):</p>\n<pre><code class=\"language-json\">// request\n{ \"start\": \"2004-01-01T00:00:00\", \"end\": \"2004-01-01T23:59:59\", \"granularity\": \"gio\",\n  \"loai_cau_hoi\": \"trach_thoi\", \"tz\": \"+07:00\", \"kinh_do\": 106.7,\n  \"co_truong_phai\": { \"dingju_method\": \"chaibu\", \"pan_method\": \"zhuan\", \"yin_yang_pan\": \"duong\" },\n  \"top_n\": 2 }\n\n// result\n{ \"windows\": [\n    { \"start\": \"2004-01-01T10:00:00\", \"end\": \"2004-01-01T12:00:00\", \"score\": 1.7,\n      \"cat\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"青龍返首\", \"score\": 0.9,\n                 \"citations\": [\"Yen Ba Dieu Tau Ca\"] } ],\n      \"hung\": [], \"cast_ref\": \"qmdg:2004-01-01T11:00+07:...\" },\n    { \"start\": \"2004-01-01T06:00:00\", \"end\": \"2004-01-01T08:00:00\", \"score\": 0.4,\n      \"cat\": [ { \"id\": \"...\", \"name\": \"...\", \"score\": 0.4, \"citations\": [\"...\"] } ],\n      \"hung\": [ { \"id\": \"...\", \"name\": \"...\", \"score\": -0.0, \"citations\": [\"...\"] } ],\n      \"cast_ref\": \"qmdg:2004-01-01T07:00+07:...\" }\n  ],\n  \"request_echo\": { \"...\": \"the request above\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Default granularity: QiMen changes per gio (2h) and per cuc; default gio-level scan, with an optional coarser day-level pre-filter for long ranges (perf). Confirm against p95 once FR-QMDG-006 timing is known; FR-PLAT-006 cache makes the scan cheap.</li><li>Cross-engine timing: should the optimizer also consult LiuRen for a concrete yes/no on the chosen time? Deferred to FR-STRAT-004 (cross-system validate); STRAT-001 is QiMen-only, which is its trach thoi strength (Claude-07 s1.1).</li><li>Score weights: fixed or per-question configurable? Default fixed and documented; expose as config only if evals show a need. The weighting is versioned so a change is auditable.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>STRAT recomputes a chart</td><td>optimizer implements casting itself</td><td>forbidden; must call FR-QMDG-006; a test asserts the engine is invoked</td></tr><tr><td>Non-deterministic ranking</td><td>unstable sort / tie not broken</td><td>deterministic tie-break (earliest instant first); repeat-run equality test</td></tr><tr><td>Unbounded scan</td><td>a huge range at fine granularity</td><td>cap candidate count / require coarser granularity for long ranges; typed error past the cap</td></tr><tr><td>Score not explainable</td><td>window score != sum of its listed patterns</td><td>explainability test ties the score to the contributing cach_cuc</td></tr><tr><td>Inverted range</td><td><code>end &lt; start</code></td><td>typed error, not an empty result</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>Package <code>tamthuc_strat</code> (Python, DEC-2). The Timing Optimizer is the product's headline strategic tool: it turns the QiMen engine's per-instant cat / hung cach cuc into a ranked set of recommended windows for a real decision (trach thoi, Claude-07 s1.1). STRAT is where the \"Strategem\" positioning lives - a scored, explainable, cited timing recommendation framed as decision support, never a verdict (strategy 7). It scans via the engine and never casts a chart itself; the score is a pure, documented function of the envelope's <code>cach_cuc</code>, so every recommendation is auditable. FR-STRAT-002 builds Scenario Comparison on top of this. refs Claude-07 s1.1, Grok-02.</p>";
