window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-REPORT-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR provides sample report templates per question type: trach thoi 擇時 (timing), phuong vi 方位 (direction), chu-khach 主客 (competitor / risk), and vi mo (macro outlook). Each template SHALL shape how a FR-REPORT-001 <code>StructuredReport</code> is presented - the section order, which dung than 用神 to foreground, and the emphasis - and SHALL document the mapping from question type to the right system and dung than per Claude-07 s1: trach thoi -&gt; Ky Mon (Luc Nham for a specific act); phuong vi -&gt; Ky Mon (cuu cung, eight directions); chu-khach -&gt; Thai At / Ky Mon / Luc Nham (host-guest); vi mo -&gt; Thai At (macro, long-range).</p>\n<p>The templates SHALL be presentation over the assembled report: read-only, with no re-casting, no re-interpretation, and no field mutation (strategy 4.3). Each template SHALL ship with a sample filled report (Grok-08) demonstrating the shape. A loader SHALL map a report's <code>loai_cau_hoi</code> to its template, with a typed default fallback for an unknown type.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>A timing question and a macro-outlook question want different reports even though both come through the same assembler (Grok-08). A trach thoi report leads with the recommended window and the cat / hung reasons; a chu-khach report leads with the two-sided posture; a macro report leads with the long-range trend. Hard-coding one layout would serve none of them well.</p>\n<p>Templating the presentation per question type - while keeping REPORT-001 as the single assembler - gives each question the report shape that fits it without duplicating assembly logic. The templates also encode the Claude-07 s1 mapping (which system and which dung than each question type calls for) as a documented, shipped artifact, so the reason a timing report uses Ky Mon and foregrounds the acting palace is written down, not folklore. Templates stay read-only over the report for the same reason everything downstream of the envelope is: the deterministic / AI boundary must not be crossed at presentation (strategy 4.3), and a template must never quietly re-cast to a system it thinks is \"better\" than the one actually cast.</p>\n<h2>§3 - Contract (mapping and loader)</h2>\n<h3>Mapping (Claude-07 s1)</h3>\n<table><thead><tr><th>Question type</th><th>System (he)</th><th>dung than focus</th><th>Report leads with</th></tr></thead><tbody><tr><td>trach thoi 擇時 (timing)</td><td>Ky Mon (Luc Nham for a specific act)</td><td>the matter + the chosen hour</td><td>the recommended window + cat / hung</td></tr><tr><td>phuong vi 方位 (direction)</td><td>Ky Mon (cuu cung, 8 huong)</td><td>the direction palace</td><td>the favorable / unfavorable directions</td></tr><tr><td>chu-khach 主客 (competitor / risk)</td><td>Thai At / Ky Mon / Luc Nham</td><td>self (chu) + other (khach)</td><td>the two-sided posture</td></tr><tr><td>vi mo (macro outlook)</td><td>Thai At</td><td>the trend / cycle</td><td>the long-range trend</td></tr></tbody></table>\n<h3>Loader (<code>templates.py</code>)</h3>\n<pre><code class=\"language-python\">from pydantic import BaseModel, ConfigDict\nfrom tamthuc_report.models import StructuredReport\n\nclass ReportTemplate(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    question_type: str                # trach_thoi | phuong_vi | chu_khach | vi_mo\n    he: list[str]                     # recommended system(s) per Claude-07 s1\n    dung_than_focus: str\n    section_order: list[str]          # how to order the StructuredReport sections\n    sample_path: str                  # the shipped sample report\n\ndef template_for(loai_cau_hoi: str) -&gt; ReportTemplate: ...   # question type -&gt; template, typed default fallback\n\ndef render_sections(report: StructuredReport, template: ReportTemplate) -&gt; list[\"Section\"]:\n    # reorder / label the report's existing sections per the template; read-only, no re-compute\n    ...</code></pre>\n<p>Each question-type markdown ships the mapping plus a sample filled report (Grok-08). <code>render_sections</code> only reorders and labels the report's own content; it adds nothing and derives nothing.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Four templates exist - <code>trach_thoi</code>, <code>phuong_vi</code>, <code>chu_khach</code>, <code>vi_mo</code> - each documenting its system(s), dung than focus, and section order per Claude-07 s1.</li><li><code>template_for</code> maps a report's <code>loai_cau_hoi</code> to the right template; an unknown type returns a typed default (not a crash).</li><li><code>render_sections</code> reorders / emphasizes the <code>StructuredReport</code>'s existing sections and never adds, recomputes, or mutates content (read-only).</li><li>Each template ships a sample filled report (Grok-08) matching its shape.</li><li>The mapping matches Claude-07 s1 (timing -&gt; Ky Mon, direction -&gt; Ky Mon cuu cung, chu-khach -&gt; Thai At / Ky Mon / Luc Nham, macro -&gt; Thai At).</li><li>The templates feed FR-REPORT-002 (PDF) and FR-WEB-005 (report view) without changing the assembled report.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>test_templates.py</code>: <code>template_for</code> returns the right template per question type; an unknown type returns the default; <code>render_sections</code> over a golden <code>StructuredReport</code> reorders sections without changing their content (set-equality of content before and after, order differs).</li><li>Read-only: the <code>StructuredReport</code> is byte-identical after <code>render_sections</code> (only an ordered list of sections / labels is produced).</li><li>Mapping: assert each template's <code>he</code> and <code>dung_than_focus</code> match the Claude-07 s1 table.</li><li>Samples: each shipped sample report validates against the FR-REPORT-001 schema.</li><li>Gates: <code>python -m pytest packages/tamthuc_report</code>, <code>ruff check</code>, <code>mypy packages/tamthuc_report</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>templates.py</code>: <code>ReportTemplate</code>, <code>template_for</code>, <code>render_sections</code> (read-only reorder / label).</li><li><code>question_types/{trach_thoi,phuong_vi,chu_khach,vi_mo}.md</code>: the mapping + a sample filled report each (Grok-08).</li><li>The typed default-fallback template for an unknown type.</li><li><code>tests/test_templates.py</code>: the mapping, <code>template_for</code>, the read-only render, and sample-schema validation.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-REPORT-001 (the <code>StructuredReport</code> whose sections it reorders; read-only). Aligns with FR-QMDG-007 (dung than by question type - the same mapping the templates encode) and FR-STRAT-003 (the chu-khach template mirrors the chu-khach decision frame). Consumed by FR-REPORT-002 (PDF) and FR-WEB-005 (report view). Nothing depends on it (blocks empty).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// ReportTemplate for a timing question\n{ \"question_type\": \"trach_thoi\",\n  \"he\": [\"ky_mon\", \"luc_nham\"],\n  \"dung_than_focus\": \"the matter + the chosen hour\",\n  \"section_order\": [\"recommended_window\", \"detected_patterns\", \"beginner\", \"expert\",\n                    \"recommendations\", \"ai_disclosure\", \"citations\"],\n  \"sample_path\": \"templates/question_types/trach_thoi.md\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Does the template pick the system, or only reflect what was cast? Default: the template documents the recommended system per Claude-07 s1 and reflects the system actually cast; it never re-casts to a \"preferred\" system.</li><li>Overlap with FR-QMDG-007 (the dung than mapping). Default: QMDG-007 owns the engine-side dung than palace; REPORT-003 owns the presentation mapping; both cite the same Claude-07 s1 source.</li><li>More question types later (e.g. hon nhan, tai loc). Default: the four canonical types at MVP; add templates as question types are formalized.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Template re-computes</td><td>render adds / derives content</td><td>forbidden; read-only reorder / label only; byte-equality test</td></tr><tr><td>Wrong system mapping</td><td>a timing template points at Thai At</td><td>mapping fixed to Claude-07 s1; a test asserts</td></tr><tr><td>Unknown question type crashes</td><td>no template for a type</td><td>a typed default fallback, not a crash</td></tr><tr><td>Sample drifts from schema</td><td>a shipped sample is invalid</td><td>the sample validates against the FR-REPORT-001 schema in CI</td></tr><tr><td>Layout hides the AI boundary</td><td>a template interleaves engine + AI content</td><td>the section order keeps the deterministic and AI sections distinct (FR-REPORT-002 renders them separated)</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>Package <code>tamthuc_report</code> (Python, DEC-2). Report templates give each question type the report shape that fits it - trach thoi leads with the window, chu-khach with the two-sided posture, macro with the long-range trend - while keeping REPORT-001 as the single assembler and staying read-only over the assembled report (Grok-08, strategy 4.3). Each template also encodes the Claude-07 s1 mapping from question type to system and dung than as a shipped, documented artifact (timing -&gt; Ky Mon, direction -&gt; Ky Mon cuu cung, competitor / risk -&gt; Thai At / Ky Mon / Luc Nham, macro -&gt; Thai At), so the presentation reasons are written down. Feeds the PDF export (FR-REPORT-002) and the report-view screen (FR-WEB-005).</p>";
