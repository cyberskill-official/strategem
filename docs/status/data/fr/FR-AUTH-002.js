window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR defines authorization: the four roles a user can hold, the capabilities each role grants, and the per-tier rate-limit quotas that FR-API-003 enforces. It extends the <code>tamthuc_auth</code> package. It owns the tier and quota configuration as a single source of truth; it does NOT implement the counting or the 429 responses (that is FR-API-003) nor issue tokens (FR-AUTH-001).</p>\n<p>The role set SHALL be the closed enum Free, Premium, Enterprise, Admin. Each role SHALL map to a capability set (which endpoints and features it may reach) and, for the request-serving tiers, to a rate-limit quota. The quotas SHALL be: Free 100 requests/day, Premium 5000 requests/day, Enterprise custom (a per-account override), with Admin unmetered for operational use. Tier and quota configuration SHALL live in one machine-readable artifact (<code>docs/contracts/rbac-tiers.json</code>) that both this package and FR-API-003 read, so the limit a request is judged against and the limit the product advertises are the same number in one place.</p>\n<p>Authorization SHALL be expressed as FastAPI dependencies (<code>require_tier</code>, <code>require_role</code>, <code>require_capability</code>) that read the <code>tier</code> claim from the FR-AUTH-001 access token and reject an under-privileged request with a 403 in the FR-API-001 error envelope. Enterprise clients SHALL additionally be able to authenticate machine-to-machine with an API key (distinct from a user JWT); an Enterprise API key SHALL resolve to an Enterprise-tier principal with its account's custom quota. A tier change SHALL take effect on the next token issuance or refresh, and SHALL be an auditable action (FR-API-004).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Tiering is both the product's commercial model and a safety control. The commercial side is obvious - Free, Premium, and Enterprise are the plan ladder (Grok-36). The safety side is why it lives in AUTH and not only in billing: the rate-limit quota is the primary abuse and cost control on an endpoint that calls an LLM, and the tier a user holds is exactly the input the limiter needs (Grok-12). Keeping the quota numbers in one config that both AUTH and API-003 read prevents the classic drift where the enforced limit and the documented limit diverge and a user is throttled at a number no page mentions.</p>\n<p>Separating capability (what you may do) from quota (how much) keeps the two axes independent: Admin is unmetered but that is a role capability, not a huge quota; Enterprise has a custom quota but standard capabilities plus API-key auth. Expressing authorization as dependencies rather than scattered inline checks means every protected route declares its requirement in its signature, which is auditable and hard to forget. Enterprise API-key auth is a deliberate second principal type because machine clients should not carry a user's refresh token; a scoped, revocable key that resolves to the account's tier is the right grant for server-to-server use.</p>\n<h2>§3 - Contract (schema / types / config)</h2>\n<h3>Roles and capabilities (<code>tamthuc_auth/rbac.py</code>)</h3>\n<pre><code class=\"language-python\">class Role(str, Enum): free = \"Free\"; premium = \"Premium\"; enterprise = \"Enterprise\"; admin = \"Admin\"\n\nclass Capability(str, Enum):\n    calculate_single = \"calculate_single\"     # /calculate/{qimen,liuren,taiyi}\n    calculate_all = \"calculate_all\"           # /calculate/all (cross-system)\n    timing_optimize = \"timing_optimize\"       # /timing/optimize (STRAT)\n    scenario_compare = \"scenario_compare\"     # /scenario/compare (STRAT)\n    report_generate = \"report_generate\"\n    api_key_auth = \"api_key_auth\"             # machine-to-machine\n    admin_console = \"admin_console\"\n\nROLE_CAPABILITIES: dict[Role, set[Capability]]   # e.g. Free lacks calculate_all + api_key_auth</code></pre>\n<h3>Tier quota config (<code>docs/contracts/rbac-tiers.json</code>, read by this package and FR-API-003)</h3>\n<pre><code class=\"language-json\">{\n  \"Free\":       { \"requests_per_day\": 100,  \"capabilities\": [\"calculate_single\", \"report_generate\"] },\n  \"Premium\":    { \"requests_per_day\": 5000, \"capabilities\": [\"calculate_single\", \"calculate_all\",\n                                                              \"timing_optimize\", \"scenario_compare\", \"report_generate\"] },\n  \"Enterprise\": { \"requests_per_day\": \"custom\", \"capabilities\": [\"calculate_single\", \"calculate_all\",\n                                                              \"timing_optimize\", \"scenario_compare\",\n                                                              \"report_generate\", \"api_key_auth\"] },\n  \"Admin\":      { \"requests_per_day\": \"unmetered\", \"capabilities\": [\"admin_console\"] }\n}</code></pre>\n<p><code>tiers.py</code> loads this file into typed <code>TierConfig</code> objects; <code>enterprise</code> accounts carry a per-account numeric override resolved at auth time. This JSON is the one place the quota numbers live.</p>\n<h3>Authorization dependencies (<code>tamthuc_auth/scopes.py</code>)</h3>\n<pre><code class=\"language-python\">def require_role(*roles: Role) -&gt; Callable: ...              # 403 if current tier not in roles\ndef require_tier(min_tier: Role) -&gt; Callable: ...            # ordered Free &lt; Premium &lt; Enterprise &lt; Admin\ndef require_capability(cap: Capability) -&gt; Callable: ...     # 403 if cap not in the tier's capability set\ndef quota_for(principal) -&gt; int | Literal[\"unmetered\"]: ...  # the number FR-API-003 enforces</code></pre>\n<h3>Enterprise API key (<code>tamthuc_auth/apikey.py</code>)</h3>\n<pre><code class=\"language-python\">def issue_api_key(account_id: str) -&gt; str: ...              # hashed at rest, shown once\ndef resolve_api_key(key: str) -&gt; Principal | None: ...      # -&gt; Enterprise principal + custom quota, or None\ndef revoke_api_key(key_id: str) -&gt; None: ...</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Role</code> is exactly {Free, Premium, Enterprise, Admin}; <code>ROLE_CAPABILITIES</code> maps each to its capability set, and <code>docs/contracts/rbac-tiers.json</code> agrees with it (a parity test rejects drift).</li><li><code>require_capability(calculate_all)</code> rejects a Free principal with a 403 in the error envelope and allows a Premium principal.</li><li><code>quota_for</code> returns 100 for Free, 5000 for Premium, the account override for Enterprise, and <code>\"unmetered\"</code> for Admin; FR-API-003 consumes exactly this.</li><li>An Enterprise API key resolves via <code>resolve_api_key</code> to an Enterprise principal with the account's custom quota; a revoked key resolves to <code>None</code>; the key is stored hashed, never in plaintext.</li><li><code>require_tier(Premium)</code> uses the documented ordering Free &lt; Premium &lt; Enterprise &lt; Admin, so an Enterprise principal passes a Premium gate.</li><li>A tier change is reflected on the next token issuance/refresh and produces an auditable action record consumed by FR-API-004.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_rbac.py</code>: role/capability map completeness; the config-vs-code parity test; per-capability allow/deny cases across all four roles; the tier ordering; <code>quota_for</code> for each tier including the Enterprise override; API-key issue/resolve/revoke with hashed-at-rest assertion.</li><li>Config parity: a test loads <code>docs/contracts/rbac-tiers.json</code> and asserts every role and capability matches the enums, so the advertised and enforced limits cannot diverge.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_auth</code>, <code>pytest packages/tamthuc_auth</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>rbac.py</code>: <code>Role</code>, <code>Capability</code>, <code>ROLE_CAPABILITIES</code>.</li><li><code>tiers.py</code>: load <code>docs/contracts/rbac-tiers.json</code> into <code>TierConfig</code>; resolve the Enterprise per-account override.</li><li><code>scopes.py</code>: <code>require_role</code> / <code>require_tier</code> / <code>require_capability</code> FastAPI dependencies and <code>quota_for</code>.</li><li><code>apikey.py</code>: Enterprise API-key issue (hash at rest), resolve, revoke.</li><li>Author <code>docs/contracts/rbac-tiers.json</code> as the single source; wire the parity test.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-AUTH-001 (the user, the <code>tier</code> claim in the access token, and the token-issuance path a tier change flows through). Blocks FR-API-003 (rate limiting reads <code>quota_for</code> and <code>rbac-tiers.json</code> to enforce the per-tier limit) and FR-PLAT-007 (security hardening's authorization controls build on these roles). The authorization dependencies are consumed by FR-API-001's protected routes; tier-change audit rows are written by FR-API-004.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// a Free user calling /calculate/all\n// require_capability(calculate_all) -&gt; 403 in the FR-API-001 envelope:\n{ \"error\": { \"code\": \"FORBIDDEN_TIER\", \"message\": \"This endpoint requires the Premium tier or higher.\",\n             \"details\": { \"required\": \"Premium\", \"current\": \"Free\" } } }</code></pre>\n<pre><code class=\"language-json\">// quota_for a principal, consumed by FR-API-003\n{ \"principal\": \"user:...\", \"tier\": \"Premium\", \"requests_per_day\": 5000 }\n{ \"principal\": \"apikey:...\", \"tier\": \"Enterprise\", \"requests_per_day\": 50000 }   // account override</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether quotas are per-day only or also per-minute burst. Default: this FR fixes the per-day quota (the documented plan number); FR-API-003 may add a burst window on top, reading the same config. Keep the burst config in <code>rbac-tiers.json</code> if added, so it stays single-source.</li><li>Enterprise custom quota storage: a column on the account vs an entry in the config. Default: a per-account override resolved at auth time (config holds the sentinel <code>\"custom\"</code>, the account holds the number), so the shared config file stays free of per-customer data.</li><li>Capability granularity. Default: coarse capabilities at MVP (single vs all vs strategic tools vs report vs admin); finer per-feature flags are deferred until a plan needs them. Revisit with billing.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Advertised vs enforced drift</td><td>quota number changed in code but not config (or vice versa)</td><td>one source (<code>rbac-tiers.json</code>); the parity test fails CI on divergence</td></tr><tr><td>Privilege escalation</td><td>a Free token reaches a Premium endpoint</td><td><code>require_capability</code> / <code>require_tier</code> reject with 403; every protected route declares its requirement</td></tr><tr><td>API key in plaintext</td><td>key stored bare or logged</td><td>keys hashed at rest, shown once at issue; a test asserts no plaintext storage</td></tr><tr><td>Stale tier</td><td>tier changed but old token still honored at old tier</td><td>tier resolved from the token claim; change takes effect on next issue/refresh; short access TTL bounds staleness</td></tr><tr><td>Admin over-metered / under-guarded</td><td>Admin treated as a huge quota instead of a capability</td><td>Admin is <code>unmetered</code> capability-gated to <code>admin_console</code>, not a numeric quota</td></tr><tr><td>Enterprise key over-privileged</td><td>key grants user-level actions</td><td>an API key resolves to a machine principal scoped to the account tier, not a user's full session</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR is small but load-bearing twice over: it is the commercial plan ladder and the primary cost/abuse control on an LLM-backed endpoint. The one discipline that matters most is single-source config - the quota a request is judged against and the quota a pricing page shows must be the same JSON. Keep the split clean: AUTH-002 decides what a principal may do and how much; FR-API-003 does the counting and returns the 429. The package <code>tamthuc_auth</code> is shared with FR-AUTH-001/003/004; this FR extends it with the authorization layer the FastAPI gateway leans on.</p>";
