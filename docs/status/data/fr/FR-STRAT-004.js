window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-STRAT-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR implements cross-system validation: for one question, run two or more engines (FR-QMDG-006 and FR-LN-006 now; FR-TAT-006 when it lands) and return a per-system read plus an agreement view that shows where the systems agree and where they diverge (Claude-07 s1.4, Grok-02).</p>\n<p>The module SHALL accept a <code>CrossSystemRequest</code> (one question: datetime, tz, kinh do, loai_cau_hoi, and per-system school flags) and SHALL obtain each system's la so by calling that engine; it SHALL NOT re-cast, re-implement, or merge charts (strategy 4.3). For each system it SHALL produce a <code>SystemRead</code>: the he, a documented cat / hung stance derived from the envelope cach cuc, the system's scope, a reference to the cast, and optionally its FR-RAG-003 interpretation. It SHALL then produce an <code>AgreementView</code> that compares the stances and marks agreement vs divergence WITHOUT collapsing them into one verdict, and SHALL annotate each system's scope so a divergence is not misread as a contradiction (Claude-07 s1.4: the three systems cover different scopes). The module SHALL be deterministic, and the result is decision support, never a verdict (strategy 7).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>A tempting but wrong feature would be a single merged answer across the three systems. It is wrong because the three systems do not answer the same question at the same scope (Claude-07 s1.4): Luc Nham is tactical and per-hour (a specific act, yes or no), Ky Mon is tactical and layout (timing plus direction for a campaign), Thai At is strategic and long-range (a macro trend, a cycle). Forcing them into one verdict would hide exactly the information a cross-system reading exists to surface.</p>\n<p>So the honest artifact is a per-system read plus an agreement view that says, in effect, \"these two agree that the near-term is supportive; this one speaks to a longer horizon and is neutral there\" - agreement and divergence both shown, each with its scope. Like the other strategic tools, it calls the engines and never re-casts (strategy 4.3), and the stance is a documented function of the envelope cach cuc, so the agreement view is explainable rather than an opaque merge. Annotating scope is what keeps a legitimate difference of horizon from reading as a contradiction.</p>\n<h2>§3 - Contract (models and process)</h2>\n<h3>Models (<code>packages/tamthuc_strat/cross_system.py</code>)</h3>\n<pre><code class=\"language-python\">from typing import Literal\nfrom datetime import datetime\nfrom pydantic import BaseModel, ConfigDict\n\nclass CrossSystemRequest(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    datetime: datetime\n    tz: str\n    kinh_do: float\n    loai_cau_hoi: str\n    co_truong_phai: dict[str, dict]   # per-system flags: {\"ky_mon\": {...}, \"luc_nham\": {...}}\n\nclass SystemRead(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    he: str                           # ky_mon | luc_nham | thai_at\n    stance: Literal[\"favorable\", \"mixed\", \"unfavorable\"]   # documented fn of cach_cuc\n    scope: str                        # \"tactical/hourly\" | \"tactical/layout\" | \"strategic/long-range\"\n    cat: list[dict]                   # contributing cat cach cuc (id, name, citations)\n    hung: list[dict]\n    cast_ref: str                     # QMDG-006 / LN-006 envelope reference\n    interp_ref: str | None = None     # optional RAG-003 interpretation reference\n\nclass AgreementView(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    agree: bool                       # do the comparable stances align\n    summary: str                      # \"both favorable near-term; TaiYi neutral at longer horizon\"\n    by_scope: list[dict]              # agreement annotated per scope, so divergence != contradiction\n\nclass CrossSystemResult(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    reads: list[SystemRead]           # one per engine run\n    agreement: AgreementView\n    request_echo: CrossSystemRequest</code></pre>\n<h3>The scope table (Claude-07 s1.4)</h3>\n<table><thead><tr><th>He</th><th>Scope</th><th>Question it fits</th></tr></thead><tbody><tr><td>Luc Nham</td><td>tactical / hourly</td><td>a specific act, yes or no</td></tr><tr><td>Ky Mon</td><td>tactical / layout</td><td>choose the time and direction for a campaign</td></tr><tr><td>Thai At</td><td>strategic / long-range</td><td>a macro trend, a cycle, over years</td></tr></tbody></table>\n<h3>Process</h3>\n<ol><li>For each system in the request, call its engine (FR-QMDG-006, FR-LN-006; FR-TAT-006 when present) with the shared question plus that system's flags -&gt; la so envelope. Never re-cast.</li><li>Derive each <code>SystemRead</code>'s stance from the envelope cach cuc by a documented rule (net cat vs hung on the dung than palace / lesson), attach cat / hung + citations, and stamp the system's scope.</li><li>Build the <code>AgreementView</code>: compare stances at comparable scope; mark agree / divergence; annotate <code>by_scope</code> so a long-range-vs-hourly difference reads as different-scope, not contradiction. Never merge into one verdict.</li></ol>\n<p>Determinism follows from the engines plus a pure stance rule. A requested system with no engine yet returns a typed \"system unavailable\", not a silent drop.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>For a question with two systems, the result has one <code>SystemRead</code> per system, each with a stance, scope, cat / hung + citations, and a <code>cast_ref</code>.</li><li>The module obtains each chart via its engine; a test asserts it calls QMDG-006 and LN-006 and does not cast or merge charts itself.</li><li><code>stance</code> is a documented, pure function of the envelope cach cuc; the same envelope yields the same stance (explainable).</li><li>The <code>AgreementView</code> marks agreement vs divergence and annotates scope; a divergence at different scopes is labeled as such, not as contradiction (Claude-07 s1.4).</li><li>The result is never a single merged verdict; both reads remain present and separable.</li><li>Determinism: the same request twice yields an identical result.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>test_cross_system.py</code>: stub QMDG-006 + LN-006 engines returning known envelopes; assert the per-system reads, the stance derivation, the scope stamps, and the agreement view (an agree case and a divergent-scope case) against <code>fixtures/cross_system_agreement.json</code>.</li><li>No-merge test: the result exposes both reads; there is no single verdict field.</li><li>No-recompute test: a spy asserts each engine is called once and STRAT does not cast or merge.</li><li>Explainability: <code>stance</code> equals the documented aggregation of the read's cat / hung.</li><li>Scope: a favorable-hourly vs neutral-long-range case is labeled different-scope, not a contradiction.</li><li>Gates: <code>python -m pytest packages/tamthuc_strat</code>, <code>ruff check</code>, <code>mypy packages/tamthuc_strat</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>cross_system.py</code>: <code>CrossSystemRequest</code>, <code>SystemRead</code>, <code>AgreementView</code>, <code>CrossSystemResult</code>.</li><li><code>validate(request, engines) -&gt; CrossSystemResult</code>: call each engine; derive the stance from cach cuc; stamp the scope; build the agreement view.</li><li>The documented stance rule + the s1.4 scope table.</li><li>Wire <code>/calculate/all</code> in the API (FR-API-001); tests with stub engines and golden agreement fixtures.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-QMDG-006 and FR-LN-006 (the two engines it runs; FR-TAT-006 joins at P2 for the third system, which is why this FR is P2). Reads the FR-PLAT-002 envelopes and, optionally, FR-RAG-003 interpretations per system. Exposed via FR-API-001 (<code>/calculate/all</code>). Sibling to FR-STRAT-001/002/003. Nothing depends on it (blocks empty).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// CrossSystemResult (abridged) - Ky Mon and Luc Nham agree near-term\n{ \"reads\": [\n    { \"he\": \"ky_mon\", \"stance\": \"favorable\", \"scope\": \"tactical/layout\",\n      \"cat\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"青龍返首\", \"citations\": [\"Yen Ba Dieu Tau Ca\"] } ],\n      \"hung\": [], \"cast_ref\": \"qmdg:...\", \"interp_ref\": null },\n    { \"he\": \"luc_nham\", \"stance\": \"favorable\", \"scope\": \"tactical/hourly\",\n      \"cat\": [ { \"id\": \"ln_khoa_...\", \"name\": \"...\", \"citations\": [\"...\"] } ],\n      \"hung\": [], \"cast_ref\": \"ln:...\", \"interp_ref\": null } ],\n  \"agreement\": { \"agree\": true,\n    \"summary\": \"Both systems read the near-term as supportive at tactical scope.\",\n    \"by_scope\": [ { \"scope\": \"tactical\", \"agree\": true } ] },\n  \"request_echo\": { \"...\": \"the request above\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Does the agreement view include a RAG-003 interpretation per system or only the deterministic stance? Default: the deterministic stance + cat / hung for the agreement; interpretation is optional (<code>interp_ref</code>) and per-system, never merged.</li><li>Stance granularity (3-band favorable / mixed / unfavorable vs a score). Default: 3-band from the documented cach cuc rule; a score is an internal detail, the band is what the agreement compares.</li><li>Two engines vs all three at MVP. Default: two (Ky Mon + Luc Nham); the third (Thai At) activates at FR-TAT-006 (P2), which is why this FR is P2.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Merged verdict</td><td>the result collapses systems into one answer</td><td>forbidden; per-system reads kept separate; no verdict field (Claude-07 s1.4)</td></tr><tr><td>Re-cast / merge chart</td><td>STRAT casts or fuses charts</td><td>forbidden; calls the engines; a test asserts no direct casting</td></tr><tr><td>Scope ignored</td><td>divergent scopes reported as a contradiction</td><td>scope stamped per read; the agreement annotates <code>by_scope</code></td></tr><tr><td>Stance not explainable</td><td><code>stance</code> != its cat / hung</td><td>documented pure rule; explainability test</td></tr><tr><td>Missing engine</td><td>a requested system has no engine yet (e.g. Thai At pre-TAT-006)</td><td>typed \"system unavailable\", not a silent drop</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>Package <code>tamthuc_strat</code> (Python, DEC-2). Cross-system validate is the P2 strategic tool that runs two or more engines for one question and shows agreement vs divergence, honestly annotated by scope (Claude-07 s1.4): Luc Nham tactical / hourly, Ky Mon tactical / layout, Thai At strategic / long-range. It calls the engines and never re-casts (strategy 4.3), derives each stance from the envelope cach cuc by a documented rule, and never merges the systems into a single verdict - the divergence is the signal. Decision support, not a prediction (strategy 7). Depends on FR-QMDG-006 + FR-LN-006; the third system joins at FR-TAT-006.</p>";
