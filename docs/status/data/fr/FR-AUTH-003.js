window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR completes the two account-lifecycle flows FR-AUTH-001 left as hooks: email verification and password reset. It extends the <code>tamthuc_auth</code> package. It owns the verification and reset token lifecycle and the email dispatch seam; it does NOT re-issue session tokens (FR-AUTH-001 owns access/refresh) nor own the email transport itself (that is a provider behind the <code>email.py</code> seam).</p>\n<p>Registration in FR-AUTH-001 creates an unverified user (<code>email_verified=false</code>) and emits a verification event; this FR SHALL issue the verification token, send it, and confirm it. A verification token SHALL be single-use, SHALL expire after a bounded window, SHALL be stored hashed at rest (never in plaintext), and on confirmation SHALL set <code>email_verified=true</code> and be consumed so it cannot be replayed. Re-requesting verification SHALL invalidate any prior outstanding token for that address. Provider-asserted social emails (Google/Apple) MAY be treated as already verified per the FR-AUTH-001 decision and SHALL NOT require this step.</p>\n<p>Password reset SHALL follow the same token discipline: a reset request SHALL issue a single-use, expiring, hashed-at-rest token bound to the account; confirmation SHALL require the token plus a new password, SHALL hash the new password with argon2 (FR-AUTH-001 <code>passwords.py</code>), and SHALL consume the token and revoke outstanding refresh tokens (FR-AUTH-001 <code>revoke_refresh</code>) so a reset ends other sessions. Both flows SHALL be account-enumeration-safe: the request endpoints SHALL return the same generic response whether or not the email exists, matching the FR-AUTH-001 posture, and SHALL NOT reveal account existence through status codes, timing, or message text. All failures SHALL return the FR-API-001 structured error envelope. Verification, reset-request, reset-confirm, and the resulting session revocation SHALL be audited via FR-API-004.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>These two flows are small but they are exactly where account-security bugs live, so the discipline is the point. A verification or reset token is a bearer credential to an account; if it is long-lived, reusable, or stored in plaintext, it is a standing key under the doormat. Making every token single-use, expiring, and hashed at rest means a leaked database row is not a usable token and a token cannot be replayed after it is spent. Invalidating prior tokens on re-request closes the window where several valid tokens float around for one address. Revoking refresh tokens on a successful password reset is the property users actually expect from \"reset my password\" - it ends the sessions a compromised password left open, rather than resetting the password while the attacker's session keeps running.</p>\n<p>Account-enumeration safety carries over directly from FR-AUTH-001 and matters twice as much here, because reset and verification request endpoints are unauthenticated and take an email as input - a naive implementation that says \"no such account\" turns the reset form into an email-existence oracle an attacker can scrape. Returning the same generic response either way, and being careful that timing and status do not leak, keeps these endpoints from becoming a reconnaissance tool against a userbase whose accounts hold sensitive birth data (RISK-5). Keeping the email transport behind a seam means the security-relevant logic (token lifecycle, enumeration safety) is testable without a real mail provider, and the provider is swappable.</p>\n<h2>§3 - Contract (tokens / endpoints / email)</h2>\n<h3>Token store (<code>migrations/0002_email_tokens.sql</code>; run by the PLAT migration runner)</h3>\n<table><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>id</td><td>uuid PK</td><td></td></tr><tr><td>user_id</td><td>uuid FK users(id)</td><td></td></tr><tr><td>purpose</td><td>text</td><td><code>email_verify</code> \\</td><td><code>password_reset</code></td></tr><tr><td>token_hash</td><td>text</td><td>hash of the token; the bare token is never stored</td></tr><tr><td>expires_at</td><td>timestamptz</td><td>bounded lifetime</td></tr><tr><td>consumed_at</td><td>timestamptz</td><td>null until spent; single-use</td></tr><tr><td>created_at</td><td>timestamptz</td><td></td></tr></tbody></table>\n<h3>Verification (<code>verification.py</code>) and reset (<code>reset.py</code>)</h3>\n<pre><code class=\"language-python\">async def issue_verification(user_id: str) -&gt; None:            # create token, invalidate priors, send email\nasync def confirm_verification(token: str) -&gt; None:            # validate + consume -&gt; email_verified=true\nasync def request_password_reset(email: str) -&gt; None:          # enumeration-safe: same response either way\nasync def confirm_password_reset(token: str, new_password: str) -&gt; None:\n    # validate + consume; argon2-hash the new password; revoke outstanding refresh tokens (FR-AUTH-001)</code></pre>\n<h3>Endpoints (mounted by FR-API-001)</h3>\n<pre><code>POST /auth/verify/request                     (Bearer or email)  -&gt; 200 generic\nPOST /auth/verify/confirm         { token }                      -&gt; 200 { email_verified: true }\nPOST /auth/password/reset/request { email }                      -&gt; 200 generic (no account disclosure)\nPOST /auth/password/reset/confirm { token, new_password }        -&gt; 200; refresh tokens revoked</code></pre>\n<h3>Email seam (<code>email.py</code>)</h3>\n<p>A provider-agnostic <code>send(template, to, context)</code> with a fake for tests; the security logic never depends on the concrete provider.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A verification token is single-use and expiring: confirming it sets <code>email_verified=true</code>; a second confirm of the same token fails; an expired token fails; the token is stored hashed (no plaintext token in the DB).</li><li>Re-requesting verification invalidates any prior outstanding token for that address (only the newest is valid).</li><li>A password reset issues a single-use expiring token; confirming with the token + a new password argon2-hashes the new password and consumes the token; the old password no longer authenticates.</li><li>A successful password reset revokes outstanding refresh tokens (other sessions end); a revoked refresh <code>jti</code> is rejected afterward.</li><li>Both request endpoints are account-enumeration-safe: the response for a known and an unknown email is identical (status, body, and no timing/message tell); a test asserts indistinguishability.</li><li>Social accounts with a provider-asserted verified email skip email verification (per FR-AUTH-001); every flow event is audited via FR-API-004 and errors use the FR-API-001 envelope.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_verification.py</code>: issue/confirm happy path; single-use (double-confirm fails); expiry; hashed-at-rest (no plaintext token column); prior-token invalidation on re-request.</li><li><code>tests/test_reset.py</code>: reset issue/confirm; argon2 re-hash; token single-use + expiry; refresh-token revocation on reset; the enumeration-safe request response for known vs unknown email.</li><li>Security checks: no token logged in plaintext (log-capture assertion); timing of the request endpoints does not distinguish known vs unknown email within tolerance.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_auth</code>, <code>pytest packages/tamthuc_auth</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>migrations/0002_email_tokens.sql</code>: the hashed, single-use, expiring token store (verify + reset share it via <code>purpose</code>).</li><li><code>verification.py</code>: issue (invalidate priors, send), confirm (validate + consume -&gt; <code>email_verified=true</code>).</li><li><code>reset.py</code>: request (enumeration-safe), confirm (validate + consume, argon2 re-hash, revoke refresh tokens).</li><li><code>email.py</code>: the provider-agnostic send seam + a fake for tests.</li><li>Wire the endpoints into the FR-API-001 router; emit audit events (FR-API-004) for verify/reset/revocation; render errors in the FR-API-001 envelope.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-AUTH-001 (the unverified-user hook and the emitted verification event this completes, the argon2 <code>passwords.py</code> used to re-hash on reset, the <code>revoke_refresh</code> used to end sessions, and the account-enumeration-safe response posture it mirrors). The token-store DDL is run through the PLAT migration runner (as with the FR-AUTH-001 <code>users</code> table). Endpoints are mounted by FR-API-001 and errors use its envelope; flow events are audited by FR-API-004. Coordinates with FR-LEGAL-002 (verification and reset communications are transactional, not marketing, and fall under the consent/communications policy).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// POST /auth/password/reset/request  -&gt; identical response whether or not the email exists\n{ \"message\": \"If an account exists for that address, a reset link has been sent.\" }</code></pre>\n<pre><code class=\"language-json\">// POST /auth/verify/confirm { \"token\": \"...\" }  -&gt; on success\n{ \"email_verified\": true }\n// stored token row (illustrative) - hashed, single-use, expiring\n{ \"purpose\": \"email_verify\", \"token_hash\": \"argon2/sha256:...\", \"expires_at\": \"...\", \"consumed_at\": null }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Token lifetimes: verification vs reset windows. Default: a longer verification window (hours to a day) and a short reset window (minutes to an hour, the higher-risk credential); both are config, not hardcoded, and both remain single-use.</li><li>Whether an unverified user can use the product before verifying. Default: allow read/limited use but gate sensitive or paid actions until verified (a soft gate, coordinated with FR-AUTH-002 capabilities); a hard block at registration is a stricter option if abuse appears.</li><li>Reset delivery channel beyond email (e.g. SMS) for higher-tier accounts. Default: email only at MVP behind the <code>email.py</code> seam; a second factor/channel is a later hardening step that reuses the same single-use-token discipline.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Account enumeration</td><td>request endpoint reveals whether an email exists</td><td>forbidden; identical generic response, no status/timing/message tell (RISK-5)</td></tr><tr><td>Token replay</td><td>a spent or expired token reused</td><td>single-use + expiring; consumed tokens rejected; a double-confirm fails</td></tr><tr><td>Token in plaintext</td><td>bare token stored or logged</td><td>tokens stored hashed; never logged; a test asserts no plaintext token</td></tr><tr><td>Stale valid tokens</td><td>multiple outstanding tokens per address</td><td>re-request invalidates priors; only the newest is valid</td></tr><tr><td>Reset without session cut</td><td>password reset leaves old sessions live</td><td>reset revokes outstanding refresh tokens; other sessions end</td></tr><tr><td>Unaudited lifecycle event</td><td>verify/reset with no audit row</td><td>verify, reset-request, reset-confirm, and revocation are audited via FR-API-004</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR is small and entirely about discipline: every verification and reset token is single-use, expiring, hashed at rest, and invalidated-on-reissue, and both request endpoints are account-enumeration-safe exactly as FR-AUTH-001's login is. The one behavior users expect and implementations often miss is revoking refresh tokens on a successful password reset, so a reset actually ends the compromised sessions. It extends the same <code>tamthuc_auth</code> package (adding <code>verification</code>, <code>reset</code>, <code>email</code> modules and the token-store migration), keeps the email provider behind a seam so the security logic is testable without real mail, and audits every lifecycle event through FR-API-004.</p>";
