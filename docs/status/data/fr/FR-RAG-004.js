window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-RAG-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR is the runtime human-in-the-loop for interpretation: a review queue that intercepts AI interpretations flagged important or low-confidence, holds them for an expert to approve or reject with a reason, and gates them before they reach the user. It is the operational form of the third anti-hallucination layer (human-in-the-loop) that FR-RAG-003 declares (Claude-06 s4.3; strategy 4.4). It consumes the <code>requires_human_review</code> flag and the <code>AIDisclosure</code> that FR-RAG-003 produces, and it owns the review policy that FR-RAG-003 consults to set that flag.</p>\n<p>When an <code>Interpretation</code> has <code>requires_human_review = false</code>, the gate SHALL release it, set <code>disclosure.review_status = \"not_required\"</code>, and let it reach the user unchanged. When <code>requires_human_review = true</code>, the gate SHALL NOT release the interpretation to the user; it SHALL enqueue a <code>ReviewTicket</code>, set <code>disclosure.review_status = \"pending\"</code>, and return a withheld view that carries no confident uncited claims. A reviewer holding the review role (FR-AUTH-002) SHALL approve or reject each ticket with a non-empty <code>reason</code>; the decision SHALL be recorded immutably with the reviewer id and timestamp (an audit row). On approve, the gate SHALL set <code>review_status = \"approved\"</code> and release the interpretation (with the reviewer's optional edits to the interpretation text and recommendations only); on reject, it SHALL set <code>review_status = \"rejected\"</code>, withhold the interpretation, and surface a safe under-review message instead of the raw AI text.</p>\n<p>The gate SHALL own the <code>ReviewPolicy</code> - the confidence threshold and the high-stakes question-type set - that FR-RAG-003's guard reads to decide <code>requires_human_review</code>, so the flag and the queue share one policy. The gate SHALL NOT touch the la so envelope: it operates only on the <code>Interpretation</code> object and the review record, and it SHALL NEVER write <code>ban</code>, <code>cach_cuc</code>, <code>lich_phap</code>, or <code>co_truong_phai</code> (strategy 4.3). A reviewer edit SHALL change only the interpretation text, recommendations, and citations shown, never the chart the engine cast, and the <code>AIDisclosure</code> SHALL be preserved with only <code>review_status</code> updated. Every interpretation that reaches the user SHALL carry an <code>AIDisclosure</code> whose <code>review_status</code> is one of <code>not_required</code> or <code>approved</code>; a <code>pending</code> or <code>rejected</code> interpretation SHALL never be presented as a finished reading.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The three anti-hallucination layers only work if the third one is real: citation-required and retrieval-only keep the model honest about its sources, but for a consequential judgment a person still has to be able to catch a reading that is technically cited yet wrong, misleading, or over-confident for the stakes (Claude-06 s4.3). A HumanReviewGate is where that person stands. Without it, \"human-in-the-loop\" is a slogan; with it, an interpretation flagged low-confidence or high-stakes physically cannot reach the user until a reviewer has looked at it and said yes. For a heritage subject under VN law, that is also the difference between decision-support education and unsupervised fortune-telling for profit (strategy 7, RISK-3, RISK-4): the gate is the control that keeps consequential output from going out unattended.</p>\n<p>Owning the policy here, rather than scattering thresholds across the interpreter, is what keeps the flag and the queue in agreement. FR-RAG-003 sets <code>requires_human_review</code> by asking this FR's policy; this FR then queues exactly the interpretations it flagged, so there is no case where the interpreter thinks a reading is fine but the gate would have caught it, or vice versa. The withheld view is a small but load-bearing detail: while a reading is pending or after it is rejected, the user must not see the raw AI text, because that text is precisely what has not yet been trusted; showing a neutral under-review state instead is the honest interface for \"we are not confident enough to show this yet\". Keeping the gate off the la so envelope is the same architectural boundary the whole platform rests on - a reviewer improves the words, never the chart, because the chart is the deterministic engine's and is reproducible from its inputs and flags (strategy 4.3).</p>\n<h2>§3 - Contract (policy, models, queue, gate)</h2>\n<h3>Review policy (<code>tamthuc_rag/review/policy.py</code>)</h3>\n<pre><code class=\"language-python\">class ReviewPolicy(BaseModel):\n    confidence_threshold: float = 0.55        # below this -&gt; requires review\n    high_stakes_question_types: set[str] = {  # always review regardless of confidence\n        \"suc_khoe\", \"phap_ly\", \"tai_chinh_lon\", \"hon_nhan_dai_su\" }\n    def requires_review(self, confidence: float, question_type: str) -&gt; bool:\n        return confidence &lt; self.confidence_threshold or question_type in self.high_stakes_question_types</code></pre>\n<p>FR-RAG-003's <code>guard.enforce</code> calls <code>ReviewPolicy.requires_review(...)</code> to set <code>requires_human_review</code>; this FR is the single owner of the policy so the flag and the queue cannot disagree.</p>\n<h3>Ticket and outcome (<code>tamthuc_rag/review/models.py</code>)</h3>\n<pre><code class=\"language-python\">class ReviewStatus(str, Enum):\n    not_required = \"not_required\"; pending = \"pending\"; approved = \"approved\"; rejected = \"rejected\"\n\nclass ReviewTicket(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    id: str\n    query_id: str\n    interpretation_id: str              # the persisted interpretation (FR-API-004)\n    persona_level: str\n    confidence: float\n    reason_flagged: str                 # \"low_confidence\" | \"high_stakes:&lt;type&gt;\"\n    status: ReviewStatus\n    interpretation: dict                # snapshot for the reviewer (FR-RAG-003 Interpretation)\n    disclosure: dict                    # the AIDisclosure snapshot\n    created_at: datetime\n\nclass ReviewOutcome(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    ticket_id: str\n    reviewer_id: str                    # holds the review role (FR-AUTH-002)\n    decision: Literal[\"approve\", \"reject\"]\n    reason: str                         # required, non-empty\n    edited_interpretation: dict | None  # optional reviewer correction (text/recs/citations only)\n    decided_at: datetime</code></pre>\n<h3>Queue (<code>tamthuc_rag/review/queue.py</code>)</h3>\n<pre><code class=\"language-python\">class ReviewQueue(Protocol):\n    def enqueue(self, interp: Interpretation, query_id: str, reason: str) -&gt; ReviewTicket: ...\n    def pending(self, limit: int = 50) -&gt; list[ReviewTicket]: ...\n    def get(self, ticket_id: str) -&gt; ReviewTicket | None: ...\n    def decide(self, ticket_id: str, reviewer_id: str,\n               decision: Literal[\"approve\", \"reject\"], reason: str,\n               edited_interpretation: dict | None = None) -&gt; ReviewTicket: ...\n\nclass RelationalReviewQueue:   # default: review_ticket + review_outcome in the shared Postgres\n    ...\nclass InMemoryReviewQueue:     # tests\n    ...</code></pre>\n<p><code>decide</code> SHALL require the review role and a non-empty reason, update <code>review_status</code>, persist the outcome immutably, and write an audit row.</p>\n<h3>Gate (<code>tamthuc_rag/review/gate.py</code>)</h3>\n<pre><code class=\"language-python\">class GateResult(BaseModel):\n    released: bool\n    interpretation: dict | None    # the releasable interpretation when released; else None\n    ticket_id: str | None          # set when withheld for review\n    withheld_view: dict | None     # safe under-review payload for the UI when not released\n\ndef gate(interp: Interpretation, query_id: str, queue: ReviewQueue) -&gt; GateResult:\n    if not interp.requires_human_review:\n        interp.disclosure.review_status = \"not_required\"\n        return GateResult(released=True, interpretation=interp.model_dump())\n    ticket = queue.enqueue(interp, query_id, reason=flag_reason(interp))\n    interp.disclosure.review_status = \"pending\"\n    return GateResult(released=False, ticket_id=ticket.id, withheld_view=under_review_view(interp))\n\ndef apply_outcome(ticket: ReviewTicket, outcome: ReviewOutcome) -&gt; GateResult:\n    # approve: review_status=approved; release (with edited text/recs/citations if provided); audit\n    # reject:  review_status=rejected; withhold; return the safe under-review view; audit</code></pre>\n<p><code>under_review_view</code> strips confident claims and shows a neutral \"awaiting expert review\" state; it is what FR-WEB-001's <code>HumanReviewGate</code> component renders. <code>apply_outcome</code> mutates only the interpretation's text, recommendations, and citations on an approved edit; it never mutates the chart fields, and it refreshes only <code>disclosure.review_status</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>An interpretation with <code>requires_human_review = false</code> is released unchanged with <code>review_status = \"not_required\"</code>.</li><li>An interpretation with <code>requires_human_review = true</code> is withheld: a <code>ReviewTicket</code> is enqueued, <code>review_status = \"pending\"</code>, and the returned <code>withheld_view</code> contains no confident uncited claims (not the raw AI text).</li><li><code>decide</code> requires the review role and a non-empty reason; a decision by a non-reviewer or with an empty reason is rejected for both approve and reject.</li><li>On approve, <code>review_status = \"approved\"</code> and the interpretation is released; a reviewer edit changes only interpretation text / recommendations / citations, and a byte-equality check shows the la so envelope and the chart <code>cach_cuc</code> are untouched.</li><li>On reject, <code>review_status = \"rejected\"</code>, the interpretation is withheld, and the user-facing result is the safe under-review view, never the rejected AI text.</li><li><code>ReviewPolicy.requires_review</code> drives FR-RAG-003's flag: a low-confidence or high-stakes case flags and queues; a confident low-stakes case does not - the flag and the queue agree by construction.</li><li>Every decision writes an immutable audit row (reviewer, ticket, decision, reason, timestamp); no interpretation with <code>review_status</code> in {<code>pending</code>, <code>rejected</code>} is ever presented as finished.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_review_gate.py</code>: the not-required release path; the pending-withhold path with the stripped <code>withheld_view</code>; the role guard and required-reason guard; approve-with-edit asserting the la so and <code>cach_cuc</code> are byte-identical before and after (read-only invariant); reject producing the safe view; the policy-drives-flag parity with a FR-RAG-003 stub; audit-row-per-decision.</li><li>Contract: <code>ReviewTicket</code> / <code>ReviewOutcome</code> validate against <code>docs/contracts/review-ticket.schema.json</code>; Pydantic/JSON-Schema parity in CI; <code>AIDisclosure.review_status</code> transitions are asserted (<code>pending -&gt; approved | rejected</code>, <code>not_required</code> terminal).</li><li>Backend parity: run the lifecycle through the <code>RelationalReviewQueue</code> (test Postgres or fake) and the <code>InMemoryReviewQueue</code>; assert identical <code>pending</code> / <code>get</code> results.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_rag</code>, <code>pytest packages/tamthuc_rag</code> (default in-memory; the Postgres path behind a marker).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>review/policy.py</code>: the <code>ReviewPolicy</code> (threshold + high-stakes types) that FR-RAG-003 consults; a default config plus an override hook.</li><li><code>review/models.py</code>: <code>ReviewTicket</code>, <code>ReviewOutcome</code>, <code>ReviewStatus</code>; author <code>docs/contracts/review-ticket.schema.json</code>.</li><li><code>review/queue.py</code>: the <code>ReviewQueue</code> protocol, <code>RelationalReviewQueue</code>, <code>InMemoryReviewQueue</code>; <code>migrations/0001_review_queue.sql</code> (<code>review_ticket</code> + immutable <code>review_outcome</code> + audit).</li><li><code>review/gate.py</code>: <code>gate</code> (release vs withhold), <code>apply_outcome</code> (approve/reject), <code>under_review_view</code> (safe payload), the read-only-envelope guard on reviewer edits.</li><li>Wire the role check to FR-AUTH-002 and the <code>withheld_view</code> shape to FR-WEB-001's <code>HumanReviewGate</code> component.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-RAG-003 (which produces the <code>Interpretation</code>, the <code>requires_human_review</code> flag, and the <code>AIDisclosure</code> this gate consumes; this FR in turn owns the <code>ReviewPolicy</code> FR-RAG-003 reads). Uses FR-AUTH-002 for the reviewer role, FR-PLAT-003 for the <code>review_ticket</code> / <code>review_outcome</code> tables and audit rows (soft edge; applied through the PLAT runner), and FR-API-004 for the persisted interpretation the ticket references. Surfaced by FR-WEB-001 (the <code>HumanReviewGate</code> component) and FR-WEB-003 (the results screen, which shows the under-review state instead of a pending reading). Blocks nothing in the catalog, but it is the runtime gate every AI-facing surface passes flagged output through. Sibling to FR-KB-004: same human-in-the-loop principle at a different stage (AI interpretations at query time here, knowledge content at curation time there).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// a ReviewTicket for a high-stakes flagged interpretation\n{ \"id\": \"tkt_00042\", \"query_id\": \"q_9931\", \"interpretation_id\": \"interp_5521\",\n  \"persona_level\": \"beginner\", \"confidence\": 0.48, \"reason_flagged\": \"high_stakes:phap_ly\",\n  \"status\": \"pending\", \"created_at\": \"2026-07-08T12:05:00Z\",\n  \"interpretation\": { \"beginner_interpretation\": \"...\", \"citations\": [\"yba_dieu_012\"], \"confidence\": 0.48 },\n  \"disclosure\": { \"is_ai_generated\": true, \"model\": \"gpt-4o-mini\", \"prompt_version\": \"sys-1.0.0\",\n    \"retrieved_citation_ids\": [\"yba_dieu_012\"], \"review_status\": \"pending\" } }</code></pre>\n<pre><code class=\"language-json\">// the reviewer's approve outcome (reason required; edits touch text/recs only)\n{ \"ticket_id\": \"tkt_00042\", \"reviewer_id\": \"master_tue\", \"decision\": \"approve\",\n  \"reason\": \"Cited grounding is correct; softened the legal-adjacent phrasing to decision-support framing.\",\n  \"edited_interpretation\": { \"beginner_interpretation\": \"...softened...\", \"citations\": [\"yba_dieu_012\"] },\n  \"decided_at\": \"2026-07-08T12:40:00Z\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>The exact confidence threshold and the high-stakes question-type set. Default: a conservative threshold (0.55) plus a health/legal/large-financial/major-life allowlist; tune from review outcomes and FR-RAG-006 evidence, keeping the policy in one place so FR-RAG-003's flag follows.</li><li>What the user sees while pending: a neutral \"awaiting expert review\" state versus a partial reading. Default: neutral under-review view with no confident claims; a partial cited summary only if review latency proves too slow, and only content that already passed the citation guard.</li><li>Whether reject offers the user a rule-based fallback reading. Default: show the safe under-review message at MVP; wiring the FR-RAG-007 degraded reading in on reject is a follow-up once both exist.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Flagged reading leaks</td><td>a <code>requires_human_review</code> interpretation reaches the user</td><td>gate withholds; only <code>not_required</code> or <code>approved</code> interpretations are presented</td></tr><tr><td>Reasonless decision</td><td>approve/reject with an empty reason</td><td>rejected; a non-empty reason is required for both outcomes</td></tr><tr><td>Unauthorized reviewer</td><td>a non-review role decides</td><td>rejected; only the FR-AUTH-002 review role may decide</td></tr><tr><td>Reviewer edits the chart</td><td>an edit mutates <code>ban</code>/<code>cach_cuc</code></td><td>impossible: edits touch only interpretation text/recs/citations; byte-equality test on the envelope</td></tr><tr><td>Flag/queue disagreement</td><td>interpreter flags differ from what is queued</td><td>one <code>ReviewPolicy</code> owned here drives both; parity test</td></tr><tr><td>Raw rejected text shown</td><td>rejected AI text surfaced to the user</td><td>rejected -&gt; withheld + safe view; the raw text never renders</td></tr><tr><td>Missing/rewritten disclosure</td><td>output without a truthful <code>review_status</code></td><td>gate always sets <code>review_status</code>; only that field changes; schema requires the disclosure</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR makes the third anti-hallucination layer operational, and its whole reason to exist is that a consequential reading must not reach a user unattended. Two invariants are non-negotiable and mirror FR-RAG-003: the gate reads the interpretation and never writes the la so, and no interpretation reaches the user without a truthful <code>review_status</code> on its <code>AIDisclosure</code>. Keep the <code>ReviewPolicy</code> here as the single source so the flag FR-RAG-003 sets and the queue this FR runs can never drift. It is a deliberate sibling of FR-KB-004, not a merge - different objects, different review focus, same principle (strategy 4.4). The package <code>tamthuc_rag</code> is shared with FR-RAG-001/002/003/005/006/007; this FR adds the <code>review/</code> module, so the RAG branch stays one installable, mypy-clean unit.</p>";
