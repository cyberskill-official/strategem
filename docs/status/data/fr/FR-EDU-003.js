window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-EDU-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR builds the bilingual classical library: the learning flow's search-and-read surface over the FR-KB-003 three-layer classical-text store, so a learner reads the original Han (chu Han) beside the bach thoai gloss and the translation, always with a citation, and reaches the primary source rather than a second-hand summary (Claude-07 s3.3, s4.3).</p>\n<p>The module SHALL present each retrieved unit as a <code>Passage</code> carrying all three layers - <code>han</code>, <code>bach_thoai</code>, <code>dich</code> - never dropping the original Han (strategy 7, the cultural-respect rule). It SHALL expose search over the store (<code>searchLibrary</code>) and single-passage retrieval by id (<code>getPassage</code>), both backed by a library endpoint over the FR-KB-003 store; this FR owns the client, the view, and the request / result contract, not the store or the retrieval engine (those are FR-KB-003 and the RAG retriever). It SHALL resolve a citation id (<code>resolveCitation</code>): given the citation string a pattern (FR-RULE-001) or an AI interpretation (FR-RAG-003) carries, it SHALL deep-link to the exact passage, so a citation shown anywhere in the product is a live link into the source text.</p>\n<p>Every rendered passage SHALL show its citation (work, and locator where present) and SHALL keep the Han visible at all breakpoints (the dau chong / diacritic-clipping rule from the design system applies to the mixed Han + Vietnamese text). The surface SHALL render inside the FR-WEB-001 shell using Design System v1.3.0 components with no new tokens, and SHALL be i18n-ready (FR-WEB-006) for its own chrome while the three text layers are content, not translatable labels.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The library is where the product keeps faith with its sources (Claude-07 s4.3, strategy 7). Tam Thuc is the intellectual heritage of East Asian culture; a responsible product cites the classical text, keeps the original Han beside transliteration and translation, and anchors to real scholarship rather than paraphrasing it away. Presenting three layers at once - Han, bach thoai, translation - lets a beginner read in their own language while a serious learner checks the gloss against the original character, which is exactly the \"reach the source\" value the training framework calls out (Claude-07 s3.3: kho nguyen van, bach thoai, va dich, co trich dan, de nguoi hoc tra tan goc).</p>\n<p>Making a citation id resolve to a passage is what closes the chain the rest of the platform builds. A pattern row carries citation ids into the corpus (FR-RULE-001); an AI interpretation cites the passages it stands on (FR-RAG-003, strategy 4.4). Until those citations are clickable, they are just strings; this FR is where <code>yba_thien_can_khac_ung_12</code> becomes a readable Han-plus-translation passage the learner can open. That turns citation-required interpretation from a claim into something a learner can verify, which is both the anti-hallucination rule (strategy 4.4) and the cultural-respect rule (strategy 7) seen from the reading surface - the same principle from two sides.</p>\n<p>Keeping this FR to the client, the view, and the contract - and letting FR-KB-003 own the store and chunking - is the same layering as the rest of EDU: the heavy retrieval lives in the Python / KB layer, the TypeScript surface reads it through a stable endpoint. The three text layers are content, so they are never run through the label-translation path; only the library's own chrome is i18n.</p>\n<h2>§3 - Contract (types and API)</h2>\n<h3>Types (<code>apps/web/src/features/edu/library/types.ts</code>)</h3>\n<pre><code class=\"language-ts\">export type Layer = \"han\" | \"bach_thoai\" | \"dich\";\n\nexport interface Citation {\n  id: string;              // e.g. \"yba_thien_can_khac_ung_12\"; resolves into the FR-KB-003 corpus\n  work: string;            // e.g. \"Yen Ba Dieu Tau Ca\"\n  work_han?: string;       // 煙波釣叟歌 (original title preserved)\n  locator?: string;        // chapter / verse / line range, when the store has one\n}\n\n// One retrieved unit of classical text, three layers side by side. Han is never dropped.\nexport interface Passage {\n  id: string;              // chunk id in the FR-KB-003 store\n  work: string;\n  work_han?: string;\n  han: string;             // original Han (always present)\n  bach_thoai: string;      // vernacular gloss\n  dich: string;            // translation (VN; EN mirror via FR-WEB-006 later)\n  citation: Citation;\n  system?: \"luc_nham\" | \"ky_mon\" | \"thai_at\" | \"chung\";  // which system the passage belongs to\n}\n\nexport interface LibrarySearchRequest {\n  q: string;               // query text\n  work?: string;           // filter to a work\n  layer?: Layer;           // which layer to search (default: all)\n  system?: Passage[\"system\"];\n  limit?: number;          // default 20\n}\n\nexport interface LibrarySearchResult {\n  passages: Passage[];\n  total: number;\n  query: string;\n}</code></pre>\n<h3>Library client (<code>apps/web/src/features/edu/library/api.ts</code>)</h3>\n<pre><code class=\"language-ts\">// Search the three-layer store (backed by FR-KB-003 through a library endpoint).\nexport function searchLibrary(req: LibrarySearchRequest): Promise&lt;LibrarySearchResult&gt;;\n\n// Fetch one passage by its chunk id - the deep-link target for a citation.\nexport function getPassage(id: string): Promise&lt;Passage&gt;;\n\n// Resolve a citation id (from a pattern or an AI interpretation) to its passage.\nexport function resolveCitation(c: Citation): Promise&lt;Passage&gt;;   // getPassage(c.id) with work fallback</code></pre>\n<p>The three functions call the library endpoint; this FR does not implement retrieval (FR-KB-003 owns the store; the endpoint may reuse the FR-RAG-002 retriever's index). <code>resolveCitation</code> is <code>getPassage(c.id)</code> with a graceful fallback to a <code>work</code> + <code>locator</code> lookup when an id is not directly addressable, so a citation never dead-ends.</p>\n<h3>Reader view (<code>apps/web/src/features/edu/library/LibraryReader.tsx</code>)</h3>\n<p>A three-column (or stacked, on narrow screens) passage reader: Han, bach thoai, dich, with the citation footer always shown. The Han column keeps its own line-height so mixed Han + Vietnamese diacritics are not clipped (the design-system dau chong rule). A passage opened via <code>resolveCitation</code> scrolls to and highlights the cited chunk.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A <code>Passage</code> always carries non-empty <code>han</code>, <code>bach_thoai</code>, and <code>dich</code>, and its <code>citation</code>; a render test asserts the Han layer is present and visible (never dropped) at the app's breakpoints.</li><li><code>searchLibrary</code> returns a <code>LibrarySearchResult</code> whose passages match the query and honor the <code>work</code> / <code>layer</code> / <code>system</code> filters; a test asserts filtering narrows results and that <code>total</code> reflects the unfiltered match count.</li><li><code>getPassage(id)</code> returns the passage for a known chunk id and a typed not-found for an unknown one (never a partial or empty passage).</li><li><code>resolveCitation</code> turns a pattern / interpretation citation id (for example <code>yba_thien_can_khac_ung_12</code>) into the correct passage, and falls back to a <code>work</code> + <code>locator</code> lookup when the id is not directly addressable.</li><li>The reader renders the three layers side by side with the citation footer, keeps the Han un-clipped (dau chong test), and uses DS v1.3.0 components with no new tokens.</li><li>The library chrome is i18n-ready (FR-WEB-006) while the three text layers are treated as content, not passed through label translation.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>Vitest (<code>library.test.ts</code>): a fixture corpus of a few passages (Han + bach thoai + dich + citation) mirroring the FR-KB-003 shape. Search-and-filter cases; <code>getPassage</code> hit and typed miss; <code>resolveCitation</code> for a real pattern citation id (shared with the FR-RULE-001 seed) and the <code>work</code> + <code>locator</code> fallback.</li><li>Contract test: the <code>Passage</code> shape matches the FR-KB-003 store's export (field parity), so a store change surfaces here.</li><li>Render test: <code>LibraryReader</code> shows all three layers and the citation; a dau chong test asserts the Han and Vietnamese diacritics are not clipped at 100 / 200 / 400 percent, light and dark.</li><li>Deep-link test: a passage opened from a citation scrolls to and highlights the cited chunk.</li><li>Gates: <code>pnpm test</code>, <code>tsc --noEmit</code>, <code>eslint</code> (the WEB toolchain).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>types.ts</code>: <code>Layer</code>, <code>Citation</code>, <code>Passage</code>, <code>LibrarySearchRequest</code>, <code>LibrarySearchResult</code>.</li><li><code>api.ts</code>: <code>searchLibrary</code>, <code>getPassage</code>, <code>resolveCitation</code> over the library endpoint (FR-KB-003-backed).</li><li><code>search.ts</code>: query-state helpers (filters, pagination) for the reader.</li><li><code>LibraryReader.tsx</code>: the three-layer reader in the WEB-001 shell, citation footer, deep-link highlight, dau chong-safe Han column.</li><li><code>library.test.ts</code>: the fixture corpus and the acceptance cases above.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-KB-003 (the three-layer classical-text store - Han / bach thoai / dich - and its chunking); this FR is the search-and-cite surface over it, not the store. Consumes citation ids emitted by FR-RULE-001 patterns and FR-RAG-003 interpretations (it is where those citations resolve to readable passages). Renders in the FR-WEB-001 shell and is i18n-ready via FR-WEB-006. Related to FR-EDU-001 (the curriculum links out to library passages) and FR-EDU-002 (a wrong practice step can link to the relevant source passage).</p>\n<h2>§8 - Example payloads</h2>\n<p>A search result and a resolved citation:</p>\n<pre><code class=\"language-ts\">await searchLibrary({ q: \"青龍返首\", system: \"ky_mon\", limit: 5 })\n// -&gt;\n{\n  query: \"青龍返首\",\n  total: 2,\n  passages: [\n    {\n      id: \"yba_thien_can_khac_ung_12\",\n      work: \"Yen Ba Dieu Tau Ca\", work_han: \"煙波釣叟歌\",\n      han: \"戊加丙兮青龍返首 ...\",\n      bach_thoai: \"戊 gia Binh, goi la Thanh Long hoi dau ...\",\n      dich: \"When 戊 is over 丙, the Green Dragon turns its head - a great-fortune configuration for major undertakings.\",\n      citation: { id: \"yba_thien_can_khac_ung_12\", work: \"Yen Ba Dieu Tau Ca\", work_han: \"煙波釣叟歌\", locator: \"thien can khac ung, cau 12\" },\n      system: \"ky_mon\",\n    },\n  ],\n}</code></pre>\n<pre><code class=\"language-ts\">// a pattern's citation id (FR-RULE-001) becomes a readable passage\nawait resolveCitation({ id: \"yba_thien_can_khac_ung_12\", work: \"Yen Ba Dieu Tau Ca\" });\n// -&gt; the Passage above</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Search backend: does the library reuse the FR-RAG-002 vector index or a lighter lexical index over the FR-KB-003 store? Default: reuse the FR-RAG-002 retriever's index through the library endpoint so Han, bach thoai, and translation are all searchable; a dedicated index is a later optimization if learner search differs from interpretation retrieval.</li><li>Layer for EN users: the <code>dich</code> layer is Vietnamese at MVP; the EN translation is deferred to FR-WEB-006. Default: keep <code>dich</code> as authored and add an <code>en</code> layer through the i18n content pipeline, never machine-translating the classical text inline.</li><li>Locator granularity: some works have verse / line locators, others only a work-level citation. Default: show whatever locator the store provides and fall back to work-level; do not fabricate a locator.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Han dropped</td><td>a passage rendered without the original characters</td><td>render test fails; the <code>han</code> layer is mandatory and always shown (strategy 7)</td></tr><tr><td>Dead citation</td><td>a citation id resolves to nothing</td><td><code>resolveCitation</code> falls back to work + locator; a truly missing source is a typed not-found, surfaced, not silent</td></tr><tr><td>Diacritic clipping</td><td>mixed Han + Vietnamese clips at zoom</td><td>dau chong test fails at 100 / 200 / 400 percent</td></tr><tr><td>Store drift</td><td>FR-KB-003 changes the passage shape</td><td><code>Passage</code> field-parity contract test fails</td></tr><tr><td>Machine-translated source</td><td>classical text run through label i18n</td><td>text layers are content, not labels; a test asserts the three layers bypass the translation path</td></tr><tr><td>New design token</td><td>reader adds a token</td><td>snapshot/lint asserts DS v1.3.0 tokens only</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the surface that lets a learner reach the source (Claude-07 s3.3): original Han beside bach thoai and translation, always cited. It is the cultural-respect rule (strategy 7) and the anti-hallucination rule (strategy 4.4) meeting on the reading surface - keep the Han, keep the citation, and make every citation a live link into the passage it names. This FR owns the client, the reader, and the contract; FR-KB-003 owns the store and the chunking. Language is TypeScript (DEC-2) rendering in the WEB-001 shell; the heavy retrieval stays in the KB / RAG layer behind the library endpoint. refs Claude-07 s3.3, s4.3.</p>";
