window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLAT-007"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR is the security-hardening pass: a STRIDE-driven control set, transport and secret hygiene, dependency scanning, an OWASP Top 10 review, a penetration-test cadence, and an incident-response playbook with regulatory breach notification. It hardens the system the FR-PLAT-004 pipeline ships and the FR-AUTH-002 tiers govern. It owns the threat model, the control mapping, and the operational security artifacts; it does NOT re-implement auth (FR-AUTH-001), tiers (FR-AUTH-002), rate limiting (FR-API-003), or audit (FR-API-004) - it composes them into a defensible whole and adds the missing controls.</p>\n<p>The control set SHALL be derived from a STRIDE threat model and SHALL map each threat class to a control: Spoofing to JWT authentication plus rate limiting; Tampering to input validation plus signed calculations; Repudiation to audit logging; Information disclosure to AES-256 encryption at rest plus least-privilege access; Denial of service to rate limiting plus autoscaling plus a WAF; Elevation of privilege to RBAC. All personal data SHALL be protected in transit with TLS 1.3. Secrets SHALL come from a secret manager or the environment, never from the repository or a config committed to source. Dependencies SHALL be scanned continuously (Snyk, Dependabot, Trivy) and a high or critical finding SHALL fail the FR-PLAT-004 build, overridable only by a reviewable allowlist entry.</p>\n<p>The codebase SHALL be reviewed against the OWASP Top 10 with a checked-in checklist, and a penetration test SHALL be run at least annually and after any significant change. An incident-response playbook SHALL define detection, containment, eradication, recovery, and notification, and SHALL encode the regulatory clocks: GDPR 72-hour breach notification and the VN PDPD notification obligation (FR-LEGAL-002). Oracle libraries SHALL remain CI-only references, never embedded runtime dependencies, so their licenses and code never enter the shipped artifact (RISK-6).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Security here is not a generic checklist bolted on late; it is the technical guarantee behind the product's legal footing (strategy 7). The data at stake is birth data and question text - sensitive personal data under VN PDPD and GDPR (RISK-5) - so a breach is simultaneously a privacy harm and a legal event with a statutory clock. Driving the control set from STRIDE rather than from a vibe means every threat class has a named, testable control and nothing important is left implicit: the mapping is the contract. Most of the controls already exist in other FRs (JWT in AUTH-001, RBAC in AUTH-002, rate limiting in API-003, audit in API-004, at-rest crypto in AUTH-001); this FR's job is to prove they compose into full STRIDE coverage and to add the pieces no single feature FR owns - TLS 1.3, secret custody, dependency scanning, WAF, the OWASP review, the pentest cadence, and the incident playbook.</p>\n<p>The incident-response playbook with the 72-hour clock is the control that turns a bad day into a survivable one. A breach with no rehearsed playbook is a breach plus a missed regulatory deadline plus a scramble; a breach with a playbook is a defined sequence with the notification obligations already written down. Keeping secrets out of the repo and failing the build on a high/critical dependency finding closes the two most common real-world holes (leaked credentials and known-vulnerable dependencies) at the pipeline, where FR-PLAT-004 already has the teeth. Keeping the oracle libraries as CI-only references (RISK-6) means their licenses never contaminate the commercial artifact - a legal control expressed as a build rule.</p>\n<h2>§3 - Contract (STRIDE map / controls / policies)</h2>\n<h3>STRIDE control mapping (<code>docs/security/stride-threat-model.md</code>)</h3>\n<table><thead><tr><th>Threat</th><th>Control</th><th>Owner / where enforced</th></tr></thead><tbody><tr><td>Spoofing</td><td>JWT authentication + rate limiting</td><td>FR-AUTH-001 tokens; FR-API-003 limiter</td></tr><tr><td>Tampering</td><td>input validation + signed calculations</td><td><code>security/validation.py</code>, <code>security/signing.py</code></td></tr><tr><td>Repudiation</td><td>audit logging (append-only)</td><td>FR-API-004 <code>audit_logs</code></td></tr><tr><td>Information disclosure</td><td>AES-256 at rest + least-privilege access</td><td>FR-AUTH-001 crypto; FR-PLAT-003 RLS + DB grants</td></tr><tr><td>Denial of service</td><td>rate limiting + autoscaling + WAF</td><td>FR-API-003; FR-PLAT-010 HPA; <code>deploy/security/waf-rules.md</code></td></tr><tr><td>Elevation of privilege</td><td>RBAC</td><td>FR-AUTH-002 roles/capabilities</td></tr></tbody></table>\n<p>Each row SHALL have evidence: a test, a policy file, or a pipeline gate. A threat class with no evidenced control is a hardening gap.</p>\n<h3>Transport, secrets, headers</h3>\n<ul><li>TLS 1.3 in transit for all personal data, terminated at the edge (<code>deploy/security/tls-config.md</code>); HTTP redirects to HTTPS; HSTS set.</li><li>Secrets from a secret manager / environment only (<code>deploy/security/secrets.md</code>); a committed-secret scan runs in the pipeline; no secret in the repo or an image layer.</li><li>Security response headers (<code>security/headers.py</code>): HSTS, <code>X-Content-Type-Options</code>, <code>X-Frame-Options</code>/CSP, <code>Referrer-Policy</code>.</li></ul>\n<h3>Signed calculations (<code>security/signing.py</code>)</h3>\n<p>An engine cast result carries an integrity signature so a tampered chart in transit or at rest is detectable; the la so envelope's <code>provenance</code> is covered by a signature the API verifies before trusting a stored/forwarded chart (anti-tampering for the deterministic branch).</p>\n<h3>Dependency scanning (<code>.github/workflows/dependency-scan.yml</code>)</h3>\n<p>Snyk + Dependabot + Trivy across the Rust/Python/Node manifests and the built images; a HIGH/CRITICAL finding fails the FR-PLAT-004 build; suppression only via a checked-in allowlist with owner + reason.</p>\n<h3>OWASP + pentest + incident response</h3>\n<p><code>docs/security/owasp-top10-checklist.md</code> (reviewed each release), <code>docs/security/pentest-schedule.md</code> (annual + post-significant-change), <code>docs/security/incident-response-playbook.md</code> (detect/contain/eradicate/recover/notify, with GDPR 72h and VN PDPD notification steps).</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The STRIDE threat model maps every threat class to a named control, and each control has evidence (a test, a policy file, or a pipeline gate); a threat class with no evidenced control fails the review.</li><li>All personal-data traffic is TLS 1.3; a probe of a plaintext or downgraded connection is refused, and HSTS + the security headers are present on responses.</li><li>No secret is readable from the repository or an image layer; the committed-secret scan passes and a planted test secret is caught.</li><li>A dependency with a known HIGH/CRITICAL CVE fails the dependency-scan workflow and the FR-PLAT-004 build; adding an allowlist entry with owner + reason passes it (proof the gate bites and the override is explicit).</li><li>Input validation rejects malformed/oversized/injection-shaped input with the FR-API-001 error envelope; a signed calculation detects a tampered chart (the signature check fails on a mutated envelope).</li><li>The OWASP Top 10 checklist is complete for the release; the pentest schedule is defined (annual + post-change); the incident-response playbook encodes the GDPR 72h and VN PDPD notification clocks (FR-LEGAL-002).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_security.py</code>: security headers present and correct; input-validation rejection cases; the signed-calculation tamper-detection test; a least-privilege check that a non-admin principal cannot reach an admin control.</li><li>A pipeline check: the dependency-scan workflow fails on a planted known-vulnerable dependency and passes with an allowlist entry; the committed-secret scan catches a planted secret.</li><li>A TLS probe (in staging) confirms TLS 1.3 and refuses a downgraded/plaintext connection; HSTS asserted.</li><li>A tabletop review of the incident-response playbook confirms the detect-&gt;notify sequence and the 72h / PDPD clocks; the STRIDE evidence table is checked row-by-row.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_api</code>, <code>pytest packages/tamthuc_api</code>; the security workflows run in the FR-PLAT-004 pipeline.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Author <code>docs/security/stride-threat-model.md</code> mapping each STRIDE class to its control and evidence; identify the gaps this FR must fill.</li><li><code>security/headers.py</code>, <code>security/validation.py</code>, <code>security/signing.py</code>: the response-header middleware, the input-validation layer, and the signed-calculation integrity check.</li><li><code>deploy/security/*</code>: TLS 1.3 config, WAF rules, secret-manager custody notes; wire the committed-secret scan.</li><li><code>.github/workflows/dependency-scan.yml</code>: Snyk + Dependabot + Trivy; fail on HIGH/CRITICAL; the allowlist convention (extends the FR-PLAT-004 security scan).</li><li><code>docs/security/owasp-top10-checklist.md</code>, <code>pentest-schedule.md</code>, <code>incident-response-playbook.md</code> with the GDPR 72h / VN PDPD notification steps.</li><li>Confirm oracle libraries are CI-only (not runtime deps) and record the license review (RISK-6).</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-PLAT-004 (the pipeline that runs the dependency and secret scans and where the build fails on a finding) and FR-AUTH-002 (the RBAC/tier model that is the Elevation-of-privilege and the authorization control this hardens). Composes FR-AUTH-001 (JWT + AES-256 at rest), FR-API-003 (rate limiting as the DoS/Spoofing control), FR-API-004 (audit as the Repudiation control), and FR-PLAT-003 (RLS + least-privilege DB grants). Coordinates with FR-PLAT-010 (autoscaling as a DoS control) and FR-LEGAL-002 (the breach-notification obligations and the consent/retention contracts the playbook references).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-yaml\"># .github/workflows/dependency-scan.yml (abridged) - fail on high/critical\n- uses: aquasecurity/trivy-action@...\n  with: { scan-type: fs, severity: 'HIGH,CRITICAL', exit-code: '1', trivyignores: .trivyignore }\n- uses: snyk/actions/python@...      # + Dependabot alerts enabled on the repo</code></pre>\n<pre><code class=\"language-json\">// incident-response notification obligations (from the playbook)\n{ \"regime\": \"GDPR\", \"clock_hours\": 72, \"trigger\": \"personal data breach likely to risk rights\" }\n{ \"regime\": \"VN_PDPD\", \"action\": \"notify authority + affected data subjects per Nghi dinh 13/2023\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>WAF choice: a managed WAF (cloud provider) vs an app-layer WAF (e.g. Coraza/ModSecurity rules). Default: the managed edge WAF at MVP for DoS + common-attack coverage, with app-layer input validation as defense in depth; revisit under FR-PLAT-010.</li><li>Secret manager: cloud KMS/Secrets Manager vs a self-hosted vault. Default: align with the FR-AUTH-001 master-key custody decision (envelope encryption works with either); the birth_data master key and the app secrets share the chosen manager.</li><li>Pentest sourcing: external firm vs internal + external hybrid. Default: an external pentest annually and after any significant change (the schedule is fixed here; the vendor is an ops decision), with continuous automated scanning between engagements.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>STRIDE gap</td><td>a threat class with no evidenced control</td><td>forbidden; every class maps to a control with a test/policy/gate; the review fails on a gap</td></tr><tr><td>Secret in repo/image</td><td>credential committed or baked into a layer</td><td>committed-secret scan fails the build; secrets come from the manager/env only</td></tr><tr><td>Vulnerable dependency ships</td><td>HIGH/CRITICAL finding ignored</td><td>dependency-scan fails the FR-PLAT-004 build; only a reviewable allowlist entry passes it</td></tr><tr><td>Plaintext/downgraded transit</td><td>TLS not enforced</td><td>forbidden; TLS 1.3 required, HSTS set, downgraded/plaintext refused</td></tr><tr><td>Tampered chart trusted</td><td>a mutated envelope accepted</td><td>the signed-calculation check rejects it; provenance integrity verified before trust</td></tr><tr><td>Unrehearsed breach</td><td>no playbook, missed clock</td><td>the incident-response playbook encodes GDPR 72h + VN PDPD notification and is tabletop-tested</td></tr><tr><td>Oracle license contamination</td><td>an oracle lib vendored as a runtime dep</td><td>forbidden; oracles are CI-only references; the license review confirms it (RISK-6)</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR is where the platform's legal footing becomes a set of enforced controls rather than intentions. Hold three things: the STRIDE map is the contract (every threat class -&gt; a control with evidence), secrets and vulnerable dependencies die at the FR-PLAT-004 pipeline (never in the repo, never HIGH/CRITICAL past the gate), and the incident-response playbook carries the real regulatory clocks (GDPR 72h, VN PDPD). Most controls are composed from AUTH/API/PLAT FRs; this FR proves the composition and adds TLS 1.3, secret custody, dependency scanning, the OWASP review, the pentest cadence, and the playbook. The Python controls live in <code>tamthuc_api/security</code>, the operational artifacts under <code>docs/security/</code> and <code>deploy/security/</code>, reflecting the iac/python split. Oracle libraries stay CI-only so their licenses never enter the shipped artifact (RISK-6).</p>";
