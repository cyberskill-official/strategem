window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-KB-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR fills the pattern-as-data ruleset with real content: 150-200 named patterns across the three systems, each expressed as a JSON row conforming to the FR-RULE-001 pattern shape (<code>docs/contracts/knowledge-pattern.schema.json</code>) and each carrying at least one citation into the classical corpus. It does NOT define the pattern schema (FR-RULE-001) nor the condition DSL grammar (FR-RULE-002); it authors data against them. The same 150-200 rows SHALL double as the interpretation-quality validation dataset named in strategy RISK-9.</p>\n<p>The seed SHALL be language-neutral canonical data under <code>data/patterns/&lt;system&gt;.json</code>, one file per system, each a JSON array of pattern rows. Every row SHALL populate <code>id</code>, <code>system</code>, <code>name</code>, <code>name_han</code> where a Han name exists, <code>conditions</code> (a DSL tree valid under FR-RULE-002), <code>polarity</code> (cát/hung/trung), <code>meaning_classical</code>, <code>meaning_modern</code>, <code>citations</code> (non-empty for any <code>active</code> row), <code>version</code> (&gt;= 1), <code>confidence</code>, and <code>status</code>. No <code>active</code> row SHALL ship without a citation, because a pattern with no textual source cannot ground a claim under the anti-hallucination rule (strategy 4.4, FR-RAG-003).</p>\n<p>Coverage SHALL be weighted to the P0 flagship: QiMen SHALL receive the largest share (the cát and hung cách cục of the thập can khắc ứng plus the headline named formations - Thanh Long Hồi Đầu 青龍返首, Phi Điểu Diệt Huyệt 飛鳥跌穴, and the like), with LiuRen khóa thể and TaiYi cách cục seeded to a smaller but representative set so the schema is exercised across all three. Every <code>meaning_modern</code> SHALL be framed as decision support, never as a medical, legal, or financial verdict, and never as a certain future event (strategy 7). Meanings SHALL present schools fairly: where a pattern is read differently by different schools, the difference SHALL be recorded rather than silently resolved.</p>\n<p>The build SHALL validate every row against the FR-RULE-001 validator and the FR-RULE-002 DSL checker before load, and SHALL load <code>active</code> rows into the <code>knowledge_patterns</code> table (FR-PLAT-003). A malformed or uncited <code>active</code> row SHALL fail the whole build with the offending <code>id</code> named; nothing partial loads.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The rule engine is only as good as its ruleset, and both source sets say the ruleset is data an expert authors and reviews, not code (Grok-24 curation, Grok-37 seeding). Seeding 150-200 real patterns is the moment the abstract pattern-as-data machinery meets the actual domain, and it is where quality is won or lost. Concentrating on QiMen first mirrors DEC-4 (QiMen is the P0 flagship) and RISK-7 (build the school-variant-heavy engine's ruleset first so the flag discipline is forced early); LiuRen and TaiYi get a representative sample now and grow in P1 and P2.</p>\n<p>Making the seed double as the validation dataset is the direct mitigation for RISK-9: interpretation quality cannot be measured, so regressions ship silently, unless there is a fixed corpus of known cases to score against. Because each pattern already pairs a machine-checkable <code>conditions</code> tree with a cited <code>meaning_classical</code>, the same 150-200 rows are simultaneously the ruleset the engine detects and the answer key the eval loop (FR-RAG-006) grades interpretation faithfulness against. Authoring them once, cited, and versioned means the ruleset and the test set can never drift apart. The citation-required rule is the anti-hallucination principle at the data layer: the chain source -&gt; pattern -&gt; detected cách cục -&gt; cited interpretation must have no link where meaning appears without a textual source.</p>\n<h2>§3 - Contract (data, build, validation)</h2>\n<h3>Canonical seed files (<code>data/patterns/&lt;system&gt;.json</code>)</h3>\n<p>One array per system, each entry a pattern row in the FR-RULE-001 shape (minus <code>created_at</code>/<code>updated_at</code>, which the DB sets). Example row:</p>\n<pre><code class=\"language-json\">{\n  \"id\": \"qimen_thanh_long_hoi_dau\",\n  \"system\": \"qimen\",\n  \"name\": \"Thanh Long Hồi Đầu\",\n  \"name_han\": \"青龍返首\",\n  \"conditions\": { \"type\": \"and\", \"rules\": [\n    { \"field\": \"truc_phu.cung\", \"operator\": \"eq\", \"value\": \"ban.thien_ban.cung_of.丙\" },\n    { \"field\": \"door\", \"operator\": \"in\", \"value\": [\"Sinh\", \"Khai\", \"Tu\"] } ] },\n  \"polarity\": \"cat\",\n  \"meaning_classical\": \"丙加值符, cát khí tụ tập, lợi cho khởi sự và cầu kiến.\",\n  \"meaning_modern\": \"A strong-timing configuration for initiating or requesting; frame as a favorable window, not a guarantee.\",\n  \"citations\": [\"yba_thien_can_khac_ung_12\", \"kmdg_cach_cuc_thanh_long\"],\n  \"version\": 1, \"confidence\": 0.9, \"status\": \"active\"\n}</code></pre>\n<h3>Coverage target (indicative, not a hard split)</h3>\n<table><thead><tr><th>System</th><th>Target rows</th><th>Emphasis</th></tr></thead><tbody><tr><td>qimen</td><td>90-110</td><td>thập can khắc ứng cát/hung pairs, named formations, nhập mộ / không vong / phản-phục ngâm</td></tr><tr><td>liuren</td><td>35-50</td><td>the khóa thể (課體) families and headline tam truyền configurations</td></tr><tr><td>taiyi</td><td>20-35</td><td>the cách cục and chủ-khách thắng bại configurations, representative not exhaustive</td></tr></tbody></table>\n<p>Total lands in the 150-200 band; QiMen carries the P0 weight per DEC-4.</p>\n<h3>Sources and citation ids (<code>data/patterns/SOURCES.md</code>)</h3>\n<p>Each <code>citations</code> entry is a stable id that resolves into the FR-KB-003 classical corpus. Provenance of the seed content:</p>\n<ul><li>QiMen: Yên Ba Điếu Tẩu Ca (煙波釣叟歌) and a Joey Yap QiMen compendium for cross-checking modern glosses.</li><li>LiuRen: Đại Lục Nhâm (大六壬) classical material for the khóa thể and tam truyền readings.</li><li>TaiYi: Thái Ất Kim Kính (太乙金鏡) for the cách cục and toán configurations.</li></ul>\n<p><code>SOURCES.md</code> records, per source, the work, the citation-id prefix it owns, and the layer availability (Han / bạch thoại / dịch) so FR-KB-003 can align the corpus to the citations these patterns reference.</p>\n<h3>Build and load (<code>tamthuc_kb/seed/build_patterns.py</code>, <code>loader.py</code>)</h3>\n<pre><code class=\"language-python\">def build(paths: list[Path]) -&gt; BuildReport:\n    # 1. read each data/patterns/&lt;system&gt;.json\n    # 2. validate every row with the FR-RULE-001 validator (envelope + shallow conditions)\n    # 3. deep-validate conditions with the FR-RULE-002 DSL checker\n    # 4. assert every active row has &gt;= 1 citation and a resolvable citation-id prefix\n    # 5. assert id uniqueness within and across files; report counts per system + polarity\n    # fail the whole build on any error, naming the offending id\n\ndef load(rows: list[Pattern], db) -&gt; LoadReport:\n    # upsert active rows into knowledge_patterns (FR-PLAT-003), keyed by id; bump version on change</code></pre>\n<h3>Validation-dataset view (<code>tamthuc_kb/seed/validation.py</code>)</h3>\n<pre><code class=\"language-python\">def validation_cases(system: System | None = None) -&gt; list[ValidationCase]:\n    # projects each seeded pattern into a {id, version, conditions, expected_polarity,\n    # meaning_classical, citations} case; this is the fixed corpus FR-RAG-006 scores\n    # interpretation faithfulness and citation accuracy against (RISK-9).</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>data/patterns/qimen.json</code>, <code>liuren.json</code>, and <code>taiyi.json</code> together contain 150-200 rows, every row valid under the FR-RULE-001 validator and the FR-RULE-002 DSL checker.</li><li>Every <code>active</code> row carries at least one citation whose prefix is declared in <code>SOURCES.md</code>; a row with an empty <code>citations</code> and <code>status = active</code> fails the build with its <code>id</code> named.</li><li><code>id</code> is unique across all three files; a duplicate id anywhere fails the build.</li><li>Every <code>meaning_modern</code> passes a lint that rejects medical / legal / financial verdict phrasing and absolute future-event phrasing (strategy 7); a deliberately non-compliant fixture row is caught.</li><li><code>build</code> reports counts per system and per polarity, and QiMen is the largest share; <code>load</code> upserts <code>active</code> rows into <code>knowledge_patterns</code> idempotently (re-load changes nothing without a version bump).</li><li><code>validation_cases()</code> returns one case per seeded pattern with its <code>(id, version)</code> stamp, and FR-RAG-006 can consume it as the RISK-9 corpus.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_pattern_seed.py</code>: validates the committed seed sample (<code>fixtures/pattern_seed_sample.json</code>) end to end; asserts the citation-required, id-uniqueness, and meaning-lint cases; asserts <code>build</code> fails whole-file on one injected bad row with the <code>id</code> surfaced; asserts <code>validation_cases</code> parity with the loaded rows.</li><li>Schema conformance: every row validates against <code>docs/contracts/knowledge-pattern.schema.json</code> (the FR-RULE-001 contract), so the seed cannot drift from the row shape.</li><li>DSL conformance: every <code>conditions</code> tree parses and deep-validates under the FR-RULE-002 checker (fields, operators, depth), run as part of the build test.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_kb</code>, <code>pytest packages/tamthuc_kb</code>. The full 150-200 seed is linted in CI; the DB load runs behind a marker when a test Postgres is present.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Author <code>data/patterns/qimen.json</code> first (the P0 flagship weight), then <code>liuren.json</code> and <code>taiyi.json</code> to representative coverage, each row cited.</li><li>Write <code>data/patterns/SOURCES.md</code> mapping citation-id prefixes to the classical works and their layer availability.</li><li><code>seed/build_patterns.py</code>: read, validate (RULE-001 + RULE-002), id-uniqueness, meaning lint, per-system/polarity counts, whole-file fail.</li><li><code>seed/loader.py</code>: idempotent upsert of <code>active</code> rows into <code>knowledge_patterns</code> with version-bump-on-change.</li><li><code>seed/validation.py</code>: the <code>validation_cases</code> projection for FR-RAG-006.</li><li>Commit a small <code>fixtures/pattern_seed_sample.json</code> (a handful of rows across systems) as the test exemplar; the full files are the shipped seed.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-RULE-001 (the pattern row shape, the validator, and the seed-file format this FR fills) and FR-KB-001 (pattern <code>conditions</code> reference graph node ids and relations, and the cách cục / khóa thể / thần sát nodes live in the graph taxonomy). Loads into the <code>knowledge_patterns</code> table that FR-PLAT-003 migrates from the FR-RULE-001 shape (soft edge; the physical table is PLAT-003's). Citations resolve into the FR-KB-003 classical corpus (a parallel P1 build; the citation-id prefixes are agreed here in <code>SOURCES.md</code> so KB-003 can align to them). Blocks FR-KB-004 (the curation workflow reviews and versions these rows) and FR-RAG-006 (the eval loop scores interpretation against this seed as the RISK-9 validation set). Read at runtime by FR-RULE-003 (the loader serving <code>active</code> patterns to the engines) and enriches the chart summary in FR-RAG-003.</p>\n<h2>§8 - Example payloads</h2>\n<p>A LiuRen khóa thể row and a TaiYi cách cục row from the seed:</p>\n<pre><code class=\"language-json\">[\n  { \"id\": \"liuren_nguyen_thai\", \"system\": \"liuren\", \"name\": \"Nguyên Thai\", \"name_han\": \"元胎\",\n    \"conditions\": { \"type\": \"and\", \"rules\": [\n      { \"field\": \"khoa_the\", \"operator\": \"eq\", \"value\": \"nguyen_thai\" } ] },\n    \"polarity\": \"trung\", \"meaning_classical\": \"Khóa thể cơ bản, chỉ sự sinh khởi.\",\n    \"meaning_modern\": \"Baseline lesson-type; read the tam truyền for direction, not a fixed outcome.\",\n    \"citations\": [\"dllr_khoa_the_01\"], \"version\": 1, \"confidence\": 0.7, \"status\": \"active\" },\n\n  { \"id\": \"taiyi_chu_thang\", \"system\": \"taiyi\", \"name\": \"Chủ Toán Thắng\", \"name_han\": \"主算勝\",\n    \"conditions\": { \"type\": \"and\", \"rules\": [\n      { \"field\": \"chu_toan\", \"operator\": \"gt\", \"value\": \"khach_toan\" } ] },\n    \"polarity\": \"cat\", \"meaning_classical\": \"Chủ toán vượng hơn khách toán, lợi cho bên chủ.\",\n    \"meaning_modern\": \"Favors the initiating party in the framed decision; a lens on posture, not a prediction.\",\n    \"citations\": [\"taks_cach_cuc_chu_thang_03\"], \"version\": 1, \"confidence\": 0.65, \"status\": \"active\" }\n]</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Exact QiMen/LiuRen/TaiYi split inside the 150-200 band. Default: QiMen-heavy per DEC-4 and the coverage table; the precise counts follow from how many named formations each source actually documents. Recorded, not fixed, until the QiMen file is authored.</li><li>Whether school-variant readings of the same pattern are separate rows or one row with a variant-keyed meaning. Default: one <code>id</code> with the school difference recorded in <code>meaning_classical</code> / an <code>attrs</code>-style note, so detection stays single-keyed; split into versioned variant rows only if a school reads the polarity itself differently. Coordinate with FR-KB-004.</li><li>How much of the seed is expert-signed before P0 ship vs marked <code>draft</code>. Default: rows ship <code>active</code> only once cited and reviewed; unreviewed rows stay <code>draft</code> and are excluded from the loaded ruleset but kept in the validation corpus for tracking. FR-KB-004 owns the sign-off workflow.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Uncited active pattern</td><td><code>status = active</code>, <code>citations = []</code></td><td>build fails with the <code>id</code>; DB check constraint also rejects (FR-RULE-001)</td></tr><tr><td>Fortune-telling phrasing</td><td><code>meaning_modern</code> asserts a certain outcome or a medical/legal/financial verdict</td><td>meaning lint fails the row (strategy 7)</td></tr><tr><td>Duplicate id</td><td>same <code>id</code> in two files or twice in one</td><td>build fails on the id-uniqueness check</td></tr><tr><td>Invalid conditions</td><td><code>conditions</code> tree malformed or uses an unknown field/operator</td><td>FR-RULE-002 DSL checker fails the build at that row</td></tr><tr><td>Ruleset / test-set drift</td><td>someone edits the loaded patterns but not the validation corpus</td><td>they are the same rows; <code>validation_cases</code> derives from the loaded set, so drift is impossible by construction</td></tr><tr><td>Partial load</td><td>one bad row among many</td><td>whole-file fail; nothing loads until the file is clean</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the content FR that turns the pattern-as-data machinery into a working interpreter, and it is the single most leverage-heavy quality artifact in P0: the same 150-200 cited rows are the ruleset the engine detects, the answer key the eval loop grades, and the substrate the citation cards render. Keep it language-neutral data under <code>data/patterns/</code> so both the Rust engine (offline seed) and the Python DB loader consume one source; the small <code>crates/cyberos-rule/seed/qimen.json</code> from FR-RULE-001 stays only as the format exemplar and fixture. Author QiMen first and cite everything - an uncited pattern is not shippable. FR-KB-004 later adds the expert-review and versioning workflow over exactly these rows.</p>";
