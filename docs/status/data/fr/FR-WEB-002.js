window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR builds the query input screen - the left panel of the two-column chart-casting layout (FR-WEB-001) - through which a user specifies the moment and the question and casts a chart. It is the lookup flow's entry point (strategy 1, the \"ask\" step of ask -&gt; cast -&gt; read). It owns the form and the typed API client call; it does NOT render the chart or the interpretation (FR-WEB-003) and does NOT compute anything - it collects input and calls the gateway.</p>\n<p>The form SHALL collect: a datetime (local date and time of the question or event) with an explicit timezone (default <code>+07:00</code>); a place, resolvable to a longitude (<code>kinh_do</code>), with a manual longitude override; a question type (<code>loai_cau_hoi</code>, e.g. trach_thoi, hon_nhan, ...); the system selection via the top system tabs (LiuRen / QiMen / TaiYi, with QiMen the P0 default); an optional persona level (beginner default); and optional school-flag overrides (<code>co_truong_phai</code>), defaulting to the engine defaults when omitted. The primary action SHALL be a single Ochre cast button at control height md <code>44px</code> (FR-WEB-001), and it SHALL be the only primary action on the view.</p>\n<p>On submit, the screen SHALL POST to <code>POST /api/v1/calculate/{system}</code> (FR-API-001) with a body matching the <code>QueryRequest</code> contract, SHALL show a loading state while the nine-step flow runs, and SHALL route to the results screen (FR-WEB-003) with the returned <code>query_id</code> / response on success. It SHALL render the FR-API-001 structured error envelope faithfully: a validation error (400) inline on the offending field, an auth error (401) to sign-in, a tier error (403 <code>FORBIDDEN_TIER</code>, e.g. a Free principal requesting <code>all</code>) as a clear capability message (FR-AUTH-002), and a rate-limit error (429) with the reset hint. The screen SHALL surface the in-product disclaimer (FR-LEGAL-001) so the framing is present at the point of asking, not buried.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The input screen is where the product's determinism begins: every field here is an input that changes the chart, so the screen's job is to collect a complete, well-formed casting request and hand it to the gateway unaltered. Making datetime, timezone, and longitude explicit (rather than inferring silently from the browser) matters because a chart is reproducible only from its stated inputs plus flags (strategy 4.3) - a hidden timezone or an assumed longitude would make two \"identical\" casts diverge. The place-to-longitude resolution with a manual override respects that the longitude, not the place name, is the astronomical input.</p>\n<p>The screen is a thin client on purpose. It validates shape for a fast local error, but the authoritative validation, calendar resolution, casting, and interpretation all happen server-side through the one orchestrator (strategy 4.2, FR-API-001); the screen never re-implements any of it. Rendering the server's structured error envelope faithfully - field-level for validation, capability for tier, reset-hint for rate limit - is what lets one error contract serve the whole UI (FR-API-001 §2). Placing the disclaimer at the point of asking, not only in a footer, is the legal-and-ethical framing shown where the user forms the expectation (FR-LEGAL-001, strategy 7).</p>\n<h2>§3 - Contract (screen / form / API call)</h2>\n<h3>Layout (Claude-07 s5.2, Grok-51 wireframe)</h3>\n<p>Left panel of the FR-WEB-001 two-column shell. Top: the system tabs (in the top bar) select the engine. Body: the form fields top to bottom. Bottom: the Ochre <code>44px</code> cast button, full-width of the panel, the single primary action. The right panel shows an empty/prompt state until a cast returns (FR-WEB-003 fills it).</p>\n<h3>Form fields</h3>\n<table><thead><tr><th>Field</th><th>Maps to <code>QueryRequest</code></th><th>Notes</th></tr></thead><tbody><tr><td>date + time</td><td><code>datetime</code> (ISO local)</td><td>explicit, no silent browser inference</td></tr><tr><td>timezone</td><td><code>tz</code></td><td>default <code>+07:00</code></td></tr><tr><td>place</td><td><code>place</code> (+ resolves <code>kinh_do</code>)</td><td>autocomplete; longitude override visible</td></tr><tr><td>longitude override</td><td><code>kinh_do</code></td><td>manual <code>double</code>; the astronomical input</td></tr><tr><td>question type</td><td><code>question_type</code></td><td>select of <code>loai_cau_hoi</code> values</td></tr><tr><td>system</td><td><code>systems</code></td><td>from the top tabs ([\"qimen\"] default)</td></tr><tr><td>persona level</td><td><code>persona_level</code></td><td>beginner default</td></tr><tr><td>school flags (advanced)</td><td><code>co_truong_phai</code></td><td>optional; omitted -&gt; engine defaults</td></tr></tbody></table>\n<h3>API client (<code>lib/api/client.ts</code>, <code>lib/api/schemas.ts</code>)</h3>\n<pre><code class=\"language-ts\">// mirrors the FR-API-001 QueryRequest / QueryResponse (validated with zod)\ntype QueryRequest = {\n  datetime: string; tz: string;\n  place?: string; kinh_do?: number;\n  question_type: string;\n  systems: string[];            // [\"qimen\"] | [\"qimen\",\"liuren\"] | [\"all\"]\n  persona_level?: \"beginner\" | \"expert\";\n  co_truong_phai?: Record&lt;string, string&gt;;\n};\n\nasync function cast(system: string, body: QueryRequest): Promise&lt;QueryResponse&gt;;\n// POST /api/v1/calculate/{system}; sends the JWT Bearer; on non-2xx, parses the\n// FR-API-001 error envelope { error: { code, message, details, request_id } } and throws a typed error.</code></pre>\n<p>The client attaches the auth token (FR-AUTH-001), validates the request shape with zod before sending, and parses the structured error envelope on failure into a typed error the form maps to UI.</p>\n<h3>Error handling (renders the FR-API-001 envelope)</h3>\n<table><thead><tr><th>HTTP / code</th><th>UI</th></tr></thead><tbody><tr><td>400 <code>VALIDATION_ERROR</code></td><td>inline message on the offending field</td></tr><tr><td>401 <code>UNAUTHENTICATED</code></td><td>route to sign-in, preserve the draft</td></tr><tr><td>403 <code>FORBIDDEN_TIER</code></td><td>capability message (e.g. \"all-systems needs Premium\"), FR-AUTH-002</td></tr><tr><td>429 <code>RATE_LIMITED</code></td><td>disable cast + show <code>reset_at</code> from <code>details</code></td></tr><tr><td>502/503 <code>UPSTREAM_*</code></td><td>non-blocking \"engine/LLM unavailable, retry\" (FR-PLAT-008)</td></tr></tbody></table>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The form collects datetime, timezone, place/longitude, question type, system (from the tabs), persona level, and optional school flags, and builds a <code>QueryRequest</code> matching the FR-API-001 contract.</li><li>The cast button is a single Ochre primary at md <code>44px</code>; there is no second primary action on the view (FR-WEB-001).</li><li>Submitting POSTs to <code>/api/v1/calculate/{system}</code> with the correct body and auth token; a valid response routes to the results screen (FR-WEB-003) carrying the response/<code>query_id</code>.</li><li>Timezone and longitude are explicit inputs (defaulted, editable), never silently inferred; a place resolves to a longitude and the override is honored.</li><li>A 400 renders inline on the offending field; a 403 <code>FORBIDDEN_TIER</code> renders a capability message; a 429 disables cast and shows the reset hint; each is driven by the parsed error envelope, not ad hoc.</li><li>The in-product disclaimer (FR-LEGAL-001) is visible on the screen at the point of asking.</li><li>A loading state is shown while the nine-step flow runs and the cast button is disabled to prevent double submits.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/query-form.test.tsx</code>: builds a <code>QueryRequest</code> from filled fields and asserts the body shape; asserts the tabs drive <code>systems</code>; asserts the place/longitude override; asserts a stubbed <code>cast()</code> success routes to results and a stubbed error envelope (400/403/429) renders the correct UI branch; asserts the single-Ochre-primary rule and the disclaimer presence.</li><li>Contract: the zod <code>QueryRequest</code> schema is checked against the FR-API-001 <code>QueryRequest</code> (a shared fixture) so the client and the gateway agree; a drift fails the test.</li><li>Accessibility: labeled fields, keyboard submit, focus-visible Ochre ring, and the stacked-diacritics clip test (FR-WEB-001) over Vietnamese field labels and question-type options.</li><li>Gates: <code>pnpm --filter web lint</code>, <code>pnpm --filter web test</code>, <code>next build</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>lib/api/schemas.ts</code> + <code>lib/api/client.ts</code>: the zod <code>QueryRequest</code>/<code>QueryResponse</code>, the <code>cast()</code> call with auth + error-envelope parsing.</li><li><code>components/query/*</code>: <code>datetime-field</code> (date+time+tz), <code>place-field</code> (autocomplete + longitude override), <code>question-type-select</code>, <code>system-tabs</code> (bound to the top bar), and <code>query-form</code> composing them with the cast button.</li><li><code>app/cast/page.tsx</code>: the left panel wired into the FR-WEB-001 shell; the empty right-panel prompt state; the disclaimer slot (FR-LEGAL-001).</li><li>Loading + error states from the parsed envelope; route to FR-WEB-003 on success.</li><li><code>tests/query-form.test.tsx</code> + the shared <code>QueryRequest</code> contract fixture.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-WEB-001 (the shell, tokens, and the Button/tabs library) and FR-API-001 (the <code>/calculate/{system}</code> endpoint and the <code>QueryRequest</code>/error-envelope contracts it calls). Blocks FR-WEB-003 (the results screen it routes to on a successful cast). Uses FR-AUTH-002 capabilities for the tier error, FR-WEB-006 for label i18n, and FR-LEGAL-001 for the disclaimer copy. It never casts a chart itself - the gateway does (strategy 4.2).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-ts\">// built by query-form, sent by cast(\"qimen\", body)\nconst body: QueryRequest = {\n  datetime: \"2004-01-01T10:30:00\", tz: \"+07:00\",\n  place: \"Ha Noi\", kinh_do: 105.85,\n  question_type: \"trach_thoi\", systems: [\"qimen\"], persona_level: \"beginner\"\n};\n// POST /api/v1/calculate/qimen  (Authorization: Bearer &lt;jwt&gt;)</code></pre>\n<pre><code class=\"language-json\">// 403 error envelope the screen renders as a capability message\n{ \"error\": { \"code\": \"FORBIDDEN_TIER\",\n    \"message\": \"All-systems casting is a Premium capability.\",\n    \"details\": { \"required_tier\": \"premium\" }, \"request_id\": \"req_...\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Place-to-longitude resolution source: a bundled VN gazetteer vs a geocoding API. Default: a small bundled set of common VN localities (offline, privacy-friendly) plus the manual longitude override for anything else; a geocoding integration is a later enhancement. The longitude is the real input, so the override is always available.</li><li>School-flag overrides in the P0 UI: expose the full <code>co_truong_phai</code> set now vs defaults-only. Default: defaults-only in the primary form with an \"advanced\" disclosure for flags, since the flag set is engine-specific (FR-QMDG-006) and most users cast under the defaults; the full config surface is the management flow (FR-WEB-007).</li><li>Client-side validation depth: how much to validate before the server. Default: shape and obvious-range checks (a parseable datetime, a longitude in range) for a fast local error; the gateway remains the authority (strategy 4.2), so the client never rejects what only the server can judge.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Silent timezone/longitude</td><td>inferred from the browser without showing it</td><td>forbidden; tz and longitude are explicit, defaulted, editable inputs (reproducibility, strategy 4.3)</td></tr><tr><td>Client re-computes a chart</td><td>UI tries to cast locally</td><td>forbidden; the screen only POSTs to the gateway; no engine logic in the client</td></tr><tr><td>Ad hoc error text</td><td>error shown without parsing the envelope</td><td>the parsed FR-API-001 envelope drives the UI branch (field/tier/rate-limit); no bespoke strings</td></tr><tr><td>Double submit</td><td>user double-clicks cast</td><td>cast is disabled during the loading state; one in-flight request</td></tr><tr><td>Missing disclaimer</td><td>framing only in a footer</td><td>the FR-LEGAL-001 disclaimer is present on the screen at the point of asking</td></tr><tr><td>Tier bypass illusion</td><td>Free user shown <code>all</code> as castable</td><td>the tab/capability state reflects FR-AUTH-002; <code>all</code> for Free surfaces the 403 capability message</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This screen is the front door of the lookup flow and a thin client by design: it collects a complete, reproducible casting request (explicit datetime, timezone, longitude, question type, system, flags) and hands it to the one orchestrator that actually casts (strategy 4.2, FR-API-001). Keep the cast button the single Ochre primary, keep the error rendering driven by the parsed structured envelope, and keep the disclaimer at the point of asking. The moment the client tries to infer a hidden input or compute anything locally, reproducibility and the deterministic boundary are compromised.</p>";
