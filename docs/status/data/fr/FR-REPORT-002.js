window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-REPORT-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR renders the FR-REPORT-001 <code>StructuredReport</code> into a PDF. The module SHALL take a <code>StructuredReport</code> and produce a templated, CyberSkill-branded, bilingual (vi / en) PDF. The chart summary and detected patterns (the deterministic half) SHALL be rendered in a section visually separated from the AI interpretation (the AI half) - the engine / AI boundary made visible on the page (strategy 4.1). The AIDisclosure SHALL be printed on the report, and the citations SHALL be printed as a reference list keeping the Han + phien am / bach thoai + dich + locator (the FR-RAG-003 citation cards).</p>\n<p>The module SHALL be read-only over the <code>StructuredReport</code>: it renders what REPORT-001 assembled and MUST NOT re-cast, re-interpret, recompute confidence, or alter any field. Branding SHALL follow the CyberSkill Design System v1.3.0 (Umber #45210E, Ochre #F4BA17, Be Vietnam Pro), and glass / elevation SHALL collapse to solid surfaces on print (Claude-07 s5.4). The output SHALL be deterministic given a fixed report (modulo an embedded timestamp).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>A report becomes a durable, shareable, printable artifact at PDF (Grok-08); it is the thing a user keeps and forwards, so two properties matter most. First, the engine / AI boundary must stay visible on paper, not just on screen: the deterministic chart summary sits in its own panel and the AI interpretation in another, so a reader always knows which part is oracle-exact calculation and which is cited AI reading (strategy 4.1, Claude-07 s5.3). Printing the AIDisclosure and the full citation list is the same discipline - the label and the sources travel with the document.</p>\n<p>Second, the exporter must be a pure renderer. REPORT-001 already validated citation-required and the AIDisclosure and carried confidence unchanged; if the exporter recomputed or tidied anything, a printed report could disagree with the stored one. So it reads the <code>StructuredReport</code> and only lays it out. The print-specific rules from the design system (glass collapses to solid, elevation becomes an Ochre border in high contrast) exist because Han and Vietnamese diacritics need contrast to stay legible (Claude-07 s5.1, s5.4); a translucent surface or a too-tight line-height clips a dau nga or a dau nang and corrupts the meaning.</p>\n<h2>§3 - Contract (renderer / template)</h2>\n<h3>Entry point (<code>pdf_export.py</code>)</h3>\n<pre><code class=\"language-python\">from typing import Literal\nfrom tamthuc_report.models import StructuredReport\n\ndef export_pdf(report: StructuredReport, lang: Literal[\"vi\", \"en\", \"bi\"] = \"bi\") -&gt; bytes:\n    # render report.html.j2 + report.css with the report:\n    #   - chart-summary + detected-patterns panel, visually separated from the interpretation panel\n    #   - AIDisclosure block + citation reference list (han + bach_thoai + dich + locator)\n    #   - CyberSkill brand (Umber / Ochre, Be Vietnam Pro); solid surfaces on print\n    # read-only: never mutates the report, never calls the engine / RAG. returns PDF bytes.</code></pre>\n<h3>Template structure (<code>report.html.j2</code>)</h3>\n<ul><li>Header: the CyberSkill brand (Umber band, Ochre mark), <code>report_id</code> / <code>query_id</code>, <code>created_at</code>.</li><li>Panel A (deterministic): <code>he</code>, <code>dau_vao</code> echo, <code>lich_phap_summary</code>, <code>key_positions</code>, and the <code>detected_patterns</code> table with Han. Labeled and boxed as \"Engine - deterministic\".</li><li>Panel B (AI): the beginner interpretation, the expert interpretation, and the recommendations. Labeled \"AI interpretation\".</li><li>AIDisclosure block: <code>model</code>, <code>limits</code>, <code>review_status</code>, printed from the report.</li><li>Citations: a reference list, each with <code>source</code>, <code>locator</code>, <code>han</code>, <code>bach_thoai</code>, <code>dich</code>.</li><li>Bilingual: vi and en, sectioned per lang (the FR-WEB-006 content split).</li></ul>\n<h3>Print rules (<code>report.css</code>)</h3>\n<p>Brand tokens (Umber / Ochre, Be Vietnam Pro); solid surfaces on print (no glass); elevation becomes a high-contrast border; a diacritic-safe line-height so Vietnamese dau chong and Han are not clipped (Claude-07 s5.1). Panels A and B are distinct, labeled blocks.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>export_pdf(report)</code> returns PDF bytes rendering the <code>StructuredReport</code> with a chart-summary / patterns panel and an interpretation panel that are visually separated (distinct, labeled sections).</li><li>The AIDisclosure (<code>model</code>, <code>limits</code>, <code>review_status</code>) is printed on the report.</li><li>The citations are printed as a reference list keeping <code>han</code> + <code>bach_thoai</code> + <code>dich</code> + <code>locator</code> for each.</li><li>Branding follows the design system (Umber / Ochre, Be Vietnam Pro); surfaces are solid on print (no glass), diacritics and Han are not clipped.</li><li>Bilingual output renders vi and en; the <code>lang</code> argument selects vi, en, or bilingual.</li><li><code>export_pdf</code> is read-only: it never mutates the report and never calls the engine / RAG (a spy + a before/after equality check).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>test_pdf_export.py</code>: render a golden <code>StructuredReport</code> (from the FR-REPORT-001 fixtures) -&gt; PDF; extract the text; assert the chart-summary section, the interpretation section, the AIDisclosure, and every citation (han + dich + locator) are present, and that the two panels are distinct sections.</li><li>Separation test: assert the deterministic panel and the AI panel are separate blocks (distinct section markers / headings), not interleaved.</li><li>Read-only: deep-copy the report, export, assert it is byte-identical after and that no engine / RAG client was called (a spy).</li><li>Branding / print: assert the CSS applies solid surfaces on print and the brand colors / fonts; a dau chong (diacritic-clip) check on a Vietnamese + Han sample line.</li><li>Determinism: two exports of the same report are identical modulo the embedded timestamp.</li><li>Gates: <code>python -m pytest packages/tamthuc_report</code>, <code>ruff check</code>, <code>mypy packages/tamthuc_report</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>report.html.j2</code>: header, Panel A (deterministic), Panel B (AI), the AIDisclosure block, the citation list; bilingual.</li><li><code>report.css</code>: the brand tokens + print rules (solid surfaces, high-contrast borders, diacritic-safe line-height).</li><li><code>pdf_export.py</code>: <code>export_pdf(report, lang)</code> rendering HTML + CSS to PDF (a headless renderer), read-only.</li><li><code>tests/test_pdf_export.py</code>: golden report -&gt; PDF text extraction; separation, read-only, branding, determinism.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-REPORT-001 (the <code>StructuredReport</code> it renders; reuses its models and the RAG-003 citation cards / AIDisclosure). Follows the FR-WEB-006 vi / en content split for bilingual output and the FR-LEGAL-001 AI-disclosure copy. Renders the same object FR-WEB-005 shows on screen. Consumes the CyberSkill Design System v1.3.0 tokens (Claude-07 s5). Nothing depends on it (blocks empty).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code># Rendered PDF layout (schematic) - two separated panels + disclosure + citations\n[ Umber header | Ochre mark | report_id | created_at ]\n\n+-- Panel A - Engine (deterministic) ------------------+\n| he: ky_mon   Tu tru 癸未 甲子 戊午 丁巳   tiet khi 冬至 |\n| detected_patterns: 青龍返首 (cat, cung 1, score 0.9)  |\n+------------------------------------------------------+\n\n+-- Panel B - AI interpretation -----------------------+\n| beginner: ... (cited)   expert: ... (cited)          |\n| recommendations: ...                                 |\n+------------------------------------------------------+\n\nAIDisclosure: model=... | decision support, not a verdict | review: pending\nCitations: [1] Yen Ba Dieu Tau Ca - cach cat - 青龍返首 - bach thoai ... - dich ...</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Bilingual layout: side-by-side columns vs vi-then-en sections. Default: sectioned per lang for readability with Han; side-by-side is a later refinement.</li><li>Renderer choice (WeasyPrint vs headless Chromium). Default: an HTML + CSS-to-PDF renderer that honors the design-system CSS and embeds Be Vietnam Pro; decide in implementation and keep the template renderer-agnostic.</li><li>Whether the 9-palace chart image is embedded (from the FR-CHART-004 export) or the PDF is text + table only. Default: text + the cach cuc table at MVP; embed the chart image when the CHART-004 export is available.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Boundary invisible</td><td>chart summary and interpretation interleaved</td><td>forbidden; two visually separated, labeled panels (strategy 4.1)</td></tr><tr><td>Missing AIDisclosure / citations</td><td>the printed report drops the label or the sources</td><td>required on every export; a test asserts presence</td></tr><tr><td>Re-compute at export</td><td>the exporter re-interprets or recomputes confidence</td><td>forbidden; read-only renderer; spy + equality test</td></tr><tr><td>Clipped diacritics / Han</td><td>line-height too tight on print</td><td>dau chong check; diacritic-safe CSS (Claude-07 s5.1)</td></tr><tr><td>Glass on print</td><td>a translucent surface reduces contrast</td><td>collapses to solid on print (Claude-07 s5.4)</td></tr><tr><td>Non-deterministic output</td><td>render varies run to run</td><td>deterministic modulo the embedded timestamp</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>Package <code>tamthuc_report</code> (Python, DEC-2). PDF export is the report made durable and shareable (Grok-08). Two things carry over from REPORT-001's discipline: the engine / AI boundary stays visible (a deterministic panel separated from the AI interpretation panel), and the AIDisclosure and full citation list are printed so the label and the sources travel with the document (strategy 4.1, 7). It is a pure renderer - read-only over the <code>StructuredReport</code>, never re-computing - and it follows the CyberSkill Design System v1.3.0 with print rules that keep Han and Vietnamese diacritics legible (Claude-07 s5.1, s5.4). FR-REPORT-003 supplies the per-question-type template this export renders.</p>";
