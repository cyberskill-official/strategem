window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-STRAT-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR builds Scenario Comparison on top of the Timing Optimizer. Given several named candidate scenarios (each an option or a date range for the same decision), it runs FR-STRAT-001 for each and returns their ranked windows side by side, every window carrying its contributing cat / hung cach cuc and citations (Grok-02, Claude-07 s1.1).</p>\n<p>The module SHALL accept a <code>ScenarioSet</code> (a list of named <code>Scenario</code>, each wrapping a STRAT-001 <code>TimingRequest</code>) and SHALL obtain each scenario's ranked windows by calling the FR-STRAT-001 optimizer. It SHALL NOT cast a chart, re-implement casting, or re-score windows itself - STRAT-001 (through the FR-QMDG-006 engine) is the only source of windows and scores (strategy 4.3). It SHALL produce a <code>ScenarioComparison</code>: per scenario the ranked windows (top_n) with their reasons, plus a cross-scenario view ranking the scenarios by their best window and surfacing the single best window overall. The module SHALL be deterministic: identical scenario set plus identical flags yields an identical comparison. The result is a decision-support signal, never a verdict: every window keeps its patterns and citations so the comparison is explainable, and any interpretation prose attached downstream carries an AIDisclosure (strategy 7).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The Timing Optimizer answers \"which time is best for this one action\". The next question a decision-maker asks is comparative: \"option A next Tuesday, option B next month, option C at our preferred venue - which scenario is best, and why\" (Grok-02). That is Scenario Comparison.</p>\n<p>Building it as a thin composition over STRAT-001, rather than a new scorer, is deliberate. STRAT-001 already casts through the engine and produces explainable, cited windows, so comparison is a matter of running it per scenario and laying the results side by side. If STRAT-002 re-scored or re-cast, it could diverge from the engine-gated results the user already trusts from STRAT-001 - so it reuses them exactly. The FR-PLAT-006 chart cache makes N scenarios cheap because overlapping instants are cast once. Keeping each window's cat / hung reasons through the comparison is what makes the answer auditable, a transparent side-by-side rather than a leaderboard of bare numbers (strategy 7).</p>\n<h2>§3 - Contract (models and process)</h2>\n<h3>Models (<code>packages/tamthuc_strat/scenario_compare.py</code>)</h3>\n<p>Reuses <code>TimingRequest</code> and <code>ScoredWindow</code> from FR-STRAT-001.</p>\n<pre><code class=\"language-python\">from pydantic import BaseModel, ConfigDict\nfrom .models import TimingRequest, ScoredWindow\n\nclass Scenario(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    label: str                        # \"Option A - launch 12 Mar\", ...\n    request: TimingRequest            # a STRAT-001 request\n\nclass ScenarioSet(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    scenarios: list[Scenario]\n    top_n: int = 3                    # windows kept per scenario\n\nclass ScenarioResult(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    label: str\n    windows: list[ScoredWindow]       # from STRAT-001, ranked\n    best_score: float                 # windows[0].score, or -inf if none\n\nclass ScenarioComparison(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    results: list[ScenarioResult]     # one per scenario\n    ranked_labels: list[str]          # scenarios ranked by best_score\n    best_overall: ScoredWindow | None</code></pre>\n<h3>Process</h3>\n<ol><li>For each <code>Scenario</code>, call the FR-STRAT-001 optimizer with its <code>TimingRequest</code> -&gt; <code>TimingResult</code> (which casts via the engine, cache-keyed). Never cast or score here.</li><li>Build a <code>ScenarioResult</code> per scenario from its windows; <code>best_score</code> = the top window's score (or -inf if the scenario yielded no windows).</li><li>Rank scenarios by <code>best_score</code> (tie-break: earliest best window, then input order) -&gt; <code>ranked_labels</code>; <code>best_overall</code> = the single highest window across all scenarios.</li></ol>\n<p>Determinism follows from STRAT-001's determinism plus a stable sort. An empty <code>ScenarioSet</code> returns an empty comparison; a scenario with no windows has <code>best_score = -inf</code> and sorts last, never silently dropped.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>For a set of scenarios, the module returns one <code>ScenarioResult</code> per scenario, each with STRAT-001's ranked windows (top_n) and their cat / hung + citations preserved.</li><li>STRAT-002 obtains windows only via STRAT-001; a test asserts it calls the optimizer and does not cast or score charts itself.</li><li><code>ranked_labels</code> orders scenarios by <code>best_score</code>, ties broken by earliest best window then input order; <code>best_overall</code> is the single highest window across scenarios.</li><li>Each preserved window still lists its contributing patterns and citations (explainability carried through unchanged from STRAT-001).</li><li>Determinism: the same <code>ScenarioSet</code> twice yields an identical comparison; overlapping instants hit the FR-PLAT-006 cache.</li><li>Empty set returns an empty comparison; a scenario yielding zero windows sorts last with <code>best_score = -inf</code>, not dropped.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>pytest with a stub STRAT-001 optimizer returning known <code>TimingResult</code>s per scenario: assert per-scenario windows, <code>best_score</code>, <code>ranked_labels</code>, <code>best_overall</code>, and the tie-breaks.</li><li>No-recompute test: a spy on the engine client asserts STRAT-002 never calls it directly; only STRAT-001 does.</li><li>Explainability: a compared window's patterns and citations equal STRAT-001's for that window (carried through).</li><li>Determinism: repeat-run equality; a cache hit on overlapping instants.</li><li>Boundary: empty set; a scenario yielding zero windows sorts last, still present.</li><li>Gates: <code>python -m pytest packages/tamthuc_strat</code>, <code>ruff check</code>, <code>mypy packages/tamthuc_strat</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>scenario_compare.py</code>: <code>Scenario</code>, <code>ScenarioSet</code>, <code>ScenarioResult</code>, <code>ScenarioComparison</code>.</li><li><code>compare(scenario_set, optimizer) -&gt; ScenarioComparison</code>: call STRAT-001 per scenario; build results.</li><li>Stable ranking by <code>best_score</code> + <code>best_overall</code> + tie-break.</li><li>Expose the STRAT comparison endpoint; tests with a stub optimizer and golden scenarios (<code>fixtures/scenarios.json</code>).</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-STRAT-001 (the Timing Optimizer whose <code>TimingResult</code>s it composes; reuses its <code>TimingRequest</code> / <code>ScoredWindow</code> models). Benefits from FR-PLAT-006 (the chart cache makes N scenarios cheap). Reads the FR-QMDG-006 engine only transitively through STRAT-001. Feeds FR-WEB-007 (the management / compare flow). Sibling to FR-STRAT-003 and FR-STRAT-004. Nothing depends on it (blocks empty).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// ScenarioComparison (abridged) - two candidate launch options\n{ \"results\": [\n    { \"label\": \"Option A - 12 Mar\",\n      \"windows\": [ { \"start\": \"2026-03-12T10:00:00\", \"end\": \"2026-03-12T12:00:00\", \"score\": 1.7,\n        \"cat\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"青龍返首\", \"score\": 0.9,\n                   \"citations\": [\"Yen Ba Dieu Tau Ca\"] } ], \"hung\": [], \"cast_ref\": \"qmdg:...\" } ],\n      \"best_score\": 1.7 },\n    { \"label\": \"Option B - 3 Apr\",\n      \"windows\": [ { \"start\": \"2026-04-03T06:00:00\", \"end\": \"2026-04-03T08:00:00\", \"score\": 0.4,\n        \"cat\": [ { \"id\": \"...\", \"name\": \"...\", \"score\": 0.4, \"citations\": [\"...\"] } ],\n        \"hung\": [], \"cast_ref\": \"qmdg:...\" } ],\n      \"best_score\": 0.4 } ],\n  \"ranked_labels\": [\"Option A - 12 Mar\", \"Option B - 3 Apr\"],\n  \"best_overall\": { \"start\": \"2026-03-12T10:00:00\", \"end\": \"2026-03-12T12:00:00\", \"score\": 1.7,\n    \"cat\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"青龍返首\", \"score\": 0.9,\n               \"citations\": [\"Yen Ba Dieu Tau Ca\"] } ], \"hung\": [], \"cast_ref\": \"qmdg:...\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Should scenarios allow different question types or systems, or must they share one? Default: the same <code>loai_cau_hoi</code> for a meaningful comparison; a mix is allowed but the UI labels the difference.</li><li>Cross-scenario normalization: scores are comparable because they share STRAT-001's documented scale; no re-normalization. If scenarios use different granularities, note it; per-window scores stay comparable.</li><li>How many scenarios is reasonable (perf). Default: a small cap (e.g. 8) with the cache; a typed error past the cap, like STRAT-001's scan cap.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>STRAT re-casts / re-scores</td><td>comparison computes windows itself</td><td>forbidden; must call STRAT-001; a test asserts no direct casting or scoring</td></tr><tr><td>Non-deterministic ranking</td><td>unstable sort / tie not broken</td><td>deterministic tie-break (earliest best window, then input order); repeat-run equality</td></tr><tr><td>Dropped empty scenario</td><td>a zero-window scenario silently omitted</td><td>it sorts last with <code>best_score = -inf</code>, still present</td></tr><tr><td>Reasons lost</td><td>a compared window drops its patterns / citations</td><td>carried through unchanged from STRAT-001; explainability test</td></tr><tr><td>Unbounded set</td><td>too many scenarios</td><td>cap + typed error</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>Package <code>tamthuc_strat</code> (Python, DEC-2). Scenario Comparison is the second strategic tool and a thin, honest composition over the Timing Optimizer: run STRAT-001 per candidate option or date, lay the ranked windows side by side, rank the scenarios by their best window, and keep every window's cat / hung reasons and citations (Grok-02, Claude-07 s1.1). It never casts or scores a chart itself - STRAT-001, through the FR-QMDG-006 engine, is the single source of windows - so a comparison can never disagree with the optimizer the user already trusts. Decision support, not a verdict (strategy 7).</p>";
