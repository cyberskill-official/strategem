window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-AUTH-001"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR builds the identity layer: user registration and login, JWT-based session tokens, social login, the user profile that holds sensitive birth_data, and the encryption that protects that data at rest. It is the birth of the <code>tamthuc_auth</code> Python package. It owns the <code>users</code> table shape and the token and crypto primitives; it does NOT own tier permissions or rate-limit quotas (FR-AUTH-002) nor the DSAR export/erasure flows (FR-AUTH-004).</p>\n<p>Authentication SHALL issue a short-lived JWT access token and a longer-lived refresh token on successful login. The API SHALL support three login methods: email plus password, Google OIDC, and Apple OIDC. Passwords SHALL be hashed with a memory-hard algorithm (argon2 via passlib), never stored or logged in plaintext. JWTs SHALL be signed and verified with python-jose; the access token SHALL carry <code>sub</code> (user id), <code>tier</code> (from FR-AUTH-002, defaulting to Free), <code>iat</code>, <code>exp</code>, and a <code>jti</code>; the refresh token SHALL be independently revocable. Social login SHALL be handled through Authlib against the provider's OIDC discovery, verifying the provider id token before provisioning or linking a local user (just-in-time provisioning on first social login, linked to an existing email where one matches).</p>\n<p>The user profile SHALL hold <code>birth_data</code> (date, time, place, and derived coordinates for chart casting) as sensitive personal data. <code>birth_data</code> SHALL be encrypted at rest with AES-256-GCM using a key that is never stored in the database (envelope encryption: a per-record data key wrapped by a KMS or an application master key held outside the DB). Personal data SHALL be TLS-protected in transit (enforced at the platform edge, FR-PLAT-007), and every sensitive read or write of <code>birth_data</code> SHALL be auditable (the audit row is written by FR-API-004). This is the AUTH half of RISK-5.</p>\n<p>Registration SHALL create an unverified user and trigger an email-verification flow whose token issuance and confirmation endpoint are completed in FR-AUTH-003; this FR SHALL emit the verification event and mark <code>email_verified=false</code> so the P0 path is verification-ready. All auth failures SHALL return the structured error envelope (FR-API-001) with a generic message that does not reveal whether an email exists.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Birth data is the crown-jewel sensitivity of this product. A person's date, time, and place of birth is precisely the input a divination platform needs and precisely the data that, leaked, is both privately harmful and legally exposing under VN PDPD and GDPR (RISK-5, strategy 4.4). Encrypting it at rest with a key held outside the database means a database dump alone does not disclose it; envelope encryption with a wrapped per-record key means key rotation does not require re-encrypting every row by hand. This is not gold-plating - it is the minimum posture for handling this category of data, and the whole platform's legal footing (strategy 7) rests on it.</p>\n<p>JWT plus refresh is the standard the Grok backend spec calls for (Grok-36), and it fits the split architecture: the stateless access token lets the FastAPI gateway authorize a request without a session lookup on the hot path, while the revocable refresh token gives back the control a pure-stateless design loses (a compromised session can be cut). Social login through Authlib against OIDC discovery, verifying the provider id token rather than trusting a client-passed profile, closes the most common social-login hole. Generic auth-failure messages avoid the account-enumeration leak that specific \"no such user\" versus \"wrong password\" responses create.</p>\n<h2>§3 - Contract (schema / API / types)</h2>\n<h3>users table shape (<code>packages/tamthuc_auth/migrations/0001_users.sql</code>; owned here, run by the PLAT migration runner)</h3>\n<table><thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>id</td><td>uuid PK</td><td></td></tr><tr><td>email</td><td>citext unique</td><td>unique index; case-insensitive</td></tr><tr><td>password_hash</td><td>text</td><td>argon2; null for social-only accounts</td></tr><tr><td>birth_data</td><td>jsonb</td><td>AES-256-GCM ciphertext envelope; never plaintext at rest</td></tr><tr><td>preferences</td><td>jsonb</td><td>language, default systems, school-flag defaults</td></tr><tr><td>email_verified</td><td>boolean</td><td>default false; set by FR-AUTH-003</td></tr><tr><td>tier</td><td>text</td><td>enum Free/Premium/Enterprise/Admin (FR-AUTH-002); default Free</td></tr><tr><td>created_at</td><td>timestamptz</td><td></td></tr><tr><td>updated_at</td><td>timestamptz</td><td></td></tr></tbody></table>\n<p>This is the <code>users</code> table the master catalog assigns to FR-PLAT-003; AUTH is its natural owner, so the DDL lives here and PLAT-003 runs it as part of the umbrella migration set (see section 7).</p>\n<h3>Token model (<code>tamthuc_auth/tokens.py</code>, contract in <code>docs/contracts/auth-tokens.md</code>)</h3>\n<pre><code class=\"language-python\">class AccessClaims(BaseModel):\n    sub: str; tier: str; iat: int; exp: int; jti: str        # short TTL (e.g. 15 min)\n\ndef issue_access(user_id: str, tier: str) -&gt; str: ...        # python-jose, signed\ndef issue_refresh(user_id: str) -&gt; str: ...                  # long TTL, revocable by jti\ndef verify_access(token: str) -&gt; AccessClaims: ...           # raises typed AuthError on bad/expired\ndef revoke_refresh(jti: str) -&gt; None: ...</code></pre>\n<h3>birth_data crypto (<code>tamthuc_auth/crypto.py</code>)</h3>\n<pre><code class=\"language-python\">def encrypt_birth_data(plaintext: dict, master_key: bytes) -&gt; dict:\n    # AES-256-GCM; returns { \"alg\": \"AES-256-GCM\", \"iv\": ..., \"ct\": ..., \"tag\": ...,\n    #                        \"wrapped_dek\": ... }  -- the data key is wrapped, never stored bare\ndef decrypt_birth_data(envelope: dict, master_key: bytes) -&gt; dict: ...</code></pre>\n<h3>Endpoints (<code>tamthuc_auth/routes.py</code>, mounted by FR-API-001)</h3>\n<pre><code>POST /auth/register   { email, password, birth_data? }      -&gt; { user_id, email_verified:false }\nPOST /auth/login      { email, password }                   -&gt; { access, refresh }\nPOST /auth/login/google  { id_token }                       -&gt; { access, refresh }   (Authlib OIDC verify)\nPOST /auth/login/apple   { id_token }                       -&gt; { access, refresh }\nPOST /auth/refresh    { refresh }                           -&gt; { access, refresh }   (rotates refresh)\nGET  /auth/me         (Bearer)                              -&gt; { user_id, email, tier, preferences }</code></pre>\n<h3>Dependency (<code>tamthuc_auth/deps.py</code>)</h3>\n<pre><code class=\"language-python\">async def get_current_user(token: str = Depends(bearer)) -&gt; CurrentUser: ...\n# verifies the access token, loads the user, exposes id + tier for downstream authorization (FR-AUTH-002)</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Register then login with email/password returns an access and a refresh token; the password is stored as an argon2 hash and never appears in the DB or logs in plaintext.</li><li><code>verify_access</code> accepts a freshly issued token and rejects an expired one, a tampered one, and one signed with the wrong key, each with a typed <code>AuthError</code>.</li><li><code>/auth/refresh</code> issues a new access token and rotates the refresh token; a revoked refresh <code>jti</code> is rejected.</li><li>Google and Apple login verify the provider id token via Authlib OIDC before provisioning; an invalid or wrong-audience id token is rejected, and a first-time social login provisions a user linked by email where one exists.</li><li><code>encrypt_birth_data</code> / <code>decrypt_birth_data</code> round-trip a birth_data dict; the stored envelope contains no plaintext field, the data key is wrapped, and decrypting with the wrong master key fails the GCM tag check rather than returning garbage.</li><li>Auth failures return the FR-API-001 error envelope with a generic message; login against a non-existent email and login with a wrong password are indistinguishable to the caller (no account enumeration).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_auth.py</code>: register/login/refresh happy paths; token expiry, tamper, and wrong-key rejection; refresh rotation and revocation; social-login id-token verification with a stubbed OIDC provider (valid, expired, wrong-audience); the account-enumeration-safe failure responses.</li><li><code>tests/test_crypto.py</code>: AES-256-GCM round-trip; wrong-key GCM failure; envelope contains no plaintext; key-rotation path re-wraps the data key without re-encrypting the payload.</li><li>Security checks: passwords never logged (a log-capture assertion); <code>birth_data</code> never serialized in plaintext by any response model (<code>/auth/me</code> does not return it).</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_auth</code>, <code>pytest packages/tamthuc_auth</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Create the <code>tamthuc_auth</code> package (<code>pyproject.toml</code>, <code>uv</code>-managed per FR-PLAT-001); this FR owns its birth, FR-AUTH-002/003/004 add modules.</li><li><code>models.py</code>: <code>User</code>, <code>Profile</code>, <code>CurrentUser</code>; the response models that deliberately exclude <code>birth_data</code>.</li><li><code>passwords.py</code>: argon2 hash/verify via passlib.</li><li><code>tokens.py</code>: python-jose access/refresh issue + verify + revoke; the <code>jti</code> revocation store.</li><li><code>crypto.py</code>: AES-256-GCM envelope encrypt/decrypt with wrapped data keys; the master-key provider (KMS or config, never the DB).</li><li><code>social.py</code>: Authlib OIDC clients for Google and Apple; id-token verification and JIT provisioning.</li><li><code>routes.py</code> + <code>deps.py</code> + <code>config.py</code>: the endpoints, <code>get_current_user</code>, and settings; <code>migrations/0001_users.sql</code>.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-PLAT-001 (the Python workspace). The <code>users</code> table DDL is owned here and applied through the PLAT migration runner into the shared Postgres; the master catalog lists <code>users</code> under FR-PLAT-003, so PLAT-003 coordinates the umbrella migration set while AUTH owns the auth columns and the encryption contract - a reconciliation note, with the hard <code>depends_on</code> kept at PLAT-001 per the catalog. Blocks FR-AUTH-002 (tiers and quotas key off this user and token), FR-AUTH-003 (email verification / password reset extends this flow), FR-AUTH-004 (DSAR reads and erases this profile), FR-API-001 (<code>get_current_user</code> is the gateway's auth dependency), and FR-LEGAL-002 (the PDPD/GDPR pack's retention and erasure contracts operate on this data). TLS in transit and secrets management are enforced by FR-PLAT-007; the audit row for sensitive <code>birth_data</code> access is written by FR-API-004.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// POST /auth/register\n{ \"email\": \"user@example.com\", \"password\": \"...\", \"birth_data\":\n  { \"date\": \"1990-05-01\", \"time\": \"10:30\", \"place\": \"Ha Noi\", \"tz\": \"+07:00\", \"kinh_do\": 105.85 } }\n// -&gt; stored users.birth_data (envelope, illustrative)\n{ \"alg\": \"AES-256-GCM\", \"iv\": \"b64...\", \"ct\": \"b64...\", \"tag\": \"b64...\", \"wrapped_dek\": \"b64...\" }</code></pre>\n<pre><code class=\"language-json\">// POST /auth/login -&gt; tokens\n{ \"access\": \"eyJ... (15-min JWT, claims sub/tier/iat/exp/jti)\", \"refresh\": \"eyJ... (revocable)\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Master-key custody: cloud KMS vs an app-held master key in a secret manager. Default: envelope encryption with a wrapped per-record data key so either custody works; the KMS adapter is preferred in production, the secret-manager path is the MVP fallback. Decide with FR-PLAT-007.</li><li>Refresh-token storage: opaque token in a revocation table vs a stateless JWT with a <code>jti</code> denylist. Default: <code>jti</code>-based revocation with a short denylist window matched to the refresh TTL; revisit if multi-device session management (FR-AUTH-003 scope) needs per-device records.</li><li>Whether Apple's private-relay email counts as a verified email. Default: treat the provider-asserted email as verified for social accounts and skip the email-verification step for them; local email/password accounts still verify. Confirm in FR-AUTH-003.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Plaintext birth_data at rest</td><td>encryption skipped or misconfigured</td><td>write path refuses to store <code>birth_data</code> without a valid envelope; a test asserts no plaintext column</td></tr><tr><td>Account enumeration</td><td>distinct errors for unknown-email vs wrong-password</td><td>one generic auth-failure response for both; test asserts indistinguishability</td></tr><tr><td>Forged social login</td><td>client passes an unverified profile</td><td>id token verified via Authlib OIDC (issuer, audience, signature, expiry) before any provisioning</td></tr><tr><td>Token forgery / replay</td><td>tampered or expired JWT</td><td><code>verify_access</code> rejects with typed <code>AuthError</code>; revoked refresh <code>jti</code> rejected on rotation</td></tr><tr><td>Master key in the DB</td><td>key stored alongside ciphertext</td><td>forbidden by design; the master key comes from KMS/secret manager, never a DB column; code review + config check</td></tr><tr><td>Password in logs</td><td>hash or plaintext logged</td><td>passwords and hashes are never logged; a log-capture test asserts absence</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR carries the product's highest data-sensitivity obligation, so treat the birth_data encryption and the account-enumeration-safe responses as non-negotiable, not polish. Keep the boundary clean: this FR owns identity, tokens, and the at-rest crypto; FR-AUTH-002 owns what a user is allowed to do and how much; FR-AUTH-004 owns export and erasure; FR-LEGAL-002 owns the consent, retention, and disclosure contracts around all of it. The package <code>tamthuc_auth</code> is shared with FR-AUTH-002/003/004; they extend it, so auth is one installable, mypy-clean unit that the FastAPI gateway (FR-API-001) mounts.</p>";
