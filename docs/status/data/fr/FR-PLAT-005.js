window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLAT-005"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR is the observability stack: the metrics, dashboards, structured logs, error tracking, product analytics, and alerting that make the running platform legible. It extends the deployed environments FR-PLAT-004 defines with a Prometheus + Grafana metrics pipeline, Sentry error tracking, Loki log aggregation, PostHog/Mixpanel product analytics, and Alertmanager/PagerDuty alert routing. It owns the metric catalog, the dashboards, and the alert rules; it does NOT own the behaviors those alerts observe - resilience (FR-PLAT-008), rate limiting (FR-API-003), or security controls (FR-PLAT-007) - though it is the instrument that makes them measurable.</p>\n<p>The platform SHALL expose Prometheus metrics across three families. Business metrics SHALL include queries/day, conversion, and retention (D1/D7/D30). Technical metrics SHALL include chart-generation latency (p50/p95/p99), LLM call latency, error rate, and database performance. Quality metrics SHALL include the expert-validation pass rate and NPS. Every metric SHALL carry stable labels (route, system/he, tier, engine_version) so a dashboard can slice by them. The instrumented services SHALL emit structured JSON logs shipped to Loki, and SHALL report unhandled exceptions to Sentry with a request-id that correlates a log line, a trace, and a Sentry issue. Product-analytics events (query cast, report viewed, sign-up, upgrade) SHALL flow to PostHog/Mixpanel behind a consent flag, and SHALL NOT carry sensitive personal data (birth_data, full question text) into the analytics sink (RISK-5).</p>\n<p>Alerting SHALL be rule-driven and SHALL fire at minimum on: chart-generation p95 &gt; 5s, error rate &gt; 1%, LLM downtime (the LLM provider probe failing), and database connection issues (pool exhaustion or connection errors). Alerts SHALL route through Alertmanager to PagerDuty with a severity, a runbook link, and a deduplication key, and SHALL NOT fire into a silent channel. Metric cardinality SHALL be bounded: labels are a closed set, never free-form user input.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>A platform that gives divination interpretations under legal constraints cannot be operated blind. The three metric families answer the three questions that matter: is the product growing (business), is it healthy (technical), and is it good (quality). Splitting them is deliberate - a green technical dashboard with a falling expert-validation pass rate is a product on fire that a pure-infra view would miss, and the quality family (validation pass rate, NPS) is exactly the signal the interpretation branch's correctness rides on (RISK-3, RISK-9). Keeping the quality metrics first-class, next to latency and error rate, is what makes interpretation regressions visible instead of anecdotal.</p>\n<p>The specific alert thresholds are the operability floor, not decoration. Chart-generation p95 &gt; 5s is the line where the casting path stops feeling instant; error rate &gt; 1% is the line where something is systematically broken rather than a one-off; LLM downtime and DB connection issues are the two external-dependency failures that take the whole flow down, and they are precisely what FR-PLAT-008's circuit breaker and degradation react to - so PLAT-008 depends on this FR to see those transitions. Routing to PagerDuty with a runbook link rather than a chat message keeps an incident from being missed at 3am. Correlating logs, traces, and Sentry by request-id turns \"the API is slow\" into \"this request, this route, this upstream, this stack\" in one hop. Excluding sensitive data from the analytics sink keeps a growth tool from becoming a second breach surface.</p>\n<h2>§3 - Contract (metrics / dashboards / alerts)</h2>\n<h3>Metric catalog (<code>docs/contracts/metrics-catalog.md</code>, emitted by <code>observability/metrics.py</code>)</h3>\n<table><thead><tr><th>Family</th><th>Metric</th><th>Type</th><th>Labels</th></tr></thead><tbody><tr><td>business</td><td><code>tt_queries_total</code></td><td>counter</td><td>route, he, tier</td></tr><tr><td>business</td><td><code>tt_conversion_total</code></td><td>counter</td><td>funnel_step, tier</td></tr><tr><td>business</td><td><code>tt_retention_active_users</code></td><td>gauge</td><td>cohort (d1/d7/d30)</td></tr><tr><td>technical</td><td><code>tt_chart_generation_seconds</code></td><td>histogram</td><td>he, engine_version</td></tr><tr><td>technical</td><td><code>tt_llm_call_seconds</code></td><td>histogram</td><td>provider, model</td></tr><tr><td>technical</td><td><code>tt_request_errors_total</code></td><td>counter</td><td>route, code</td></tr><tr><td>technical</td><td><code>tt_db_query_seconds</code></td><td>histogram</td><td>op, table</td></tr><tr><td>technical</td><td><code>tt_db_pool_in_use</code></td><td>gauge</td><td>pool</td></tr><tr><td>quality</td><td><code>tt_expert_validation_pass_ratio</code></td><td>gauge</td><td>he</td></tr><tr><td>quality</td><td><code>tt_nps</code></td><td>gauge</td><td>segment</td></tr></tbody></table>\n<p>Histograms carry the buckets needed to compute p50/p95/p99. Chart-generation and LLM latency are measured at the orchestrator seam (FR-API-001), so they reflect the user-visible cast, not an internal sub-step.</p>\n<h3>Alert rules (<code>deploy/observability/prometheus/rules/alerts.yml</code>)</h3>\n<pre><code class=\"language-yaml\">groups:\n  - name: tamthuc-slo\n    rules:\n      - alert: ChartGenP95High\n        expr: histogram_quantile(0.95, sum(rate(tt_chart_generation_seconds_bucket[5m])) by (le)) &gt; 5\n        for: 10m\n        labels: { severity: warning }\n        annotations: { runbook: \"deploy/observability/README.md#chart-gen-latency\" }\n      - alert: ErrorRateHigh\n        expr: sum(rate(tt_request_errors_total[5m])) / sum(rate(tt_requests_total[5m])) &gt; 0.01\n        for: 5m\n        labels: { severity: critical }\n      - alert: LLMDowntime            # provider probe failing\n        expr: max_over_time(tt_llm_up[5m]) == 0\n        for: 2m\n        labels: { severity: critical }\n      - alert: DBConnectionIssues     # pool exhaustion / connection errors\n        expr: tt_db_pool_in_use / tt_db_pool_size &gt; 0.9 or increase(tt_db_connection_errors_total[5m]) &gt; 0\n        for: 5m\n        labels: { severity: critical }</code></pre>\n<h3>Structured logging (<code>observability/logging.py</code>) and error tracking (<code>observability/sentry.py</code>)</h3>\n<p>JSON logs <code>{ ts, level, request_id, route, principal_hash, msg, ... }</code> shipped to Loki; <code>request_id</code> is the correlation key across logs, metrics exemplars, and Sentry. Sentry captures unhandled exceptions with the same <code>request_id</code> and scrubs sensitive fields before send. No log line or Sentry event contains plaintext <code>birth_data</code> or full question text.</p>\n<h3>Analytics (<code>observability/analytics.py</code>)</h3>\n<p>PostHog/Mixpanel events (<code>query_cast</code>, <code>report_viewed</code>, <code>signup</code>, <code>upgrade</code>) keyed by a pseudonymous id, gated on the analytics-consent flag (FR-LEGAL-002), carrying no sensitive payload.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The API and engine service expose a Prometheus scrape endpoint carrying all three metric families; a scrape returns the business, technical, and quality metrics with their documented labels and bounded cardinality.</li><li>Grafana loads the three dashboards (business, technical, quality) and each renders from live metrics: queries/day and retention D1/D7/D30 on business; chart-gen p50/p95/p99, LLM latency, error rate, and DB performance on technical; expert-validation pass rate and NPS on quality.</li><li>The four alert rules fire under synthetic load: chart-gen p95 driven above 5s fires <code>ChartGenP95High</code>; forced errors above 1% fire <code>ErrorRateHigh</code>; an LLM probe failure fires <code>LLMDowntime</code>; a saturated pool fires <code>DBConnectionIssues</code>; each routes to PagerDuty with a severity and a runbook link.</li><li>A request's <code>request_id</code> correlates its structured log line in Loki, its metric exemplar, and its Sentry issue; the same id appears in all three.</li><li>No metric label, log line, Sentry event, or analytics event contains plaintext <code>birth_data</code> or full question text; a redaction test asserts absence across all sinks.</li><li>Alerts route through Alertmanager to PagerDuty (no silent channel); a silenced/misrouted alert is caught by a routing test.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_metrics.py</code>: asserts each catalog metric is registered with the right type and labels; asserts cardinality is bounded (labels are a closed enum, not free-form input); asserts the p95 histogram buckets support the 5s threshold.</li><li>A dashboards-as-code check: the three Grafana JSON dashboards load and their panel queries reference only metrics in the catalog (a drift check fails on an undefined metric).</li><li>An alert-rule unit test (<code>promtool test rules</code>) drives each of the four expressions across pass/fire fixtures.</li><li>A redaction test feeds a request carrying birth_data + question text and asserts none of it reaches logs, Sentry, metrics labels, or analytics.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_api</code>, <code>pytest packages/tamthuc_api</code>; <code>promtool check rules</code>; the alert YAML and dashboards lint in the FR-PLAT-004 pipeline.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>observability/metrics.py</code>: register the three metric families (prometheus_client), instrument the orchestrator seam for chart-gen and LLM latency, the exception path for error rate, the repositories for DB performance.</li><li><code>observability/logging.py</code>: structured JSON logging with <code>request_id</code> propagation; ship to Loki.</li><li><code>observability/sentry.py</code>: Sentry init with a before-send scrubber; wire the <code>request_id</code> scope.</li><li><code>observability/analytics.py</code>: PostHog/Mixpanel client behind the consent flag; the event schema with no sensitive payload.</li><li><code>deploy/observability/*</code>: Prometheus scrape config, the alert rules, the three Grafana dashboards, Loki config, Alertmanager -&gt; PagerDuty routing; the runbook <code>README.md</code>.</li><li>Wire the metrics endpoint and middleware into <code>app.py</code>; document the catalog in <code>docs/contracts/metrics-catalog.md</code>.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-PLAT-004 (the deployed staging/production environments this instruments, and the pipeline that lints the alert YAML and dashboards). Blocks FR-PLAT-008 (resilience: the circuit-breaker and degradation transitions must be observable here, and PLAT-008's health signals feed these alerts). Instruments FR-API-001 (the orchestrator seam where chart-gen and LLM latency are measured), FR-API-003 (rate-limit and abuse events surface as metrics and alerts), and FR-RAG-007 (LLM fallback state feeds the <code>LLMDowntime</code> signal). Coordinates with FR-PLAT-007 (security events are audited and alertable) and FR-LEGAL-002 (the analytics-consent flag).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// structured log line (to Loki) - correlated by request_id, no sensitive payload\n{ \"ts\": \"2026-07-08T12:00:00Z\", \"level\": \"info\", \"request_id\": \"req_abc\",\n  \"route\": \"/api/v1/calculate/qimen\", \"he\": \"ky_mon\", \"tier\": \"Premium\",\n  \"chart_gen_ms\": 820, \"llm_ms\": 1400, \"msg\": \"query served\" }</code></pre>\n<pre><code class=\"language-json\">// PagerDuty alert (via Alertmanager) - severity + runbook, dedup key\n{ \"alert\": \"ChartGenP95High\", \"severity\": \"warning\", \"value_s\": 6.2,\n  \"runbook\": \"deploy/observability/README.md#chart-gen-latency\", \"dedup_key\": \"chartgen-p95\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Analytics vendor: PostHog (self-hostable, open) vs Mixpanel (managed). Default: PostHog at MVP for data-residency control (VN PDPD context), keeping the event schema vendor-neutral so a swap is a client change, not a schema change. Confirm with FR-LEGAL-002.</li><li>Tracing depth: metrics + logs + Sentry now, full distributed tracing (OpenTelemetry spans) later. Default: emit a <code>request_id</code> and metric exemplars now so traces can be added without re-instrumenting; a full OTel backend (Tempo/Jaeger) is a later step.</li><li>Where the expert-validation pass rate is computed. Default: FR-RAG-006's eval loop produces the number and this FR exposes it as <code>tt_expert_validation_pass_ratio</code>; until RAG-006 lands, the gauge reads from the KB-002 validation set run.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Blind operation</td><td>no metrics/alerts on a legal-sensitive surface</td><td>forbidden; the three metric families and the four alert rules are the floor before P1 exit</td></tr><tr><td>Sensitive data in telemetry</td><td>birth_data / question text in a label, log, Sentry event, or analytics</td><td>scrubbed at every sink; the redaction test asserts absence (RISK-5)</td></tr><tr><td>Silent alert</td><td>alert routed to a dead channel or none</td><td>Alertmanager -&gt; PagerDuty with severity + runbook; a routing test catches a silent path</td></tr><tr><td>Cardinality explosion</td><td>free-form input used as a metric label</td><td>labels are a closed enum; the cardinality test rejects unbounded labels</td></tr><tr><td>Quality invisibility</td><td>only infra metrics, no validation pass rate / NPS</td><td>the quality family is first-class; the quality dashboard renders it</td></tr><tr><td>Uncorrelated signals</td><td>logs, metrics, Sentry cannot be joined</td><td>one <code>request_id</code> correlates all three; the correlation test asserts it</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR is the instrument, not the machine: it measures the platform but changes no product behavior. Hold three disciplines - three metric families (business, technical, quality) so growth, health, and correctness are all visible; sensitive data never in telemetry (RISK-5); and no silent alerts, everything routes to PagerDuty with a runbook. The Python instrumentation lives in the <code>tamthuc_api</code> package and the IaC (Prometheus/Grafana/Loki/Alertmanager config) under <code>deploy/observability/</code>, reflecting the iac/python split. FR-PLAT-008 builds directly on this: its circuit-breaker and degradation transitions are only useful if they are observable here.</p>";
