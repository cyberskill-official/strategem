window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-QMDG-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR implements the pivot that makes a QiMen chart specific to the hour: find the truc phu (值符, the leading star) and truc su (值使, the leading door), then rotate the thien ban (sky plate) so the truc phu and its nghi sit over the palace that holds the hour stem. It is the step that binds the still earth plate (FR-QMDG-002) to the moment of the question, and it is one of the sharpest school-fork points in the whole engine (s5.2).</p>\n<p>The module SHALL, per the four steps of s5.1: (1) from the hour ganzhi, find the tuan thu (旬首, the jia-decade head that contains the hour) and its hidden nghi from the six-tuan table in §3; (2) locate that nghi on the earth plate to get the tuan-thu palace; (3) read the resting cuu tinh star and resting bat mon door at that palace as truc phu and truc su; (4) rotate the sky plate so the truc phu and its nghi move to the hour-stem palace, carrying the other stems, while the truc su is carried by counting from the tuan-thu palace to the hour palace.</p>\n<p>The module SHALL support two rotation lineages behind the flag <code>pan_method</code> in {<code>zhuan</code>, <code>fei</code>}, default <code>zhuan</code> (chuyen ban): <code>zhuan</code> rotates the nine stars as a rigid wheel; <code>fei</code> (phi ban) flies each star by its own Luoshu number, so the two produce different charts at the same hour. It SHALL resolve center-palace lodging behind the flag <code>zhong_gong_ky</code> in {<code>khon2</code>, <code>giu_nguyen</code>}, default <code>khon2</code> (a star or door landing in Trung 5 lodges to Khôn 2). Both flags SHALL be stamped into <code>co_truong_phai</code> (FR-PLAT-002).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Truc phu and truc su are the hinge of the chart: every star and door turns around them, so a wrong hour-stem palace is a wrong chart everywhere at once (s10.3). That is why this step gets the second-highest test density after dinh cuc.</p>\n<p>The chuyen-ban / phi-ban fork is not a rounding difference - the two lineages give genuinely different charts for the same instant, and their users reject each other's results (strategy RISK-2). Hardcoding either one would silently pick a side. So the rotation is a stamped flag with <code>zhuan</code> as the default because it is the most common thoi-gia practice, and the oracle gate runs both. The center-palace rule is a second, smaller fork: Trung 5 has no trigram of its own in many operations, so a piece landing there lodges to Khôn 2 by the usual convention - but that convention also varies, so it too is a flag rather than a constant.</p>\n<h2>§3 - Contract (algorithm)</h2>\n<h3>Tuan thu and hidden nghi (Claude-03 s5.1, verbatim)</h3>\n<p>Each of the six jia-decades hides one nghi, the yang stem that stands in for the hidden Giáp in the layout. The hidden nghi is used to find the tuan-thu palace on the earth plate.</p>\n<table><thead><tr><th>Tuần</th><th>Tuần thủ</th><th>Nghi ẩn</th></tr></thead><tbody><tr><td>甲子 Giáp Tý</td><td>甲子</td><td>戊 Mậu</td></tr><tr><td>甲戌 Giáp Tuất</td><td>甲戌</td><td>己 Kỷ</td></tr><tr><td>甲申 Giáp Thân</td><td>甲申</td><td>庚 Canh</td></tr><tr><td>甲午 Giáp Ngọ</td><td>甲午</td><td>辛 Tân</td></tr><tr><td>甲辰 Giáp Thìn</td><td>甲辰</td><td>壬 Nhâm</td></tr><tr><td>甲寅 Giáp Dần</td><td>甲寅</td><td>癸 Quý</td></tr></tbody></table>\n<p>Each tuan spans ten hours. Example: an hour in tuan Giáp Tý has tuan thu Giáp Tý, hidden under nghi Mậu; find Mậu on the earth plate, and the star and door resting there are truc phu and truc su.</p>\n<h3>The four steps (Claude-03 s5.1)</h3>\n<ol><li>From the hour ganzhi, compute the tuan thu (the jia-decade head containing the hour) and read its hidden nghi from the table above.</li><li>Find that nghi's palace on the <code>DiaBan</code> from FR-QMDG-002 - this is the tuan-thu palace.</li><li>The resting cuu tinh star at that palace is the truc phu; the resting bat mon door at that palace is the truc su. (The resting rings - each star's and door's home palace - are the fixtures formalized in FR-QMDG-004; this FR consumes them.)</li><li>Rotate the sky plate so the truc phu and its nghi move to the palace holding the hour stem; the other stems follow. The truc su is carried by counting from the tuan-thu palace to the hour palace.</li></ol>\n<h3>chuyen ban vs phi ban (Claude-03 s5.2, flag <code>pan_method</code>)</h3>\n<ul><li><code>zhuan</code> (轉盤, chuyen ban): treat the nine stars as a rigid wheel. The truc phu moves to the hour-stem palace and the other eight stars keep their relative order and rotate along. The most common thoi-gia method. Default.</li><li><code>fei</code> (飛盤, phi ban): each star flies by its own Luoshu number, not as a rigid block; the landing palace of the truc phu is computed by a formula on the so cuc and the stem position.</li></ul>\n<p>The two give different charts at the same hour, so <code>pan_method</code> is stamped and both are gated. Do not hardcode one as the only correct method.</p>\n<h3>Center palace and ky cung (Claude-03 s5.3, flag <code>zhong_gong_ky</code>)</h3>\n<p>Trung 5 has no trigram of its own in many steps, so a star or door landing in Trung lodges to Khôn 2 under the common convention (<code>khon2</code>, default); <code>giu_nguyen</code> keeps it in place. The rule must be applied consistently across every step or the oracle diverges.</p>\n<h3>Public types and entry point</h3>\n<pre><code class=\"language-rust\">pub enum PanMethod { Zhuan, Fei }            // default Zhuan\npub enum ZhongGongKy { Khon2, GiuNguyen }    // default Khon2\n\npub struct TrucPhuSu {\n    pub truc_phu: CuuTinh,        // leading star (type from FR-QMDG-004)\n    pub truc_su: BatMon,          // leading door\n    pub tuan_thu: GanChi,         // the jia-decade head\n    pub nghi_an: Can,             // hidden nghi\n    pub cung_tuan_thu: u8,        // 1..=9, tuan-thu palace on the earth plate\n    pub cung_gio: u8,             // 1..=9, palace holding the hour stem\n    pub thien_ban: [Can; 9],      // rotated sky-plate stems by palace 1..9\n    pub xoay: i8,                 // rotation offset consumed by FR-QMDG-004\n}\n\npub fn truc_phu_truc_su(dia: &amp;DiaBan, gio: GanChi, flags: &amp;QiMenFlags)\n    -&gt; Result&lt;TrucPhuSu, QiMenError&gt;;</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The tuan thu and hidden nghi are correct for a probe in each of the six tuan; a unit test enumerates all sixty hour ganzhi and checks the tuan thu and nghi against the §3 table.</li><li>The tuan-thu palace equals the palace of the hidden nghi on the earth plate; a unit test cross-checks against FR-QMDG-002 output.</li><li>Under <code>zhuan</code>, the sky plate is a rigid rotation carrying the truc phu to the hour-stem palace; a golden test on a worked hour asserts every palace stem.</li><li>Under <code>fei</code>, the sky plate matches the phi-ban formula; the two methods differ on at least the documented worked hour, proving the fork is real.</li><li><code>zhong_gong_ky = khon2</code> lodges a Trung-landing piece to Khôn 2; a unit test exercises an hour whose hour stem is in the center.</li><li><code>truc_phu_truc_su</code> matches the kinqimen truc phu, truc su, and sky plate across a sample, for every combination of <code>pan_method</code> and <code>zhong_gong_ky</code>.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/truc_phu_su_oracle.rs</code> loads truc phu / truc su / sky-plate rows from the kinqimen fixture and asserts an exact match, iterating <code>pan_method</code> x <code>zhong_gong_ky</code>.</li><li>Enumeration test over the sixty hour ganzhi for the tuan thu and hidden nghi.</li><li>Golden worked-hour test asserting the full rotated sky plate under <code>zhuan</code>, plus the divergence assertion between <code>zhuan</code> and <code>fei</code>.</li><li>Center-palace test: an hour whose hour stem lodges through Trung, checked under both <code>zhong_gong_ky</code> values.</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-qimen -- -D warnings</code>, <code>cargo test -p cyberos-qimen</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Add <code>truc_phu_su.rs</code>; encode the six-tuan hidden-nghi table and the tuan-thu computation from the hour ganzhi.</li><li>Locate the hidden nghi on the <code>DiaBan</code>; read the resting star and door (rings from FR-QMDG-004, imported as consts).</li><li>Implement <code>zhuan</code> (rigid-wheel rotation, compute <code>xoay</code>), then <code>fei</code> (per-star Luoshu flight).</li><li>Apply <code>zhong_gong_ky</code> lodging uniformly; add <code>pan_method</code> and <code>zhong_gong_ky</code> to <code>QiMenFlags</code> with their defaults.</li><li>Wire the oracle test, the enumeration test, and the divergence test.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-QMDG-002 (reads the earth plate). Blocks FR-QMDG-004 (the star / door / god rings are placed using the <code>xoay</code> offset and the truc phu / truc su this FR identifies) and FR-QMDG-006 (assembly). The resting cuu tinh and bat mon rings are formalized in FR-QMDG-004 and imported here; within one crate they are shared consts, so there is no cyclic build dependency.</p>\n<h2>§8 - Example payloads</h2>\n<p>The truc phu / truc su summary and the rotated sky plate for a worked hour:</p>\n<pre><code class=\"language-json\">{ \"truc_phu\": \"天蓬\", \"truc_su\": \"休門\",\n  \"thien_ban\": { \"1\": \"戊\", \"2\": \"己\", \"9\": \"乙\", \"...\": \"...\" },\n  \"co_truong_phai\": { \"pan_method\": \"zhuan\", \"zhong_gong_ky\": \"khon2\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>The phi-ban landing formula has minor variants across texts. Default: implement the mainstream so-cuc-plus-stem-offset form and lock it against kinqimen; if the oracle uses a different phi-ban variant, record which and gate to it, since <code>fei</code> is not the default and can be pinned precisely later.</li><li>Whether <code>giu_nguyen</code> (keep a Trung-landing piece in place) is ever the kinqimen convention, or only <code>khon2</code> is: default to <code>khon2</code>, keep <code>giu_nguyen</code> as a reserved value, and only exercise it if a school in scope requires it.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Wrong hour-stem palace</td><td>tuan thu or nghi lookup wrong</td><td>whole chart wrong; enumeration test fails; do not ship</td></tr><tr><td>pan_method hardcoded</td><td>only zhuan implemented</td><td>fei oracle rows fail; divergence test absent</td></tr><tr><td>Rigid-wheel offset wrong</td><td>zhuan rotation miscomputed</td><td>golden sky-plate test fails</td></tr><tr><td>Center lodging inconsistent</td><td>ky cung applied in some steps only</td><td>oracle diverges on center-landing hours</td></tr><tr><td>Resting-ring drift</td><td>rings differ from FR-QMDG-004</td><td>shared const import prevents divergence; test cross-checks</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR and FR-QMDG-004 are tightly coupled: this one identifies the pivot and rotates the stems and exposes the <code>xoay</code> offset; the next one paints the three dynamic rings using that offset. Keep the split clean - rotation math lives here, ring placement lives there - so each has a focused oracle gate. The chuyen / phi fork is the RISK-2 exemplar for QiMen: never let a default become an implicit \"only correct\" answer.</p>";
