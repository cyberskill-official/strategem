window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-PLAT-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR is the continuous-delivery pipeline. It extends the three-lane CI gate FR-PLAT-001 established (lint/typecheck/test for Rust, Python, and web) with the release half: unit and integration tests against real service containers, docker image builds for the three deployables (the Rust engine service, the Python API, the Next.js web app), a security scan (Trivy for images and dependencies, Snyk for dependency advisories), and a deploy that promotes to staging automatically and to production only behind a manual approval gate (Grok-22, Grok-39). It owns the pipeline and the Dockerfiles; it does NOT own the runtime infrastructure definitions (FR-PLAT-010 Terraform/K8s), the metrics stack (FR-PLAT-005), or the security controls themselves (FR-PLAT-007), though it is where those are exercised in CI.</p>\n<p>The pipeline SHALL, on every pull request, run the FR-PLAT-001 gate plus the integration tests (including the FR-PLAT-003 RLS isolation test against an ephemeral Postgres) and the security scan, and SHALL block merge on any failure. On merge to the main branch it SHALL build and tag the three docker images, push them to the registry, and deploy to staging automatically. Promotion from staging to production SHALL require a manual approval (a GitHub Environments protection rule / required reviewer); production SHALL NOT deploy without that approval. The security scan SHALL fail the build on a high or critical severity finding in an image or a dependency, and SHALL be overridable only by an explicit, recorded allowlist entry, never silently.</p>\n<p>Deploys SHALL be reproducible and rollback-able: images are content-addressed by tag (git SHA), the deployed tag is recorded, and a rollback re-deploys a prior known-good tag. Secrets SHALL come from the CI secret store and the environment, never from the repository (the control set is FR-PLAT-007; this FR consumes it).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The gate that stops bad code and the pipeline that ships good code are one continuous system, but they have different stakes, so they are split into two workflows: <code>ci.yml</code> (FR-PLAT-001, fast, runs on every push, blocks merge) and <code>cd.yml</code> (this FR, runs on merge, builds and deploys). Keeping the security scan and the integration tests on the pull-request path means a vulnerable dependency or a broken RLS policy is caught before merge, not after it has shipped - and RLS isolation is exactly the check that must never regress silently (RISK-5), so it runs as an integration test in the pipeline, not only as a local one.</p>\n<p>The staging -&gt; production manual approval gate is the single most important line in this FR. This is a product that gives divination interpretations under VN legal constraints; an unreviewed auto-deploy to production is the wrong default for a heritage-and-legal-sensitive surface. Auto-deploy to staging keeps iteration fast; a required human approval before production keeps a person in the loop for the surface real users see, mirroring the same human-in-the-loop principle the interpretation layer uses (strategy 4.4). Content-addressed image tags and a recorded deployed tag make rollback a re-deploy of a known SHA rather than a scramble.</p>\n<h2>§3 - Contract (pipeline stages / images / gate)</h2>\n<h3>Workflows</h3>\n<table><thead><tr><th>Workflow</th><th>Trigger</th><th>Stages</th></tr></thead><tbody><tr><td><code>ci.yml</code> (FR-PLAT-001)</td><td>push, pull_request</td><td>rust / python / web lanes (lint, typecheck, unit test)</td></tr><tr><td><code>security-scan.yml</code> (this FR)</td><td>pull_request, schedule</td><td>Trivy (fs + image), Snyk (deps); fail on high/critical</td></tr><tr><td><code>cd.yml</code> (this FR)</td><td>pull_request (integration), push to main (deploy)</td><td>integration tests -&gt; build+push images -&gt; deploy staging -&gt; [approval] -&gt; deploy production</td></tr></tbody></table>\n<h3>Pipeline stages (<code>cd.yml</code>)</h3>\n<pre><code>1. integration    services: postgres (FR-PLAT-003), redis (FR-PLAT-006 when present)\n                  run: migrations apply + RLS isolation test + API integration tests\n2. build          build engine.Dockerfile, api.Dockerfile, web.Dockerfile\n                  tag each image with ${{ github.sha }} (+ a moving 'staging'/'prod' tag)\n3. scan           Trivy scan the three built images; fail on HIGH|CRITICAL (allowlist file only)\n4. push           push images to the registry (GHCR or the chosen registry)\n5. deploy-staging (auto, on push to main) roll the staging environment to ${{ github.sha }}\n6. approve        GitHub Environment 'production' protection rule: required reviewer\n7. deploy-prod    (only after approval) roll production to the same ${{ github.sha }}</code></pre>\n<h3>The three images</h3>\n<ul><li><code>engine.Dockerfile</code> - multi-stage Rust build (<code>cargo build --release</code> of the engine service bin crate), a slim runtime image; this is the deterministic branch exposed over the la so envelope.</li><li><code>api.Dockerfile</code> - the <code>tamthuc_api</code> (and RAG/KB/auth) Python service, <code>uv</code>-synced, running FastAPI under an ASGI server.</li><li><code>web.Dockerfile</code> - the <code>apps/web</code> Next.js production build.</li></ul>\n<h3>Security scan gate</h3>\n<p>Trivy scans image filesystems and the built images; Snyk scans the Rust/Python/Node dependency manifests. A HIGH or CRITICAL finding fails the build. Suppression is only via a checked-in allowlist (<code>.trivyignore</code> / a Snyk policy file) with a comment and an owner, so every exception is reviewable in a diff (RISK-6 supply-chain hygiene; the deeper control set is FR-PLAT-007).</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>On a pull request, the pipeline runs the FR-PLAT-001 gate, the integration tests (migrations + RLS isolation against an ephemeral Postgres), and the security scan; any failure blocks merge.</li><li>On merge to main, the three docker images build, are tagged with the git SHA, are scanned, pushed, and deployed to staging automatically.</li><li>Production deploy does not run until a manual approval is granted via the <code>production</code> GitHub Environment protection rule; without approval, production stays on its prior tag.</li><li>A HIGH or CRITICAL Trivy/Snyk finding fails the build; the only way past is a checked-in allowlist entry with an owner and reason (verified by a test PR that introduces a known-vulnerable dependency).</li><li>The deployed tag is recorded, and a rollback re-deploys a prior git-SHA image and is exercised once in staging.</li><li>No secret is read from the repository; the pipeline sources secrets from the CI secret store / environment (FR-PLAT-007), verified by a scan for committed secrets in the security workflow.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>A test pull request that adds a dependency with a known CVE is shown to fail the <code>scan</code> stage; adding the allowlist entry (with owner+reason) is shown to pass - proof the gate bites and the override is explicit.</li><li>The integration stage is shown to run the FR-PLAT-003 RLS isolation test against a service Postgres and to fail the pipeline if isolation regresses.</li><li>A staging deploy is shown to roll to the merged SHA; a production deploy is shown to wait for approval and only then roll.</li><li>A rollback drill: re-deploy the previous SHA to staging and confirm the environment reports the rolled-back tag.</li><li>Gates: this FR is the pipeline; its own YAML is linted (<code>actionlint</code>) in the CI web/util lane; Dockerfiles are linted (<code>hadolint</code>).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>security-scan.yml</code>: Trivy (fs + image) and Snyk (deps) jobs; the <code>.trivyignore</code> / Snyk policy allowlist convention; a committed-secret scan.</li><li><code>deploy/docker/*.Dockerfile</code>: the three multi-stage images + <code>.dockerignore</code>; lint with hadolint.</li><li><code>cd.yml</code> integration job: spin up postgres (+ redis when FR-PLAT-006 lands), apply migrations, run the RLS isolation + API integration tests.</li><li><code>cd.yml</code> build/scan/push jobs: build the three images tagged by SHA, scan, push to the registry.</li><li><code>cd.yml</code> deploy jobs: <code>deploy-staging</code> (auto on main), the <code>production</code> Environment with a required-reviewer protection rule, <code>deploy-prod</code> gated on approval; record the deployed tag.</li><li><code>deploy/environments/*.md</code> + <code>deploy/README.md</code>: the staging/production environment contracts, the secret list (sourced, not stored), and the rollback runbook.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on FR-PLAT-001 (extends its CI skeleton and reuses the <code>just</code> gate recipes). Exercises FR-PLAT-003 in the integration stage (migrations + RLS isolation as a pipeline test). Blocks FR-PLAT-005 (observability wires into the deployed environments this FR defines), FR-PLAT-007 (the security controls are enforced through this pipeline's scan and secret sourcing), FR-PLAT-008 (resilience is deployed and smoke-tested here), and FR-PLAT-010 (Terraform/K8s replace the compose-based staging target this FR bootstraps). Consumes the secret store defined by FR-PLAT-007.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-yaml\"># cd.yml (abridged) - the production approval gate\ndeploy-prod:\n  needs: [deploy-staging]\n  environment:\n    name: production          # protection rule: required reviewer -&gt; manual approval\n  steps:\n    - run: ./deploy/roll.sh production ${{ github.sha }}   # same SHA promoted from staging</code></pre>\n<pre><code class=\"language-yaml\"># security-scan.yml (abridged) - fail on high/critical, allowlist only\n- uses: aquasecurity/trivy-action@...\n  with:\n    image-ref: ghcr.io/cyberskill/tamthuc-api:${{ github.sha }}\n    severity: HIGH,CRITICAL\n    exit-code: '1'\n    trivyignores: .trivyignore   # every entry has an owner + reason</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Registry and deploy target at MVP: GHCR + a compose-based single host (the cyberos deploy pattern) vs a managed platform. Default: GHCR images + a compose staging target now, so the pipeline is real before FR-PLAT-010 introduces Terraform/K8s; the deploy step is a thin script the IaC FR later replaces without changing stages 1-4.</li><li>Snyk vs Trivy overlap and licensing: run both, or Trivy alone. Default: Trivy for images+fs (open, no seat limit) plus Snyk for richer dependency advisories where a token exists; the gate condition (fail on high/critical) is identical regardless of which reports it.</li><li>Whether integration tests run on every PR or only pre-deploy. Default: on every PR (the RLS isolation test is too important to defer, RISK-5), accepting the extra minutes; move heavier suites behind a label if PR latency becomes a problem.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Unreviewed prod deploy</td><td>auto-deploy straight to production</td><td>forbidden; production is behind a required-reviewer Environment gate; staging is auto, prod is not</td></tr><tr><td>Vulnerable image ships</td><td>high/critical finding ignored</td><td>scan stage fails the build; only a checked-in allowlist entry (owner+reason) passes it</td></tr><tr><td>RLS regression ships</td><td>isolation test not in the pipeline</td><td>the FR-PLAT-003 isolation test runs in the integration stage and blocks merge/deploy on failure</td></tr><tr><td>Secret in repo</td><td>credential committed</td><td>committed-secret scan fails; secrets are sourced from the store, never the repo (FR-PLAT-007)</td></tr><tr><td>Unrollbackable deploy</td><td>mutable / non-SHA image tags</td><td>images are tagged by git SHA; the deployed tag is recorded; rollback re-deploys a prior SHA</td></tr><tr><td>Staging/prod drift</td><td>different images per environment</td><td>the same SHA-tagged images are promoted from staging to prod, never rebuilt for prod</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR turns the empty-but-gated repo (FR-PLAT-001) into a shipping system, and its one inviolable rule is the manual approval before production - a heritage-and-legal-sensitive surface does not auto-deploy to real users (strategy 4.1, RISK-4/RISK-5 context). Keep <code>ci.yml</code> (fast, blocks merge) and <code>cd.yml</code> (builds, deploys) separate but complementary, keep the security gate failing on high/critical with only a reviewable allowlist to pass it, and keep images content-addressed by SHA so rollback is a promotion, not a rebuild. The compose-based staging target is a bootstrap; FR-PLAT-010 swaps in Terraform/K8s behind the same pipeline stages.</p>";
