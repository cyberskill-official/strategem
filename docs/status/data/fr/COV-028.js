window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["COV-028"]="<h1>COV-028 — LMStudio local AI — OpenAI-compatible LLM client + honest degraded path</h1>\n<h2>Goal</h2>\n<p>Interpretation/RAG uses a <strong>configurable OpenAI-compatible HTTP client</strong> whose default local profile targets <strong>LMStudio</strong> (<code>http://127.0.0.1:1234/v1</code> or compose <code>host.docker.internal:1234</code>) with a selectable model id — no cloud API key required for the local path. When LMStudio is down, the product MUST fall back to the existing template/degraded path with an honest badge (no fake RAG/LLM claims).</p>\n<p>Closes residual gaps from:</p>\n<ul><li>Enterprise OBJECTIVE: fully worked locally with LMStudio as local AI</li><li>Today <code>tamthuc_rag.llm</code> only ships <code>StubLlm</code> (CI); no OpenAI-compatible production client</li><li>Dual-benchmark RAG dimensions need a real local AI entry for enterprise claims</li></ul>\n<h2>§1 Acceptance criteria (normative)</h2>\n<ol><li>MUST implement <code>OpenAICompatibleLlm</code> (or equivalent name) in <code>packages/tamthuc_rag</code> implementing the existing <code>LlmClient</code> protocol (<code>complete(prompt) -&gt; dict</code> with beginner/expert/recommendations shape).</li><li>MUST configure via env (names documented): base URL (default <code>http://127.0.0.1:1234/v1</code>), model id, optional API key header (<code>LM_API_KEY</code> / <code>OPENAI_API_KEY</code> — empty ok for LMStudio), timeout, and enable flag (<code>LLM_BACKEND=openai_compatible|stub|off</code>).</li><li>MUST call OpenAI-compatible <code>POST {base}/chat/completions</code> (or documented equivalent) and parse structured JSON from the model (repair/reject invalid schema per RAG-003 guard).</li><li>MUST wire factory used by interpret/orchestrator so production API path can select LMStudio without code edits.</li><li>MUST preserve degraded/template path when backend unreachable or circuit open (<code>RAG-007</code>); disclosure/badge MUST NOT claim live LLM when stub/template used.</li><li>MUST document LMStudio load model + base URL + Docker <code>extra_hosts: host.docker.internal:host-gateway</code> in <code>docs/deploy/local-docker-lmstudio.md</code>.</li><li>MUST unit-test client against a local HTTP mock (contract); optional live dual-run when LMStudio is up. Capture logs or honest probe-failure + mock evidence under operator scratch.</li><li>MUST NOT require cloud OpenAI as the default for local enterprise path.</li></ol>\n<h2>§2 Non-goals</h2>\n<ul><li>Shipping or containerizing LMStudio itself.</li><li>Multi-provider router sprawl (one OpenAI-compatible adapter is enough; cloud is optional same client).</li><li>Replacing anti-hallucination / HumanReviewGate (RAG-003/004).</li></ul>\n<h2>§3 Verification</h2>\n<ul><li>Unit tests: mock server returns chat.completions → structured interpret dict; timeout/connection → raises or triggers degraded.</li><li>Docs + env sample present.</li><li>When LMStudio running: two interpretation calls succeed with model disclosure; when not: degraded path honest.</li><li><code>bash .cyberos/cuo/gates/run-gates.sh</code> green for Python lane changes.</li><li>Human sets FR <code>done</code> only after HITL.</li></ul>\n<h2>§4 Dependencies</h2>\n<p>depends_on: RAG-003, RAG-007, COV-011 (product default path; client can land first)</p>\n<p>Note: Implementation MAY land the client before COV-011 product default flip; COV-011 remains the productization gate for default rag|template mode.</p>\n<h2>§5 Refs</h2>\n<p>enterprise-local-objective, Claude-06 s4, Grok-32, benchmark-claude §2 RAG</p>";
