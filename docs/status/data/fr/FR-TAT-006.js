window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-TAT-006"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This FR assembles the whole Thai At engine from the slices FR-TAT-001..005 into one deterministic pipeline, emits the la so envelope for <code>he = \"thai_at\"</code>, stamps the full flag set, and gates the engine 100% against the kintaiyi oracle. It is the terminal MUST of the TAT module and the culmination of the <code>cyberos-thaiat</code> crate.</p>\n<p>The module SHALL run the pipeline in order (Claude-04 s7.1): compute the tich for the chosen level and reduce to the cuc (FR-TAT-001, and FR-TAT-004 for nguyet / nhat / thoi ke), seat Thai At and lay the sixteen than (FR-TAT-002), place the eight tuong and compute the toan (FR-TAT-003), then recognize the cach and compute tam tai and the four criteria (FR-TAT-005). Every step is deterministic, so the assembled chart SHALL be reproducible from <code>dau_vao</code> plus the stamped flags alone.</p>\n<p>The module SHALL emit the s7.1 engine JSON mapped into the FR-PLAT-002 envelope: <code>he = \"thai_at\"</code> at the top level; <code>dau_vao</code>; <code>lich_phap</code> from FR-CORE-005; the engine plates (<code>tich</code>, <code>thai_at_cung</code>, <code>thai_at_ring</code>, <code>thap_luc_than</code>, <code>bat_tuong</code>, <code>cac_toan</code>, <code>tam_tai</code>) inside <code>ban</code>; each recognized cach as an envelope-level <code>cach_cuc</code> entry; and the flag set in <code>co_truong_phai</code>. The module SHALL NOT invent an engine-local envelope shape - it fills the shared PLAT-002 envelope.</p>\n<p>The module SHALL stamp the full flag set into <code>co_truong_phai</code> (Claude-04 s7.2): <code>epoch</code> (default <code>kim_kinh</code>, the ~60-year-gap school flag from FR-TAT-001), <code>dem_toan</code> (default <code>truoc_thai_at</code>, the count-stop flag from FR-TAT-003), the time-level <code>cap</code> (nien / nguyet / nhat / thoi), and the than-name variant handling flag (FR-TAT-002). Every flag that changed the chart SHALL be stamped, or it is a reproduction defect (RISK-2). The module SHALL compute the chart cache key per FR-PLAT-002 (a stable hash keyed by year + cap + epoch and the sorted flag set), since the whole pipeline is deterministic and cacheable (Claude-04 s7.1).</p>\n<p>Acceptance SHALL be a 100% match to the kintaiyi oracle per epoch AND per time level, with dedicated boundary tests around the don switch (Dong Chi / Ha Chi) and the cuc wrap (72 -&gt; 1), and overflow tests on the large tich (Claude-04 s7.2). This gate SHALL run in CI and is a stop-ship if it fails.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The assembly is where the la so envelope contract (FR-PLAT-002) and the flag discipline (RISK-2) become real for TaiYi. Every prior TAT slice emitted a fragment into <code>ban</code>; this FR wires them into the one envelope shape the interpretation branch reads, so the AI layer sees a Thai At chart in exactly the same frame as a QiMen or LiuRen chart, distinguished only by the <code>he</code> tag. Filling the shared envelope rather than a bespoke shape is what keeps the deterministic-engine / AI-layer boundary a hard, typed contract instead of a per-engine special case (strategy 4.3, RISK-8).</p>\n<p>The flag stamp is the load-bearing correctness property here. TaiYi's epoch flag re-casts the entire chart across a ~60-year gap, the <code>dem_toan</code> flag shifts both toan by a mark, the <code>cap</code> selects among four whole plates, and the than-variant flag changes displayed names; a chart that does not stamp every one of these cannot be reproduced or defended, and two schools cannot tell under which conventions it was cast (Claude-04 s7.2, strategy 4.4). Stamping them all, and deriving the cache key from them, is the technical expression of the cultural-fairness rule (strategy section 7) and the thing that makes the oracle gate meaningful - you can only claim a 100% match if you also say against which flags.</p>\n<p>The oracle gate is the whole point of building the engine in Rust to the digit. TaiYi is the most calculation-heavy of the three systems and its base is a count near ten million reduced mod seventy-two, so the failure modes are silent arithmetic drift and mis-transcribed counting rules, not crashes. The only defense that scales is an exact match to kintaiyi across every flag combination and every time level, backed by the boundary and overflow tests the source explicitly calls for (Claude-04 s7.2). This is TaiYi's equivalent of the kinqimen gate for QiMen and the kinliuren gate for LiuRen.</p>\n<h2>§3 - Contract (pipeline, envelope, flags)</h2>\n<h3>The engine pipeline (Claude-04 s7.1)</h3>\n<pre><code>def cast_thai_at(dau_vao, flags, lich):            # lich = FR-CORE-005 output (Dong Chi / Ha Chi anchor)\n    tich  = tich_theo_cap(flags.cap, dau_vao, flags.epoch, lich)     # FR-TAT-001 / FR-TAT-004\n    seat  = an_thai_at(tich.nhap_cuc, tich.duong_don)                # FR-TAT-002\n    ring  = THAP_LUC_THAN                                            # FR-TAT-002\n    tuong = bat_tuong(tich, seat, dau_vao.nam_chi, flags.dem_toan)   # FR-TAT-003\n    toan  = cac_toan(tuong, seat, flags.dem_toan)                    # FR-TAT-003\n    cach  = nhan_dien_cach_cuc(tuong, seat.thai_at_ring)             # FR-TAT-005 (SHOULD; else empty)\n    tt    = tinh_tam_tai(ban)                                        # FR-TAT-005 (SHOULD; else None)\n    ban   = ThaiAtBan { tich, seat, ring, tuong, toan, tam_tai: tt }\n    return to_envelope(dau_vao, lich, ban, cach, flags)             # map into FR-PLAT-002</code></pre>\n<h3>The s7.1 engine JSON (reproduced faithfully)</h3>\n<pre><code class=\"language-json\">{\n  \"he\": \"thai_at\",\n  \"dau_vao\": { \"nam_ce\": 2004, \"cap\": \"nien_ke\", \"epoch\": \"kim_kinh\" },\n  \"tich\": { \"tich_nien\": 10155921, \"nhap_cuc\": 33, \"can_chi\": \"甲申\", \"duong_don\": true },\n  \"thai_at_cung\": 1,\n  \"thap_luc_than\": { },\n  \"bat_tuong\": {\n    \"van_xuong\":\"...\", \"thuy_kich\":\"...\",\n    \"chu_dai_tuong\":\"...\", \"khach_dai_tuong\":\"...\",\n    \"chu_tham_tuong\":\"...\", \"khach_tham_tuong\":\"...\",\n    \"ke_than\":\"...\"\n  },\n  \"cac_toan\": {\n    \"chu_toan\":0, \"khach_toan\":0,\n    \"chu_truong_doan\":\"truong\", \"khach_truong_doan\":\"doan\"\n  },\n  \"cach_cuc\": [\"掩\", \"格\"],\n  \"tam_tai\": \"du\",\n  \"co_truong_phai\": {\n    \"epoch\": \"kim_kinh\",\n    \"dem_toan\": \"truoc_thai_at\"\n  }\n}</code></pre>\n<h3>Mapping into the PLAT-002 envelope</h3>\n<p>The flat s7.1 shape maps into the shared envelope thus: <code>he</code>, <code>dau_vao</code>, <code>lich_phap</code> (FR-CORE-005), and <code>provenance</code> at the top level; <code>tich</code>, <code>thai_at_cung</code>, <code>thai_at_ring</code>, <code>thap_luc_than</code>, <code>bat_tuong</code>, <code>cac_toan</code>, <code>tam_tai</code> inside <code>ban</code>; the s7.1 <code>cach_cuc</code> string list becomes the envelope-level <code>cach_cuc</code> array of FR-PLAT-002 <code>CachCuc</code> objects (FR-TAT-005); <code>co_truong_phai</code> is the full flag set below.</p>\n<h3>The flag set (Claude-04 s7.2)</h3>\n<pre><code class=\"language-rust\">#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\npub struct CoTruongPhaiTat {\n    pub epoch: Epoch,                    // default KimKinh (FR-TAT-001); re-casts the whole chart\n    pub dem_toan: DemToan,               // default TruocThaiAt (FR-TAT-003); shifts both toan by a mark\n    pub cap: Cap,                        // nien / nguyet / nhat / thoi (FR-TAT-004)\n    pub bien_the_ten_than: BienTheTenThan,   // than-name variant handling (FR-TAT-002)\n}\n// stamped whole into co_truong_phai as a sorted map, so the cache key is stable across languages</code></pre>\n<h3>Public types (<code>crates/cyberos-thaiat/src/</code>)</h3>\n<pre><code class=\"language-rust\">pub struct ThaiAtBan {\n    pub tich: TichCap, pub thai_at_cung: u8, pub thai_at_ring: u8,\n    pub thap_luc_than: [Than; 16], pub bat_tuong: BatTuong, pub cac_toan: CacToan,\n    pub tam_tai: Option&lt;TamTai&gt;,         // filled by FR-TAT-005; None if that SHOULD is not built\n}\n\npub fn cast_thai_at(dau_vao: &amp;DauVaoTat, flags: &amp;CoTruongPhaiTat, lich: &amp;LichPhap) -&gt; LaSo;\npub fn cache_key(dau_vao: &amp;DauVaoTat, flags: &amp;CoTruongPhaiTat) -&gt; String;   // year + cap + epoch + sorted flags</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>cast_thai_at</code> runs the full pipeline and emits a valid FR-PLAT-002 envelope with <code>he = \"thai_at\"</code>; the golden 2004 nien ke chart matches the s5.1 / s7.1 numbers (tich 10,155,921, cuc 33, Giap Than, duong don) and round-trips through the envelope.</li><li>The oracle gate passes 100% against kintaiyi per epoch (<code>kim_kinh</code>, <code>co_dien</code>) AND per time level (nien / nguyet / nhat / thoi ke); the fixture covers both dons and both <code>dem_toan</code> values.</li><li><code>co_truong_phai</code> stamps <code>epoch</code>, <code>dem_toan</code>, <code>cap</code>, and the than-variant flag on every chart; a reproduction test recasts from the stamp alone and reproduces the chart byte-for-byte; an unstamped flag fails it.</li><li><code>cache_key</code> is a stable hash of year + cap + epoch + the sorted flag set (FR-PLAT-002); two casts with identical inputs and flags produce identical keys, and changing any stamped flag changes the key.</li><li>Boundary tests pass: the don switch at Dong Chi and Ha Chi, the cuc wrap (72 -&gt; 1), and overflow tests on the large tich (nhat / thoi ke) all match kintaiyi.</li><li>The envelope's <code>ban</code> is opaque to non-<code>thai_at</code> readers (FR-PLAT-002) and the interpretation branch never writes <code>ban</code> / <code>cach_cuc</code> / <code>lich_phap</code> / <code>co_truong_phai</code>.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>Unit: the golden 2004 nien ke chart end to end; the flag-stamp reproduction test; the cache-key stability / sensitivity test.</li><li>Oracle: <code>tests/engine_kintaiyi.rs</code> loads <code>fixtures/engine_kintaiyi_all_flags.csv</code> (generated once from kintaiyi across the epoch x cap x dem_toan product, many years, both dons, with day / hour probes) and asserts the whole chart - tich, cuc, Thai At seat, sixteen than, eight tuong, both toan, the cach set, tam tai - matches exactly. This is the TaiYi stop-ship gate (RISK-2), and it MUST run in CI. It composes the FR-TAT-001..005 oracle fixtures into one engine-level gate and stands on FR-CORE-006's harness for the shared solstice anchors.</li><li>Property: over a wide year span and the full flag product, <code>cast_thai_at</code> is total and deterministic (same input + flags -&gt; byte-identical chart); the cache key round-trips.</li><li>Boundary: the don switch (both solstices), the cuc wrap, and the large-tich overflow (nhat / thoi ke) each have a dedicated case vs kintaiyi.</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-thaiat -- -D warnings</code>, <code>cargo test -p cyberos-thaiat</code>, plus the kintaiyi oracle gate in CI.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>ban.rs</code>: the <code>ThaiAtBan</code> type and its serialization into the FR-PLAT-002 <code>ban</code> slot for <code>he = \"thai_at\"</code>; the s7.1 -&gt; envelope mapping.</li><li><code>engine.rs</code>: <code>cast_thai_at</code> running the FR-TAT-001..005 pipeline in order; the <code>CoTruongPhaiTat</code> flag set with the s7.2 defaults; <code>cache_key</code> per FR-PLAT-002 (year + cap + epoch + sorted flags).</li><li>Wire <code>lich_phap</code> from FR-CORE-005 (the Dong Chi / Ha Chi anchor, shared with QiMen / LiuRen) into the envelope and into FR-TAT-004's nhat / thoi ke.</li><li>Fill the envelope <code>cach_cuc</code> array from FR-TAT-005's <code>map_to_envelope_cach_cuc</code>; set <code>tam_tai</code> in <code>ban</code>; leave both empty / None if the SHOULD FR-TAT-005 / FR-TAT-004 levels are not yet built (the MUST nien ke path still emits a valid envelope).</li><li>Generate the all-flags kintaiyi fixture once (documented script, not run in CI) across the epoch x cap x dem_toan product and commit; wire the engine oracle, reproduction, cache-key, boundary, and overflow tests.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Hard depends on FR-TAT-003 (the tuong and toan - the MUST spine that makes a chart meaningful) and FR-CORE-006 (the oracle cross-check harness and the shared solstice anchors). Soft-consumes FR-TAT-004 (the nguyet / nhat / thoi ke levels - without them the assembly still emits and gates nien ke; with them the gate extends to all four levels) and FR-TAT-005 (the cach set and tam tai - without them the envelope <code>cach_cuc</code> is empty and <code>tam_tai</code> is None, still valid). Transitively stands on FR-TAT-001 (tich / reductions / epoch), FR-TAT-002 (ring / seat), and FR-PLAT-002 (the envelope, the version rule, the cache-key rule). Blocks FR-CHART-003 (the TaiYi chart view renders this envelope: cuu cung, sixteen than, tuong). Soft-feeds STRAT-004 (cross-system validate can add TaiYi as a third opinion once this lands) and EDU-002 (the TaiYi engine as an auto-grader at curriculum level 3).</p>\n<h2>§8 - Example payloads</h2>\n<p>Full envelope for the 2004 nien ke golden chart (engine plates illustrative except the pinned s5.1 tich; the rest is asserted against kintaiyi):</p>\n<pre><code class=\"language-json\">{ \"envelope_version\": 1, \"he\": \"thai_at\",\n  \"dau_vao\": { \"nam_ce\": 2004, \"cap\": \"nien_ke\", \"epoch\": \"kim_kinh\" },\n  \"lich_phap\": { \"...\": \"from FR-CORE-005; supplies the Dong Chi / Ha Chi anchor for the don\" },\n  \"ban\": {\n    \"tich\": { \"tich_nien\": 10155921, \"nhap_cuc\": 33, \"can_chi\": \"甲申\", \"duong_don\": true },\n    \"thai_at_cung\": 1, \"thai_at_ring\": 14,\n    \"thap_luc_than\": { \"...\": \"the 16-than ring, FR-TAT-002\" },\n    \"bat_tuong\": { \"van_xuong\": 8, \"thuy_kich\": 3, \"ke_than\": 5,\n                   \"chu_dai_tuong\": 4, \"khach_dai_tuong\": 12,\n                   \"chu_tham_tuong\": 6, \"khach_tham_tuong\": 2 },\n    \"cac_toan\": { \"chu_toan\": 15, \"khach_toan\": 8,\n                  \"chu_truong_doan\": \"truong\", \"khach_truong_doan\": \"doan\" },\n    \"tam_tai\": \"du\"\n  },\n  \"cach_cuc\": [\n    { \"id\": \"taiyi_yem\", \"name\": \"掩\", \"cung\": 1, \"polarity\": \"hung\", \"citations\": [\"Thong Tong Bao Giam q6\"] },\n    { \"id\": \"taiyi_cach\", \"name\": \"格\", \"cung\": 5, \"polarity\": \"trung\", \"citations\": [\"Kim Kinh Thuc Kinh\"] }\n  ],\n  \"co_truong_phai\": { \"epoch\": \"kim_kinh\", \"dem_toan\": \"truoc_thai_at\",\n                      \"cap\": \"nien_ke\", \"bien_the_ten_than\": \"chuan\" },\n  \"provenance\": { \"engine\": \"tat\", \"engine_version\": \"0.1.0\", \"cast_at\": \"...\", \"cache_key\": \"...\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether <code>ban</code> should become a tagged union in the shared <code>laso-envelope</code> crate now that all three engines exist (the deferral in FR-PLAT-002 §9 named TAT-006 as the revisit point). Default: keep <code>ban</code> opaque (<code>serde_json::Value</code> in the shared crate, strongly typed <code>ThaiAtBan</code> in this crate); revisit only if the interpretation branch needs a cross-engine typed reader.</li><li>The cache-key granularity for nhat / thoi ke: nien / nguyet key cleanly by year + cap + epoch, but hour-level casts vary within a day. Default: include the casting instant at the cap's granularity in the key (per FR-PLAT-002's \"dau_vao rounded to the casting granularity\"); confirm the rounding with PLAT-006.</li><li>Whether kintaiyi implements both <code>dem_toan</code> conventions and both epochs identically to our reading; if it covers only one, the other flag combination is gated against a second source or documented as flagged-but-unverified, never silently shipped as matched.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Bespoke envelope shape</td><td>engine invents its own JSON instead of PLAT-002</td><td>envelope schema validation fails; assembly must fill the shared shape</td></tr><tr><td>Unstamped flag</td><td>chart cast under one flag, stamped as another (or not at all)</td><td>reproduction test recasts from the stamp and diverges -&gt; fail (RISK-2)</td></tr><tr><td>Oracle drift on a flag combo</td><td>a cuc / tuong / toan off under some epoch x cap</td><td>the all-flags kintaiyi gate fails per combo; stop-ship</td></tr><tr><td>Non-deterministic cache key</td><td>flags hashed in map order</td><td>cross-language key-equality test fails (FR-PLAT-002)</td></tr><tr><td>AI writes to ban / cach_cuc</td><td>interpretation branch mutates the chart</td><td>forbidden by read-only consumers; no setter exposed</td></tr><tr><td>Large-tich overflow at hour level</td><td>nhat / thoi ke tich in a narrow int</td><td>overflow test on the large tich fails; use u64 per FR-TAT-004</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This FR closes the TaiYi engine: it is the third and last of the three casting engines (QiMen QMDG-006, LiuRen LN-006, TaiYi TAT-006), each ending in the same shape - assemble the slices, fill the PLAT-002 envelope, stamp every flag, gate 100% against its oracle (kinqimen / kinliuren / kintaiyi). TaiYi's acceptance is uniquely two-dimensional - per epoch AND per time level - because it carries both the epoch school split and the four-plate structure, so the all-flags fixture is larger than the other two engines' (Claude-04 s7.2). Keep the engine one cargo-testable crate: FR-TAT-001 owned the birth, FR-TAT-002..005 extended it, and this FR only assembles and gates - it adds no new casting rule, so any oracle divergence points back into an upstream slice, not here. Because TaiYi speaks to the largest matters, the deterministic facts stop at this envelope; the cited, cautious, human-gated reading is FR-RAG-003's, and it never writes these fields (strategy 4.4, section 7, tat module notes).</p>";
