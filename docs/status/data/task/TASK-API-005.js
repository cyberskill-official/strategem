window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-API-005"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<ol><li><code>GET /api/v1/knowledge/patterns</code> MUST honor the <code>system</code> query parameter as the canonical filter for classical system (<code>qimen</code> | <code>liuren</code> | <code>taiyi</code>), and MUST continue to accept <code>he=</code> as an alias (including Vietnamese codes <code>ky_mon</code> / <code>luc_nham</code> / <code>thai_at</code>). When either filter is set, every returned row MUST match the requested system; <code>total</code> MUST equal the filtered set size (not the unfiltered catalog size).</li></ol>\n<ol><li>An unknown <code>system</code> / <code>he</code> value MUST return HTTP 200 with <code>patterns: []</code> and <code>total: 0</code> (not the full catalog, not 4xx). Omitting both filters MUST return the full seeded catalog (subject to <code>limit</code>).</li></ol>\n<ol><li>The OpenAPI sketch at <code>docs/contracts/openapi-v1.md</code> MUST document <code>GET /api/v1/knowledge/patterns</code> with query params <code>system</code>, <code>he</code>, <code>q</code>, and <code>limit</code>, stating that <code>system</code> is canonical and <code>he</code> is the alias.</li></ol>\n<ol><li><code>scripts/smoke-prod-full.sh</code> MUST assert the filter, not merely that the request returns 200: after <code>?system=qimen</code>, every returned row's <code>system</code> MUST be <code>qimen</code>, and filtered <code>total</code> MUST be strictly less than the unfiltered catalog <code>total</code> when the catalog contains more than one system.</li></ol>\n<ol><li>The patterns browse UI (<code>apps/web/app/patterns/page.tsx</code>) SHOULD send <code>system=</code> when a system filter is selected (keeping <code>he=</code> acceptable as a transitional alias). Either param is valid server-side; the UI MUST NOT regress to a path that ignores the selected system.</li></ol>\n<ol><li>When acceptance criteria in §4 pass, the deferred bullet in <code>docs/tasks/_audits/2026-07-25-live-truth-audit.md</code> that claims <code>?system=</code> ignores the filter MUST be marked remediated with a dated note citing this task — not silently deleted without a trail.</li></ol>\n<h3>Re-verify note (authoring session 2026-07-27)</h3>\n<p>Live prod already filters correctly (<code>total</code> 175 unfiltered → 105 <code>qimen</code> / 40 <code>liuren</code> / 30 <code>taiyi</code>; aliases <code>ky_mon</code> work). Handler + unit test <code>test_filter_by_system_query_param</code> exist on <code>main</code>. This task is therefore a <strong>contract lock + acceptance hardening</strong> improvement: close the audit finding, strengthen smoke/OpenAPI/UI alignment, and prevent regression — not a greenfield filter implementation.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p><strong>Why keep <code>he=</code> as alias (§1 #1)?</strong> TASK-COV-019 and the browse UI shipped with <code>he=</code>; TASK-API-001's contract names <code>?system=</code>. Dual acceptance avoids breaking the live UI while making the documented API name work. Canonicalizing on <code>system</code> matches the la so / engine vocabulary used elsewhere in the gateway.</p>\n<p><strong>Why empty-set for unknown system (§1 #2)?</strong> Returning the full catalog on a typo reintroduced the 2026-07-25 live-audit defect class (\"filter ignored\"). Empty is the honest answer and is cheap to assert.</p>\n<p><strong>Why strengthen smoke (§1 #4)?</strong> Today's smoke logs <code>patterns filter system=…</code> without failing when the first row is missing or wrong-system. Unit tests alone did not catch the pre-fix prod path; smoke must guard deploy drift.</p>\n<p><strong>Why UI SHOULD prefer <code>system=</code> (§1 #5)?</strong> Keeps the product surface and the OpenAPI name aligned so the next live audit does not re-flag a \"works via alias only\" gap as a contract miss.</p>\n<h2>§3 - Contract (endpoint / filter / smoke)</h2>\n<h3>Response shape (unchanged)</h3>\n<pre><code class=\"language-json\">{\n  \"patterns\": [ { \"id\": \"...\", \"system\": \"qimen\", \"he\": \"ky_mon\", \"name\": \"...\", \"...\": \"...\" } ],\n  \"total\": 105,\n  \"source\": \"tamthuc_kb.seed\"\n}</code></pre>\n<h3>Filter resolution (<code>packages/tamthuc_api/.../routes/knowledge.py</code>)</h3>\n<pre><code class=\"language-python\">he_n = (system or he or \"\").strip().lower()\n# alias map: ky_mon→qimen, luc_nham→liuren, thai_at→taiyi (+ english ids)\n# rows kept only when r.system == want OR r.he == he_n</code></pre>\n<h3>OpenAPI addition (<code>docs/contracts/openapi-v1.md</code>)</h3>\n<p>Document:</p>\n<ul><li><code>GET /api/v1/knowledge/patterns?system=&amp;he=&amp;q=&amp;limit=</code></li><li><code>system</code> canonical; <code>he</code> alias; both optional; <code>limit</code> 1..500 default 200</li></ul>\n<h3>Smoke assertion (<code>scripts/smoke-prod-full.sh</code>)</h3>\n<p>After unfiltered <code>total=T0</code> and filtered <code>?system=qimen</code> body:</p>\n<ol><li>HTTP 200</li><li>every <code>patterns[i].system == \"qimen\"</code> (or empty list only if catalog has zero qimen — not the case for seed ≥150)</li><li><code>total &lt; T0</code> when <code>T0</code> spans multiple systems</li></ol>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><strong>System filter subsets</strong> — <code>GET .../patterns?system=qimen&amp;limit=500</code> returns <code>total</code> strictly less than unfiltered <code>total</code>, and every row has <code>system == \"qimen\"</code>.</li><li><strong>Alias parity</strong> — <code>?system=ky_mon</code> and <code>?he=qimen</code> return the same <code>total</code> and the same system set as <code>?system=qimen</code>.</li><li><strong>Cross-system exclusion</strong> — <code>?system=liuren</code> rows are all <code>liuren</code>; none are <code>qimen</code> or <code>taiyi</code>.</li><li><strong>Unknown system empty</strong> — <code>?system=nope</code> returns <code>patterns: []</code> and <code>total: 0</code>.</li><li><strong>Smoke hard-fail</strong> — <code>scripts/smoke-prod-full.sh</code> fails if filtered rows violate §4 #1 (not a soft <code>pass</code> log).</li><li><strong>OpenAPI lists the route</strong> — <code>docs/contracts/openapi-v1.md</code> names <code>GET /api/v1/knowledge/patterns</code> and the four query params.</li><li><strong>Audit trail closed</strong> — live-truth-audit deferred bullet for <code>?system=</code> is marked remediated with task id + date.</li><li><strong>UI filter still works</strong> — selecting Kỳ Môn / Lục Nhâm / Thái Ất on <code>/patterns</code> loads a system-consistent list (via <code>system=</code> or still-supported <code>he=</code>).</li></ol>\n<h2>§5 - Verification</h2>\n<pre><code class=\"language-bash\"># unit (must stay green; extend for aliases + unknown)\ncd packages/tamthuc_api &amp;&amp; uv run pytest tests/test_knowledge_patterns_cov019.py -q\n\n# prod / staging smoke (API_BASE required)\nAPI_BASE=https://api.strategem.cyberskill.world bash scripts/smoke-prod-full.sh\n# the patterns?system= step must FAIL if total is unfiltered or mixed systems</code></pre>\n<p>Add/extend tests in <code>test_knowledge_patterns_cov019.py</code>:</p>\n<ul><li><code>test_filter_by_system_query_param</code> (existing) — keep</li><li><code>test_system_alias_ky_mon_matches_qimen</code></li><li><code>test_unknown_system_returns_empty</code></li></ul>\n<p>Manual UI: open <code>/patterns</code>, select each system, confirm counts move and names stay in-family.</p>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Confirm <code>list_patterns</code> already applies <code>system or he</code> (likely already true on <code>main</code>); fix only if a regression reappears.</li><li>Extend unit tests for aliases + unknown system.</li><li>Harden <code>smoke-prod-full.sh</code> assertions (compare totals; assert every row's <code>system</code>).</li><li>Document the route in <code>openapi-v1.md</code>.</li><li>Optionally switch <code>apps/web/app/patterns/page.tsx</code> from <code>params.set(\"he\", he)</code> to <code>params.set(\"system\", he)</code> when non-empty.</li><li>Annotate the live-truth-audit deferred item as remediated under TASK-API-005.</li></ol>\n<h2>§7 - Dependencies</h2>\n<ul><li>Depends on TASK-API-001 (endpoint ownership + <code>?system=</code> in the gateway contract) and the seeded catalog from TASK-RULE-001 / TASK-COV-019.</li><li>Does not block other modules; closes a deferred live-audit finding that product truth-up waves should not re-open blindly.</li></ul>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-http\">GET /api/v1/knowledge/patterns?system=qimen&amp;limit=500</code></pre>\n<pre><code class=\"language-json\">{\n  \"patterns\": [\n    {\n      \"id\": \"qimen_example\",\n      \"system\": \"qimen\",\n      \"he\": \"ky_mon\",\n      \"name\": \"...\",\n      \"meaning_modern\": \"Educational classical pattern note (see citations).\",\n      \"citations\": []\n    }\n  ],\n  \"total\": 105,\n  \"source\": \"tamthuc_kb.seed\"\n}</code></pre>\n<pre><code class=\"language-http\">GET /api/v1/knowledge/patterns?system=nope&amp;limit=500</code></pre>\n<pre><code class=\"language-json\">{ \"patterns\": [], \"total\": 0, \"source\": \"tamthuc_kb.seed\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<p>All resolved for this slice:</p>\n<ul><li>Canonical param name: <code>system</code> (per TASK-API-001); <code>he</code> remains alias.</li><li>Unknown system: empty 200 (not 400) — keeps browse UX simple.</li><li>Whether to remove <code>he=</code> later: Deferred to a future deprecation pass under TASK-API-002; out of scope here.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td><code>system=</code> ignored (full catalog)</td><td>unit + smoke total compare</td><td>fail CI / fail smoke</td><td>restore filter branch in <code>list_patterns</code></td></tr><tr><td>Alias <code>ky_mon</code> ignored</td><td>alias unit test</td><td>fail pytest</td><td>keep alias map complete</td></tr><tr><td>Unknown system returns all</td><td>unknown-system unit test</td><td>fail pytest</td><td>empty-set branch</td></tr><tr><td>Smoke soft-pass on bad filter</td><td>hardened smoke asserts</td><td>fail smoke</td><td>fix assert + redeploy</td></tr><tr><td>OpenAPI omits route</td><td>doc review / AC #6</td><td>task not done</td><td>add openapi lines</td></tr><tr><td>UI sends neither param when filter selected</td><td>manual / Playwright later</td><td>wrong browse UX</td><td>set <code>system=</code> in page.tsx</td></tr><tr><td>Mixed systems in filtered page</td><td>row-wise assert</td><td>fail test/smoke</td><td>fix predicate</td></tr><tr><td>Prophecy wording reintroduced</td><td>existing COV-019 blob assert</td><td>fail pytest</td><td>keep strip list</td></tr><tr><td>Deploy lag (code fixed, prod old)</td><td>smoke against prod</td><td>fail smoke</td><td>redeploy API image</td></tr><tr><td>Audit doc still claims defect after fix</td><td>AC #7</td><td>task not done</td><td>annotate audit file</td></tr></tbody></table>\n<h2>§11 - Implementation notes</h2>\n<ul><li>Prefer a minimal diff: do not refactor the KB loader or graph neighbors route in this task.</li><li>Do not invent PayOS / counsel work here; this is a knowledge-catalog truth-up only.</li><li>If prod probe at implement time still shows correct filter totals, still land the smoke hardening + OpenAPI + audit annotation — those are the remaining acceptance gaps.</li><li><code>backlog.yaml</code> W0 downgrade noise is unrelated; do not flip unrelated API-* rows to <code>done</code> as part of this task.</li></ul>\n<p><em>End of task-API-005.</em></p>";
