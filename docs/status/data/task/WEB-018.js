window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["WEB-018"]="<h1>WEB-018</h1>\n<h2>Goal</h2>\n<p>Prevent silent loss of story/layout CSS (partial HMR / truncate) by asserting critical selectors exist in source CSS and in <code>.next</code> output after build.</p>\n<h2>§1</h2>\n<ol><li>Unit test lists required classnames (<code>cs-story-rail</code>, <code>cs-cta-band</code>, <code>cs-visual-card</code>, …) and fails if missing from <code>wow.css</code>/<code>globals.css</code>.</li><li>Post-<code>next build</code> smoke scans compiled CSS under <code>.next</code> for the same set (CI web gate).</li></ol>";
