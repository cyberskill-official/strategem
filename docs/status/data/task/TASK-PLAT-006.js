window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLAT-006"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task is the Redis caching layer. Redis is the primary cache. It caches the cast chart (the la so envelope) with a 24-hour TTL keyed on the TASK-PLAT-002 cache key, invalidates on a pattern-update, warms the cache for common patterns, and additionally caches RAG top-k retrieval results and common interpretations. It owns the cache clients, keys, TTLs, and invalidation; it does NOT own the cache-key definition (TASK-PLAT-002) nor the durable store (TASK-PLAT-003), and it never becomes a source of truth - a cache miss always recomputes from the engine or re-retrieves, never fails the request.</p>\n<p>The chart cache SHALL key entries on the TASK-PLAT-002 cache key exactly - the stable hash of <code>(he, dau_vao rounded to casting granularity, co_truong_phai sorted, lich_phap.co_lich_phap sorted)</code> - so two identical casts hit the same entry regardless of language or instance, and two casts that differ in any stamped flag do NOT collide. Chart entries SHALL expire after 24 hours. The cache SHALL be read at the orchestrator seam (TASK-API-001 step 3) before the engine is called and written after a successful cast; a cache hit SHALL return an envelope byte-identical to what the engine would have produced (the read-only invariant holds through the cache).</p>\n<p>Invalidation SHALL be event-driven: when a <code>knowledge_patterns</code> row is updated (TASK-RULE-001 / TASK-KB-004 curation), entries whose interpretation depends on that pattern SHALL be invalidated, so a corrected pattern never serves stale detection. Cache warming SHALL pre-populate the cache for common patterns/queries (for example the current period's charts and the highest-traffic question types) so the first user of a common cast does not pay the cold-cast cost. The RAG caches SHALL store top-k retrieval results and common interpretations under their own keys and TTLs. All cache operations SHALL fail open to recompute on any Redis error - a cache outage degrades latency, never correctness.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Chart casting is deterministic and pure: identical input plus identical flags yields an identical chart (strategy 4.4). That is exactly the property that makes a cache safe here - the cached value cannot go stale as long as the key captures everything that changed the result, which is precisely what the TASK-PLAT-002 cache key is engineered to do. Reusing that key rather than inventing a cache key is the whole game: if the cache keyed on anything less than the full stamped-flag set, two users of different schools would collide and one would silently get the other's chart (RISK-2). Keying on the PLAT-002 hash makes a collision impossible by construction and a 24-hour TTL a simple freshness bound rather than a correctness risk.</p>\n<p>Failing open is the non-negotiable posture. A cache is a latency optimization, never a source of truth; a caching layer that fails a request when Redis blinks has converted a performance tool into an availability risk. So every miss and every error recomputes from the engine or re-retrieves from the store. Invalidation on pattern-update matters because the one thing that can make a cached interpretation wrong is a change to the knowledge it was grounded in - so a curation edit to <code>knowledge_patterns</code> must reach into the cache and drop the affected entries, or a corrected pattern would be shadowed by a stale reading. Warming common patterns turns the cache from reactive to proactive for the traffic that dominates, which is where the latency budget is actually spent (Grok-48).</p>\n<h2>§3 - Contract (keys / TTLs / invalidation)</h2>\n<h3>Cache clients (<code>cache/redis_cache.py</code>, <code>cache/chart_cache.py</code>)</h3>\n<pre><code class=\"language-python\">class RedisCache(Protocol):\n    async def get(self, key: str) -&gt; bytes | None: ...\n    async def set(self, key: str, value: bytes, ttl_s: int) -&gt; None: ...\n    async def delete(self, *keys: str) -&gt; None: ...\n    async def scan(self, match: str) -&gt; list[str]: ...     # for pattern-scoped invalidation\n\nclass ChartCache:\n    # key: chart:{plat002_cache_key}   ; value: the la so envelope JSON ; ttl: 24h\n    async def get_or_cast(self, cache_key: str, cast: Callable[[], Awaitable[LaSo]]) -&gt; LaSo: ...</code></pre>\n<h3>Key + TTL table (<code>docs/contracts/cache-keys.md</code>)</h3>\n<table><thead><tr><th>Cache</th><th>Key pattern</th><th>Value</th><th>TTL</th><th>Invalidated by</th></tr></thead><tbody><tr><td>chart</td><td><code>chart:{plat002_cache_key}</code></td><td>la so envelope (PLAT-002)</td><td>24h</td><td>pattern-update (indirect), TTL</td></tr><tr><td>rag_topk</td><td><code>rag:topk:{query_hash}</code></td><td>retrieved chunk ids + scores</td><td>6h</td><td>KB re-embed, TTL</td></tr><tr><td>rag_interp</td><td><code>rag:interp:{chart_key}:{persona}</code></td><td>common interpretation</td><td>12h</td><td>pattern-update, TTL</td></tr></tbody></table>\n<p>The <code>plat002_cache_key</code> is taken verbatim from TASK-PLAT-002's <code>cache_key()</code>; this task never re-derives it. <code>chart:*</code> values are the envelope byte-for-byte, so a hit is indistinguishable from a fresh cast.</p>\n<h3>Invalidation (<code>cache/invalidation.py</code>)</h3>\n<pre><code class=\"language-python\">async def on_pattern_update(pattern_key: str) -&gt; None:\n    # a knowledge_patterns row changed (RULE-001 / KB-004): drop dependent rag_interp entries\n    # and any chart entries whose cach_cuc references the pattern\n    ...</code></pre>\n<h3>Warming (<code>cache/warming.py</code>)</h3>\n<pre><code class=\"language-python\">async def warm_common(now: datetime) -&gt; int:\n    # pre-cast and cache the current period's common charts + top question types; returns entries warmed\n    ...</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A cast is cached under <code>chart:{plat002_cache_key}</code> with a 24h TTL; a second identical cast is served from cache and its envelope is byte-identical to the first (read-only invariant through the cache).</li><li>Two casts that differ in any stamped <code>co_truong_phai</code> flag produce different keys and do NOT collide; a test over a flag matrix asserts no cross-school collision.</li><li>On a Redis error or miss, <code>get_or_cast</code> recomputes from the engine and still returns a correct chart; a simulated Redis outage never fails the request (fail open).</li><li>A <code>knowledge_patterns</code> update triggers <code>on_pattern_update</code>, which invalidates the dependent <code>rag_interp</code> and chart entries; a re-read after invalidation reflects the corrected pattern, not the stale one.</li><li><code>warm_common</code> pre-populates the cache for common patterns; the first real request for a warmed cast is a hit, not a cold cast.</li><li>RAG top-k and common-interpretation caches store and expire under their documented keys/TTLs and are consulted by TASK-RAG-002 before a fresh retrieval.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_chart_cache.py</code>: hit/miss/TTL behavior against a fake/embedded Redis; the byte-identical-on-hit assertion; the flag-matrix no-collision test; the fail-open-on-outage test.</li><li><code>tests/test_invalidation.py</code>: a pattern-update invalidates exactly the dependent entries and no others; a re-cast after invalidation reflects the change; warming produces hits for the warmed set.</li><li>Integration: the TASK-API-001 orchestrator consults the chart cache before the engine and writes after a successful cast; a metered hit is faster and skips the engine call.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_api</code>, <code>pytest packages/tamthuc_api</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>cache/redis_cache.py</code>: the <code>RedisCache</code> protocol, a Redis implementation (redis-py async), and an in-memory fake for tests; all operations fail open.</li><li><code>cache/chart_cache.py</code>: <code>get_or_cast</code> keyed on the TASK-PLAT-002 <code>cache_key</code>, 24h TTL, envelope stored verbatim.</li><li><code>cache/invalidation.py</code>: <code>on_pattern_update</code> scoped invalidation; wire it to the RULE-001 / KB-004 pattern-write path.</li><li><code>cache/warming.py</code>: <code>warm_common</code> for the current period's common charts and top question types; a scheduled trigger (Celery in TASK-PLAT-010).</li><li>Add the RAG top-k and interpretation caches (consumed by TASK-RAG-002/003); document all keys/TTLs in <code>docs/contracts/cache-keys.md</code>.</li><li>Wire the chart cache into the TASK-API-001 orchestrator seam.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-PLAT-003 per the catalog (the durable store the cache sits in front of; a miss recomputes and persists there). Reads the TASK-PLAT-002 cache-key rule as a hard contract coupling - the chart cache key IS the PLAT-002 <code>cache_key</code>, never a local re-derivation - so PLAT-002 is a contract dependency even though the catalog keeps the hard <code>depends_on</code> at PLAT-003. Consumed by TASK-API-001 (the orchestrator seam that reads/writes the chart cache), TASK-API-003 (Redis also backs the rate-limit counters, same instance), and TASK-RAG-002 (top-k and interpretation caches). Warming is scheduled via the TASK-PLAT-010 Celery workers.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code># chart cache entry (illustrative)\nGET  chart:ck_9f3a...             -&gt; &lt;la so envelope JSON, byte-identical to a fresh cast&gt;\nTTL  chart:ck_9f3a...             -&gt; 86400   (24h)</code></pre>\n<pre><code class=\"language-python\"># orchestrator seam (TASK-API-001 step 3) - cache in front of the engine, fail open\nchart = await chart_cache.get_or_cast(\n    cache_key=envelope_cache_key(sys, ctx, req),         # TASK-PLAT-002 cache_key\n    cast=lambda: engine.cast(sys, ctx, req),\n)</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether the cache module stays inside <code>tamthuc_api</code> or becomes a shared <code>tamthuc_cache</code> package once RAG also imports the <code>RedisCache</code> primitive. Default: keep it in <code>tamthuc_api</code> at MVP and export the primitive for RAG to import; extract a shared package only if a third consumer appears.</li><li>Window for chart-cache TTL: fixed 24h vs tied to the next jieqi boundary (a chart's calendar context is stable only within a solar term). Default: a flat 24h TTL now (simple, and casts are keyed on the full lich_phap flags anyway); revisit if term-boundary charts show staleness.</li><li>Cache stampede on a cold common cast under load. Default: a single-flight lock around <code>get_or_cast</code> per key so a thundering herd casts once; warming reduces the cold surface further.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Cross-school collision</td><td>cache key weaker than the full stamped-flag set</td><td>forbidden; the key IS the PLAT-002 hash over all flags; the flag-matrix test asserts no collision (RISK-2)</td></tr><tr><td>Fail closed on outage</td><td>Redis down fails the request</td><td>forbidden; every miss/error recomputes from the engine; fail open, latency degrades not correctness</td></tr><tr><td>Stale after correction</td><td>pattern updated but cache serves the old reading</td><td><code>on_pattern_update</code> invalidates dependent entries; a re-read reflects the change</td></tr><tr><td>Cache as source of truth</td><td>request served only from cache with no recompute path</td><td>cache is never authoritative; a miss always recomputes/re-retrieves</td></tr><tr><td>Mutated on hit</td><td>cached envelope altered before return</td><td>stored and returned byte-for-byte; the read-only invariant holds through the cache</td></tr><tr><td>Unbounded growth</td><td>no TTL / no eviction</td><td>every entry carries a TTL; keys are scoped and evictable</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task is a latency optimization with one hard rule and one hard reuse: fail open (a cache outage never fails a request), and key the chart cache on the TASK-PLAT-002 cache key verbatim (never a local re-derivation), so determinism makes the cache trivially safe and the full stamped-flag set makes a cross-school collision impossible. Redis is the primary cache and the same instance backs the TASK-API-003 rate-limit counters. Invalidate on pattern-update so a corrected pattern is never shadowed, warm the common patterns so the dominant traffic is never cold, and keep the RAG top-k and interpretation caches under their own keys. It extends the <code>tamthuc_api</code> package as one more module on the same app.</p>";
