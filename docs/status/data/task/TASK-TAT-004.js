window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-TAT-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task generalizes the Thai At chart from one plate per year to four plates keyed to four time granularities: nien ke (year), nguyet ke (month), nhat ke (day), thoi ke (hour). It extends the <code>cyberos-thaiat</code> crate, reusing the epoch / reduction machinery of TASK-TAT-001 and the ring seating of TASK-TAT-002; only the tich formula and the don anchor differ per level.</p>\n<p>The module SHALL compute a tich for each of the four levels and reduce it mod seventy-two into a cuc (Claude-04 s5): nien ke tich = 10,153,917 + CE, cuc = tich nien mod 72 (the TASK-TAT-001 formula, identical); nguyet ke tich = tich nien times twelve with its own leap-month handling, cuc = tich nguyet mod 72; nhat ke anchored on Dong Chi with tich nhat = accumulated-days-from-the-Giap-Ty-anchor times 365.2425, cuc = (tich nhat mod 72) + 1 then advanced by day; thoi ke tich = tich nhat times twelve, cuc = (tich thoi mod 72) + 1 then advanced by hour. One hour SHALL be one cuc and six days SHALL close one seventy-two-cuc circuit.</p>\n<p>The module SHALL take the don direction for nhat ke and thoi ke from the Dong Chi / Ha Chi anchor (Claude-04 s5.2): after Dong Chi duong don runs forward (thuan), after Ha Chi am don runs backward (nghich). The Dong Chi and Ha Chi instants SHALL come from the calendar core (TASK-CORE-001 via TASK-CORE-005), the same solstice instants TASK-TAT-001 keys its don off, so TaiYi and the calendar core MUST agree on the solstice to the second. The nguyet ke leap-month handling SHALL use a dedicated tich nguyet solve, not a naive month multiply.</p>\n<p>Each level SHALL reduce to the same 1..=72 cuc contract as TASK-TAT-001 (a tich divisible by seventy-two maps to 72, not 0, before the level-specific \"+1\" where the source calls for it). The module SHALL feed the resulting cuc and don into TASK-TAT-002 seating (and, in the assembly, TASK-TAT-003 tuong) so that any of the four levels produces a full chart. The module SHALL extend the <code>dau_vao.cap</code> selector and stamp the chosen level; the oracle is kintaiyi, which supports all four levels.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Thai At is not a single yearly plate: the same machine casts at four resolutions so a reading can range from the multi-year backdrop (nien ke) down to the hour (thoi ke), and the user picks the level that matches the scope of the question (Claude-04 s5.2). Because all four reduce to a cuc mod seventy-two and then seat Thai At the same way, the right design is to reuse TASK-TAT-001's reduction and TASK-TAT-002's seating unchanged and vary only the tich formula and the don anchor - which is why this task is small (SHOULD, 8h) despite adding three whole new plates.</p>\n<p>The nhat ke and thoi ke anchors are the reason this task touches the calendar core. Their tich is measured from Dong Chi, and the don flips at Dong Chi (forward) and Ha Chi (backward), so the solstice instant has to be the exact same instant TASK-TAT-001 and TASK-CORE-001 use; a disagreement of even a day at the anchor shifts the cuc and re-casts the plate (RISK-1 propagation). Stating the shared-anchor requirement here keeps the four levels consistent with the yearly plate and with QiMen / LiuRen, which read the same solstices.</p>\n<p>Nguyet ke gets a dedicated leap-month solve rather than a flat times-twelve because the lunisolar calendar inserts leap months unevenly; a naive multiply would drift the month tich and the cuc with it. The \"+1 then advance\" on nhat ke and thoi ke is transcribed verbatim from the s5.2 table rather than rationalized, and pinned to kintaiyi, because the small constant is exactly the kind of detail that is easy to drop and hard to notice without the oracle.</p>\n<h2>§3 - Contract (algorithm and types)</h2>\n<h3>The four calculations (Claude-04 s5.2, reproduced faithfully)</h3>\n<table><thead><tr><th>Phep</th><th>Tich</th><th>So cuc</th></tr></thead><tbody><tr><td>Nien ke 年計</td><td>Tich nien = 10.153.917 + nam CE</td><td>Tich nien mod 72</td></tr><tr><td>Nguyet ke 月計</td><td>Tich nguyet = tich nien x 12 (chinh thang nhuan rieng)</td><td>Tich nguyet mod 72</td></tr><tr><td>Nhat ke 日計</td><td>Lay Dong Chi lam moc; tich nhat = nam tich Giap Ty x 365,2425</td><td>Tich nhat mod 72 cong 1, roi tien theo ngay</td></tr><tr><td>Thoi ke 時計</td><td>Tich thoi = tich nhat x 12</td><td>Tich thoi mod 72 cong 1, roi tien theo gio</td></tr></tbody></table>\n<p>One hour is one cuc; six days is seventy-two cuc, closing a circuit. Nhat ke and thoi ke anchor on Dong Chi: after Dong Chi duong don thuan, after Ha Chi am don nghich. Nguyet ke handles the leap month through its own tich nguyet solve.</p>\n<h3>Nien ke worked example (Claude-04 s5.1, ties to TASK-TAT-001)</h3>\n<p>Year 2004: tich nien = 10,153,917 + 2,004 = 10,155,921; mod 60 = 21 (Giap Than); mod 72 = 33 (duong don, cuc 33). Identical to the TASK-TAT-001 golden numbers - nien ke IS TASK-TAT-001's path, listed here as the first of the four levels.</p>\n<h3>Pseudocode</h3>\n<pre><code>def tich_theo_cap(cap, dau_vao, epoch, lich):     # lich supplies Dong Chi / Ha Chi (TASK-CORE-005)\n    if cap == \"nien_ke\":\n        tich = tich_nien(dau_vao.nam_ce, epoch)                      # TASK-TAT-001\n        cuc  = map_1_72(tich % 72)\n        don  = duong_don_from_solstice(dau_vao, lich)\n    elif cap == \"nguyet_ke\":\n        tich = tich_nguyet(dau_vao, epoch)                           # tich nien x 12 + leap-month solve\n        cuc  = map_1_72(tich % 72)\n        don  = duong_don_from_solstice(dau_vao, lich)\n    elif cap == \"nhat_ke\":\n        tich = tich_nhat(dau_vao, lich.dong_chi_anchor)              # days-from-anchor x 365.2425\n        cuc  = map_1_72(tich % 72) + 1                               # +1, then advance by day\n        don  = after_dong_chi(dau_vao, lich)                         # thuan after Dong Chi, nghich after Ha Chi\n    elif cap == \"thoi_ke\":\n        tich = tich_nhat(dau_vao, lich.dong_chi_anchor) * 12\n        cuc  = map_1_72(tich % 72) + 1                               # +1, then advance by hour\n        don  = after_dong_chi(dau_vao, lich)\n    return TichCap(cap, tich, cuc, don)                              # cuc + don then feed TASK-TAT-002 seating</code></pre>\n<h3>Public types (<code>crates/cyberos-thaiat/src/</code>)</h3>\n<pre><code class=\"language-rust\">#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]\n#[serde(rename_all = \"snake_case\")]\npub enum Cap { NienKe, NguyetKe, NhatKe, ThoiKe }\n\n#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]\npub struct TichCap {\n    pub cap: Cap,\n    pub tich: u64,          // tich nien / nguyet / nhat / thoi (u64: nhat/thoi are large)\n    pub nhap_cuc: u8,       // 1..=72 (after the level-specific +1 where the source calls for it)\n    pub duong_don: bool,    // from the Dong Chi / Ha Chi anchor\n}\n\npub fn tich_theo_cap(cap: Cap, dau_vao: &amp;DauVaoTat, epoch: Epoch, lich: &amp;LichPhap) -&gt; TichCap;</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>tich_theo_cap(NienKe, 2004, KimKinh, ...)</code> reproduces the s5.1 numbers exactly: tich 10,155,921, cuc 33, Giap Than, duong don - identical to TASK-TAT-001.</li><li>Nguyet ke tich is tich nien times twelve adjusted by the leap-month solve (not a flat multiply); a leap-month year and a common year are both checked against kintaiyi.</li><li>Nhat ke and thoi ke apply the <code>+1</code> after the mod-72 reduction and advance by day / hour respectively; a probe just after Dong Chi runs duong don and one just after Ha Chi runs am don, both matching kintaiyi.</li><li>All four tich use <code>u64</code>; nhat ke and thoi ke (the large ones, days x 365.2425 and x 12) neither overflow nor lose precision; a wide date range is checked.</li><li>The 1..=72 mapping holds at each level (a tich divisible by 72 maps to 72, not 0, before any level-specific <code>+1</code>).</li><li>The Dong Chi / Ha Chi instant used here is byte-identical to the one TASK-TAT-001 and TASK-CORE-001 use; a cross-check test asserts the same anchor.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>Unit: the s5.1 nien ke numbers; a leap-month nguyet ke; a nhat ke and thoi ke probe on each side of Dong Chi and Ha Chi.</li><li>Property: over a wide date span, all four tich are exact under <code>u64</code> (no overflow, no f64 rounding on the 365.2425 term beyond the documented tolerance); each cuc is 1..=72.</li><li>Oracle: <code>tests/bonphep_oracle.rs</code> loads <code>fixtures/bonphep_kintaiyi.csv</code> (generated once from kintaiyi across all four levels and both epochs, spanning years and including day / hour probes) and asserts the tich, cuc, and don match exactly per level. This extends the TASK-TAT-006 gate from per-epoch to per-epoch-and-per-level.</li><li>Boundary: the don switch at Dong Chi and Ha Chi (nhat / thoi ke), the six-day / seventy-two-cuc circuit close, the cuc wrap (72 -&gt; 1), and a leap-month boundary for nguyet ke.</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-thaiat -- -D warnings</code>, <code>cargo test -p cyberos-thaiat</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>bonphep.rs</code>: the <code>Cap</code> enum, the four tich formulas, and <code>tich_theo_cap</code> reusing TASK-TAT-001's <code>tich_nien</code> and 1..=72 mapping.</li><li>Nguyet ke: the leap-month tich nguyet solve (consult TASK-CORE for the lunisolar month index); do not flat-multiply.</li><li>Nhat ke / thoi ke: take the Dong Chi anchor from <code>lich</code> (TASK-CORE-005), apply the <code>days x 365.2425</code> and <code>x 12</code> tich, the <code>+1</code> and advance-by-day / hour, and the after-Dong-Chi / after-Ha-Chi don.</li><li>Route the resulting <code>(cuc, don)</code> into TASK-TAT-002 seating so any level yields a full chart; keep nien ke bit-identical to TASK-TAT-001.</li><li>Generate the kintaiyi four-level fixture once (documented script, not run in CI) and commit; wire the oracle, property, and boundary tests.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-TAT-002 (seating the cuc / don on the ring) and transitively on TASK-TAT-001 (the tich nien formula, the 1..=72 mapping, the epoch flag). Needs the Dong Chi / Ha Chi instants and the lunisolar month index from the calendar core (TASK-CORE-001 via TASK-CORE-005), reached transitively through TASK-TAT-001's CORE-005 dependency. Soft-feeds TASK-TAT-006: the assembly's MUST path (nien ke) does not hard-require this task, but the SHOULD extension of the oracle gate to all four levels does - so this task blocks the per-level portion of the TASK-TAT-006 gate, not the yearly assembly itself. Emits into the TASK-PLAT-002 envelope via <code>dau_vao.cap</code>.</p>\n<h2>§8 - Example payloads</h2>\n<p><code>dau_vao</code> + <code>tich</code> fragment for a thoi ke (hour-level) cast (values illustrative; pinned to kintaiyi):</p>\n<pre><code class=\"language-json\">{ \"dau_vao\": { \"nam_ce\": 2004, \"cap\": \"thoi_ke\", \"datetime\": \"2004-01-01T10:30:00\", \"tz\": \"+07:00\", \"epoch\": \"kim_kinh\" },\n  \"ban\": {\n    \"tich\": { \"cap\": \"thoi_ke\", \"tich\": 44476835160, \"nhap_cuc\": 12, \"duong_don\": true },\n    \"thai_at_cung\": 3, \"thai_at_ring\": 2,\n    \"thap_luc_than\": { \"...\": \"from TASK-TAT-002\" },\n    \"bat_tuong\": { \"...\": \"from TASK-TAT-003, cast at this level's cuc\" },\n    \"cac_toan\": { \"...\": \"from TASK-TAT-003\" }\n  },\n  \"lich_phap\": { \"...\": \"supplies the Dong Chi / Ha Chi anchor (TASK-CORE-005)\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>The nhat ke \"nam tich Giap Ty x 365,2425\" term (s5.2) mixes a whole-day accumulation with a mean-tropical-year constant; whether tich nhat should be integer days from the anchor or the 365.2425-scaled figure is resolved against kintaiyi before lock, since it drives the mod-72 cuc.</li><li>Nguyet ke leap-month handling is stated only as \"chinh thang nhuan rieng\" (its own leap-month solve); the exact rule (which leap months count, how the month index maps to tich nguyet) is taken from TASK-CORE's lunisolar month module and confirmed against kintaiyi.</li><li>Whether the <code>+1</code> on nhat / thoi ke applies before or after the 1..=72 remap is transcribed as \"mod 72, then +1\"; a boundary case at a tich divisible by 72 pins the order against the oracle.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Nhat / thoi ke tich overflow</td><td>days x 365.2425 x 12 in a narrow int or f32</td><td>use u64 and the documented precision path; wide-range test asserts exact cuc</td></tr><tr><td>Wrong don at a solstice probe</td><td>Dong Chi / Ha Chi anchor swapped or off by a day</td><td>boundary test on each side of both solstices vs kintaiyi</td></tr><tr><td>Nguyet ke leap-month drift</td><td>flat times-twelve without the leap solve</td><td>leap-year nguyet ke test diverges from kintaiyi</td></tr><tr><td>Missing +1 on nhat / thoi ke</td><td>s5.2 constant dropped</td><td>per-level oracle fixture diverges</td></tr><tr><td>Solstice anchor disagrees with CORE</td><td>TaiYi computes its own solstice</td><td>cross-check test asserts the same instant as TASK-TAT-001 / TASK-CORE-001</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task is small because it reuses everything: TASK-TAT-001's reduction and 1..=72 mapping, TASK-TAT-002's seating, and (in the assembly) TASK-TAT-003's tuong. It varies only the tich formula and the don anchor per level, so the four plates are one code path parameterized by <code>Cap</code>. Its value is coverage: it lifts the TASK-TAT-006 oracle gate from per-epoch to per-epoch-and-per-time-level, which is the tat module's stated acceptance bar. The shared Dong Chi / Ha Chi anchor is the load-bearing detail - the same solstice instant TaiYi, QiMen, and LiuRen all read - so a divergence here is a RISK-1 calendar-core problem, not a TaiYi problem. Nien ke stays bit-identical to TASK-TAT-001; the other three are the SHOULD extension.</p>";
