window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLAT-013"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task defines the <strong>custom VPS</strong> runtime for the Strategem backend: the Python API (<code>tamthuc-api</code>), optional engine/cast-cli sidecar or baked binary, and Caddy as the public HTTPS edge for <code>api.&lt;domain&gt;</code>. Following CyberOS VPS practice:</p>\n<ul><li>Images are built on CI and pushed to a registry (GHCR); the VPS <strong>pulls and restarts</strong>, it does not <code>cargo build</code> on a small box.</li><li><code>deploy/vps/deploy.sh</code> is the single roll entry (manual or SSH from Actions).</li><li>Config is <code>.env</code> on the host (from <code>.env.example</code>); never committed with secrets.</li><li>On roll: <code>git pull</code> (compose/Caddyfile) → <code>migrate.sh</code> → <code>docker compose pull</code> → <code>up -d</code>.</li></ul>\n<p>The VPS SHALL NOT be required to host the Next.js user UI (that is Vercel, TASK-PLAT-014).</p>\n<h2>§2 - Why this design</h2>\n<p>CyberOS proved that small VPS boxes die under release builds; GHCR + pull keeps deploys fast. Caddy gives automatic TLS. Compose keeps the stack readable. Separating API host from Vercel web matches the operator preference and reduces origin concerns (CORS / public API base URL).</p>\n<h2>§3 - Contract</h2>\n<h3>Services (compose)</h3>\n<table><thead><tr><th>Service</th><th>Image</th><th>Port (internal)</th></tr></thead><tbody><tr><td>api</td><td><code>ghcr.io/&lt;org&gt;/strategem-api:&lt;tag&gt;</code></td><td>8000</td></tr><tr><td>caddy</td><td>caddy:2</td><td>80/443</td></tr></tbody></table>\n<p><code>CAST_CLI</code> path inside the api image or a volume-mounted binary. <code>DATABASE_URL</code> points at Supabase.</p>\n<h3>deploy.sh</h3>\n<pre><code class=\"language-text\">git pull --ff-only\nbash deploy/vps/migrate.sh\ndocker compose -f deploy/vps/docker-compose.api.yml --env-file .env pull\ndocker compose ... up -d\nhealthcheck: curl -fsS https://api.&lt;domain&gt;/healthz || curl localhost:8000/healthz</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Compose file and Caddyfile exist and reference env substitution.</li><li><code>.env.example</code> lists every required secret name without values.</li><li><code>deploy.sh</code> is idempotent and fails closed on migrate failure.</li><li>README documents one-time VPS bootstrap (Docker, deploy key, GHCR login, DNS).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>docker compose config</code> validates (when Docker available).</li><li>Doc review against cyberos <code>deploy/vps/deploy.sh</code> structure.</li></ul>";
