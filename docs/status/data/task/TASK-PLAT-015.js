window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLAT-015"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task splits continuous delivery by surface (CyberOS auto-deploy pattern + operator topology):</p>\n<table><thead><tr><th>Trigger</th><th>Web (Vercel)</th><th>API/engine (VPS)</th></tr></thead><tbody><tr><td>push <code>main</code></td><td>Vercel Git integration deploys <code>apps/web</code></td><td>Actions: build image → GHCR → SSH <code>deploy.sh</code></td></tr><tr><td>PR</td><td>Vercel preview (optional)</td><td>CI only (no VPS roll)</td></tr></tbody></table>\n<p>Production VPS roll MAY stay behind GitHub Environment <code>production</code> approval (TASK-PLAT-004 human gate). Staging VPS can auto-roll.</p>\n<p>Secrets: <code>VPS_HOST</code>, <code>VPS_USER</code>, <code>VPS_SSH_KEY</code>, registry via <code>GITHUB_TOKEN</code>. No Vercel token required if Git integration is linked.</p>\n<h2>§2 - Why this design</h2>\n<p>One monorepo, two deploy machines: Vercel owns edge web; VPS owns stateful API. CI stays the quality gate; CD is promotion only.</p>\n<h2>§3 - Contract</h2>\n<p>Workflow <code>deploy-vps.yml</code>:</p>\n<ol><li>checkout + build <code>deploy/docker/api.Dockerfile</code> (+ engine if needed)</li><li>push <code>ghcr.io/&lt;owner&gt;/strategem-api:&lt;sha&gt;</code> and <code>:main</code></li><li>SSH: run <code>deploy/vps/deploy.sh</code> with env tag</li></ol>\n<p>Document Vercel project settings separately (TASK-PLAT-014).</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Workflow file exists and is <code>workflow_dispatch</code> + <code>push</code> to main capable.</li><li>Docs list required GitHub secrets.</li><li>Does not break existing <code>ci.yml</code> / <code>cd.yml</code> until cutover is documented.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>YAML validates; dry-run instructions in docs.</li></ul>";
