window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task builds the results screen - the right panel of the chart-casting layout (TASK-WEB-001) - which presents the outcome of a cast: the chart, the deterministic patterns the engine and rule layer found, and the AI's cited interpretation, each clearly attributed to its source (strategy 4.4). It is the \"read\" step of the lookup flow. It owns the results presentation; it does NOT render the chart internals (TASK-CHART-001) nor produce interpretation (TASK-RAG-003) - it composes their outputs and is where the deterministic/AI boundary is made legible to the user.</p>\n<p>The screen SHALL render, from the TASK-API-001 <code>QueryResponse</code>: (a) the chart via TASK-CHART-001, reading <code>charts[i]</code> (the la so envelope, <code>he = \"ky_mon\"</code> at P0); (b) the detected patterns from <code>charts[i].cach_cuc</code> / <code>patterns[]</code> as a list with each pattern's name, Han name, palace (<code>cung</code>), and cat/hung polarity shown with icon and text, never color alone (TASK-WEB-001); and (c) the interpretation from <code>interpretation</code> (TASK-RAG-003) with both persona readings (beginner/expert, toggleable), the recommendations, and the citation cards (Han + bach thoai + dich + locator).</p>\n<p>The screen SHALL display an <code>AIDisclosureBadge</code> (TASK-WEB-001) on the interpretation, always, fed by the <code>ai_disclosure</code> block - it is mandatory on any AI output (strategy 4.4). Where <code>interpretation.requires_human_review</code> is true (or <code>review_status</code> is pending), the screen SHALL render a <code>HumanReviewGate</code> (TASK-WEB-001, TASK-RAG-004) and SHALL clearly distinguish a not-yet-reviewed interpretation from an approved one. The screen SHALL visually separate the deterministic region (chart + patterns, sourced from the engine) from the interpreted region (AI reading, sourced from RAG and labeled), so a user can always tell computed fact from AI interpretation. It SHALL treat the la so envelope as read-only and SHALL NOT mutate <code>ban</code>, <code>cach_cuc</code>, <code>lich_phap</code>, or <code>co_truong_phai</code> (strategy 4.3).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The results screen is where the platform's central promise becomes visible: the engine computed these facts, the AI interpreted them, and you can see which is which and whether a human vetted it (strategy 4.4). That is why the layout physically separates the deterministic region (the chart and the detected cach cuc, which came from the engine and match an oracle) from the interpreted region (the AI reading, which is grounded, cited, and labeled). Collapsing them into one undifferentiated block would erase the boundary the whole architecture exists to protect and would let a user read an AI sentence as if it carried the engine's determinism.</p>\n<p>The AIDisclosureBadge is mandatory and the citation cards are first-class because interpretation without visible sourcing is exactly the failure mode that turns heritage education into unaccountable fortune-telling (strategy 7, RISK-3/RISK-4). Showing each claim's citations (the three text layers plus a locator) lets a user trace a reading back to a classical passage; showing the badge lets them see the model and its limits. The HumanReviewGate, shown when the interpretation is flagged, keeps a person in the loop for consequential readings and makes the review state honest - a pending reading must not look approved. Presenting cat/hung with icon and text (never color alone) is the same accessibility floor as the rest of the system, and it matters most here where a polarity could be misread as a verdict.</p>\n<h2>§3 - Contract (screen / regions / data)</h2>\n<h3>Layout (Claude-07 s6, the right panel of TASK-WEB-001)</h3>\n<pre><code>+-------------------- results panel (right) --------------------+\n| [ chart region - deterministic ]                             |\n|   &lt;NinePalaceChart envelope={charts[0]} /&gt;   (TASK-CHART-001)  |\n| [ patterns region - deterministic ]                          |\n|   cach cuc list: name / Han / cung / cat|hung (icon + text)  |\n| --------------- boundary (visually separated) -------------- |\n| [ interpretation region - AI, labeled ]                      |\n|   AIDisclosureBadge  +  persona toggle (beginner | expert)   |\n|   reading text ... [citation refs]                           |\n|   recommendations ...                                        |\n|   citation cards (Han / bach thoai / dich / locator)         |\n|   HumanReviewGate  (only when requires_human_review)         |\n+--------------------------------------------------------------+</code></pre>\n<h3>Data source (the TASK-API-001 <code>QueryResponse</code>)</h3>\n<pre><code class=\"language-ts\">type QueryResponse = {\n  query_id: string;\n  charts: LaSo[];                 // TASK-PLAT-002 envelope(s); read-only here\n  patterns: CachCuc[];            // detected cach cuc\n  interpretation: Interpretation; // TASK-RAG-003: beginner/expert, recommendations, citations, confidence, requires_human_review, disclosure\n  ai_disclosure: AIDisclosure;    // mandatory\n};</code></pre>\n<ul><li>Chart region: passes <code>charts[0]</code> to TASK-CHART-001 (<code>he = \"ky_mon\"</code> at P0); the component reads <code>ban</code> and marks <code>cach_cuc</code> palaces. Read-only.</li><li>Patterns region: <code>pattern-list</code> renders each <code>cach_cuc</code> with <code>name</code>, <code>name</code>/Han, <code>cung</code>, and <code>polarity</code> (cat/hung) as an icon+text badge using the semantic tokens (never color alone).</li><li>Interpretation region: <code>interpretation-view</code> renders the persona-selected reading, the recommendations (each with its citations), and the <code>citation-card</code>s; <code>AIDisclosureBadge</code> is always present; <code>HumanReviewGate</code> renders when <code>interpretation.requires_human_review</code> is true.</li></ul>\n<h3>Persona toggle and citation card</h3>\n<p><code>persona-toggle</code> switches between <code>beginner_interpretation</code> and <code>expert_interpretation</code> (both present in one response, TASK-RAG-003). <code>citation-card</code> shows the three text layers (<code>han</code>, <code>bach_thoai</code>, <code>dich</code>) and the <code>locator</code>; in-text citation refs link to their card.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The screen renders the chart (TASK-CHART-001) from <code>charts[0]</code>, the detected cach cuc list, and the interpretation from a <code>QueryResponse</code>, in the three regions above.</li><li>The deterministic region (chart + patterns) is visually separated from the interpreted region (AI reading), so computed fact and AI interpretation are distinguishable.</li><li><code>AIDisclosureBadge</code> is present on the interpretation on every result carrying AI output; a result without it fails a test (mandatory, strategy 4.4).</li><li>cat/hung polarity on each pattern is shown with icon and text, never color alone (TASK-WEB-001).</li><li>When <code>interpretation.requires_human_review</code> is true (or <code>review_status</code> is pending), a <code>HumanReviewGate</code> is shown and the reading is marked not-yet-approved, distinct from an approved reading.</li><li>Citation cards render the three text layers (Han / bach thoai / dich) and a locator; in-text citation refs resolve to their card; the persona toggle switches beginner/expert without a re-fetch.</li><li>The la so envelope is unchanged by the screen (read-only): no code path writes <code>ban</code>/<code>cach_cuc</code>/<code>lich_phap</code>/<code>co_truong_phai</code>.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/results-panel.test.tsx</code>: renders a <code>QueryResponse</code> fixture (with and without <code>requires_human_review</code>); asserts the three regions, the deterministic/AI separation, the always-present <code>AIDisclosureBadge</code>, the cat/hung icon+text badges, the citation-card three layers + locator, the persona toggle, and the <code>HumanReviewGate</code> presence only when flagged; asserts the envelope is byte-identical before/after render (read-only).</li><li>Accessibility: <code>jest-axe</code> clean; the <code>HumanReviewGate</code> state announced to screen readers (TASK-WEB-001); the stacked-diacritics clip test over the interpretation text and pattern names on light+dark.</li><li>Contract: the <code>Interpretation</code> / <code>AIDisclosure</code> types are checked against the TASK-RAG-003 shapes (shared fixture) so the screen and the interpreter agree; a drift fails the test.</li><li>Gates: <code>pnpm --filter web lint</code>, <code>pnpm --filter web test</code>, <code>next build</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>app/results/[queryId]/page.tsx</code>: fetch/receive the <code>QueryResponse</code> (routed from TASK-WEB-002 or fetched by <code>query_id</code>), mount the right panel in the TASK-WEB-001 shell.</li><li><code>results-panel.tsx</code>: the three regions and the visual boundary; pass <code>charts[0]</code> to TASK-CHART-001.</li><li><code>pattern-list.tsx</code>: the cach cuc list with cat/hung icon+text badges (semantic tokens).</li><li><code>interpretation-view.tsx</code> + <code>persona-toggle.tsx</code> + <code>citation-card.tsx</code>: the AI region with the mandatory <code>AIDisclosureBadge</code>, the persona toggle, the recommendations, and the citation cards.</li><li>Conditional <code>HumanReviewGate</code> on <code>requires_human_review</code>; mark pending vs approved.</li><li><code>tests/results-panel.test.tsx</code> + the shared TASK-RAG-003 contract fixture.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-WEB-002 (routes here on a successful cast, carrying the response/<code>query_id</code>), TASK-CHART-001 (the 9-palace chart it embeds), and TASK-RAG-003 (the <code>Interpretation</code> and <code>AIDisclosure</code> it renders). Uses TASK-WEB-001 (<code>AIDisclosureBadge</code>, <code>HumanReviewGate</code>, tokens, the cat/hung icon+text convention) and pairs with TASK-RAG-004 behind the gate. Reads the TASK-API-001 <code>QueryResponse</code>. Blocks TASK-WEB-005 (the report view builds on this presentation) and TASK-WEB-007 (the management flow lists past results). Reads the TASK-PLAT-002 envelope read-only (strategy 4.3).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// QueryResponse (abridged) this screen renders\n{ \"query_id\": \"q_...\",\n  \"charts\": [ { \"he\": \"ky_mon\", \"ban\": { \"...\": \"KyMonBan\" },\n    \"cach_cuc\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"青龍返首\", \"cung\": 1, \"polarity\": \"cat\" } ] } ],\n  \"patterns\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"polarity\": \"cat\" } ],\n  \"interpretation\": { \"persona_level\": \"beginner\",\n    \"beginner_interpretation\": \"A favorable window to begin... [yba_dieu_012].\",\n    \"expert_interpretation\": \"Thanh Long Hoi Dau on cung 1... [yba_dieu_012].\",\n    \"recommendations\": [ { \"text\": \"Treat the near-term as supportive.\", \"citations\": [\"yba_dieu_012\"] } ],\n    \"citations\": [ { \"citation_id\": \"yba_dieu_012\", \"han\": \"丙加值符...\", \"bach_thoai\": \"...\",\n      \"dich\": \"Binh gia truc phu...\", \"locator\": \"dieu 12\" } ],\n    \"confidence\": 0.72, \"requires_human_review\": false,\n    \"disclosure\": { \"is_ai_generated\": true, \"model\": \"gpt-4o-mini\", \"review_status\": \"not_required\" } } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Multi-chart layout for <code>/calculate/all</code> (P1+): tabs, stacked, or side-by-side per system. Default: render <code>charts[0]</code> at P0 (QiMen only); the panel already takes a list, so a per-system tab set is additive when LiuRen/TaiYi land (TASK-CHART-002/003, TASK-STRAT-004).</li><li>Where the persona default comes from: the request's <code>persona_level</code> vs a user preference. Default: honor the request's persona as the initial toggle state; both readings are present in one response, so switching is local and free (TASK-RAG-003 §9).</li><li>How prominent the confidence value is. Default: show <code>confidence</code> alongside the AIDisclosureBadge as supporting context, not as a headline number, so it informs without implying false precision; the badge carries the limits copy (TASK-LEGAL-001).</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Boundary erased</td><td>chart/patterns and AI reading in one undifferentiated block</td><td>forbidden; deterministic and interpreted regions are visually separated (strategy 4.4)</td></tr><tr><td>Missing disclosure</td><td>interpretation shown without the badge</td><td>forbidden; <code>AIDisclosureBadge</code> is mandatory on AI output; a test rejects its absence</td></tr><tr><td>Color-only polarity</td><td>cat/hung shown by color alone</td><td>forbidden; polarity is icon + text + color (TASK-WEB-001)</td></tr><tr><td>Pending looks approved</td><td>flagged interpretation shown as final</td><td>the <code>HumanReviewGate</code> renders and the reading is marked not-yet-approved, distinct from approved</td></tr><tr><td>Envelope mutated</td><td>screen writes an engine field</td><td>forbidden; read-only; a byte-equality test asserts the envelope is unchanged</td></tr><tr><td>Uncited claim rendered as sourced</td><td>interpretation text without a resolvable citation</td><td>citations come from TASK-RAG-003 (guarded there); the card/ref link must resolve, or the claim is not shown as sourced</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This screen is where the deterministic/AI boundary becomes something a user can see, so its non-negotiables are visible attribution and separation: the chart and the detected cach cuc are the engine's deterministic output (matched to an oracle), the reading is the AI's grounded, cited, labeled interpretation, and the two are never blurred (strategy 4.4). The <code>AIDisclosureBadge</code> is mandatory, the citation cards are first-class, the <code>HumanReviewGate</code> keeps pending readings honest, and cat/hung is never color alone. The screen reads the la so envelope and never writes it - the same read-only invariant the interpretation branch honors (strategy 4.3), enforced here by a byte-equality test.</p>";
