window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["API-READY"]="<h1>API-READY</h1>\n<h2>Goal</h2>\n<p>Operators can probe whether the API process is healthy and whether <code>CAST_CLI</code> is configured and executable. Optional strict mode fails readiness if CLI missing.</p>\n<h2>§1</h2>\n<ol><li><code>GET /healthz</code> remains liveness-only (<code>status: ok</code>).</li><li><code>GET /ready</code> returns structured <code>checks</code> including <code>cast_cli_configured</code>, <code>cast_cli_present</code>, <code>engine_mode</code>.</li><li>Env <code>READY_REQUIRE_CAST_CLI=1</code> → HTTP 503 when CLI not present/executable.</li></ol>";
