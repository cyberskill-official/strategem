window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-QMDG-006"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task assembles the QiMen engine end to end and is its acceptance gate. It runs the pipeline - dinh cuc (TASK-QMDG-001) -&gt; bo dia ban (TASK-QMDG-002) -&gt; truc phu / truc su + thien ban rotation (TASK-QMDG-003) -&gt; cuu tinh / bat mon / bat than (TASK-QMDG-004) -&gt; cach cuc (TASK-QMDG-005) - and emits the result as the la so JSON envelope (TASK-PLAT-002) with <code>he = \"ky_mon\"</code>.</p>\n<p>The module SHALL build a strongly-typed <code>KyMonBan</code> (the four plates plus dinh cuc, truc phu, truc su) and place it in the envelope <code>ban</code> slot; SHALL place the TASK-QMDG-005 hits in the envelope <code>cach_cuc</code> array; SHALL carry the CORE calendar output (TASK-CORE-005) in <code>lich_phap</code>; and SHALL stamp the complete school-flag set into <code>co_truong_phai</code>. The complete flag set (s8.2) is <code>dingju_method</code>, <code>pan_method</code>, <code>yin_yang_pan</code>, <code>zhong_gong_ky</code>, and <code>chan_thai_duong_thoi</code>.</p>\n<p>Acceptance SHALL be a 100% match against the kinqimen oracle across ALL flag combinations, over a large sample covering the 24 tiet khi x 3 nguyen, with dedicated edge tests for sieu than tiep khi, tri nhuan, and the center palace (s8.2). The chart SHALL be reproducible from <code>dau_vao</code> + <code>co_truong_phai</code> + <code>lich_phap</code> flags alone, and the cache key SHALL be computed per the TASK-PLAT-002 rule. The whole pipeline is deterministic once the flags are fixed (s1.2, s10.4), so the chart is cacheable by rounded instant, longitude, and flag set.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Everything upstream is a stage with its own oracle gate; this task proves the stages compose into a chart that matches kinqimen as a whole, under every flag combination. QiMen is the most school-variant engine (three orthogonal flag axes plus two smaller ones), so \"passes on the default config\" is not acceptance - the matrix must run per flag combination, or a school that a user actually casts under could be silently wrong (strategy RISK-7). Building this discipline here, on the flagship engine, sets the pattern LiuRen and TaiYi inherit.</p>\n<p>Mapping the engine-native s8.1 shape onto the shared PLAT-002 envelope is deliberate: the engine keeps its natural four-plate structure inside <code>ban</code>, while the cross-cutting fields the interpretation branch reads - <code>he</code>, <code>cach_cuc</code>, <code>co_truong_phai</code>, <code>lich_phap</code> - sit at the envelope top level where every module expects them. That is what lets the Python interpretation branch read a QiMen chart without knowing QiMen internals, and lets two users of different schools see exactly which conventions cast the chart.</p>\n<h2>§3 - Contract (assembly + envelope)</h2>\n<h3>Engine-native chart (Claude-03 s8.1, verbatim shape)</h3>\n<pre><code class=\"language-json\">{\n  \"he\": \"ky_mon\",\n  \"dau_vao\": { \"datetime\": \"...\", \"tz\": \"+07:00\", \"kinh_do\": 106.7, \"chan_thai_duong_thoi\": true },\n  \"tu_tru\": { \"nam\": \"...\", \"thang\": \"...\", \"ngay\": \"...\", \"gio\": \"甲子\" },\n  \"dinh_cuc\": { \"tiet_khi\": \"冬至\", \"tam_nguyen\": \"上元\", \"duong_don\": true, \"so_cuc\": 1 },\n  \"dia_ban\": {}, \"thien_ban\": {}, \"cuu_tinh\": {}, \"bat_mon\": {}, \"bat_than\": {},\n  \"truc_phu\": \"天蓬\", \"truc_su\": \"休門\",\n  \"cach_cuc\": [\"青龍返首\"],\n  \"co_truong_phai\": { \"dingju_method\": \"chaibu\", \"pan_method\": \"zhuan\", \"yin_yang_pan\": \"duong\" }\n}</code></pre>\n<h3>Mapping onto the PLAT-002 envelope</h3>\n<ul><li><code>he</code> -&gt; envelope <code>he = \"ky_mon\"</code>.</li><li><code>dau_vao</code> -&gt; envelope <code>dau_vao</code>; <code>chan_thai_duong_thoi</code> also mirrored in <code>co_truong_phai</code>.</li><li><code>tu_tru</code> and <code>dinh_cuc.tiet_khi</code> / <code>tam_nguyen</code> -&gt; envelope <code>lich_phap</code> (from TASK-CORE-005); the engine-native Han tam nguyen (上元/中元/下元) maps to the romanized <code>lich_phap.tiet_khi.tam_nguyen</code> (thuong/trung/ha).</li><li><code>dinh_cuc</code> (with <code>duong_don</code>, <code>so_cuc</code>), <code>dia_ban</code>, <code>thien_ban</code>, <code>cuu_tinh</code>, <code>bat_mon</code>, <code>bat_than</code>, <code>truc_phu</code>, <code>truc_su</code> -&gt; envelope <code>ban</code> as the typed <code>KyMonBan</code>.</li><li><code>cach_cuc</code> -&gt; envelope <code>cach_cuc</code> as <code>CachCuc</code> objects (TASK-QMDG-005 <code>CachCucHit</code>), not bare strings.</li><li><code>co_truong_phai</code> -&gt; envelope <code>co_truong_phai</code>, the complete flag set below.</li></ul>\n<h3>Complete flag set (Claude-03 s8.2, verbatim)</h3>\n<table><thead><tr><th>Cờ</th><th>Giá trị</th><th>Mặc định</th></tr></thead><tbody><tr><td>dingju_method</td><td>chaibu, zhirun, maoshan</td><td>chaibu</td></tr><tr><td>pan_method</td><td>zhuan, fei</td><td>zhuan</td></tr><tr><td>yin_yang_pan</td><td>duong, am</td><td>duong</td></tr><tr><td>zhong_gong_ky</td><td>khon2, giu_nguyen</td><td>khon2</td></tr><tr><td>chan_thai_duong_thoi</td><td>true, false</td><td>true</td></tr></tbody></table>\n<p>The default configuration is thoi-gia duong-ban chaibu chuyen-ban: <code>dingju_method = chaibu</code>, <code>pan_method = zhuan</code>, <code>yin_yang_pan = duong</code>, <code>zhong_gong_ky = khon2</code>, <code>chan_thai_duong_thoi = true</code>.</p>\n<h3>Public types and entry point</h3>\n<pre><code class=\"language-rust\">#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]\npub struct KyMonBan {\n    pub dinh_cuc: DinhCuc,\n    pub dia_ban: DiaBan,\n    pub thien_ban: [Can; 9],\n    pub cuu_tinh: [CuuTinh; 9],\n    pub bat_mon: [Option&lt;BatMon&gt;; 9],\n    pub bat_than: [Option&lt;BatThan&gt;; 9],\n    pub truc_phu: CuuTinh,\n    pub truc_su: BatMon,\n}\n\n// The single engine entry point: takes the CORE calendar context and the flag set,\n// returns the full la so envelope with he = ky_mon.\npub fn cast(ctx: &amp;LichPhap, flags: &amp;QiMenFlags) -&gt; Result&lt;LaSo, QiMenError&gt;;\n\nimpl KyMonBan { pub fn cache_key(&amp;self, dau_vao: &amp;DauVao, flags: &amp;QiMenFlags) -&gt; String; }</code></pre>\n<p><code>cache_key</code> follows TASK-PLAT-002: a stable hash of <code>(he, dau_vao rounded to the casting granularity, co_truong_phai sorted, lich_phap.co_lich_phap sorted)</code>; <code>co_truong_phai</code> is a sorted map so the key is identical in Rust and Python.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>cast</code> matches kinqimen 100% - dia ban, thien ban, cuu tinh, bat mon, bat than, truc phu, truc su, and the cach-cuc set - across a sample covering all 24 tiet khi x 3 nguyen.</li><li>The oracle gate runs for EVERY flag combination in the product of <code>dingju_method</code> x <code>pan_method</code> x <code>yin_yang_pan</code> x <code>zhong_gong_ky</code> x <code>chan_thai_duong_thoi</code>, not only the default; a mismatch on any combination fails the build (strategy RISK-7).</li><li>Dedicated edge tests pass: sieu than tiep khi boundary days, tri nhuan insertion at Mang Chung / Dai Tuyet, and center-palace (Trung 5) lodging.</li><li>The emitted envelope validates against the PLAT-002 schema; <code>he = \"ky_mon\"</code>, <code>co_truong_phai</code> carries all five flags, and <code>cach_cuc</code> entries are <code>CachCuc</code> objects.</li><li>Reproducibility: recasting from <code>dau_vao</code> + <code>co_truong_phai</code> + <code>lich_phap</code> alone yields a byte-identical chart; a round-trip test asserts it.</li><li><code>cache_key</code> is stable and identical across two casts of the same input and flags, and matches the Python <code>laso_envelope</code> key for the same fixture.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/kinqimen_oracle.rs</code> loads <code>fixtures/kinqimen_all_flags.csv</code> (day, hour, longitude, full flag tuple, and the expected four plates + truc phu / truc su + cach-cuc set) and asserts an exact match per row, iterating the full flag product. This is the QiMen acceptance gate; it MUST run in CI (strategy RISK-7).</li><li>Edge fixtures: a labelled subset for sieu than tiep khi, tri nhuan, and center palace, asserted per <code>dingju_method</code>.</li><li>Envelope tests: schema validation against PLAT-002, the reproducibility round-trip, and the cross-language cache-key equality with <code>laso_envelope</code>.</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-qimen -- -D warnings</code>, <code>cargo test -p cyberos-qimen</code>; the CORE oracle harness (TASK-CORE-006) must be green first, since QiMen inherits the calendar context.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Add <code>ban.rs</code> with <code>KyMonBan</code> (serde) and its mapping to the envelope <code>ban</code> slot; add <code>engine.rs</code> with <code>cast</code>.</li><li>Wire the pipeline: call TASK-QMDG-001..005 in order, assemble <code>KyMonBan</code>, collect <code>CachCucHit</code>s.</li><li>Build the envelope: set <code>he</code>, <code>dau_vao</code>, <code>lich_phap</code> (from TASK-CORE-005), <code>ban</code>, <code>cach_cuc</code>, <code>co_truong_phai</code> (all five flags), <code>provenance</code>.</li><li>Implement <code>cache_key</code> per PLAT-002; add the cross-language equality test.</li><li>Generate <code>fixtures/kinqimen_all_flags.csv</code> once from kinqimen across the flag product and the tiet-khi x nguyen sample (script documented, not run in CI); commit it. Wire the oracle test and the edge fixtures.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-QMDG-005 (the last pipeline stage) and TASK-CORE-006 (the calendar oracle harness must be green, since QiMen stands on the CORE context). Emits the TASK-PLAT-002 envelope. Blocks TASK-CHART-001 (the 9-palace UI renders this chart), TASK-STRAT-001 (the Timing Optimizer scans this engine), and TASK-QMDG-007 (dung than reads the assembled chart). This is the QiMen node on the P0 critical path: <code>CORE-006 + QMDG-006 -&gt; CHART-001 / STRAT-001</code>.</p>\n<h2>§8 - Example payloads</h2>\n<p>The assembled envelope (abridged; <code>ban</code> holds the full <code>KyMonBan</code>):</p>\n<pre><code class=\"language-json\">{\n  \"envelope_version\": 1,\n  \"he\": \"ky_mon\",\n  \"dau_vao\": { \"datetime\": \"2004-01-01T10:30:00\", \"tz\": \"+07:00\", \"kinh_do\": 106.7, \"loai_cau_hoi\": \"trach_thoi\" },\n  \"lich_phap\": { \"tu_tru\": { \"gio\": \"丁巳\" }, \"tiet_khi\": { \"hien_hanh\": \"冬至\", \"tam_nguyen\": \"thuong\" } },\n  \"ban\": { \"dinh_cuc\": { \"duong_don\": true, \"so_cuc\": 1 }, \"truc_phu\": \"天蓬\", \"truc_su\": \"休門\",\n           \"dia_ban\": {}, \"thien_ban\": {}, \"cuu_tinh\": {}, \"bat_mon\": {}, \"bat_than\": {} },\n  \"cach_cuc\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"青龍返首\", \"cung\": 1, \"polarity\": \"cat\", \"score\": 0.9, \"citations\": [\"Yên Ba Điếu Tẩu Ca\"] } ],\n  \"co_truong_phai\": { \"dingju_method\": \"chaibu\", \"pan_method\": \"zhuan\", \"yin_yang_pan\": \"duong\", \"zhong_gong_ky\": \"khon2\", \"chan_thai_duong_thoi\": \"true\" },\n  \"provenance\": { \"engine\": \"qmdg\", \"engine_version\": \"0.1.0\", \"cast_at\": \"2026-07-08T12:00:00Z\", \"cache_key\": \"...\" }\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Flag-product size: five flags give a modest product (3 x 2 x 2 x 2 x 2 = 48), but <code>am</code> (yin_yang_pan) is specified only in principle at MVP. Default: gate the full <code>duong</code> product hard and gate <code>am</code> as far as kinqimen supports it, marking <code>am</code> combinations that are not yet oracle-backed as <code>ignored</code> with a tracking note rather than silently skipped.</li><li>Should the engine ship as a service, a PyO3 binding, or both for the Python orchestrator (strategy 4.1 DEC-2)? Default: the crate is the unit of truth; the service / binding wrapper is a PLAT concern and does not change this envelope.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Default-only testing</td><td>matrix runs one config</td><td>RISK-7 violated; CI must iterate the full flag product</td></tr><tr><td>Flag not stamped</td><td>a used flag missing from co_truong_phai</td><td>reproducibility round-trip diverges; test fails before ship</td></tr><tr><td>Envelope drift</td><td>ban shape diverges from PLAT-002</td><td>schema validation fails in the contract test</td></tr><tr><td>Non-deterministic cache key</td><td>flags hashed in map order</td><td>cross-language key-equality test fails</td></tr><tr><td>CORE not green</td><td>calendar context wrong</td><td>QMDG oracle inherits the error; TASK-CORE-006 gate must pass first</td></tr><tr><td>Edge case unhandled</td><td>sieu than / tri nhuan / center palace</td><td>labelled edge fixtures fail; do not ship</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the QiMen stop-ship gate: a mismatch with kinqimen on any flag combination is a defect, and the default config (thoi-gia duong-ban chaibu chuyen-ban) is the most-used but not the only tested one. Keep the flag product in one place (<code>QiMenFlags</code>) so the matrix is generated, not hand-listed, and it stays complete as flags evolve. The envelope this task emits is the contract every downstream module (CHART, STRAT, RAG via interpretation) consumes; treat any change to it as a PLAT-002 versioned change, not a local edit.</p>";
