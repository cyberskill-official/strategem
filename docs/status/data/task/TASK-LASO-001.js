window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-LASO-001"]="<h1>TASK-LASO-001 - Single source of truth for supported envelope versions</h1>\n<h2>1. Normative clauses</h2>\n<ol><li>The set of supported <code>envelope_version</code> values MUST be defined once, as an importable module-level constant <code>SUPPORTED_ENVELOPE_VERSIONS: frozenset[int]</code> in <code>laso_envelope.models</code>, and MUST be re-exported from the package root (<code>laso_envelope</code>).</li><li><code>require_supported_version</code> MUST decide acceptance solely by membership in <code>SUPPORTED_ENVELOPE_VERSIONS</code> (no inline literal set), and its <code>ValueError</code> message MUST list the supported versions.</li><li>Behaviour MUST be unchanged for the current supported set <code>{1}</code>: an envelope with <code>envelope_version == 1</code> is accepted; any other integer version raises <code>ValueError</code>.</li><li>Tests MUST assert: (a) <code>1 in SUPPORTED_ENVELOPE_VERSIONS</code>; (b) a v1 envelope is accepted by <code>require_supported_version</code>; (c) an unsupported version raises <code>ValueError</code> whose message names the supported set.</li></ol>\n<h2>2. Scope</h2>\n<p>Touched: <code>packages/laso_envelope/src/laso_envelope/models.py</code>, <code>.../__init__.py</code>, <code>packages/laso_envelope/tests/</code>. Pure refactor to a single source of truth plus coverage; no schema, wire-format, or Rust-parity change.</p>\n<h2>3. Gate</h2>\n<p>Python lane, targeted: <code>uv run pytest -q packages/laso_envelope</code>. No behaviour change, so the cache-key / golden-parity tests must stay green.</p>";
