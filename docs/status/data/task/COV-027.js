window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["COV-027"]="<h1>COV-027 — Local full-stack Docker compose — build api+web+cast-cli from source (not GHCR-only)</h1>\n<h2>Goal</h2>\n<p>A developer (or CI agent) can boot the <strong>full product stack locally from source</strong> with Docker Compose: Postgres, Redis (if required), API (with <code>cast-cli</code> for real casts), and web — without depending on pre-pulled GHCR staging images or production secrets. Two consecutive boots produce healthy <code>/healthz</code> + <code>/ready</code> and a non-empty structured cast for KM/LN/TA.</p>\n<p>Closes residual gaps from:</p>\n<ul><li>Enterprise OBJECTIVE: fully worked in local with Docker</li><li><code>docs/strategy/grok-docs-coverage-benchmark-2026-07-13.md</code> ops/local run dimensions</li><li>Staging compose today is GHCR-image-only (<code>deploy/compose/docker-compose.staging.yml</code>)</li></ul>\n<h2>§1 Acceptance criteria (normative)</h2>\n<ol><li>MUST add <code>deploy/compose/docker-compose.local.yml</code> (or equivalent) that <strong>builds</strong> images from <code>deploy/docker/*.Dockerfile</code> (or multi-stage local Dockerfiles) rather than requiring GHCR pulls.</li><li>MUST produce a runnable API image: <code>python -m tamthuc_api</code> (or documented entry), <code>CAST_CLI</code> points at an in-image <code>cast-cli</code> binary built from <code>crates/cast-cli</code>, <code>READY_REQUIRE_CAST_CLI=1</code> yields <code>/ready</code> 200 when CLI is present.</li><li>MUST expose web on a documented host port (default 3000 or alternate if conflict) with <code>NEXT_PUBLIC_API_BASE</code> pointing at the API service (compose network or host-gateway as documented).</li><li>MUST include Postgres with <code>DATABASE_URL</code> wired; app MUST start when DB is healthy (in-memory fallback only when <code>DATABASE_URL</code> unset — local compose sets it).</li><li>MUST document one-command boot + dual-run verification in <code>docs/deploy/local-docker-lmstudio.md</code> (shared with COV-028).</li><li>MUST NOT require production secrets (JWT may use a documented local-dev default; no cloud keys required for cast path).</li><li>Verification: two independent <code>compose up</code> cycles; each cycle proves <code>/healthz</code>, <code>/ready</code>, and one real cast (KM or LN or TA) with non-empty envelope fields (system plate keys present). Capture logs under operator scratch.</li></ol>\n<h2>§2 Non-goals</h2>\n<ul><li>Production VPS/Vercel/Supabase secret provisioning (COV-020).</li><li>Replacing GHCR staging compose for remote staging (keep staging file; local is additive).</li><li>Bundling LMStudio inside Docker (host-side; COV-028).</li></ul>\n<h2>§3 Verification</h2>\n<ul><li><code>docker compose -f deploy/compose/docker-compose.local.yml up --build -d</code> succeeds twice.</li><li><code>curl</code> health/ready + cast API/CLI path with structured JSON body.</li><li><code>bash .cyberos/cuo/gates/run-gates.sh</code> green where Dockerfiles change affect CI lanes.</li><li>Human sets task <code>done</code> only after HITL.</li></ul>\n<h2>§4 Dependencies</h2>\n<p>depends_on: PLAT-004, API-001, PLAT-013</p>\n<h2>§5 Refs</h2>\n<p>enterprise-local-objective, benchmark-grok §8 ops, deploy/README Local</p>";
