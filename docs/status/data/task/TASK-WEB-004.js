window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task builds the dashboard - the post-login landing screen and the hub of the three product flows (Claude-07 s6, strategy 1). It is where a signed-in user arrives and chooses what to do: cast a new chart, return to a recent or saved chart, or step into one of the three flows (lookup, learning, management). It owns the landing composition and its read-only data fetch; it does NOT cast a chart (TASK-WEB-002), render results (TASK-WEB-003), or own the management surface (TASK-WEB-007) - it routes into them.</p>\n<p>The screen SHALL present, for the authenticated principal (TASK-AUTH-001): (a) a recent-charts strip - the user's most recently cast charts, newest first, each showing its system (<code>he</code>), question type, and cast time, each a link to its results (TASK-WEB-003) by <code>query_id</code>; (b) a saved-charts section - charts the user pinned; (c) a quick-cast entry - a single prominent Ochre call-to-action (control height md <code>44px</code>, TASK-WEB-001) that routes into the query input screen (TASK-WEB-002), optionally pre-seeding the last-used system tab; and (d) entry-point cards to the three flows: lookup (ask -&gt; cast -&gt; read), learning (the EDU curriculum, auto-graded practice, bilingual library), and management (saved-chart history, school-flag config, share/export - TASK-WEB-007). It SHALL be built on the TASK-WEB-001 shell (tokens, top bar, density, locale, disclaimer slot) and SHALL surface the in-product disclaimer (TASK-LEGAL-001).</p>\n<p>The dashboard SHALL be a thin client: it reads persisted chart references (TASK-API-004) and routes; it never casts or interprets, and where it renders any chart preview it treats the la so envelope as read-only and SHALL NOT write <code>ban</code>, <code>cach_cuc</code>, <code>lich_phap</code>, or <code>co_truong_phai</code> (strategy 4.3). An empty account (no charts yet) SHALL show a first-run state that leads with the quick-cast entry rather than an empty list.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>A divination-and-strategy product that spans three flows needs one place that makes all three reachable without the user relearning the interface each time (Claude-07 s6). The dashboard is that place: it is the shared front door built on the same component nervous system as every other screen, so a user learns the shell once and applies it everywhere. Leading with a single quick-cast call-to-action keeps the primary action honest - casting a chart is the product's core act, so it gets the one Ochre primary (TASK-WEB-001), and the flow cards are secondary affordances, not competing primaries.</p>\n<p>Recent and saved charts are first-class because the value of a cast is not only the moment it is read - a user returns to a chart to re-read it against how events actually unfolded, which is exactly the reflective, decision-support framing the product stands on (strategy 7, Claude-07 s2). Reading those references read-only, by <code>query_id</code>, keeps the dashboard a pure navigator over what the engine already cast and the API already persisted; the moment the landing page re-derived or \"refreshed\" a chart it would be asserting something the engine did not. The first-run state matters for the same reason the quick-cast entry does: a new user should meet the core act, not an empty table.</p>\n<h2>§3 - Contract (screen / data / routing)</h2>\n<h3>Layout (Claude-07 s6, on the TASK-WEB-001 shell)</h3>\n<pre><code>+----------------------------- dashboard -----------------------------+\n| top bar (Umber, Ochre mark, system tabs)            (TASK-WEB-001)    |\n| quick-cast: [ Ochre 44px \"Cast a chart\" -&gt; /cast ]  (single primary)|\n| recent charts: [ card ][ card ][ card ] ...  -&gt; /results/{query_id} |\n| saved charts:  [ card ][ card ] ...          -&gt; /results/{query_id} |\n| three flows: [ Lookup ][ Learning ][ Management ]  (entry cards)    |\n| disclaimer (TASK-LEGAL-001)                                           |\n+--------------------------------------------------------------------+</code></pre>\n<h3>Data (<code>lib/api/dashboard.ts</code>, reads TASK-API-004 persistence)</h3>\n<pre><code class=\"language-ts\">type ChartRef = {\n  query_id: string;\n  he: \"ky_mon\" | \"luc_nham\" | \"thai_at\";\n  question_type: string;          // loai_cau_hoi\n  cast_at: string;                // ISO\n  saved: boolean;\n};\ntype DashboardData = { recent: ChartRef[]; saved: ChartRef[] };\n\nasync function getDashboard(): Promise&lt;DashboardData&gt;;\n// GET the persisted references for the authed principal (TASK-API-004); sends the JWT Bearer;\n// read-only; never fetches or mutates the ban.</code></pre>\n<h3>Routing</h3>\n<table><thead><tr><th>Affordance</th><th>Routes to</th><th>task</th></tr></thead><tbody><tr><td>quick-cast (Ochre primary)</td><td><code>/cast</code> (optionally <code>?system=&lt;last&gt;</code>)</td><td>TASK-WEB-002</td></tr><tr><td>a recent / saved chart card</td><td><code>/results/{query_id}</code></td><td>TASK-WEB-003</td></tr><tr><td>flow card: lookup</td><td><code>/cast</code></td><td>TASK-WEB-002</td></tr><tr><td>flow card: learning</td><td>the EDU entry</td><td>TASK-EDU-001 (later)</td></tr><tr><td>flow card: management</td><td><code>/manage</code></td><td>TASK-WEB-007 (later)</td></tr></tbody></table>\n<p>Where a flow's screen is not yet built (learning at P1), its card SHALL render disabled with a \"coming soon\" state rather than a dead link.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Signed in, the dashboard renders a recent-charts strip (newest first) and a saved-charts section from <code>getDashboard()</code>, each card showing <code>he</code>, question type, and cast time, each linking to <code>/results/{query_id}</code>.</li><li>There is exactly one Ochre primary on the view - the quick-cast entry at md <code>44px</code> (TASK-WEB-001) - routing to <code>/cast</code>; the three flow cards are secondary, not primary.</li><li>The three flow entry cards (lookup / learning / management) are present; a not-yet-built flow renders disabled with a \"coming soon\" state, never a dead link.</li><li>An account with no charts shows a first-run state that leads with quick-cast, not an empty table.</li><li>The in-product disclaimer (TASK-LEGAL-001) is present on the screen.</li><li>The dashboard performs no cast and no interpretation, and where it previews a chart it does not write <code>ban</code>/<code>cach_cuc</code>/<code>lich_phap</code>/<code>co_truong_phai</code> (read-only, asserted).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/dashboard.test.tsx</code>: renders a <code>DashboardData</code> fixture (populated and empty); asserts the recent/saved cards and their <code>/results/{query_id}</code> links, the single-Ochre-primary quick-cast routing to <code>/cast</code>, the three flow cards with the disabled \"coming soon\" state for an unbuilt flow, the first-run empty state, and the disclaimer presence.</li><li>Accessibility: <code>jest-axe</code> clean; keyboard-reachable cards and CTA; focus-visible Ochre ring; the stacked-diacritics clip test (TASK-WEB-001) over question-type labels and system names.</li><li>Contract: <code>ChartRef</code> is checked against the TASK-API-004 persisted-reference shape (shared fixture); a drift fails the test.</li><li>Gates: <code>pnpm --filter web lint</code>, <code>pnpm --filter web test</code>, <code>next build</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>lib/api/dashboard.ts</code>: the typed <code>getDashboard()</code> read against TASK-API-004 with the auth token; read-only.</li><li><code>components/dashboard/quick-cast.tsx</code>: the single Ochre primary routing to <code>/cast</code> (optionally seeding the last system).</li><li><code>recent-charts.tsx</code> + <code>saved-charts.tsx</code>: the <code>ChartRef</code> card lists with <code>/results/{query_id}</code> links and the empty/first-run states.</li><li><code>flow-entry-cards.tsx</code>: the lookup / learning / management cards, with the disabled \"coming soon\" state for unbuilt flows.</li><li><code>dashboard.tsx</code> + <code>app/dashboard/page.tsx</code>: compose the sections in the TASK-WEB-001 shell; wire the disclaimer slot (TASK-LEGAL-001).</li><li><code>tests/dashboard.test.tsx</code> + the shared TASK-API-004 reference fixture.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-WEB-001 (the shell, tokens, Button, top-bar tabs, disclaimer slot). Reads TASK-API-004 (the persisted query/chart references) for the recent and saved lists, and is gated by TASK-AUTH-001 (post-login). Routes into TASK-WEB-002 (quick-cast and the lookup card), TASK-WEB-003 (a chart card opens its results), TASK-WEB-007 (the management card), and the TASK-EDU-001 learning surface when it lands. Uses TASK-LEGAL-001 for the disclaimer. It never casts a chart itself (strategy 4.2) and reads any envelope read-only (strategy 4.3).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-ts\">// getDashboard() result the landing renders\nconst data: DashboardData = {\n  recent: [\n    { query_id: \"q_9f2\", he: \"ky_mon\", question_type: \"trach_thoi\", cast_at: \"2026-07-08T09:12:00Z\", saved: false },\n    { query_id: \"q_8a1\", he: \"luc_nham\", question_type: \"hon_nhan\", cast_at: \"2026-07-07T15:40:00Z\", saved: true }\n  ],\n  saved: [\n    { query_id: \"q_8a1\", he: \"luc_nham\", question_type: \"hon_nhan\", cast_at: \"2026-07-07T15:40:00Z\", saved: true }\n  ]\n};\n// a recent card -&gt; router.push(`/results/${ref.query_id}`)   (TASK-WEB-003)\n// quick-cast    -&gt; router.push(`/cast?system=${lastSystem}`) (TASK-WEB-002)</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>What \"saved\" means at P1: an explicit user pin vs an automatic keep of the last N. Default: an explicit saved flag on the chart (toggled from results / management, TASK-WEB-007), with recent being the automatic last-N; this keeps saved a deliberate act and recent a convenience.</li><li>Whether the dashboard previews a rendered chart thumbnail or only a text reference card. Default: a text reference card at P1 (system + question + time), since a thumbnail would need to read the envelope; a read-only preview is an additive enhancement, not P1.</li><li>Where the learning flow lands before TASK-EDU-001 ships. Default: the learning card renders disabled with a \"coming soon\" state so the three-flow information architecture is visible without a dead link.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Second primary</td><td>a flow card styled as an Ochre primary</td><td>forbidden; the single primary is quick-cast; flow cards are secondary (TASK-WEB-001)</td></tr><tr><td>Dead flow link</td><td>an unbuilt flow card links nowhere</td><td>render disabled with a \"coming soon\" state, never a broken route</td></tr><tr><td>Empty-list dead end</td><td>a new account shows an empty table</td><td>show the first-run state that leads with quick-cast</td></tr><tr><td>Dashboard casts</td><td>the landing tries to compute a chart</td><td>forbidden; it reads references and routes; casting is TASK-WEB-002 / the gateway</td></tr><tr><td>Envelope mutated on preview</td><td>a chart preview writes an engine field</td><td>forbidden; read-only; a byte-equality test asserts it</td></tr><tr><td>Missing disclaimer</td><td>framing only in a footer elsewhere</td><td>the TASK-LEGAL-001 disclaimer is present on the dashboard</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>The dashboard is the hub that makes the three-flow information architecture real (Claude-07 s6): one front door, built on the TASK-WEB-001 shell, that leads to casting, to past charts, and to each flow. Keep it a thin navigator - one Ochre primary (quick-cast), read-only reference cards routing by <code>query_id</code>, and secondary flow cards - and let the screens it routes to do the casting, reading, and managing. It is a SHOULD at P1 because the P0 lookup spine (WEB-002 -&gt; WEB-003) can stand without it, but it is the screen that turns a set of pages into a coherent product.</p>";
