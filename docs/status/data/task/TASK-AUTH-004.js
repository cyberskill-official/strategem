window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-AUTH-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task is the data-subject self-service for privacy rights: export (a machine-readable copy of everything the platform holds about a user) and erasure (right to be forgotten). It extends the <code>tamthuc_auth</code> package and executes the DSAR obligations TASK-LEGAL-002 defines. It owns the export and erasure operations over the user's data; it does NOT own the legal policy - the retention windows, the lawful bases, the disclosure text - which belong to TASK-LEGAL-002; this task is the mechanism that enforces that policy on the actual tables.</p>\n<p>Export SHALL assemble a complete, machine-readable copy of the user's personal data across the TASK-PLAT-003 tables - the profile (TASK-AUTH-001, with <code>birth_data</code> decrypted for the subject's own export), queries, charts, reports, and the relevant audit trail - and SHALL return it as a structured archive (JSON), scoped strictly to the requesting user under row-level security so no other user's data can leak into an export. Erasure SHALL remove or irreversibly de-identify the user's personal data: it SHALL soft-delete the profile (<code>users.deleted_at</code>, TASK-PLAT-003) and crypto-shred <code>birth_data</code> by destroying the wrapped per-record data key (TASK-AUTH-001 envelope encryption), so the ciphertext - including copies in backups (TASK-PLAT-009) - becomes permanently unreadable without a per-row rewrite. Erasure SHALL honor the TASK-LEGAL-002 retention contract: records under a legal retention obligation (for example audit rows required for a defined period) SHALL be retained per policy rather than deleted, and the response SHALL state what was erased and what was retained and why.</p>\n<p>Both operations SHALL require a freshly authenticated principal (re-authentication or a recent session) because they are high-impact, SHALL be audited via TASK-API-004 (<code>dsar_export</code>, <code>dsar_erase</code>) without copying the exported sensitive payload into the audit row, and SHALL be idempotent and safe to retry. Export SHALL be delivered so the archive itself is protected (authenticated, time-limited access), not left at a public URL. Failures SHALL return the TASK-API-001 error envelope.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Export and erasure are legal rights under VN PDPD and GDPR, and the product's whole positioning rests on honoring them credibly (strategy 7, RISK-5). The hard part of export is completeness and scoping at once: it must gather everything about the user across every table, and it must gather nothing about anyone else. Running the export under the same fail-closed RLS the rest of the platform uses (TASK-PLAT-003) is what makes the second guarantee structural rather than a careful join - a bug in the export query cannot pull another user's rows because the database refuses them. Decrypting <code>birth_data</code> for the subject's own export is correct (it is their data) but the archive is then sensitive, so it is delivered under authenticated, time-limited access rather than a public link.</p>\n<p>Erasure is where the encryption design pays off. Hard-deleting rows across a live database, its replicas, and its backups is slow, error-prone, and often incompatible with legal retention of some records. Crypto-shredding - destroying the wrapped per-record key so the AES-256 ciphertext can never be decrypted again - erases the birth data everywhere it exists, including in backups (TASK-PLAT-009), in a single stroke, because ciphertext without its key is noise. That is exactly why TASK-AUTH-001 wrapped a per-record data key in the first place. Pairing crypto-shred with a soft-delete of the profile and an explicit honoring of the retention contract (some audit records must survive for a legally defined period) gives an erasure that is both complete for the sensitive payload and lawful about what it keeps - and the response says so, rather than pretending everything vanished. Requiring fresh authentication and auditing the action (without copying the payload) keeps a destructive, irreversible operation from being trivially triggerable and keeps a record that it happened.</p>\n<h2>§3 - Contract (export / erasure / audit)</h2>\n<h3>Export (<code>export.py</code>)</h3>\n<pre><code class=\"language-python\">async def export_user_data(user_id: str) -&gt; DsarArchive:\n    # gather, under RLS scoped to user_id:\n    #   profile (birth_data DECRYPTED for the subject), queries, charts, reports, relevant audit rows\n    # -&gt; a structured JSON archive; delivered via authenticated, time-limited access (never a public URL)</code></pre>\n<h3>Erasure (<code>erasure.py</code>)</h3>\n<pre><code class=\"language-python\">async def erase_user_data(user_id: str) -&gt; ErasureResult:\n    # 1. crypto-shred birth_data: destroy the wrapped per-record data key (TASK-AUTH-001) -&gt; ciphertext unreadable\n    # 2. soft-delete the profile (users.deleted_at, TASK-PLAT-003)\n    # 3. de-identify / delete queries+charts+reports per the TASK-LEGAL-002 retention contract\n    # 4. RETAIN records under legal retention (e.g. required audit rows); report what was kept and why\n    # -&gt; ErasureResult { erased: [...], retained: [{ table, reason }], crypto_shredded: true }</code></pre>\n<h3>Endpoints (mounted by TASK-API-001; require fresh authentication)</h3>\n<pre><code>POST /auth/me/export   (Bearer, re-auth)  -&gt; { archive_ref }   # authenticated, time-limited download\nPOST /auth/me/erase    (Bearer, re-auth)  -&gt; ErasureResult      # crypto-shred + soft-delete + retention report</code></pre>\n<h3>Audit (via TASK-API-004)</h3>\n<p><code>dsar_export</code> and <code>dsar_erase</code> audit rows record that the action occurred, by whom, and the scope - never the exported sensitive payload.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Export returns a machine-readable archive containing the user's profile (with <code>birth_data</code> decrypted for the subject), queries, charts, reports, and relevant audit rows, and contains no other user's data (RLS-scoped); a cross-user leakage test finds nothing foreign.</li><li>The export archive is delivered under authenticated, time-limited access, not a public URL; an unauthenticated or expired access attempt is refused.</li><li>Erasure crypto-shreds <code>birth_data</code> (the wrapped data key is destroyed) so the ciphertext can no longer be decrypted - verified by attempting decryption post-erasure and getting failure, not plaintext - and this holds for backup copies by construction (no key, no plaintext).</li><li>Erasure soft-deletes the profile and de-identifies/deletes the user's history per the TASK-LEGAL-002 retention contract, while retaining records under a legal retention obligation; the result reports what was erased and what was retained and why.</li><li>Both operations require a freshly authenticated principal and are audited via TASK-API-004 (<code>dsar_export</code>/<code>dsar_erase</code>) with no sensitive payload copied into the audit row; both are idempotent on retry.</li><li>All failures return the TASK-API-001 error envelope; an erased user can no longer authenticate and their <code>birth_data</code> is unrecoverable.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_export.py</code>: completeness (all of the subject's tables represented); RLS scoping (no foreign rows, driven with two users); the decrypted-birth_data-for-subject case; authenticated/time-limited delivery (expired access refused).</li><li><code>tests/test_erasure.py</code>: crypto-shred (post-erasure decryption fails, not returns plaintext); soft-delete of the profile; retention honored (a required audit row survives; the result lists it with a reason); idempotent retry; erased user cannot authenticate.</li><li>Security checks: the audit rows for export/erase carry no exported payload; a fresh-auth requirement is enforced (a stale session is rejected).</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_auth</code>, <code>pytest packages/tamthuc_auth</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>export.py</code>: gather the subject's data across the TASK-PLAT-003 tables under RLS; decrypt <code>birth_data</code> for the subject; assemble the JSON archive.</li><li><code>erasure.py</code>: crypto-shred (destroy the wrapped DEK), soft-delete the profile, de-identify/delete history per TASK-LEGAL-002 retention, retain legally required records and report them.</li><li><code>dsar.py</code>: the orchestration + the fresh-auth requirement + the authenticated, time-limited archive delivery.</li><li>Wire the two endpoints into the TASK-API-001 router; emit <code>dsar_export</code>/<code>dsar_erase</code> audit rows via TASK-API-004 (no payload); render errors in the TASK-API-001 envelope.</li><li>Align the retention rules and the disclosure text with TASK-LEGAL-002; document the DSAR contract in <code>docs/contracts/dsar.md</code>.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-AUTH-001 (the profile it exports/erases and, critically, the envelope encryption whose wrapped per-record key makes crypto-shred possible) and TASK-LEGAL-002 (the retention windows, lawful bases, and disclosure text this task enforces mechanically). Operates over the TASK-PLAT-003 tables and relies on their fail-closed RLS to scope an export; erasure's crypto-shred reaches backup copies governed by TASK-PLAT-009. Audited via TASK-API-004 (<code>dsar_export</code>/<code>dsar_erase</code>); endpoints mounted by and errors rendered through TASK-API-001. This is the AUTH mechanism behind the LEGAL-002 privacy contracts and completes the RISK-5 posture (export + erasure).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// POST /auth/me/erase -&gt; ErasureResult (reports erased vs legally retained)\n{ \"crypto_shredded\": true,\n  \"erased\": [\"users.birth_data\", \"users.profile\", \"queries\", \"charts\", \"reports\"],\n  \"retained\": [ { \"table\": \"audit_logs\", \"reason\": \"legal retention window (TASK-LEGAL-002)\", \"until\": \"2029-07-08\" } ] }</code></pre>\n<pre><code class=\"language-json\">// dsar_erase audit row (via TASK-API-004) - records the fact + scope, never the payload\n{ \"action\": \"dsar_erase\", \"user_id\": \"u_...\",\n  \"details\": { \"crypto_shredded\": true, \"retained_tables\": [\"audit_logs\"] }, \"created_at\": \"...\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Erasure of <code>queries</code>/<code>charts</code>/<code>reports</code>: hard-delete vs de-identify. Default: crypto-shred the sensitive <code>birth_data</code>, then hard-delete or de-identify the query/chart/report rows per the TASK-LEGAL-002 retention contract (charts derived from erased birth data have limited standalone value); the exact treatment is a LEGAL-002 decision this task enforces.</li><li>Export format and portability. Default: a structured JSON archive at MVP (machine-readable, GDPR-portable); a human-readable rendering or an additional format is additive and does not change the gather/scope logic.</li><li>Delivery mechanism for the archive. Default: an authenticated, time-limited download reference (not a public URL); an emailed link must itself be single-use and expiring (reuse the TASK-AUTH-003 token discipline).</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Cross-user export leak</td><td>export pulls another user's rows</td><td>forbidden; export runs under fail-closed RLS scoped to the subject; a leakage test finds nothing foreign</td></tr><tr><td>Recoverable birth_data after erasure</td><td>ciphertext still decryptable</td><td>forbidden; crypto-shred destroys the wrapped key; post-erasure decryption fails, including in backups</td></tr><tr><td>Retention violated</td><td>a legally required record deleted</td><td>records under a retention obligation are retained per TASK-LEGAL-002; the result reports what was kept and why</td></tr><tr><td>Sensitive payload in audit</td><td>exported data copied into the audit row</td><td>forbidden; the audit row records the fact + scope, never the payload</td></tr><tr><td>Trivially triggerable erasure</td><td>destructive op without fresh auth</td><td>export/erase require a freshly authenticated principal</td></tr><tr><td>Archive left public</td><td>export archive at an open URL</td><td>delivered under authenticated, time-limited access only</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task turns the AUTH-001 encryption design and the LEGAL-002 policy into working privacy rights: export gathers everything about the subject (and nothing about anyone else, guaranteed by fail-closed RLS), and erasure crypto-shreds <code>birth_data</code> by destroying its wrapped key so the ciphertext dies everywhere at once, backups included, while honoring the legal retention the LEGAL-002 contract requires and reporting exactly what was kept. Keep the boundary clean: LEGAL-002 owns the policy (windows, bases, disclosure), this task owns the mechanism (export, crypto-shred, soft-delete, retention enforcement). It extends <code>tamthuc_auth</code>, requires fresh authentication for both destructive/high-impact operations, and audits them through TASK-API-004 without copying the payload. Together with TASK-AUTH-001 it completes the RISK-5 posture.</p>";
