window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CHART-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task builds the interactive LiuRen (Dai Luc Nham) chart view - the second chart type, rendering the la so the LiuRen engine cast (TASK-LN-006). Like the 9-palace QiMen chart (TASK-CHART-001), it is a pure view over the engine's output: it reads the la so envelope and never computes or mutates it (strategy 4.3). It inherits the TASK-CHART-001 pattern - read the engine's <code>ban</code>, render the domain's native notation, encode no rule of its own. It owns the LiuRen rendering and interaction; it does NOT cast the chart (TASK-LN-006) and does NOT own the results/cross-system composition (TASK-WEB-003, TASK-STRAT-004) that embeds it.</p>\n<p>The component SHALL read the la so envelope (TASK-PLAT-002) for <code>he = \"luc_nham\"</code>, taking the <code>ban</code> slot as the LiuRen <code>ban</code> (TASK-LN-006) and the top-level <code>cach_cuc</code> for polarity. It SHALL render, in the LiuRen native notation: (1) the thien dia ban - the fixed dia ban 12-branch ring (地盤, the twelve dia chi in their fixed positions) with the rotating thien ban ring (天盤) placed over it by the nguyet tuong; (2) the tu khoa (四課, four lessons), each as its upper/lower (thuong / ha) can-chi pair with the khac direction (thuong khac 上剋 / ha khac 下剋) marked; (3) the tam truyen (三傳, three transmissions) - so truyen (初傳), trung truyen (中傳), mat truyen (末傳) - each with its chi, its thien tuong, and its luc than; and (4) the twelve thien tuong (十二天將, the heavenly generals) placed around the board thuan/nghich from the khoi quy nhan. Where a slot is empty (e.g. the degenerate phuc ngam / phan ngam cases the engine produced), it SHALL render empty, not fabricated.</p>\n<p>Hover or click on a board cell, a lesson, a transmission, or a general SHALL reveal a detail view (its full content and role). Elements carrying a cach cuc / polarity SHALL be marked cat / hung / trung by color AND icon AND text - never color alone (TASK-WEB-001). The chart SHALL be responsive and SHALL expose the export seam (TASK-CHART-004 completes export/print/accessibility) and meet baseline accessibility (keyboard-reachable cells, text alternatives). It SHALL treat the envelope as read-only and SHALL NOT write <code>ban</code>, <code>cach_cuc</code>, <code>lich_phap</code>, or <code>co_truong_phai</code>.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>A LiuRen reading is read in a fixed order through a fixed notation: the caster looks at the thien dia ban, reads the four lessons, follows the three transmissions, and weighs the twelve generals (Claude-02). That order and that notation are the domain's language, so getting the fixed dia ban ring, the thien-ban rotation, the four-lesson layout, or the transmission order wrong makes the chart unreadable to anyone who practices the art. Rendering exactly the <code>ban</code> the engine produced - including the degenerate phuc ngam / phan ngam cases as the engine cast them, empty where empty - keeps the chart an honest picture of a deterministic computation the kinliuren oracle verified (TASK-LN-006), not an illustration that smooths over an awkward cast.</p>\n<p>Making the view a pure reader is the same architectural boundary the QiMen chart rests on (strategy 4.3, TASK-CHART-001 s2). LiuRen has the most explicit, least school-contested rules of the three engines - the nine tong mon are a decision tree, the tam truyen derivation is the engine's - so the temptation to \"help\" by re-deriving a transmission in the view is real and must be refused: the moment the chart re-computes a truyen, it asserts something the engine did not, and the determinism guarantee leaks into the UI. cat/hung by color plus icon plus text is the accessibility floor applied where a favorable/unfavorable general or lesson is easy to over-read as a verdict, so it always carries an explicit icon and word, and survives grayscale print.</p>\n<h2>§3 - Contract (data / layout / interaction)</h2>\n<h3>Input (reads the TASK-PLAT-002 envelope, he = luc_nham)</h3>\n<pre><code class=\"language-ts\">// the LiuRen ban shape (TASK-LN-006), read-only\ntype LucNhamBan = {\n  thien_dia_ban: {\n    dia_ban: string[];        // [Chi;12] fixed earth ring: 子丑寅卯辰巳午未申酉戌亥\n    thien_ban: string[];      // [Chi;12] heaven ring placed over dia_ban by nguyet tuong\n    nguyet_tuong: string;     // the month-general that sets the rotation\n  };\n  tu_khoa: KhoaItem[];        // [KhoaItem;4] four lessons\n  tam_truyen: TruyenItem[];   // [so, trung, mat] three transmissions, in order\n  thien_tuong: Record&lt;string, string&gt;;  // 12 generals keyed by the branch they sit on\n  khoi_quy_nhan: string;      // the quy nhan (day/night) that seeds the general placement\n};\ntype KhoaItem = { thuong: string; ha: string; khac: \"thuong_khac\" | \"ha_khac\" | null };  // upper/lower + khac direction\ntype TruyenItem = { chi: string; thien_tuong: string; luc_than: string };\nfunction readLucNhamBan(laso: LaSo): { ban: LucNhamBan; cachCuc: CachCuc[] };  // validates he === \"luc_nham\"</code></pre>\n<h3>Layout (LiuRen native notation)</h3>\n<pre><code>thien dia ban:  outer ring = dia ban (fixed 子..亥), inner ring = thien ban (rotated by nguyet tuong)\ntu khoa:        [ khoa 1 ][ khoa 2 ][ khoa 3 ][ khoa 4 ]   each: thuong over ha, khac marked\ntam truyen:     so (初) / trung (中) / mat (末), top to bottom, each: chi + thien tuong + luc than\nthien tuong:    the 12 generals placed on the branches they occupy (thuan/nghich from khoi quy nhan)</code></pre>\n<h3>The twelve thien tuong (十二天將)</h3>\n<p>Quy Nhan 貴人, Dang Xa 螣蛇, Chu Tuoc 朱雀, Luc Hop 六合, Cau Tran 勾陳, Thanh Long 青龍, Thien Khong 天空, Bach Ho 白虎, Thai Thuong 太常, Huyen Vu 玄武, Thai Am 太陰, Thien Hau 天后. The view renders each on the branch the engine placed it on (<code>thien_tuong</code> map); it does not compute the placement.</p>\n<h3>Interaction and cat/hung marking</h3>\n<p>Hover/click a board cell, a lesson, a transmission, or a general -&gt; a detail showing its full content and role. An element referenced by a <code>cach_cuc</code> is marked cat / hung / trung as color + icon + text together (semantic tokens from TASK-WEB-001; never color alone). Cells, lessons, transmissions, and generals are keyboard-focusable and the detail is keyboard-reachable.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Given a <code>he = \"luc_nham\"</code> envelope, the component renders the thien dia ban with the fixed dia ban 12-branch ring and the thien ban rotated over it by the nguyet tuong.</li><li>It renders the tu khoa (four lessons) with each lesson's thuong/ha pair and the khac direction (thuong khac / ha khac) marked, and the tam truyen in order (so / trung / mat) each with its chi, thien tuong, and luc than.</li><li>It renders the twelve thien tuong on the branches the engine placed them on; it computes no placement, and empty/degenerate slots (phuc ngam / phan ngam) render blank, never fabricated.</li><li>Hover and click reveal a detail with the full content and role; cells, lessons, transmissions, and generals are keyboard-focusable and the detail is keyboard-reachable.</li><li>Elements carrying a cach cuc show cat/hung/trung with color AND icon AND text (never color alone), using the TASK-WEB-001 semantic tokens.</li><li>The chart is responsive from a narrow (phone) to a wide (desktop) viewport without overlap or clipping, including stacked Vietnamese diacritics (TASK-WEB-001 clip test); it exposes the export seam TASK-CHART-004 completes.</li><li>The component treats the envelope as read-only (no write to <code>ban</code>/<code>cach_cuc</code>/<code>lich_phap</code>/<code>co_truong_phai</code>).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/liuren-chart.test.tsx</code>: renders a <code>LucNhamBan</code> fixture from a real TASK-LN-006 golden envelope; asserts the fixed dia ban ring and the thien-ban rotation, the four lessons with thuong/ha + khac, the tam truyen order and each truyen's chi/thien tuong/luc than, the twelve generals on their branches, empty-slot handling for a phuc/phan ngam fixture, the cat/hung color+icon+text on a <code>cach_cuc</code>-named element, and the read-only invariant (envelope byte-identical after render).</li><li>Interaction: hover/click opens the detail with the right content; keyboard focus traverses cells/lessons/transmissions/generals and opens detail.</li><li>Responsive + a11y: layout holds at phone and desktop widths; <code>jest-axe</code> clean; elements have text alternatives; the diacritics clip test passes over the labels (Han + phien am).</li><li>Contract: <code>LucNhamBan</code> is checked against the TASK-LN-006 shape via a shared golden fixture; a drift fails the test.</li><li>Gates: <code>pnpm --filter web lint</code>, <code>pnpm --filter web test</code>, <code>next build</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>lib/chart/read-luc-nham-ban.ts</code>: read <code>ban</code> (as <code>LucNhamBan</code>) and top-level <code>cach_cuc</code>; validate <code>he === \"luc_nham\"</code>.</li><li><code>thien-dia-ban.tsx</code>: the fixed dia ban outer ring + the thien ban inner ring rotated by nguyet tuong.</li><li><code>tu-khoa.tsx</code> + <code>tam-truyen.tsx</code>: the four lessons (thuong/ha + khac) and the three transmissions in order (chi + thien tuong + luc than); empty slots blank.</li><li><code>thien-tuong-ring.tsx</code>: the twelve generals on the branches the engine placed them on.</li><li><code>liuren-chart.tsx</code>: compose the board, lessons, transmissions, and generals (SVG); responsive sizing; cat/hung marking from <code>cach_cuc</code> with color+icon+text; the TASK-CHART-001 export seam.</li><li><code>tests/liuren-chart.test.tsx</code> with the TASK-LN-006 golden fixture.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-CHART-001 (the \"pure reader of its engine's <code>ban</code>\" pattern, the export seam, and the cat/hung color+icon+text convention) and TASK-LN-006 (the LiuRen <code>ban</code> and the <code>he = \"luc_nham\"</code> envelope it reads). Uses TASK-WEB-001 (tokens, semantic markers, the shell it renders inside). Embedded by TASK-WEB-003 (results) and TASK-STRAT-004 (cross-system validate renders LiuRen alongside QiMen). Completed by TASK-CHART-004 (export/print/accessibility builds on the export seam). Reads the TASK-PLAT-002 envelope read-only (strategy 4.3); the <code>LucNhamBan</code> contract is pinned to TASK-LN-006 via a shared golden fixture.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-ts\">// the component reads charts[i] from the QueryResponse (he = \"luc_nham\")\nconst { ban, cachCuc } = readLucNhamBan(chart);\n// ban.thien_dia_ban.nguyet_tuong = \"登明\"; dia_ban = [\"子\",\"丑\",...,\"亥\"] (fixed); thien_ban rotated over it\n// ban.tu_khoa[0] = { thuong: \"寅\", ha: \"子\", khac: \"ha_khac\" }\n// ban.tam_truyen = [ { chi:\"寅\", thien_tuong:\"青龍\", luc_than:\"...\" }, { ... }, { ... } ]  // so / trung / mat\n// ban.thien_tuong = { \"寅\":\"青龍\", \"子\":\"神后/天后\", ... }   // generals on their branches\n// cachCuc = [ ... ]  // any detected khoa the / cach; polarity shown as color + icon + text</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>How to depict the thien-ban rotation over the fixed dia ban on a small screen. Default: two concentric rings on desktop (dia ban outer fixed, thien ban inner rotated); on a phone keep the rings but let the detail carry the full pairing on tap, so nothing is fabricated or dropped, only relocated (the TASK-CHART-001 progressive-reveal rule).</li><li>How much LiuRen semantics the view encodes vs leaves to the engine. Default: the view renders exactly what <code>LucNhamBan</code> contains (the rings, the four lessons, the three transmissions, the generals, the khac marks) and encodes no rule - the nguyet-tuong rotation, the tu-khoa khac test, the nine-tong-mon transmission derivation, and the general placement are all TASK-LN-006's. The chart is a faithful picture, not a second engine.</li><li>Whether to show luc than and khoa the inline or only in the detail. Default: show the core notation (rings, lessons, transmissions, generals) inline and carry luc than / khoa the into the detail, keeping the board legible while nothing is hidden.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Wrong ring / order</td><td>dia ban not fixed, thien ban mis-rotated, or transmissions out of order</td><td>forbidden; the fixed dia ban and the engine's rotation/order are tested against a golden fixture</td></tr><tr><td>Fabricated slot</td><td>a degenerate phuc/phan ngam slot rendered as filled</td><td>forbidden; an empty slot renders blank; the view never invents a lesson/transmission/general</td></tr><tr><td>View recomputes</td><td>the chart derives a truyen or a general placement</td><td>forbidden; pure reader of <code>ban</code>; read-only envelope; a byte-equality test asserts it</td></tr><tr><td>Color-only cat/hung</td><td>polarity shown by color alone</td><td>forbidden; cat/hung is color + icon + text (TASK-WEB-001)</td></tr><tr><td>Clipped on mobile</td><td>responsive layout overlaps/clips the rings or diacritics</td><td>the responsive + diacritics-clip tests fail; fix before ship</td></tr><tr><td>Envelope-shape drift</td><td><code>LucNhamBan</code> differs from TASK-LN-006</td><td>the shared golden-fixture contract test fails; treat any <code>ban</code> change as a versioned TASK-PLAT-002 change</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>The LiuRen view is the second chart type and it inherits the TASK-CHART-001 discipline exactly: faithful notation (the fixed dia ban ring, the thien-ban rotation, the four lessons with khac, the three transmissions in order, the twelve generals on their branches, empty where empty) and no computation (it reads <code>ban</code> and <code>cach_cuc</code> and writes nothing - strategy 4.3, asserted by a read-only test). cat/hung is color + icon + text, never color alone. The export seam is exposed here and completed by TASK-CHART-004, and the <code>LucNhamBan</code> contract stays pinned to TASK-LN-006 via a shared golden fixture, so a chart change is a reviewed, versioned envelope change, not a silent view edit.</p>";
