window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-005"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task builds the report view screen - the read surface for the durable, persisted report the REPORT module assembled (TASK-REPORT-001). Where the results screen (TASK-WEB-003) is the live outcome of a cast, the report view renders the saved <code>StructuredReport</code> artifact: the chart summary and detected patterns (copied from the engine), the beginner and expert interpretation, the recommendations, the citations, the confidence, and the AIDisclosure. It owns the report presentation and the PDF download trigger; it does NOT assemble the report (TASK-REPORT-001) or render it to PDF (TASK-REPORT-002) - it renders the object and asks the backend for the file.</p>\n<p>The screen SHALL render, from the TASK-REPORT-001 <code>StructuredReport</code> fetched by <code>report_id</code>: (a) a deterministic region - the <code>chart_summary</code> (<code>he</code>, echoed <code>dau_vao</code>, the human-readable <code>lich_phap_summary</code>, the <code>key_positions</code>) and the <code>detected_patterns</code> list, each pattern's <code>polarity</code> (cat/hung/trung) shown with color AND icon AND text, never color alone (TASK-WEB-001); and (b) an interpreted region - the <code>interpretation</code> (beginner and expert, toggleable), the <code>recommendations</code>, and the <code>citations</code>. The two regions SHALL be visually separated so a reader can always tell the engine's computed summary from the AI's interpretation (strategy 4.4). It MAY embed the full interactive chart (TASK-CHART-001/002/003) by the report's <code>query_id</code>; when it does, the chart is read-only.</p>\n<p>The screen SHALL display an <code>AIDisclosureBadge</code> (TASK-WEB-001) on the interpretation, always, fed by <code>ai_disclosure</code> - it is mandatory on any AI output. Where <code>ai_disclosure.review_status</code> is <code>pending</code>, the screen SHALL mark the report not-yet-approved (and MAY show a <code>HumanReviewGate</code>, TASK-RAG-004), distinct from an approved report. The screen SHALL offer a PDF download that calls the TASK-REPORT-002 export for this <code>report_id</code> and SHALL show <code>confidence</code> as supporting context, not as a headline verdict. It SHALL treat the report object and any embedded envelope as read-only.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The report is the artifact a user keeps, shares, and prints, so it is exactly where the deterministic-and-AI boundary must be most legible (strategy 4.4). REPORT-001 already assembled the two halves under a hard read-only rule; the report view is the last visible mile of that same rule, so it lays the engine's chart summary and detected patterns in one region and the AI's cited interpretation in another, physically separated. Collapsing them into one block would let a saved, exported document read an AI sentence as if it carried the engine's determinism - the one thing the whole architecture exists to prevent, and worse in a durable PDF than in a transient screen.</p>\n<p>The AIDisclosureBadge is mandatory and the citations are first-class here for the same reason REPORT enforces them at assembly: a report is durable and shareable, so an uncited claim or a missing disclosure that reaches it is worse than one caught upstream (TASK-REPORT-001 s2). Rendering confidence as supporting context rather than a headline number avoids implying a false precision the tool does not claim - the badge carries the limits copy (TASK-LEGAL-001), and the framing stays decision support, not prophecy (strategy 7). Marking a pending report as not-yet-approved keeps the review state honest into the saved artifact, so a reader never mistakes an unvetted reading for a vetted one.</p>\n<h2>§3 - Contract (screen / regions / data)</h2>\n<h3>Layout (Claude-07 s6, on the TASK-WEB-001 shell)</h3>\n<pre><code>+------------------------- report view -------------------------+\n| [ deterministic region - from the engine ]                   |\n|   chart summary: he / dau_vao / lich_phap_summary / positions|\n|   detected patterns: name / cung / cat|hung (icon + text)    |\n|   (optional) embedded chart  (TASK-CHART-001/002/003, read-only)|\n| ------------- boundary (visually separated) ---------------- |\n| [ interpreted region - AI, labeled ]                         |\n|   AIDisclosureBadge  +  persona toggle (beginner | expert)   |\n|   interpretation text ... [citation refs]                    |\n|   recommendations ...                                        |\n|   citations (source / locator / Han / bach thoai / dich)     |\n|   confidence (supporting context)                            |\n| [ PDF download ]  (TASK-REPORT-002)                            |\n+--------------------------------------------------------------+</code></pre>\n<h3>Data (<code>lib/api/report.ts</code>, reads TASK-REPORT-001)</h3>\n<pre><code class=\"language-ts\">// mirrors the TASK-REPORT-001 StructuredReport\ntype StructuredReport = {\n  report_id: string; query_id: string;\n  chart_summary: { he: string; dau_vao: Record&lt;string, unknown&gt;; lich_phap_summary: string; key_positions: string[] };\n  detected_patterns: { id: string; name: string; polarity: \"cat\" | \"hung\" | \"trung\"; cung: number | null; score: number | null; citations: Citation[] }[];\n  interpretation: { beginner: string; expert: string; recommendations: string[] };\n  citations: Citation[];\n  confidence: number;\n  ai_disclosure: { model: string; limits: string; review_status: \"pending\" | \"approved\" | \"not_required\" };\n  created_at: string;\n};\ntype Citation = { source: string; locator: string; han?: string; bach_thoai?: string; dich?: string };\n\nasync function getReport(reportId: string): Promise&lt;StructuredReport&gt;;   // read-only\nasync function downloadReportPdf(reportId: string): Promise&lt;Blob&gt;;        // TASK-REPORT-002</code></pre>\n<h3>Regions and controls</h3>\n<ul><li>Deterministic region: <code>chart-summary-section</code> renders <code>chart_summary</code> and the <code>detected_patterns</code> with the cat/hung icon+text badge (semantic tokens, never color alone). Optionally embeds the interactive chart by <code>query_id</code>.</li><li>Interpreted region: <code>interpretation-section</code> renders the persona-selected reading (beginner/expert toggle - both present in the object, no re-fetch), the <code>recommendations-list</code>, and the <code>citation-list</code> (source / locator / Han / bach thoai / dich). The <code>AIDisclosureBadge</code> is always present and reflects <code>ai_disclosure.review_status</code>.</li><li><code>pdf-download-button</code> calls <code>downloadReportPdf(report_id)</code> (TASK-REPORT-002); the export is the backend's, not a client re-render.</li></ul>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Given a <code>StructuredReport</code> fetched by <code>report_id</code>, the screen renders the deterministic region (chart summary + detected patterns) and the interpreted region (beginner/expert + recommendations + citations), visually separated.</li><li><code>AIDisclosureBadge</code> is present on the interpretation on every report; a report rendered without it fails a test (mandatory, strategy 4.4).</li><li>Each pattern's cat/hung/trung polarity is shown with color AND icon AND text, never color alone (TASK-WEB-001).</li><li>The persona toggle switches beginner/expert without a re-fetch (both are in the object); citations render source / locator and the three text layers (Han / bach thoai / dich) where present; in-text refs resolve to their citation.</li><li>When <code>ai_disclosure.review_status</code> is <code>pending</code>, the report is marked not-yet-approved, distinct from an approved report.</li><li>The PDF download calls the TASK-REPORT-002 export for the <code>report_id</code> and returns the file; the client does not re-render the PDF itself.</li><li><code>confidence</code> is shown as supporting context, not a headline verdict; the report object and any embedded envelope are unchanged by the screen (read-only).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/report-view.test.tsx</code>: renders a <code>StructuredReport</code> fixture (with <code>review_status</code> <code>not_required</code> and <code>pending</code>); asserts the two regions and their visual separation, the always-present <code>AIDisclosureBadge</code>, the cat/hung icon+text badges, the persona toggle without re-fetch, the citation three layers + locator, the pending not-yet-approved marking, the PDF-download call to TASK-REPORT-002, and the read-only invariant (object byte-identical after render).</li><li>Accessibility: <code>jest-axe</code> clean; the stacked-diacritics clip test (TASK-WEB-001) over the interpretation text, pattern names, and <code>lich_phap_summary</code> on light+dark; keyboard-operable persona toggle and download.</li><li>Contract: the <code>StructuredReport</code> / <code>Citation</code> types are checked against the TASK-REPORT-001 shapes (shared fixture); a drift fails the test.</li><li>Gates: <code>pnpm --filter web lint</code>, <code>pnpm --filter web test</code>, <code>next build</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>lib/api/report.ts</code>: <code>getReport(reportId)</code> and <code>downloadReportPdf(reportId)</code> (TASK-REPORT-002) with the auth token; read-only.</li><li><code>chart-summary-section.tsx</code>: the deterministic region - <code>chart_summary</code> + <code>detected_patterns</code> with cat/hung icon+text badges; optional embedded chart by <code>query_id</code>.</li><li><code>interpretation-section.tsx</code> (+ persona toggle) + <code>recommendations-list.tsx</code> + <code>citation-list.tsx</code>: the interpreted region with the mandatory <code>AIDisclosureBadge</code> and the pending/approved marking.</li><li><code>pdf-download-button.tsx</code>: trigger the TASK-REPORT-002 export by <code>report_id</code>.</li><li><code>report-view.tsx</code> + <code>app/report/[reportId]/page.tsx</code>: compose the regions and the visual boundary in the TASK-WEB-001 shell; wire the disclaimer slot (TASK-LEGAL-001).</li><li><code>tests/report-view.test.tsx</code> + the shared TASK-REPORT-001 contract fixture.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-WEB-003 (it builds on the results-screen presentation - the deterministic/AI separation, the mandatory badge, the cat/hung convention, the citation cards) and TASK-REPORT-001 (the <code>StructuredReport</code> object it renders). Uses TASK-REPORT-002 for the PDF download, TASK-WEB-001 (<code>AIDisclosureBadge</code>, tokens, the icon+text convention), TASK-RAG-003 (the <code>Citation</code> / <code>AIDisclosure</code> shapes carried into the report), TASK-RAG-004 behind the review marking, TASK-CHART-001/002/003 when embedding the interactive chart, and TASK-LEGAL-001 for the limits copy and disclaimer. It renders the report and any embedded envelope read-only (strategy 4.3).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// StructuredReport (abridged) this screen renders (TASK-REPORT-001)\n{ \"report_id\": \"b1c2\", \"query_id\": \"a0f1\",\n  \"chart_summary\": { \"he\": \"ky_mon\",\n    \"dau_vao\": { \"datetime\": \"2004-01-01T10:30:00\", \"tz\": \"+07:00\", \"kinh_do\": 106.7, \"loai_cau_hoi\": \"trach_thoi\" },\n    \"lich_phap_summary\": \"Tu tru 癸未 甲子 戊午 丁巳 - tiet khi 冬至 (tam nguyen thuong)\",\n    \"key_positions\": [\"truc phu cung 1\", \"truc su 休門 cung 6\"] },\n  \"detected_patterns\": [\n    { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"青龍返首\", \"polarity\": \"cat\", \"cung\": 1, \"score\": 0.9,\n      \"citations\": [ { \"source\": \"Yen Ba Dieu Tau Ca\", \"locator\": \"cach cat\" } ] } ],\n  \"interpretation\": { \"beginner\": \"... (cited)\", \"expert\": \"... (cited)\", \"recommendations\": [\"...\"] },\n  \"citations\": [ { \"source\": \"Yen Ba Dieu Tau Ca\", \"locator\": \"cach cat\", \"han\": \"青龍返首\" } ],\n  \"confidence\": 0.72,\n  \"ai_disclosure\": { \"model\": \"gpt-4o-mini\", \"limits\": \"decision support, not a verdict; no medical/legal/financial advice\", \"review_status\": \"pending\" },\n  \"created_at\": \"2026-07-08T12:00:05Z\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether to embed the full interactive chart or only the report's text chart summary. Default: render the <code>chart_summary</code> text region at P1 (it is copied into the report and always present); embed the interactive chart (TASK-CHART-001/002/003) by <code>query_id</code> as an additive enhancement, always read-only.</li><li>How confidence is displayed. Default: a supporting band or short label next to the AIDisclosureBadge, never a headline percentage, so it informs without implying false precision; REPORT carries the number, the UI chooses the presentation (TASK-REPORT-001 s9).</li><li>Where a pending report shows the HumanReviewGate: inline vs a banner. Default: mark the report not-yet-approved prominently and show the gate (TASK-RAG-004) where the reviewer acts; a plain reader sees the not-yet-approved state without the approve/reject controls.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Boundary erased</td><td>chart summary and interpretation in one undifferentiated block</td><td>forbidden; deterministic and interpreted regions are visually separated (strategy 4.4)</td></tr><tr><td>Missing disclosure</td><td>interpretation shown without the badge</td><td>forbidden; <code>AIDisclosureBadge</code> is mandatory; a test rejects its absence</td></tr><tr><td>Color-only polarity</td><td>cat/hung shown by color alone</td><td>forbidden; polarity is color + icon + text (TASK-WEB-001)</td></tr><tr><td>Pending looks approved</td><td>a <code>pending</code> report shown as final</td><td>mark not-yet-approved, distinct from approved</td></tr><tr><td>Client re-renders the PDF</td><td>the screen builds the file locally</td><td>forbidden; the PDF is the TASK-REPORT-002 export by <code>report_id</code></td></tr><tr><td>Report mutated</td><td>the screen writes a report/chart field</td><td>forbidden; read-only; a byte-equality test asserts it</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>The report view is the last visible mile of the deterministic-and-AI boundary in a durable artifact (strategy 4.4): the engine's chart summary and detected patterns in one region, the AI's cited interpretation in another, physically separated, with the AIDisclosureBadge mandatory, the citations first-class, and a pending report marked as such. It renders the TASK-REPORT-001 object and asks TASK-REPORT-002 for the PDF - it neither assembles nor re-renders the file - and it keeps the report and any embedded envelope read-only. It is a SHOULD at P1 that lifts a single result into a saved, shareable, printable report without loosening one guardrail.</p>";
