window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-008"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task extends the i18n foundation (TASK-WEB-006) with a third locale, Chinese (<code>zh</code>), and makes the layout direction-agnostic so a future right-to-left (RTL) locale can be added without re-laying-out any screen. It does two things and no more: it adds the <code>zh</code> UI-chrome catalog and registers <code>zh</code> in the locale set, and it converts the layout from physical (left/right) to logical (inline-start/inline-end) so direction becomes a single attribute, not a per-component rewrite. It reuses TASK-WEB-006's two-plane split unchanged - it adds a locale and a direction axis, it does not add a second i18n mechanism.</p>\n<p>The frontend SHALL add <code>zh</code> to the next-intl locale set (<code>[\"vi\", \"en\", \"zh\"]</code>, <code>vi</code> still default) with a <code>messages/zh.json</code> UI-chrome catalog. Domain content in <code>zh</code> SHALL be served pre-translated from the backend exactly as in TASK-WEB-006 - the UI SHALL NOT machine-translate a classical term - and because the classical source script IS Han, for <code>zh</code> the KB three-layer store's Han layer (TASK-KB-003) SHALL be surfaced as the primary domain text rather than a transliteration. The layout SHALL be made RTL-ready: components SHALL use CSS logical properties (<code>margin-inline</code>, <code>padding-inline</code>, <code>inset-inline</code>, <code>text-align: start/end</code>, <code>border-inline</code>) instead of physical <code>left</code>/<code>right</code>, and the document direction SHALL be driven by a single <code>dir</code> attribute derived from the active locale (<code>dir(\"zh\") = ltr</code>; the scaffolding admits an RTL locale returning <code>rtl</code> with no further layout change). RTL-readiness is scaffolding and a test gate, not a shipped RTL locale; the acceptance is that adding one would need no relayout, proven by a mirrored-direction snapshot.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Chinese is the special case that proves the TASK-WEB-006 split was right: the tradition's source text is Han, so for a Chinese reader the classical passage is not something to translate into - it is the primary text (Claude-07 s4.3, strategy 7). Surfacing the Han layer directly for <code>zh</code>, rather than a romanized or re-translated form, is the most faithful rendering the product can offer, and it falls straight out of the two-plane design - chrome from the catalog, domain content (here, the Han itself) from the knowledge base. Adding <code>zh</code> is therefore mostly a catalog plus a rule about which layer is primary, not a new system, which is exactly why TASK-WEB-006 is its dependency and its foundation.</p>\n<p>RTL-readiness is done now, cheaply, as scaffolding, because retrofitting direction after a dozen screens have hardcoded <code>left</code>/<code>right</code> is expensive and error-prone, while writing logical properties from the start costs almost nothing. The product has no RTL locale today, so this task does not ship one; it removes the structural blocker so that if an Arabic or Hebrew locale is ever wanted, it is a catalog plus a <code>dir</code> value, not a layout project. Keeping this a COULD at P3 reflects that: it is the hardening-phase polish that makes the internationalization foundation complete and future-proof, not an MVP need.</p>\n<h2>§3 - Contract (locale / direction / logical layout)</h2>\n<h3>Locale set (extends TASK-WEB-006 <code>routing.ts</code>)</h3>\n<pre><code class=\"language-ts\">export const routing = defineRouting({\n  locales: [\"vi\", \"en\", \"zh\"],   // zh added here; vi still default\n  defaultLocale: \"vi\",\n  localePrefix: \"as-needed\"\n});</code></pre>\n<p><code>messages/zh.json</code> is the UI-chrome catalog (same shape as <code>vi.json</code>/<code>en.json</code>). Domain content in <code>zh</code> is fetched from the backend as in TASK-WEB-006; the resolver is unchanged.</p>\n<h3>Domain content for zh (Han is primary)</h3>\n<table><thead><tr><th>Locale</th><th>Primary domain text</th><th>Layers shown</th></tr></thead><tbody><tr><td>vi</td><td>dich (Vietnamese rendering)</td><td>Han + bach thoai + dich</td></tr><tr><td>en</td><td>expert English rendering</td><td>Han + english</td></tr><tr><td>zh</td><td>the Han source (chu Han)</td><td>Han (primary) + bach thoai</td></tr></tbody></table>\n<p>For <code>zh</code>, <code>domain-content.ts</code> (TASK-WEB-006) returns the Han layer as <code>text</code>; it still never machine-translates.</p>\n<h3>Direction (<code>lib/i18n/direction.ts</code>, <code>styles/logical.css</code>)</h3>\n<pre><code class=\"language-ts\">function dir(locale: string): \"ltr\" | \"rtl\" {\n  // vi | en | zh -&gt; ltr; the scaffolding admits a future rtl locale -&gt; \"rtl\"\n}</code></pre>\n<p><code>logical.css</code> and the components use logical properties only: <code>margin-inline</code>, <code>padding-inline</code>, <code>inset-inline-start/end</code>, <code>border-inline</code>, <code>text-align: start/end</code>. The <code>&lt;html dir&gt;</code> is set from <code>dir(locale)</code>. No component uses physical <code>left</code>/<code>right</code> for layout flow.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>zh</code> is in the locale set (<code>[\"vi\",\"en\",\"zh\"]</code>, <code>vi</code> default) with a <code>messages/zh.json</code> chrome catalog; switching to <code>zh</code> renders the chrome from that catalog.</li><li>Domain content in <code>zh</code> is served pre-translated from the backend (TASK-KB-003 / TASK-RAG-003) and never machine-translated in the UI; for <code>zh</code> the Han layer is surfaced as the primary domain text.</li><li>The layout uses CSS logical properties (no physical <code>left</code>/<code>right</code> for layout flow), and <code>&lt;html dir&gt;</code> is driven by <code>dir(locale)</code>.</li><li>RTL-readiness is proven by a mirrored-direction (forced <code>rtl</code>) snapshot/test that shows the layout mirrors correctly with no per-component change - even though no RTL locale ships.</li><li>The TASK-WEB-006 two-plane split is unchanged: chrome from catalogs, domain content from the backend resolver; the Han is preserved in every locale.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/zh-i18n.test.tsx</code>: asserts <code>zh</code> in the locale set; renders the shell in <code>zh</code> and asserts chrome from <code>messages/zh.json</code>; asserts a <code>zh</code> domain term surfaces the Han layer as primary and is not machine-translated (spy asserts no translate call); asserts the Han is present.</li><li><code>tests/rtl-ready.test.tsx</code>: asserts no component uses physical <code>left</code>/<code>right</code> for layout flow (a style audit over the layout properties); forces <code>dir=\"rtl\"</code> and asserts the layout mirrors via logical properties (a mirrored snapshot) with no component edit.</li><li>Accessibility: <code>lang=\"zh\"</code> and the correct <code>dir</code>; the stacked-diacritics clip test (TASK-WEB-001) still passes for <code>vi</code>; Han renders at the chart/label sizes without clipping.</li><li>Gates: <code>pnpm --filter web lint</code>, <code>pnpm --filter web test</code>, <code>next build</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li>Extend <code>i18n/routing.ts</code> with <code>zh</code>; add <code>messages/zh.json</code> (chrome).</li><li><code>lib/i18n/direction.ts</code>: <code>dir(locale)</code> returning <code>ltr</code> for vi/en/zh and admitting a future <code>rtl</code>; set <code>&lt;html dir&gt;</code> from it.</li><li>Rule in <code>domain-content.ts</code> (TASK-WEB-006): for <code>zh</code>, return the Han layer as the primary <code>text</code>; still no translate path.</li><li><code>styles/logical.css</code> + a sweep of the TASK-WEB-001/002/003 layout to logical properties (<code>margin-inline</code>, <code>padding-inline</code>, <code>inset-inline</code>, <code>text-align: start/end</code>).</li><li><code>tests/zh-i18n.test.tsx</code> + <code>tests/rtl-ready.test.tsx</code> (including the forced-<code>rtl</code> mirrored snapshot).</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-WEB-006 (the next-intl foundation, the two-plane split, and the domain-content resolver it extends - <code>zh</code> is a locale plus a primary-layer rule, not a new mechanism). Reads <code>zh</code> domain content from TASK-KB-003 (the Han/bach thoai layers) and TASK-RAG-003 (any <code>zh</code> interpretation), served pre-translated. Uses TASK-WEB-001's shell and clip test. It ships no RTL locale; it removes the structural blocker so one could be added as a catalog plus a <code>dir</code> value.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-ts\">// zh surfaces the Han layer as primary; still never machine-translated\nconst p = await getDomainContent(\"pattern\", \"qimen_thanh_long_hoi_dau\", \"zh\");\n// -&gt; { han: \"青龍返首\", text: \"青龍返首\", locale: \"zh\", source: \"Yen Ba Dieu Tau Ca\" }  // Han is the primary text\n\ndir(\"zh\");  // \"ltr\"    dir(\"vi\"); // \"ltr\"    // scaffolding admits a future rtl locale -&gt; \"rtl\"</code></pre>\n<pre><code class=\"language-css\">/* logical, not physical - so a future rtl locale mirrors with no relayout */\n.panel { padding-inline: var(--space-4); margin-inline-start: var(--space-2); text-align: start; }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether <code>zh</code> distinguishes Traditional vs Simplified. Default: follow the KB source - the classical text is Traditional Han; <code>zh</code> surfaces it as stored, and a Simplified variant is a later KB concern, not a UI transform (the UI never converts the script).</li><li>Whether to ship an actual RTL locale now. Default: no - this task proves RTL-readiness with a forced-<code>rtl</code> test only; shipping an RTL locale waits for real demand and its own expert-translated content.</li><li>Bidi handling where Han/Latin and (future) RTL text mix. Default: rely on the browser's Unicode bidi with <code>dir</code> set correctly and logical properties; revisit isolation (<code>bdi</code>) only if a real RTL locale lands.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Machine-translated zh term</td><td>UI translates a classical term into/within zh</td><td>forbidden; domain content is pre-translated; for zh the Han layer is primary</td></tr><tr><td>Physical left/right</td><td>a component hardcodes <code>left</code>/<code>right</code> for flow</td><td>forbidden; logical properties only; the rtl-ready audit fails</td></tr><tr><td>Direction not mirrored</td><td>forced <code>rtl</code> breaks the layout</td><td>the mirrored-direction test fails; fix with logical properties, not per-component hacks</td></tr><tr><td>Second i18n mechanism</td><td>zh added outside next-intl / the TASK-WEB-006 split</td><td>forbidden; zh is a locale + primary-layer rule on the existing foundation</td></tr><tr><td>Han converted</td><td>UI transforms Traditional to Simplified (or romanizes)</td><td>forbidden; the Han is surfaced as stored; the UI never converts the script</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>TASK-WEB-008 completes the internationalization story: Chinese as a third locale where the Han source text is itself the primary domain content (the case that vindicates the TASK-WEB-006 two-plane split), plus RTL-ready scaffolding that makes direction a single attribute rather than a future relayout. It adds a locale and a direction axis on top of TASK-WEB-006 - not a second i18n system - keeps the domain-content resolver translate-free, and preserves the Han in every locale. It is a COULD at P3: hardening-phase polish that future-proofs the foundation without shipping an RTL locale before there is content for one.</p>";
