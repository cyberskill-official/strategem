window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-RULE-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task extends the TASK-RULE-002 condition DSL and evaluator to cross-system patterns: patterns whose condition references more than one chart in a set cast for the same question (for example a QiMen chart together with a LiuRen chart), plus deeper nested multi-condition trees carrying per-node weights. It is the rule-engine support the cross-system validate feature (TASK-STRAT-004) runs on (Grok-31).</p>\n<p>The module SHALL introduce a <code>ChartSet</code> - a map from <code>He</code> to the <code>LaSo</code> (TASK-PLAT-002) cast for one question - and SHALL extend the path grammar with an optional system qualifier: a <code>field</code> MAY be prefixed <code>&lt;he&gt;:</code> (one of <code>luc_nham</code>, <code>ky_mon</code>, <code>thai_at</code>) to select which chart in the set the rest of the path resolves against, after which the TASK-RULE-002 reserved-root rule applies within that chart. A bare (unqualified) path SHALL resolve against a designated primary chart, so a single-chart evaluation is exactly the existing TASK-RULE-002 behavior and nothing regresses. The module SHALL evaluate a (possibly cross-system) condition tree over a <code>ChartSet</code> (<code>evaluate_set</code>) and SHALL make the reserved per-node <code>weight</code> (TASK-RULE-002) meaningful for cross-system scoring (<code>score_set</code>).</p>\n<p>The evaluator SHALL stay total and pure exactly as in TASK-RULE-002: a qualified path whose chart is absent from the set, or whose field is absent from that chart, SHALL resolve to a defined result (<code>false</code> for <code>eq</code> / <code>in</code> / <code>gte</code> / <code>lte</code> / <code>contains</code>, the presence test for <code>exists</code>) and SHALL NOT panic. A cross-system pattern SHALL be a <code>system: all</code> row (TASK-RULE-001) whose <code>conditions</code> reference more than one chart; it SHALL emit a <code>CachCuc</code> (TASK-PLAT-002) like any pattern, with <code>cung</code> optional (a whole-set agreement pattern has no single palace). This task extends the grammar, the resolver, the evaluator, and the scorer; it SHALL NOT add operators, and it SHALL remain deterministic and offline-testable over the seed repository (TASK-RULE-003).</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The integration level of the product is cross-checking the systems against each other: cast a question in QiMen and in LiuRen and see whether they agree (strategy 4.2 step 4 for a chart set; Claude-06 s2.3 pattern detection; the L4 curriculum skill of doi chieu ket qua nhieu he). TASK-STRAT-004 is that feature. It needs a way to say, as data, \"when the QiMen chart shows this cat cach and the LiuRen tam truyen shows that, the two systems concur\" - a pattern that spans two charts. TASK-RULE-002 evaluates one chart; this task generalizes it to a set without changing the operator surface or the totality guarantee, so cross-system agreement is expressed as reviewable, cited pattern rows (Grok-31), not as bespoke comparison code.</p>\n<p>The system-qualified path is the whole trick, and it is deliberately minimal. TASK-RULE-002 already resolves a path against one envelope with a reserved-root rule; prefixing a path with <code>ky_mon:</code> or <code>luc_nham:</code> just picks which envelope in the set to apply that same rule to. That means a cross-system condition reuses every existing operator and the entire single-chart resolver unchanged - the only new thing is choosing the chart first. Keeping the bare path resolving against a primary chart means every existing TASK-RULE-002 pattern and test is a cross-system evaluation with a one-chart set, so there is no fork and no regression.</p>\n<p>The totality guarantee generalizing is what keeps this safe. TASK-RULE-002's rule that a missing path is cleanly <code>false</code> already handles a QiMen-only field on a non-QiMen chart; here the same rule handles a qualified path whose chart is not in the set - a <code>ky_mon:</code> path when only a LiuRen chart was cast simply does not fire. So a cross-system pattern degrades gracefully to whatever charts are present, which is the correct behavior for a validate feature the user may run on one or several systems. The <code>weight</code> node, reserved but ignored at MVP, becomes the way a scorer expresses \"this pattern is a stronger agreement signal when both arms match,\" feeding the TASK-STRAT-004 agreement view without a new scoring language.</p>\n<h2>§3 - Contract (chart set + qualified paths + weighted scoring)</h2>\n<h3>Chart set (<code>crates/cyberos-rule/src/cross.rs</code>)</h3>\n<pre><code class=\"language-rust\">use laso_envelope::{LaSo, He, CachCuc};   // TASK-PLAT-002\n\n/// The charts cast for ONE question, keyed by system. One or several systems.\n#[derive(Debug, Clone)]\npub struct ChartSet {\n    pub charts: BTreeMap&lt;He, LaSo&gt;,   // e.g. { KyMon: .., LucNham: .. }\n    pub primary: He,                  // the chart a bare (unqualified) path resolves against\n}\n\nimpl ChartSet {\n    pub fn single(la_so: LaSo) -&gt; Self;   // one chart; primary = its he (TASK-RULE-002 parity)\n    pub fn get(&amp;self, he: He) -&gt; Option&lt;&amp;LaSo&gt;;\n}</code></pre>\n<h3>Qualified path resolution</h3>\n<p>A path MAY carry an optional leading <code>&lt;he&gt;:</code> qualifier; the rest resolves by the TASK-RULE-002 rule within the selected chart.</p>\n<ul><li><code>ky_mon:cung.1.mon</code> -&gt; the QiMen chart's <code>ban.cung[1].mon</code></li><li><code>luc_nham:tam_truyen.0.than</code> -&gt; the LiuRen chart's <code>ban.tam_truyen[0].than</code></li><li><code>ky_mon:cach_cuc</code> -&gt; the QiMen chart's envelope-root <code>cach_cuc</code> (reserved-root rule inside the chosen chart)</li><li><code>door</code> (bare) -&gt; the primary chart's <code>ban.door</code> (unchanged TASK-RULE-002 behavior)</li></ul>\n<pre><code class=\"language-rust\">/// Resolve a (possibly `&lt;he&gt;:`-qualified) path against a chart set.\n/// Absent chart or absent field -&gt; None (leaf then evaluates to false / the exists test).\npub fn resolve_in_set&lt;'a&gt;(path: &amp;str, set: &amp;'a ChartSet) -&gt; Option&lt;&amp;'a serde_json::Value&gt;;</code></pre>\n<h3>Cross-system evaluator</h3>\n<pre><code class=\"language-rust\">use cyberos_rule::eval::Cond;   // TASK-RULE-002 Cond / Op unchanged\n\n/// Evaluate a condition tree over a chart set. Total and pure, exactly as TASK-RULE-002:\n/// identical (cond, set) -&gt; identical bool; no panics; missing path -&gt; defined result.\npub fn evaluate_set(cond: &amp;Cond, set: &amp;ChartSet) -&gt; bool;</code></pre>\n<p><code>evaluate_set</code> is <code>evaluate</code> (TASK-RULE-002) with <code>resolve_in_set</code> swapped for <code>resolve</code> at the leaf; the connective logic (<code>and</code> / <code>or</code> / <code>not</code>, empty-connective rejection) is unchanged. <code>evaluate(cond, laso)</code> equals <code>evaluate_set(cond, ChartSet::single(laso.clone()))</code>, so the single-chart path is a special case.</p>\n<h3>Grammar extension (leaf qualifier + node weight)</h3>\n<p>The TASK-RULE-002 JSON grammar gains two optional, additive fields; <code>docs/contracts/condition-dsl.schema.json</code> is amended (not replaced) and its schema note is versioned:</p>\n<pre><code class=\"language-json\">{ \"type\": \"and\", \"weight\": 1.0, \"rules\": [\n  { \"field\": \"ky_mon:cach_cuc\", \"operator\": \"contains\", \"value\": \"青龍返首\" },\n  { \"field\": \"luc_nham:tam_truyen.0.than\", \"operator\": \"in\", \"value\": [\"六合\", \"青龍\"] }\n] }</code></pre>\n<p><code>weight</code> (default <code>1.0</code>) is a per-node hint for scoring; <code>field</code> MAY begin <code>&lt;he&gt;:</code>. A qualifier that is not a valid <code>He</code> is a <code>DslError</code> (as with an unknown operator in TASK-RULE-002).</p>\n<h3>Weighted scoring (<code>crates/cyberos-rule/src/score_set.rs</code>)</h3>\n<pre><code class=\"language-rust\">/// Evaluate a cross-system pattern over a chart set; emit a CachCuc if it fires.\n/// score = pattern.confidence, optionally shaped by matched-node weights for the\n/// TASK-STRAT-004 agreement view. cung is None for whole-set patterns.\npub fn score_match_set(p: &amp;Pattern, set: &amp;ChartSet) -&gt; Option&lt;CachCuc&gt;;</code></pre>\n<p><code>score_match_set</code> mirrors TASK-RULE-002 <code>score_match</code> but runs <code>evaluate_set</code>; the emitted <code>CachCuc</code> is the same envelope type, so the loader / matcher (TASK-RULE-003) and the interpretation branch consume cross-system hits with no adapter.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>resolve_in_set</code> resolves a <code>&lt;he&gt;:</code>-qualified path to the right chart's field and a bare path to the primary chart; an absent chart or absent field yields <code>None</code> (never a panic).</li><li><code>evaluate_set</code> matches an TASK-RULE-002 single-chart result exactly when the set is <code>ChartSet::single(laso)</code> (back-compat: the existing TASK-RULE-002 truth-table suite passes through <code>evaluate_set</code> unchanged).</li><li>A cross-system <code>system: all</code> pattern with one <code>ky_mon:</code> arm and one <code>luc_nham:</code> arm fires only when both charts are present and both arms hold; with only one chart in the set the qualified path to the missing chart is <code>false</code>, so the pattern does not fire (totality generalizes).</li><li>Arbitrarily nested <code>and</code> / <code>or</code> / <code>not</code> trees mixing qualified and bare leaves evaluate correctly; an empty connective and an invalid <code>&lt;he&gt;:</code> qualifier are <code>DslError</code>s.</li><li><code>score_match_set</code> emits a <code>CachCuc</code> (TASK-PLAT-002 shape) with <code>cung: None</code> for a whole-set agreement pattern and applies node <code>weight</code> to the score deterministically; the emitted type deserializes as a <code>laso_envelope::CachCuc</code>.</li><li>The evaluator is total and deterministic: a property test over random qualified / bare paths and random chart sets never panics; 1,000 repeat evaluations of one <code>(pattern, set)</code> are identical.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/cross_system.rs</code>: <code>resolve_in_set</code> cases (qualified hit, bare-to-primary, absent chart, absent field); the back-compat suite (run TASK-RULE-002 truth tables through <code>evaluate_set</code> on single-chart sets); the two-arm cross-system pattern over <code>fixtures/chart_set_qimen_liuren.json</code> (both present -&gt; fires; QiMen-only -&gt; does not); nested mixed trees; the <code>DslError</code> cases (empty connective, bad qualifier).</li><li>Property test: 10,000 random <code>(path, operator, value, chart-set)</code> tuples - <code>evaluate_set</code> never panics and is total; a qualified path to a missing chart agrees with <code>false</code> (except <code>exists</code>).</li><li>Schema: the amended <code>condition-dsl.schema.json</code> accepts the <code>weight</code> and <code>&lt;he&gt;:</code>-qualified forms and still rejects malformed trees; a test asserts <code>Cond::from_json</code> accepts exactly what the schema accepts.</li><li>Cross-type: <code>score_match_set</code> output deserializes as a <code>laso_envelope::CachCuc</code> (TASK-PLAT-002 contract); the seed repository (TASK-RULE-003) loads a cross-system <code>system: all</code> pattern.</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-rule -- -D warnings</code>, <code>cargo test -p cyberos-rule</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>cross.rs</code>: <code>ChartSet</code> (with <code>single</code> and <code>primary</code>); <code>resolve_in_set</code> (split an optional <code>&lt;he&gt;:</code> qualifier, then delegate to the TASK-RULE-002 <code>path::resolve</code> within the chosen chart); <code>evaluate_set</code> (TASK-RULE-002 <code>evaluate</code> with the set-aware leaf).</li><li>Extend <code>Cond::from_json</code> (TASK-RULE-002) to accept an optional <code>weight</code> on connective nodes and a <code>&lt;he&gt;:</code> qualifier on leaves; reject an invalid qualifier as <code>DslError</code>.</li><li><code>score_set.rs</code>: <code>score_match_set</code> over <code>evaluate_set</code>, weight-aware; emit the envelope <code>CachCuc</code>.</li><li>Amend <code>docs/contracts/condition-dsl.schema.json</code> for <code>weight</code> and the qualified <code>field</code>; bump its schema note.</li><li>Build <code>fixtures/chart_set_qimen_liuren.json</code> (a QiMen + LiuRen set for one question) and wire the back-compat, cross-system, property, and schema tests.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-RULE-002 (the DSL grammar, the <code>Cond</code> / <code>Op</code> evaluator, and the reserved per-node <code>weight</code> this task activates); it extends that crate's <code>dsl.rs</code> / <code>eval.rs</code> and reuses <code>path::resolve</code> unchanged inside the chosen chart. Transitively uses TASK-RULE-001 (<code>Pattern</code>, <code>system: all</code>) and TASK-PLAT-002 (<code>LaSo</code>, <code>He</code>, <code>CachCuc</code>), and stays loadable through the TASK-RULE-003 matcher over the seed repository. The charts in a set are produced by TASK-QMDG-006 and TASK-LN-006.</p>\n<p>Consumer, flagged for a human: TASK-STRAT-004 (cross-system validate, <code>/calculate/all</code> + agreement view) is what runs cross-system patterns over a <code>ChartSet</code>. The catalog records TASK-STRAT-004 <code>depends_on</code> [TASK-QMDG-006, TASK-LN-006] and not TASK-RULE-004 (RULE-004 is COULD / P2, a soft enabler), so <code>blocks</code> is left empty here; when TASK-STRAT-004 is authored, add TASK-RULE-004 to its <code>depends_on</code> if the agreement view is built on data patterns rather than bespoke comparison, and record the edge there. TASK-RULE-004 lists TASK-STRAT-004 in <code>related_frs</code> to make the runtime edge explicit.</p>\n<h2>§8 - Example payloads</h2>\n<p>A cross-system agreement pattern (a <code>system: all</code> row per TASK-RULE-001) and the hit it emits:</p>\n<pre><code class=\"language-json\">// conditions - QiMen cat cach AND a concurring LiuRen tam truyen than\n{ \"type\": \"and\", \"weight\": 1.0, \"rules\": [\n  { \"field\": \"ky_mon:cach_cuc\",            \"operator\": \"contains\", \"value\": \"青龍返首\" },\n  { \"field\": \"luc_nham:tam_truyen.0.than\", \"operator\": \"in\",       \"value\": [\"六合\", \"青龍\"] }\n] }</code></pre>\n<pre><code class=\"language-json\">// emitted CachCuc when both charts are present and both arms hold (whole-set: cung null)\n{ \"id\": \"cross_qimen_luc_nham_dong_thuan_cat\", \"name\": \"Dong thuan cat (Ky Mon + Luc Nham)\",\n  \"cung\": null, \"polarity\": \"cat\", \"score\": 0.85,\n  \"citations\": [\"yba_thien_can_khac_ung_12\", \"dllr_tam_truyen_thanh_long_03\"] }</code></pre>\n<p>With only a QiMen chart in the set, <code>luc_nham:tam_truyen.0.than</code> resolves to <code>None</code> -&gt; <code>false</code>, so the pattern does not fire - the cross-system signal degrades to the single system present.</p>\n<h2>§9 - Open questions</h2>\n<ul><li>Weight semantics for the agreement view: is <code>weight</code> a linear coefficient on the sub-score, or a count of concurring systems? Default: a linear per-node coefficient on <code>confidence</code> at MVP, enough for TASK-STRAT-004 to rank agreement; a richer agreement metric (fraction of systems concurring) is a STRAT-004 concern layered on top, not a new operator here.</li><li>Primary-chart choice when a bare path appears in a cross-system pattern: implicit (the set's <code>primary</code>) or required-explicit. Default: keep <code>primary</code> on the set and allow bare paths, but lint cross-system (<code>system: all</code>) seed patterns to prefer explicit <code>&lt;he&gt;:</code> qualifiers so intent is legible; a bare path still works for single-chart back-compat.</li><li>TaiYi arm: the fixture and the worked example use QiMen + LiuRen (the P0 / P1 engines); a <code>thai_at:</code> arm is valid grammar and activates when TASK-TAT-006 lands, needing only a fixture, not a code change.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Panic on missing chart</td><td>a <code>ky_mon:</code> path when no QiMen chart is in the set</td><td><code>resolve_in_set</code> returns <code>None</code>; leaf is <code>false</code> (or the <code>exists</code> test); never panics</td></tr><tr><td>Back-compat regression</td><td><code>evaluate_set</code> diverges from TASK-RULE-002 on one chart</td><td>the TASK-RULE-002 truth-table suite run through <code>evaluate_set</code> fails; single-chart parity is required</td></tr><tr><td>Bad qualifier</td><td><code>field</code> begins <code>xx:</code> (not a <code>He</code>)</td><td><code>Cond::from_json</code> and the schema reject it as a <code>DslError</code></td></tr><tr><td>Operator creep</td><td>a new operator added for cross-system</td><td>rejected in review; this task adds a chart selector and <code>weight</code>, not operators (TASK-RULE-002 surface is fixed)</td></tr><tr><td>Non-deterministic score</td><td>weight applied via map-order iteration</td><td>fixed traversal; 1,000x determinism test; canonical JSON equality</td></tr><tr><td>Cross-system leak</td><td>a bare path silently reads the wrong chart</td><td>seed lint prefers explicit <code>&lt;he&gt;:</code> on <code>system: all</code> patterns; a bare path resolves only against the declared <code>primary</code></td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the rule-engine half of cross-system validate (TASK-STRAT-004): it lets an agreement between QiMen and LiuRen be written as a data pattern (Grok-31) rather than bespoke comparison code, using one new idea - a <code>&lt;he&gt;:</code> chart selector on the front of an otherwise unchanged TASK-RULE-002 path. Everything else is inherited: the operators, the resolver, the totality guarantee, the offline seed-testability. Because a missing chart resolves cleanly to <code>false</code>, a cross-system pattern degrades to whatever systems the user actually cast, which is the right behavior for a validate feature. Do not widen the operator set here; the only additions are the chart selector and the <code>weight</code> node TASK-RULE-002 already reserved. refs Grok-31, Claude-06 s2.3.</p>";
