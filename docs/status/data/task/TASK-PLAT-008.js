window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLAT-008"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task is the resilience layer: the reusable patterns that keep the platform serving when an external dependency fails. It provides a circuit breaker for external services (chiefly the LLM), retry with exponential backoff for transient errors, graceful degradation policies, and the mapping from a failure to the structured error envelope with the correct standard HTTP code. It owns the resilience primitives and the degradation policy; it does NOT own the error-envelope shape (TASK-API-001 defines it) nor the LLM-specific fallback wiring (TASK-RAG-007 applies these primitives to the RAG caller) - it is the shared library both consume.</p>\n<p>External-service calls SHALL be wrapped in a circuit breaker: after a threshold of failures the breaker SHALL open and fail fast (returning a degraded result or a typed upstream error) rather than piling requests onto a dead dependency, SHALL move to half-open after a cooldown to probe recovery, and SHALL close on success. Transient errors (timeouts, 502/503, connection resets) SHALL be retried with exponential backoff and jitter, bounded by a maximum attempt count and a total deadline; non-transient errors (4xx, validation) SHALL NOT be retried. Retries and breaker state transitions SHALL emit metrics and signals to TASK-PLAT-005 so an operator sees a dependency degrade.</p>\n<p>Degradation SHALL be graceful and explicit: if one engine fails in a multi-system <code>/calculate/all</code>, the gateway SHALL still return the results of the engines that succeeded, marking the failed one, rather than failing the whole request; if the LLM fails, the platform SHALL fall back to a rule-based interpretation (the detected patterns plus their cited classical text, without the LLM's synthesis) rather than returning nothing. Every failure that does surface SHALL be rendered as the TASK-API-001 structured error envelope <code>{ code, message, details }</code> with the correct HTTP status (502/503 <code>UPSTREAM_*</code> for dependency failures, not an opaque 500). Degradation SHALL NOT silently drop the AIDisclosure or the citation obligations - a rule-based fallback is still labeled and still cited.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The platform's hot path crosses two external dependencies that will fail sometimes: the LLM and (transitively) whatever the LLM provider depends on. Without resilience, a slow or dead LLM turns every query into a hung request and a cascade - the classic failure where retries against a dead dependency amplify the outage. A circuit breaker converts that into fail-fast plus a degraded-but-useful answer; exponential backoff with jitter handles the far more common transient blip without a retry storm. These are boring, well-understood patterns, and that is exactly why they belong in one shared library rather than reinvented per call site (Grok-47).</p>\n<p>Graceful degradation is where resilience meets the product's values. A multi-system reading where one engine hiccups should still show the other engines - a partial chart is far more useful than an error page. And when the LLM is down, the platform still has something true to say: the deterministic engine already cast the chart and the rule engine already detected the patterns with their citations, so a rule-based interpretation (patterns + cited text, minus the LLM's prose) is a real, grounded answer, not a stub. Crucially, this is also the safe degradation: falling back to cited patterns can never hallucinate, because there is no generative step to hallucinate (RISK-3). The one thing degradation must not do is quietly drop the disclosure or citation obligations - a fallback answer is still AI-adjacent output on a legally sensitive surface, so it stays labeled and cited. Mapping every failure to the structured envelope with the right code keeps the frontend and SDK reacting correctly instead of guessing at a bare 500.</p>\n<h2>§3 - Contract (primitives / policy / errors)</h2>\n<h3>Circuit breaker (<code>resilience/circuit_breaker.py</code>)</h3>\n<pre><code class=\"language-python\">class BreakerState(str, Enum): closed = \"closed\"; open = \"open\"; half_open = \"half_open\"\n\nclass CircuitBreaker:\n    # opens after `failure_threshold` failures in a window; half-open after `cooldown_s`; closes on success\n    def __init__(self, failure_threshold: int, cooldown_s: float, name: str): ...\n    async def call(self, fn: Callable[[], Awaitable[T]]) -&gt; T: ...   # raises BreakerOpen when open</code></pre>\n<h3>Retry with backoff (<code>resilience/retry.py</code>)</h3>\n<pre><code class=\"language-python\">async def with_retry(fn: Callable[[], Awaitable[T]], *, max_attempts: int, base_s: float,\n                     max_s: float, deadline_s: float, retry_on: tuple[type[Exception], ...]) -&gt; T:\n    # exponential backoff base_s * 2**n + jitter, capped at max_s, bounded by deadline_s;\n    # retries only `retry_on` (transient) exceptions; never retries 4xx/validation\n    ...</code></pre>\n<h3>Degradation policy (<code>resilience/degradation.py</code>, <code>docs/contracts/resilience-policy.md</code>)</h3>\n<table><thead><tr><th>Failure</th><th>Degraded behavior</th><th>Surfaced as</th></tr></thead><tbody><tr><td>one engine fails in <code>/calculate/all</code></td><td>return the succeeding engines' charts; mark the failed <code>he</code></td><td>200 with a <code>degraded</code> note per system</td></tr><tr><td>LLM fails / breaker open</td><td>rule-based interpretation (patterns + cited text, no LLM synthesis); AIDisclosure marks the fallback</td><td>200 degraded, or 503 <code>UPSTREAM_LLM</code> if even patterns are unavailable</td></tr><tr><td>engine service down (single-system)</td><td>no chart to interpret</td><td>502 <code>UPSTREAM_ENGINE</code> in the error envelope</td></tr><tr><td>DB write fails (persist)</td><td>request fails transactionally (no silent 200)</td><td>500 <code>INTERNAL</code> / 503 per TASK-API-004</td></tr></tbody></table>\n<h3>Error mapping (<code>resilience/errors.py</code>)</h3>\n<p>Maps <code>BreakerOpen</code>, timeout, and upstream errors to the TASK-API-001 envelope codes (<code>UPSTREAM_LLM</code>, <code>UPSTREAM_ENGINE</code>) with the correct HTTP status; never surfaces a raw exception as an opaque 500.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A circuit breaker around a failing dependency opens after the threshold, fails fast while open, moves to half-open after the cooldown, and closes on a successful probe; a test drives all four transitions.</li><li>A transient error is retried with exponential backoff + jitter up to the attempt/deadline bound and then surfaces the mapped upstream error; a non-transient 4xx is not retried.</li><li>In <code>/calculate/all</code>, an injected failure of one engine still returns the other engines' charts, with the failed <code>he</code> marked degraded, and does not fail the whole request.</li><li>With the LLM down (or the breaker open), the response is a rule-based interpretation built from the detected patterns and their citations, carrying the AIDisclosure marked as a fallback and keeping citations - never an empty or unlabeled answer.</li><li>Every surfaced failure is the TASK-API-001 error envelope with the correct status: a dependency failure is 502/503 <code>UPSTREAM_*</code>, not an opaque 500; a test asserts the code/status mapping.</li><li>Breaker transitions and retries emit metrics/signals to TASK-PLAT-005 (an operator can see a dependency degrade and recover).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_circuit_breaker.py</code>: closed-&gt;open-&gt;half_open-&gt;closed transitions; fail-fast while open; the cooldown probe.</li><li><code>tests/test_retry.py</code>: backoff timing (mocked clock), jitter bounds, the attempt/deadline cap, retry-only-on-transient.</li><li><code>tests/test_degradation.py</code>: the one-engine-fails-in-all case returns partial results; the LLM-down case returns a cited rule-based interpretation with the fallback AIDisclosure; the error-envelope code/status mapping for each failure.</li><li>Integration: the TASK-API-001 orchestrator uses these primitives on the engine and LLM calls; a fault-injection run confirms degrade-not-fail behavior end to end and the emitted PLAT-005 signals.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_api</code>, <code>pytest packages/tamthuc_api</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>resilience/circuit_breaker.py</code>: the <code>CircuitBreaker</code> with state machine, thresholds, cooldown, and metric hooks.</li><li><code>resilience/retry.py</code>: <code>with_retry</code> (exponential backoff + jitter, deadline bound, transient-only) as a reusable helper/decorator.</li><li><code>resilience/degradation.py</code>: the degradation policy - partial multi-engine results and the LLM-down rule-based fallback (patterns + citations + fallback AIDisclosure).</li><li><code>resilience/errors.py</code>: map breaker/timeout/upstream failures to the TASK-API-001 envelope codes and HTTP statuses.</li><li>Wire the primitives into the TASK-API-001 orchestrator's engine and LLM calls; emit transitions to TASK-PLAT-005; expose the LLM-specific application to TASK-RAG-007.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-PLAT-005 (observability: breaker/retry/degradation transitions must be observable, and they feed the LLM-downtime and error-rate alerts). Consumes the TASK-API-001 error-envelope contract (this task maps failures onto it, it does not redefine it) and plugs into the TASK-API-001 orchestrator's engine and LLM calls. Provides the shared resilience primitives TASK-RAG-007 applies to the LLM caller (RAG-007 is the LLM-specific instance of this library). Coordinates with TASK-API-003 (the limiter's fail-safe posture is the same principle applied to the rate-limit datastore) and TASK-RAG-003 (the rule-based fallback reuses the detected patterns and their citations).</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// /calculate/all with one engine degraded - partial success, not a failure\n{ \"query_id\": \"q_...\", \"charts\": [ { \"he\": \"ky_mon\", \"...\": \"...\" } ],\n  \"degraded\": [ { \"he\": \"luc_nham\", \"reason\": \"UPSTREAM_ENGINE\", \"message\": \"LiuRen engine unavailable\" } ],\n  \"interpretation\": { \"...\": \"...\" }, \"ai_disclosure\": { \"ai_generated\": true } }</code></pre>\n<pre><code class=\"language-json\">// LLM down - rule-based fallback, still labeled and cited\n{ \"interpretation\": { \"mode\": \"rule_based_fallback\",\n    \"patterns\": [ { \"id\": \"qimen_thanh_long_hoi_dau\", \"polarity\": \"cat\", \"citations\": [\"Yen Ba Dieu Tau Ca\"] } ] },\n  \"ai_disclosure\": { \"ai_generated\": false, \"fallback\": \"llm_unavailable\", \"note\": \"rule-based, citation-grounded\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Breaker granularity: one breaker per external dependency vs per provider+model. Default: per dependency at MVP (one LLM breaker, one engine-service breaker); split per provider when TASK-RAG-007 adds multi-provider fallback so a single provider's outage does not open the whole LLM path.</li><li>Whether the rule-based fallback is a distinct response mode or a reduced interpretation. Default: a distinct <code>mode: rule_based_fallback</code> so the frontend can render it honestly (patterns + citations, no synthesized prose) and the AIDisclosure can mark it; keep the response shape otherwise identical.</li><li>Retry budget interaction with the TASK-API-003 rate limit. Default: retries are internal and do not consume the user's quota (the quota counts user requests, not internal attempts); a retried upstream call is one metered request.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Cascade on dead dependency</td><td>unbounded requests to a failing LLM</td><td>circuit breaker opens and fails fast; no pile-on; half-open probe for recovery</td></tr><tr><td>Retry storm</td><td>retrying non-transient or without a cap</td><td>retry only transient errors, exponential backoff + jitter, bounded by attempts + deadline</td></tr><tr><td>All-or-nothing multi-engine</td><td>one engine failure fails <code>/calculate/all</code></td><td>return the succeeding engines; mark the failed <code>he</code> degraded</td></tr><tr><td>Empty answer on LLM outage</td><td>LLM down returns nothing</td><td>rule-based fallback from detected patterns + citations; never empty</td></tr><tr><td>Unlabeled/uncited fallback</td><td>degradation drops AIDisclosure or citations</td><td>forbidden; the fallback stays labeled and cited (RISK-3)</td></tr><tr><td>Opaque 500 on upstream failure</td><td>dependency error surfaced as generic 500</td><td>mapped to 502/503 <code>UPSTREAM_*</code> in the TASK-API-001 envelope</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task keeps the platform useful when its external dependencies are not. The reusable primitives - circuit breaker, retry-with-backoff, degradation policy, error mapping - live here as one shared library so TASK-API-001 and TASK-RAG-007 apply them instead of reinventing them. Two disciplines matter most: degrade, do not fail (partial multi-engine results; a cited rule-based interpretation when the LLM is down), and the degraded answer stays labeled and cited (the fallback is safe precisely because it has no generative step to hallucinate, but it still carries AIDisclosure and citations on this legally sensitive surface). It depends on TASK-PLAT-005 because a breaker that trips invisibly is worse than none - every transition is observable and alertable.</p>";
