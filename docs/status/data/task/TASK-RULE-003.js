window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-RULE-003"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task is the runtime entry point of the rule engine: the API a casting engine calls to turn a la so into its detected patterns. It SHALL load <code>active</code> patterns from a source, filter them to the systems relevant to a chart, cache them in memory, and expose <code>match(la_so) -&gt; Vec&lt;CachCuc&gt;</code> that runs the TASK-RULE-002 evaluator over every relevant pattern and returns the matches as envelope <code>CachCuc</code> records (TASK-PLAT-002).</p>\n<p>The loader SHALL read from a <code>PatternRepository</code> abstraction with two implementations: a Postgres-backed repository over the <code>knowledge_patterns</code> table (TASK-PLAT-003) for production, and a seed-file repository over the TASK-RULE-001 seed for tests and offline runs. The crate SHALL remain cargo-testable with no database: the Postgres repository lives behind a cargo feature, and every test uses the seed repository. Loading SHALL select only <code>status = active</code> rows.</p>\n<p>The per-system filter SHALL be derived from the chart: <code>match</code> reads <code>la_so.he</code>, maps it to a <code>System</code>, and considers patterns whose <code>system</code> is that system or <code>all</code> (a QiMen chart matches <code>qimen</code> and <code>all</code> patterns, never <code>liuren</code> or <code>taiyi</code>). The result SHALL be deterministic: matches SHALL be returned in a stable order (by polarity, then descending score, then id) and de-duplicated by pattern id. The cache SHALL be invalidatable (by system, or wholesale) so a curated pattern edit (TASK-KB-004) or a new deploy takes effect without a restart; cache correctness SHALL never change which patterns fire, only how fast they load.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>TASK-RULE-001 and TASK-RULE-002 give a ruleset and an evaluator; something has to fetch the rows, keep them hot, and hand a casting engine a one-call detector. That is this task, and it is deliberately thin (6h) because the logic lives in the two tasks beneath it. The value it adds is three separations that keep the system testable and fast. First, the repository abstraction means the deterministic engine crates never link a database driver into their test builds - the oracle-gated engine tests (TASK-QMDG-006) run against the seed repository, pure and offline, while production reads the live table. Second, the per-system filter keeps a chart from being scored against patterns that cannot apply, which is both correct (a LiuRen khoa the is meaningless on a QiMen plate) and cheaper. Third, the in-memory cache reflects the engines' cache-friendly, deterministic nature (Claude-06 s2.2): patterns change rarely, charts are scored constantly, so loading the active set once per system and reusing it is the obvious win, and because matching is a pure function the cache can never change a result - only latency.</p>\n<p>Returning the envelope <code>CachCuc</code> directly is what lets a casting engine assemble its la so without an adapter: the engine builds <code>ban</code>, calls <code>match</code>, and drops the returned vector into <code>cach_cuc</code>. The interpretation branch then reads those detected patterns with their citations already attached, closing the source -&gt; pattern -&gt; cach cuc -&gt; cited interpretation chain.</p>\n<h2>§3 - Contract (repository + cache + match API)</h2>\n<h3>Repository abstraction (<code>crates/cyberos-rule/src/repo.rs</code>)</h3>\n<pre><code class=\"language-rust\">pub trait PatternRepository: Send + Sync {\n    /// All `active` patterns whose system is `system` or `All`.\n    fn active_for(&amp;self, system: System) -&gt; Result&lt;Vec&lt;Pattern&gt;, RepoError&gt;;\n}\n\n/// Offline / test source: reads the TASK-RULE-001 seed directory and validates each row.\npub struct SeedRepository { patterns: Vec&lt;Pattern&gt; }\nimpl SeedRepository { pub fn load(dir: &amp;Path) -&gt; Result&lt;Self, RepoError&gt;; }\n\n/// Production source over the knowledge_patterns table (TASK-PLAT-003).\n#[cfg(feature = \"pg\")]\npub struct PgRepository { pool: sqlx::PgPool }</code></pre>\n<p><code>SeedRepository::load</code> calls <code>seed::load_seed</code> (TASK-RULE-001), so the same validation guards both paths. <code>PgRepository::active_for</code> runs <code>select ... from knowledge_patterns where status = 'active' and system in ($1, 'all')</code> and deserializes each row through <code>validate_pattern</code>, so a bad row from the DB is a typed error, not a silent skip.</p>\n<h3>Cache (<code>crates/cyberos-rule/src/cache.rs</code>)</h3>\n<p>An in-memory snapshot per system, behind an <code>RwLock</code>, holding an <code>Arc&lt;Vec&lt;Pattern&gt;&gt;</code> so readers never block writers mid-scan. Optional TTL for the Postgres repository (default 24h, aligned with the TASK-PLAT-006 chart cache); the seed repository is effectively immutable per process.</p>\n<h3>Matcher (<code>crates/cyberos-rule/src/matcher.rs</code>)</h3>\n<pre><code class=\"language-rust\">pub struct Matcher {\n    repo: Box&lt;dyn PatternRepository&gt;,\n    cache: RwLock&lt;HashMap&lt;System, Arc&lt;Vec&lt;Pattern&gt;&gt;&gt;&gt;,\n    ttl: Option&lt;Duration&gt;,\n}\n\nimpl Matcher {\n    pub fn new(repo: Box&lt;dyn PatternRepository&gt;) -&gt; Self;\n\n    /// Preload a system's active patterns (call at startup for warm caches).\n    pub fn warm(&amp;self, system: System) -&gt; Result&lt;(), RepoError&gt;;\n\n    /// THE engine-facing API. Reads la_so.he, loads {system, all}, scores each,\n    /// returns matches sorted (polarity, -score, id), de-duplicated by id.\n    pub fn match_laso(&amp;self, la_so: &amp;LaSo) -&gt; Result&lt;Vec&lt;CachCuc&gt;, RepoError&gt;;\n\n    /// Drop a system's snapshot (call after a curated edit or on deploy).\n    pub fn invalidate(&amp;self, system: System);\n    pub fn invalidate_all(&amp;self);\n}</code></pre>\n<p><code>match_laso</code> is the operation TASK-QMDG-005 and every engine cach cuc step calls, and the operation STRAT-001 leans on to score timing windows. Its body: map <code>la_so.he</code> to <code>System</code>; get-or-load the cached <code>Arc&lt;Vec&lt;Pattern&gt;&gt;</code> for that system (already the union of <code>system</code> and <code>all</code> from <code>active_for</code>); iterate calling <code>score::score_match(p, la_so)</code>; collect <code>Some(_)</code>; sort and dedup; return.</p>\n<h3>Determinism and ordering</h3>\n<p>Sort key: <code>polarity</code> (cat before trung before hung, a fixed enum order), then descending <code>score</code>, then ascending <code>id</code> as the final tiebreak. De-dup keeps the first occurrence per <code>id</code>. The same chart and the same active ruleset always produce the same vector, byte-for-byte, which the engine assembly oracle gates (TASK-QMDG-006) depend on.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>SeedRepository::load</code> loads the TASK-RULE-001 seed and <code>active_for(Qimen)</code> returns exactly the <code>active</code> patterns whose system is <code>qimen</code> or <code>all</code>, and none whose system is <code>liuren</code>/<code>taiyi</code> or whose status is <code>draft</code>/<code>deprecated</code>.</li><li><code>match_laso</code> on a QiMen sample chart returns the expected <code>CachCuc</code> set, sorted by (polarity, -score, id) and de-duplicated by id.</li><li>Matching is identical whether patterns come from the seed repository or a stub Postgres repository holding the same rows (repository-parity test).</li><li><code>warm</code> then <code>match_laso</code> performs zero repository reads on the second call for the same system (cache hit); <code>invalidate(system)</code> forces a reload.</li><li>A chart whose <code>he</code> has no active patterns returns an empty vector, not an error.</li><li>A malformed DB row (bad enum, missing field) surfaces as a typed <code>RepoError</code> from <code>active_for</code>, never a silently dropped pattern.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/loader_match.rs</code>: seed load + <code>active_for</code> filtering; <code>match_laso</code> over <code>fixtures/laso_qimen_sample.json</code> (shared with TASK-RULE-002) asserting the exact sorted, de-duplicated vector; the cache hit/invalidation behavior with a counting repository stub; the repository-parity test (seed vs in-memory stub Pg).</li><li>Determinism: run <code>match_laso</code> 1,000 times and assert vector equality every time.</li><li>Offline guarantee: the default <code>cargo test -p cyberos-rule</code> compiles and passes with the <code>pg</code> feature off (no database in CI for this crate).</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-rule -- -D warnings</code>, <code>cargo test -p cyberos-rule</code>; <code>cargo clippy -p cyberos-rule --features pg</code> for the Postgres path.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>repo.rs</code>: the <code>PatternRepository</code> trait, <code>SeedRepository</code> over TASK-RULE-001's <code>load_seed</code>, and the feature-gated <code>PgRepository</code> (query + row validation).</li><li><code>cache.rs</code>: the <code>RwLock&lt;HashMap&lt;System, Arc&lt;Vec&lt;Pattern&gt;&gt;&gt;&gt;</code> snapshot store with optional TTL.</li><li><code>matcher.rs</code>: <code>Matcher::new/warm/match_laso/invalidate</code>; the sort-and-dedup; the <code>he -&gt; System</code> mapping.</li><li>Wire the loader/match/cache tests with a counting repository stub to prove cache hits and invalidation.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-RULE-002 (<code>score_match</code>, the evaluator) and, transitively, TASK-RULE-001 (<code>Pattern</code>, seed) and TASK-PLAT-002 (<code>LaSo</code>, <code>CachCuc</code>). The Postgres repository depends on TASK-PLAT-003 (the <code>knowledge_patterns</code> table) and pairs well with TASK-PLAT-006 (the chart cache TTL convention).</p>\n<p>Consumers: this is the match API the casting engines call for cach cuc detection. TASK-QMDG-005 is the first consumer (its QiMen cach cuc step calls <code>match_laso</code>); TASK-STRAT-001 (Timing Optimizer) scores candidate windows through it. Note on the dependency spine (flagged for a human): the master task table records TASK-QMDG-005 <code>depends_on</code> TASK-RULE-002 (the evaluator, a hard build dependency), while TASK-QMDG-005 consumes patterns at runtime through this task's <code>match_laso</code>. When TASK-QMDG-005 is authored, add TASK-RULE-003 to its <code>depends_on</code> (RULE-003 already depends on RULE-002, so there is no cycle). This task lists TASK-QMDG-005 in <code>blocks</code> to make that runtime edge explicit.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-rust\">// Engine assembly (TASK-QMDG-005/006) usage\nlet matcher = Matcher::new(Box::new(SeedRepository::load(Path::new(\"crates/cyberos-rule/seed\"))?));\nmatcher.warm(System::Qimen)?;                 // startup\nlet cach_cuc: Vec&lt;CachCuc&gt; = matcher.match_laso(&amp;la_so)?;   // per chart\n// la_so.cach_cuc = cach_cuc;  (the engine fills the envelope slot)</code></pre>\n<pre><code class=\"language-json\">// match_laso(qimen_chart) result (sorted, de-duplicated)\n[\n  { \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"Thanh Long Hoi Dau\", \"cung\": 1,\n    \"polarity\": \"cat\", \"score\": 0.9, \"citations\": [\"yba_thien_can_khac_ung_12\"] },\n  { \"id\": \"chung_khong_vong\", \"name\": \"Khong Vong\", \"cung\": 3,\n    \"polarity\": \"hung\", \"score\": 0.6, \"citations\": [\"chung_tuan_khong_01\"] }\n]</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>TTL vs event-driven invalidation for the Postgres repository. Default: a 24h TTL plus an explicit <code>invalidate</code> hook the TASK-KB-004 curation flow calls on publish; a pub/sub invalidation is a later optimization if editors need instant propagation.</li><li>Should <code>match_laso</code> also accept a pre-fetched <code>&amp;[Pattern]</code> for callers that manage their own loading (e.g. TASK-STRAT-001 scanning many charts against one warmed set)? Default: expose a lower-level <code>match_with(&amp;self, patterns, la_so)</code> used internally and by STRAT to avoid re-locking the cache per chart in a tight scan.</li><li>Whether the cache belongs in this crate or in a shared TASK-PLAT-006 cache layer. Default: keep the in-memory pattern snapshot local (it is tiny and hot); reserve Redis for chart results.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>DB unavailable at match time</td><td>Postgres down, cache cold</td><td>typed <code>RepoError</code>; caller (engine) degrades per TASK-PLAT-008; a warm cache serves the last-known active set</td></tr><tr><td>Stale cache after edit</td><td>pattern curated but cache not invalidated</td><td>TTL bounds staleness; TASK-KB-004 publish calls <code>invalidate</code>; correctness unaffected (only which text, same fire set until reload)</td></tr><tr><td>Cross-system leak</td><td>filter admits a <code>liuren</code> pattern to a <code>qimen</code> chart</td><td><code>active_for</code> SQL/seed filter is <code>system in (he, all)</code>; test asserts no foreign-system pattern appears</td></tr><tr><td>Non-deterministic order</td><td>matches sorted by map order</td><td>fixed sort (polarity, -score, id) + dedup; 1,000x determinism test</td></tr><tr><td>Silent bad row</td><td>DB row with a legacy enum</td><td><code>active_for</code> validates each row; a bad row is a typed error, not a skip</td></tr><tr><td>Test needs a database</td><td>crate not offline-testable</td><td>seed repository is the default; <code>pg</code> feature off in the crate's CI test job</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This completes the P0 RULE spine: TASK-RULE-001 (data), TASK-RULE-002 (evaluator), TASK-RULE-003 (loader/match API). The three together are one <code>cyberos-rule</code> crate, deterministic and shared by all three casting systems, with no hardcoded pattern anywhere. Keep it offline-testable: the moment an engine test needs a live database to detect a cach cuc, the seed-repository discipline has been broken. TASK-RULE-004 (cross-system patterns, P2) extends the DSL and the matcher for multi-system nesting; it is listed in the module table and authored later.</p>";
