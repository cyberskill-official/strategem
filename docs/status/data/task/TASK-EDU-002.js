window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-EDU-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task builds the learning flow's auto-graded chart practice: the learner casts a chart step by step, and the app grades each step by diffing it against the deterministic engine's chart, pointing at the exact place the learner diverged (Claude-07 s3.3). It consumes the criteria TASK-EDU-001 marks <code>gradedBy: \"engine\"</code> and turns them into a graded exercise.</p>\n<p>The module SHALL model the casting procedure of a system as an ordered ladder of <code>CastStep</code>s, each addressing a slice of the la so envelope (TASK-PLAT-002) by JSON path: for QiMen the ladder mirrors the QMDG pipeline (dinh cuc -&gt; dia ban -&gt; truc phu / truc su -&gt; thien ban -&gt; cuu tinh / bat mon / bat than -&gt; cach cuc), for LiuRen the LN pipeline (thien dia ban -&gt; tu khoa -&gt; tam truyen -&gt; thien tuong). For a practice attempt the module SHALL obtain the canonical chart for the learner's input by casting through the existing path (TASK-API-001, which calls the TASK-QMDG-006 / TASK-LN-006 engines) and SHALL treat that la so as the answer key, because one input yields exactly one chart (strategy 4.3, Claude-07 s3.3).</p>\n<p>The grader SHALL be a pure function of <code>(step, learnerAnswer, referenceLaSo)</code>: it compares the learner's value for a step against the engine's value at that step's paths and returns a <code>StepResult</code> listing every mismatch as a <code>CellDiff</code> with a specific, localized message (for example \"you seated 天蓬 at cung 3; the engine has 天芮 there\"). Grading a whole attempt (<code>gradeCast</code>) SHALL walk the ladder in order, identify the earliest incorrect step as the root cause, and mark downstream steps that diverge only because of it as <code>blocked_by</code> that root rather than flooding the learner with cascade errors. The grader SHALL grade casting only (a fact against the engine) and SHALL NOT interpret the chart's meaning; interpretation stays in the cited AI layer (strategy 7). The practice surface SHALL render inside the TASK-WEB-001 shell using Design System v1.3.0 components with no new tokens, reusing the TASK-CHART-001 / TASK-CHART-002 chart views for input.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The determinism the engine needs for correct casting is exactly what makes it an ideal marker (Claude-07 s3.3). Because a single input produces a single chart, a learner's step-by-step answer can be diffed against the engine's chart and the exact missed step named - an mis-placed khoa, a mis-derived tam truyen, a mis-seated sao (Claude-07 s3.3 lists these three verbatim). That is the most valuable feedback when practicing a many-step skill like lap ban: immediate and specific, not \"wrong, try again.\" Building the grader on the engine rather than a hand-authored answer key means the answer key is never stale and never wrong - it is whatever the oracle-gated engine casts.</p>\n<p>Grading each step against the canonical chart, not against the learner's own earlier answer, is what keeps feedback honest and localized. If the learner picked the wrong so cuc at dinh cuc, every downstream plate will differ; scoring each step against the engine and then flagging the first divergence as the root cause tells the learner \"fix dinh cuc first\" instead of drowning them in twenty consequential mismatches. This mirrors the pipeline's own structure (each QMDG / LN stage is a stage with its own output), so a step maps cleanly onto a slice of <code>ban</code>.</p>\n<p>Keeping the grader to casting only is the same fact / verdict boundary the whole platform is built on (strategy 7). The engine diffs structure - did you place the right stem in the right palace - which is checkable and objective. What a palace means for the question is interpretation, and that is the cited AI layer's job, never the grader's. A practice tool that started scoring interpretation would cross exactly the line the product is careful to hold.</p>\n<h2>§3 - Contract (types and grader)</h2>\n<h3>Types (<code>apps/web/src/features/edu/practice/types.ts</code>)</h3>\n<pre><code class=\"language-ts\">import type { LaSo } from \"@/lib/laso\";   // TASK-PLAT-002 envelope\n\nexport type System = \"luc_nham\" | \"ky_mon\" | \"thai_at\";\n\n// One step in the casting procedure the learner performs, tied to a slice of the la so.\nexport interface CastStep {\n  id: string;              // stable, e.g. \"qimen.dinh_cuc\", \"luc_nham.tam_truyen\"\n  system: System;\n  label: string;           // display, e.g. \"Dinh cuc\", \"Tam truyen\"\n  criterionId?: string;    // the TASK-EDU-001 criterion this step contributes to\n  refPaths: string[];      // JSON paths into the reference la so (default root `ban`)\n  errorKind: ErrorKind;    // the class of mistake this step catches, for the message\n}\n\n// The three canonical practice errors (Claude-07 s3.3) plus the earlier plate steps.\nexport type ErrorKind =\n  | \"dinh_cuc\" | \"dia_ban\" | \"truc_phu_su\" | \"an_sao\"      // seated a star/plate wrong\n  | \"an_khoa\"                                              // placed a lesson wrong\n  | \"tam_truyen\"                                           // mis-derived tam truyen\n  | \"an_tuong\" | \"cach_cuc\";\n\nexport interface LearnerAnswer { stepId: string; value: unknown; }\n\nexport type StepStatus = \"correct\" | \"incorrect\" | \"blocked\";\n\nexport interface CellDiff {\n  path: string;            // where the mismatch is, e.g. \"cung.3.thien_ban.can\"\n  expected: unknown;       // from the engine's la so (the answer key)\n  got: unknown;            // from the learner\n  message: string;         // specific and localized\n}\n\nexport interface StepResult {\n  stepId: string;\n  status: StepStatus;\n  diffs: CellDiff[];       // empty when correct\n  blockedBy?: string;      // set when this step only diverges because of an earlier error\n}\n\nexport interface CastReport {\n  system: System;\n  steps: StepResult[];\n  rootErrorStepId?: string;  // the earliest incorrect step, the one to fix first\n  allCorrect: boolean;\n}</code></pre>\n<h3>The step ladders (<code>apps/web/src/features/edu/practice/steps.ts</code>)</h3>\n<p>The QiMen ladder mirrors the QMDG pipeline; the LiuRen ladder mirrors the LN pipeline. Each step names the <code>ban</code> paths it grades:</p>\n<pre><code class=\"language-ts\">export const qimenLadder: CastStep[] = [\n  { id: \"qimen.dinh_cuc\",     system: \"ky_mon\", label: \"Dinh cuc\",             refPaths: [\"dinh_cuc\"],                          errorKind: \"dinh_cuc\" },\n  { id: \"qimen.dia_ban\",      system: \"ky_mon\", label: \"Bo dia ban\",           refPaths: [\"dia_ban\"],                           errorKind: \"dia_ban\" },\n  { id: \"qimen.truc_phu_su\",  system: \"ky_mon\", label: \"Truc phu / truc su\",   refPaths: [\"truc_phu\", \"truc_su\"],               errorKind: \"truc_phu_su\" },\n  { id: \"qimen.thien_ban\",    system: \"ky_mon\", label: \"Thien ban\",            refPaths: [\"thien_ban\"],                         errorKind: \"an_sao\" },\n  { id: \"qimen.sao_mon_than\", system: \"ky_mon\", label: \"Cuu tinh / bat mon / bat than\", refPaths: [\"cuu_tinh\", \"bat_mon\", \"bat_than\"], errorKind: \"an_sao\" },\n  { id: \"qimen.cach_cuc\",     system: \"ky_mon\", label: \"Nhan dien cach cuc\",   criterionId: \"edu.crit.3to4\", refPaths: [\"$.cach_cuc\"], errorKind: \"cach_cuc\" },\n];\n\nexport const lucNhamLadder: CastStep[] = [\n  { id: \"luc_nham.thien_dia_ban\", system: \"luc_nham\", label: \"Thien dia ban\", criterionId: \"edu.crit.2to3\", refPaths: [\"thien_dia_ban\"], errorKind: \"an_sao\" },\n  { id: \"luc_nham.tu_khoa\",       system: \"luc_nham\", label: \"Tu khoa\",       criterionId: \"edu.crit.2to3\", refPaths: [\"tu_khoa\"],       errorKind: \"an_khoa\" },\n  { id: \"luc_nham.tam_truyen\",    system: \"luc_nham\", label: \"Tam truyen\",    criterionId: \"edu.crit.2to3\", refPaths: [\"tam_truyen\"],    errorKind: \"tam_truyen\" },\n  { id: \"luc_nham.thien_tuong\",   system: \"luc_nham\", label: \"An thien tuong\", criterionId: \"edu.crit.2to3\", refPaths: [\"thien_tuong\"],  errorKind: \"an_tuong\" },\n];</code></pre>\n<p>A <code>refPath</code> beginning <code>$.</code> addresses the envelope root (so <code>cach_cuc</code> reads the detected-pattern set); a bare path addresses <code>ban</code>.</p>\n<h3>Grader (<code>apps/web/src/features/edu/practice/grader.ts</code>)</h3>\n<pre><code class=\"language-ts\">// Diff one step against the engine's la so at its ref paths. Pure.\nexport function gradeStep(step: CastStep, answer: LearnerAnswer, reference: LaSo): StepResult;\n\n// Grade a whole attempt in order: mark the earliest incorrect step as the root cause;\n// downstream steps that diverge are graded but flagged `blockedBy` the root.\nexport function gradeCast(ladder: CastStep[], answers: LearnerAnswer[], reference: LaSo): CastReport;\n\n// Obtain the answer key by casting the learner's input through the normal path.\n// (TASK-API-001 -&gt; TASK-QMDG-006 / TASK-LN-006; the engine chart is the ground truth.)\nexport async function referenceChart(input: DauVao, system: System): Promise&lt;LaSo&gt;;</code></pre>\n<p><code>gradeStep</code> walks <code>refPaths</code>, reads the engine value and the learner value at each addressed cell, and emits a <code>CellDiff</code> per mismatch with an <code>errorKind</code>-shaped message. <code>status</code> is <code>correct</code> when <code>diffs</code> is empty. <code>gradeCast</code> collects results, sets <code>rootErrorStepId</code> to the first <code>incorrect</code> step, and for later steps whose diffs are explained by the root (a wrong dinh cuc shifts every downstream plate) sets <code>status: \"blocked\"</code> with <code>blockedBy</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>The QiMen and LiuRen ladders exist as ordered <code>CastStep[]</code>, each step addressing real <code>ban</code> (or root <code>cach_cuc</code>) paths of the TASK-PLAT-002 envelope; a test asserts every <code>refPath</code> resolves on a golden la so.</li><li><code>gradeStep</code> returns <code>status: \"correct\"</code> with no diffs when the learner's step equals the engine's, and one <code>CellDiff</code> per mismatched cell otherwise, each with a specific message naming the path, expected, and got.</li><li>The three canonical error classes are caught with the right <code>errorKind</code> and message: a mis-placed tu khoa (<code>an_khoa</code>), a mis-derived tam truyen (<code>tam_truyen</code>), and a mis-seated sao (<code>an_sao</code>), per Claude-07 s3.3.</li><li><code>gradeCast</code> sets <code>rootErrorStepId</code> to the earliest incorrect step, and marks downstream steps that diverge only because of it as <code>blocked</code> with <code>blockedBy</code> pointing at the root, rather than reporting them as independent errors.</li><li>The grader is pure: identical <code>(step, answer, reference)</code> yields an identical <code>StepResult</code> (no clock, no network); the reference chart is fetched separately via <code>referenceChart</code>.</li><li>The practice surface renders in the TASK-WEB-001 shell with DS v1.3.0 components, reuses the TASK-CHART-001 / TASK-CHART-002 views for input, and introduces no new design tokens; it never displays an interpretive verdict (grades casting only).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>Vitest (<code>grader.test.ts</code>): golden-la-so fixtures for one QiMen and one LiuRen chart (emitted by the engines or built to the TASK-PLAT-002 schema). Correct-answer case (no diffs); each canonical error case (an_khoa, tam_truyen, an_sao) with the expected message; the cascade case where a wrong dinh cuc yields one root and several <code>blocked</code> downstream steps.</li><li>Path-resolution test: every <code>refPath</code> in both ladders resolves to a present slice of the golden envelope (so a rename of <code>ban</code> shape is caught here).</li><li>Purity test: 1,000 repeat calls of <code>gradeStep</code> on the same inputs return deep-equal results.</li><li>Boundary test: <code>StepResult</code> and <code>CastReport</code> carry no interpretation field; a lint/assert guards that grader output has no <code>meaning</code> / verdict key.</li><li>Component/snapshot test: the practice flow renders in the app shell with DS v1.3.0 only.</li><li>Gates: <code>pnpm test</code>, <code>tsc --noEmit</code>, <code>eslint</code> (the WEB toolchain).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>types.ts</code>: <code>CastStep</code>, <code>LearnerAnswer</code>, <code>CellDiff</code>, <code>StepResult</code>, <code>CastReport</code>, <code>ErrorKind</code>.</li><li><code>steps.ts</code>: the <code>qimenLadder</code> and <code>lucNhamLadder</code>, each step tied to <code>ban</code> paths and (where applicable) an TASK-EDU-001 <code>criterionId</code>.</li><li><code>grader.ts</code>: <code>gradeStep</code> (per-cell diff + message), <code>gradeCast</code> (root-cause + <code>blockedBy</code>), <code>referenceChart</code> (cast via TASK-API-001).</li><li><code>grader.test.ts</code>: golden fixtures, the three error cases, the cascade case, purity.</li><li><code>PracticeFlow.tsx</code>: the step-by-step UI in the WEB-001 shell, reusing the CHART views for input and showing the per-step diff panel with the root-cause highlighted.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-QMDG-006 and TASK-LN-006 - the oracle-gated QiMen and LiuRen engines whose charts are the answer key; the practice surface reaches them through the normal casting path (TASK-API-001), never re-deriving a chart in TypeScript. Consumes the TASK-PLAT-002 envelope (the la so it diffs against) and the TASK-EDU-001 criteria (the steps carry <code>criterionId</code>s for the engine-graded transitions). Renders in the TASK-WEB-001 shell and reuses TASK-CHART-001 / TASK-CHART-002 for chart input. Related to TASK-EDU-003 (a wrong step can link out to the relevant library passage).</p>\n<h2>§8 - Example payloads</h2>\n<p>A mis-seated-star result (the learner put 天蓬 where the engine has 天芮):</p>\n<pre><code class=\"language-ts\">gradeStep(qimenLadder[4], { stepId: \"qimen.sao_mon_than\", value: learnerPlates }, engineLaSo)\n// -&gt;\n{\n  stepId: \"qimen.sao_mon_than\",\n  status: \"incorrect\",\n  diffs: [\n    { path: \"cuu_tinh.3\", expected: \"天芮\", got: \"天蓬\",\n      message: \"You seated 天蓬 at cung 3; the engine has 天芮 there.\" }\n  ],\n}</code></pre>\n<p>A cascade attempt (wrong dinh cuc) reported with one root and blocked downstream steps:</p>\n<pre><code class=\"language-ts\">gradeCast(qimenLadder, answers, engineLaSo)\n// -&gt;\n{\n  system: \"ky_mon\",\n  rootErrorStepId: \"qimen.dinh_cuc\",\n  allCorrect: false,\n  steps: [\n    { stepId: \"qimen.dinh_cuc\",     status: \"incorrect\", diffs: [ /* so_cuc 1 vs 7 */ ] },\n    { stepId: \"qimen.dia_ban\",      status: \"blocked\", blockedBy: \"qimen.dinh_cuc\", diffs: [] },\n    { stepId: \"qimen.truc_phu_su\",  status: \"blocked\", blockedBy: \"qimen.dinh_cuc\", diffs: [] },\n  ],\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Partial credit within a step: is a step all-or-nothing, or scored by fraction of correct cells? Default: report every <code>CellDiff</code> (so the learner sees each mistake) and treat the step as <code>incorrect</code> if any cell diverges; a numeric score is an TASK-EDU-001 progression concern, not the grader's.</li><li>Cascade detection heuristic: how do we know a downstream diff is only a consequence of the root? Default at MVP: any incorrect step after the first is marked <code>blocked</code> until the learner fixes the root and re-submits; a precise dependency model (which cells the wrong so cuc actually moved) is a later refinement.</li><li>TaiYi practice: the ladder is defined for QiMen and LiuRen (the P3 engines that are built); the TaiYi ladder is added when TASK-TAT-006 lands and is out of scope here.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Stale answer key</td><td>a hand-authored key drifts from the engine</td><td>there is no hand key; the reference is always the freshly cast engine la so</td></tr><tr><td>Cascade flood</td><td>wrong early step reported as many errors</td><td><code>gradeCast</code> names one <code>rootErrorStepId</code>; downstream diverging steps are <code>blocked</code>, not independent errors</td></tr><tr><td>Grader interprets</td><td>practice starts scoring meaning</td><td>boundary test: grader output has no verdict field; casting only (strategy 7)</td></tr><tr><td>Path drift</td><td><code>ban</code> shape changes, <code>refPath</code> misses</td><td>path-resolution test fails on the golden envelope</td></tr><tr><td>Impure grader</td><td>reads clock/network in <code>gradeStep</code></td><td>purity test (1,000x deep-equal) fails; casting is done separately in <code>referenceChart</code></td></tr><tr><td>New design token</td><td>practice UI adds a token</td><td>snapshot/lint asserts DS v1.3.0 tokens only</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the feature that turns the deterministic engine into a teacher (Claude-07 s3.3): because one input yields exactly one chart, the app diffs the learner's chart against the engine's and points to the exact step they missed - a mis-placed khoa, a mis-derived tam truyen, a mis-seated sao. The engine is never wrong here and never stale, because the answer key is whatever the oracle-gated engine casts, not a maintained key. Keep the grader pure and casting-only: it grades structure against the engine (a fact), and leaves meaning to the cited AI layer (strategy 7). Language is TypeScript (DEC-2) - a typed step ladder plus a pure diff function - rendering in the WEB-001 shell over the CHART views, and reaching the Rust engines only through the normal casting path. refs Claude-07 s3.3.</p>";
