window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-API-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task defines how the API evolves without breaking the clients that depend on it: URL versioning as the primary scheme, a header-versioning option, a deprecation policy with a support window, a changelog of breaking changes, and the invariant that separates what may change (interpretation) from what may not (calculation output). It extends the <code>tamthuc_api</code> package and wraps the TASK-API-001 routes. It owns the versioning scheme and the deprecation mechanics; it does NOT change the endpoint behavior TASK-API-001 defines - it governs how that behavior may change over time.</p>\n<p>The API SHALL version by URL as the primary scheme (<code>/api/v1</code>, <code>/api/v2</code>), and MAY additionally accept a version header as an option for clients that prefer it; when both are present the URL SHALL win. A breaking change - removing or renaming a field, changing a type, changing the meaning of a value, tightening validation - SHALL require a new major version; an additive change (a new optional field, a new endpoint) SHALL NOT. A deprecated field or endpoint SHALL be retained for at least 2-3 versions after deprecation, SHALL continue to function during that window, and SHALL be served with a <code>Deprecation</code> (and where useful <code>Sunset</code>) warning header naming the replacement, so a client is told in-band that it is on a path that will end. Every breaking change SHALL be documented in a <code>CHANGELOG.md</code> with the version, the change, and the migration guidance.</p>\n<p>One invariant SHALL hold across all versioning: the calculation output MUST stay stable while the interpretation MAY vary. The la so envelope (TASK-PLAT-002) and the deterministic chart it carries are a versioned contract that changes only through PLAT-002's own version bump with a migration note; the AI interpretation (RAG-003) is expected to improve and reword across versions and is NOT held to byte-stability. The versioning layer SHALL make this explicit so a client can rely on the chart being reproducible while accepting that the interpretation prose evolves.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>An API with real clients (the web app, a future mobile app, Enterprise API-key integrations) cannot change shape underneath them without breaking them, but it also cannot freeze forever. Versioning is the contract that lets the API evolve on a schedule the clients can follow. URL versioning is chosen as primary because it is the most visible and cache-friendly scheme - the version is right there in the path, a proxy or a log shows it, and there is no ambiguity about which contract a request expects (Grok-49). The header option exists for clients that prefer content negotiation, but the URL wins when both are present so there is always one unambiguous answer. Making breaking-versus-additive an explicit rule keeps the team from shipping a silent breaking change as a point release - a renamed field is a new major version, full stop.</p>\n<p>The deprecation window with an in-band warning header is the difference between an evolution and an outage. Keeping a deprecated field working for 2-3 versions and telling the client, in the response itself, that it is deprecated and what replaces it, gives integrators time to migrate on their own schedule rather than discovering a break in production. The changelog is the human-readable companion to that machine signal. The calculation-stable / interpretation-variable invariant is the versioning expression of the platform's spine (strategy 4.3): the whole product rests on the chart being deterministic and reproducible, so the calculation output is the last thing that may drift casually - it changes only through the TASK-PLAT-002 versioned envelope. The interpretation, by contrast, is supposed to get better, so holding it to byte-stability would freeze the very thing the RAG work is meant to improve. Separating the two tells clients exactly what they can pin against and what they cannot.</p>\n<h2>§3 - Contract (scheme / deprecation / changelog)</h2>\n<h3>Versioning scheme (<code>versioning/router.py</code>, <code>docs/contracts/api-versioning-policy.md</code>)</h3>\n<ul><li>Primary: URL - <code>/api/v1/...</code>, <code>/api/v2/...</code>. Every TASK-API-001 route is mounted under a version prefix.</li><li>Option: a version header (e.g. <code>X-API-Version: 1</code> or an <code>Accept</code> media-type parameter); the URL wins when both are present.</li><li>Breaking change -&gt; new major version. Additive change -&gt; same version. The policy file enumerates what counts as each.</li></ul>\n<h3>Deprecation (<code>versioning/deprecation.py</code>)</h3>\n<pre><code class=\"language-python\"># a deprecated field/endpoint keeps working for &gt;= 2-3 versions and carries warning headers\ndef deprecation_headers(replacement: str, sunset: str | None) -&gt; dict:\n    # Deprecation: true ; Link: &lt;replacement&gt;; rel=\"successor-version\" ; Sunset: &lt;date?&gt;\n    ...</code></pre>\n<table><thead><tr><th>Response header</th><th>Meaning</th></tr></thead><tbody><tr><td><code>Deprecation: true</code></td><td>this field/endpoint is deprecated</td></tr><tr><td><code>Link: &lt;...&gt;; rel=\"successor-version\"</code></td><td>where to migrate</td></tr><tr><td><code>Sunset: &lt;http-date&gt;</code></td><td>when support ends (&gt;= 2-3 versions out)</td></tr></tbody></table>\n<h3>The stability invariant (<code>docs/contracts/api-versioning-policy.md</code>)</h3>\n<table><thead><tr><th>Surface</th><th>Stability</th><th>Changes via</th></tr></thead><tbody><tr><td>calculation output (la so envelope, chart)</td><td>stable, reproducible</td><td>TASK-PLAT-002 <code>envelope_version</code> bump + migration note only</td></tr><tr><td>interpretation (RAG-003 prose)</td><td>may vary / improve</td><td>not version-frozen; not byte-stable</td></tr><tr><td>request/response field set</td><td>additive within a version; breaking -&gt; new major</td><td>this policy + <code>CHANGELOG.md</code></td></tr></tbody></table>\n<h3>Changelog (<code>CHANGELOG.md</code>)</h3>\n<p>Every breaking change recorded with the version, the change, and migration guidance; additive changes noted; the deprecation window stated per deprecated item.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>All TASK-API-001 routes are served under a URL version prefix (<code>/api/v1/...</code>); a request to an unknown version returns the TASK-API-001 error envelope with the correct status, not a silent fallback.</li><li>The header-versioning option is honored when present, and the URL version wins when both a URL and a header version are supplied (a conflict test asserts the precedence).</li><li>A deprecated field/endpoint continues to function and is served with <code>Deprecation</code> (and, where set, <code>Sunset</code>) headers naming the successor; a test asserts the field still works and the headers are present.</li><li>A deprecated item is retained for at least 2-3 versions after deprecation before removal; the policy and <code>CHANGELOG.md</code> record the deprecation and the planned sunset.</li><li>The calculation output is stable across API versions - the la so envelope changes only via a TASK-PLAT-002 <code>envelope_version</code> bump, never as an unversioned API change - while the interpretation is allowed to vary; a test pins the chart and does not pin the interpretation prose.</li><li>Every breaking change is documented in <code>CHANGELOG.md</code> with migration guidance; a CI check fails a breaking change that lacks a changelog entry and a version bump.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_versioning.py</code>: URL routing per version; header option honored; URL-wins-on-conflict; deprecated field still functions and carries the warning headers; unknown-version rejection in the error envelope.</li><li>A stability test: the calculation output (envelope) is byte-stable across two API versions for a golden input, while the interpretation field is explicitly not asserted byte-stable; a change to the envelope shape without a TASK-PLAT-002 version bump fails.</li><li>A changelog/contract check in CI: a route or field change flagged as breaking must have a <code>CHANGELOG.md</code> entry and a version increment, or the check fails.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_api</code>, <code>pytest packages/tamthuc_api</code>; the OpenAPI (TASK-API-001 <code>openapi-v1.md</code>) is emitted per version.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>versioning/router.py</code>: mount the TASK-API-001 routes under <code>/api/v{n}</code>; resolve the effective version (URL primary, header option, URL wins); reject unknown versions in the error envelope.</li><li><code>versioning/deprecation.py</code>: the <code>Deprecation</code>/<code>Link</code>/<code>Sunset</code> header helper and a decorator to mark a field/endpoint deprecated with its successor and sunset.</li><li><code>docs/contracts/api-versioning-policy.md</code>: the breaking-vs-additive rules, the 2-3 version support window, and the calculation-stable / interpretation-variable invariant.</li><li><code>CHANGELOG.md</code>: seed with v1; establish the entry format; wire the CI check that a breaking change carries an entry + a version bump.</li><li>Confirm the calculation-stability invariant against TASK-PLAT-002 (the envelope changes only via its own version bump).</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-API-001 (the routes, the error envelope, and the OpenAPI contract this task versions and wraps; unknown-version rejection uses the API-001 envelope). Enforces the calculation-stability half of the invariant against the TASK-PLAT-002 la so envelope contract (the chart changes only via <code>envelope_version</code>, never as an unversioned API change), while leaving the TASK-RAG-003 interpretation free to vary. Coordinates with TASK-PLAT-004 (the CI check for changelog + version bump on a breaking change runs in the pipeline) and TASK-WEB-006 (the frontend/i18n consumers pin to a version). Blocks nothing directly; it is a governance layer over the existing gateway.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code># URL versioning (primary); a deprecated field still works but is flagged\nGET /api/v1/knowledge/patterns?system=qimen\nHTTP/1.1 200 OK\nDeprecation: true\nLink: &lt;/api/v2/knowledge/patterns&gt;; rel=\"successor-version\"\nSunset: Wed, 08 Jul 2026 00:00:00 GMT</code></pre>\n<pre><code class=\"language-json\">// CHANGELOG.md entry (abridged) - a breaking change with migration guidance\n{ \"version\": \"v2\", \"breaking\": [\n  { \"change\": \"renamed response field 'cach_cuc' array item 'score' -&gt; 'weight'\",\n    \"migrate\": \"read 'weight'; 'score' remains in v1 until sunset\" } ] }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Header scheme specifics: a custom <code>X-API-Version</code> vs an <code>Accept</code> media-type parameter (<code>application/vnd.tamthuc.v2+json</code>). Default: a simple <code>X-API-Version</code> header as the option, with the URL as primary and authoritative; the media-type scheme is a later addition if a client needs strict content negotiation.</li><li>Support-window length: 2 vs 3 versions. Default: retain a deprecated item for 3 versions where cheap and at least 2 always; the exact window per item is stated in the changelog and the <code>Sunset</code> header, not left implicit.</li><li>Whether Enterprise API-key clients get a longer deprecation window than the web app. Default: one policy for all clients at MVP; a negotiated longer window for Enterprise integrations is a later contractual option that reuses the same <code>Sunset</code> mechanism.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Silent breaking change</td><td>a field renamed/removed within a version</td><td>forbidden; a breaking change requires a new major version + a CHANGELOG entry; CI fails otherwise</td></tr><tr><td>Abrupt removal</td><td>a deprecated item removed with no window</td><td>retained &gt;= 2-3 versions with <code>Deprecation</code>/<code>Sunset</code> headers naming the successor</td></tr><tr><td>Ambiguous version</td><td>URL and header disagree</td><td>the URL wins; a conflict test asserts the precedence</td></tr><tr><td>Unknown version guessed</td><td>a request to an unsupported version silently served</td><td>rejected in the TASK-API-001 error envelope, not a silent fallback</td></tr><tr><td>Calculation drift</td><td>the chart/envelope changes as an unversioned API change</td><td>forbidden; the calculation output changes only via a TASK-PLAT-002 <code>envelope_version</code> bump</td></tr><tr><td>Frozen interpretation</td><td>interpretation held to byte-stability</td><td>not required; interpretation may vary/improve across versions and is not version-frozen</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task is governance, not new behavior: it lets the API-001 gateway evolve without breaking its clients. Hold the scheme (URL primary, header optional, URL wins), the window (a deprecated item lives &gt;= 2-3 versions behind <code>Deprecation</code>/<code>Sunset</code> headers and a CHANGELOG entry), and above all the invariant that separates the two halves of the platform - calculation output is stable and reproducible, changing only through the TASK-PLAT-002 versioned envelope, while interpretation is free to improve. That split is the versioning face of the deterministic-engine / AI-layer boundary (strategy 4.3): clients pin to the chart, not to the prose. It extends the same <code>tamthuc_api</code> app as TASK-API-001/003/004, so the gateway stays one installable, mypy-clean unit.</p>";
