window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-RAG-006"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task is the measurable-quality gate for interpretation: it scores TASK-RAG-003's output on faithfulness, relevance, and citation accuracy against the TASK-KB-002 validation set of 150-200 cited cases, compares to a committed baseline, and fails CI on a regression beyond a threshold. It is the RISK-9 mitigation made executable - the mechanism by which interpretation regressions are caught before they ship (strategy RISK-9; Claude-06 s4.3). It scores the interpreter; it does not change it, and it produces no user-facing interpretation.</p>\n<p>The eval set SHALL be the TASK-KB-002 <code>validation_cases()</code> projection: each seeded pattern becomes a case carrying <code>id</code>, <code>version</code>, <code>system</code>, <code>conditions</code>, <code>expected_polarity</code>, <code>meaning_classical</code>, and <code>expected_citations</code>. For each case the loop SHALL run TASK-RAG-003's <code>interpret</code> over a retrieval context built from the case, and SHALL score three metrics: (1) faithfulness - every asserted claim is grounded in the retrieved passages it cites, with a deterministic lower bound (every claim carries a citation present in the retrieved set) and an optional stronger judge; (2) relevance - the reading addresses the case's question and conveys its <code>expected_polarity</code>; (3) citation accuracy - precision and recall of the emitted <code>citation_id</code>s against the case's <code>expected_citations</code>. Each case SHALL yield a <code>CaseScore</code>; the run SHALL aggregate an <code>EvalReport</code> stamped with the <code>prompt_version</code> under test.</p>\n<p>The gate SHALL fail when any aggregate metric falls below its absolute threshold, OR when any metric regresses beyond a delta versus the committed baseline for the same <code>prompt_version</code> lineage. The baseline SHALL be a committed artifact (<code>eval/baseline.json</code>) updated only as a reviewed diff, so a prompt or retrieval change that lowers quality is a failing check, not a silent ship. The default CI eval SHALL be deterministic - TASK-RAG-003 run with a stub or pinned LLM over the committed fixture cases - so the gate is reproducible; the full-set eval against a real pinned model SHALL run behind a marker. The loop SHALL stamp the exact <code>(case_id, case_version)</code> and <code>prompt_version</code> it scored, so a regression is always traceable to the versioned cases and prompt that produced it.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Interpretation quality is the one thing this product cannot afford to leave unmeasured, because the failure is invisible: a prompt tweak or a retrieval change can make readings subtly less grounded or less on-point, and nothing breaks, no test goes red, and the regression ships (strategy RISK-9). The only defense is a fixed corpus of known-answer cases scored every build, which is exactly why TASK-KB-002 was authored to double as the validation set: each pattern already pairs a machine-checkable condition with a cited classical meaning, so the same 150-200 rows that the engine detects are the answer key the interpreter is graded against. This task turns that answer key into a gate. Faithfulness, relevance, and citation accuracy are the three things that can silently rot, so they are the three things measured; a committed baseline turns \"is it still as good as last release\" from a memory into a diff.</p>\n<p>The determinism discipline is what makes the gate trustworthy rather than flaky. A gate that depends on a live model's stochastic output would fail randomly and get muted, defeating its purpose, so the CI gate runs on a stub or pinned interpreter over committed cases and scores primarily on reproducible signals - citation-in-retrieved, expected-citation overlap, polarity conveyed. The stronger semantic judge (does this claim actually follow from this passage) is real and worth having, but it is advisory and marker-gated, reported as a tracked band rather than a hard pass, because it cannot be both non-deterministic and a blocking gate. Stamping the case and prompt versions is the last piece: because TASK-KB-004 versions the cases and TASK-RAG-003 versions the prompt, a regression report points at the exact versioned inputs, so the fix is targeted rather than a hunt. This task is where the two invariants TASK-RAG-003 rests on - never assert beyond the sources, never cite what was not retrieved - stop being rules and become a number that must not drop.</p>\n<h2>§3 - Contract (cases, metrics, judge, run, gate)</h2>\n<h3>Eval case (<code>tamthuc_rag/eval/cases.py</code>)</h3>\n<pre><code class=\"language-python\">class EvalCase(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    id: str; version: int                 # stamped from TASK-KB-002 (case_id, case_version)\n    system: System\n    query: str                            # a probe question that should trigger this pattern\n    conditions: dict                      # the pattern conditions (to build the retrieval context)\n    expected_polarity: str                # cat | hung | trung\n    meaning_classical: str\n    expected_citations: list[str]\n\ndef load_cases(system: System | None = None) -&gt; list[EvalCase]:\n    # adapt TASK-KB-002 validation_cases() -&gt; EvalCase; build a synthetic ChartContext/RetrievalRequest\n    # per case so TASK-RAG-003.interpret can run without a live engine cast.</code></pre>\n<h3>Metrics (<code>tamthuc_rag/eval/metrics.py</code>)</h3>\n<pre><code class=\"language-python\">class CaseScore(BaseModel):\n    case_id: str; case_version: int\n    faithfulness: float          # 0..1 (deterministic lower bound; judge refines behind a marker)\n    relevance: float             # 0..1 (polarity conveyed + question addressed)\n    citation_precision: float    # emitted ids that are expected / emitted\n    citation_recall: float       # expected ids that are emitted / expected\n    passed: bool\n\ndef faithfulness(interp, retrieved) -&gt; float:  # every claim carries a citation in the retrieved set\ndef relevance(interp, case) -&gt; float:           # expected_polarity conveyed; recommendation on-topic\ndef citation_scores(interp, expected) -&gt; tuple[float, float]:  # precision, recall vs expected_citations</code></pre>\n<h3>Optional judge (<code>tamthuc_rag/eval/judge.py</code>)</h3>\n<pre><code class=\"language-python\">class Judge(Protocol):\n    version: str                 # pinned rubric + judge-prompt version, like TASK-RAG-003's prompt_version\n    def entails(self, claim: str, passage: str) -&gt; float: ...   # NLI / LLM-judge, temperature 0\n\n# advisory, marker-gated: refines faithfulness/relevance; reported as a band, never the hard gate.</code></pre>\n<h3>Run and report (<code>tamthuc_rag/eval/run.py</code>, <code>eval/baseline.json</code>)</h3>\n<pre><code class=\"language-python\">class EvalReport(BaseModel):\n    prompt_version: str\n    judge_version: str | None\n    n_cases: int\n    faithfulness: float; relevance: float\n    citation_precision: float; citation_recall: float; citation_f1: float\n    per_case: list[CaseScore]\n    regressions: list[str]       # case_ids that dropped vs baseline\n    baseline_ref: str | None\n\nclass Thresholds(BaseModel):\n    faithfulness: float = 0.90; relevance: float = 0.80; citation_f1: float = 0.85\n    max_regression_delta: float = 0.03\n\ndef evaluate(cases, interpret_fn, thresholds, baseline, judge=None) -&gt; EvalReport: ...\ndef gate(report: EvalReport, thresholds: Thresholds, baseline: EvalReport) -&gt; GateResult:\n    # fail if any aggregate &lt; its absolute threshold, OR any metric regresses &gt; max_regression_delta\n    # vs the committed baseline for this prompt_version lineage; name the failing metric + regressed cases.</code></pre>\n<p>The baseline is committed per <code>prompt_version</code>; a prompt change (new <code>prompt_version</code> from TASK-RAG-003) requires a re-run and a reviewed baseline update. The gate runs in CI (TASK-PLAT-004).</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>load_cases</code> returns one <code>EvalCase</code> per TASK-KB-002 seeded pattern with its <code>(id, version)</code> stamp and <code>expected_citations</code>; the count lands in the 150-200 band for the full set.</li><li><code>faithfulness</code> scores 1.0 for an interpretation whose every claim cites a retrieved id and below 1.0 when a claim is uncited or cites outside the retrieved set (the deterministic lower bound tracks TASK-RAG-003's guard).</li><li><code>citation_precision</code>/<code>recall</code> are correct against a case's <code>expected_citations</code> (a hand-built case with known emitted vs expected ids scores as computed); <code>relevance</code> reflects whether the <code>expected_polarity</code> is conveyed.</li><li><code>evaluate</code> aggregates an <code>EvalReport</code> stamped with <code>prompt_version</code>; <code>gate</code> fails when an aggregate is below threshold and when a metric regresses beyond <code>max_regression_delta</code> versus the committed baseline, naming the failing metric and regressed <code>case_id</code>s.</li><li>The default CI eval is deterministic: run with a stub/pinned interpreter over <code>fixtures/eval_cases_sample.json</code>, repeated runs produce an identical <code>EvalReport</code>; the real-model full-set eval is marker-gated.</li><li>A prompt change that lowers faithfulness on the fixture makes the gate fail until the baseline is updated as a reviewed diff (the RISK-9 regression catch).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_eval.py</code>: metric correctness on hand-built cases (faithful vs uncited-claim vs fabricated-citation interpretations); citation precision/recall math; relevance/polarity scoring; <code>gate</code> pass and both fail paths (below-threshold and regression-vs-baseline) with the failing metric and regressed cases named; determinism of the stub eval (repeat runs equal).</li><li>Baseline discipline: a test asserts a bumped <code>prompt_version</code> requires a matching baseline entry and that an unreviewed baseline drop fails the gate; <code>eval/baseline.json</code> validates against the committed report shape.</li><li>Judge isolation: the marker-gated judge path is exercised with a stub <code>Judge</code> (fixed scores) so CI needs no live model; the judge contributes an advisory band, not the hard gate.</li><li>Contract: <code>EvalReport</code> / <code>CaseScore</code> validate against <code>docs/contracts/eval-report.schema.json</code>; Pydantic/JSON-Schema parity in CI.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_rag</code>, <code>pytest packages/tamthuc_rag</code>; the interpretation-quality gate itself runs in CI on the fixture and behind a marker on the full set.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>eval/cases.py</code>: adapt TASK-KB-002 <code>validation_cases()</code> to <code>EvalCase</code>, build the per-case synthetic <code>RetrievalRequest</code>/context so TASK-RAG-003 runs without a live engine cast.</li><li><code>eval/metrics.py</code>: <code>faithfulness</code> (deterministic lower bound), <code>relevance</code> (polarity + on-topic), <code>citation_scores</code> (precision/recall), <code>CaseScore</code>.</li><li><code>eval/judge.py</code>: the <code>Judge</code> protocol and a stub; the marker-gated LLM/NLI judge with a pinned rubric version.</li><li><code>eval/run.py</code>: <code>evaluate</code>, <code>EvalReport</code>, <code>Thresholds</code>, <code>gate</code> (absolute + regression-vs-baseline); commit an initial <code>eval/baseline.json</code>.</li><li>Author <code>docs/contracts/eval-report.schema.json</code>; wire the CI gate (fixture deterministic; full set marker-gated) into TASK-PLAT-004.</li><li>Commit <code>fixtures/eval_cases_sample.json</code> (a handful of KB-002-shaped cases across systems, including a deliberately uncited-claim case) as the deterministic test exemplar.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-RAG-003 (the interpreter it scores; it consumes the <code>prompt_version</code> and <code>retrieved_citation_ids</code> hooks TASK-RAG-003 exposes for exactly this purpose) and TASK-KB-002 (the 150-200 cited <code>validation_cases()</code> that are the answer key - authored to double as this RISK-9 dataset). Reads retrieval quality via TASK-RAG-001/002 indirectly and provides the number that decides TASK-RAG-005's expansion trade-off (recall lift vs faithfulness/precision). Runs its gate in CI through TASK-PLAT-004; stamps the TASK-KB-004-versioned case versions so regressions trace to exact inputs. Blocks nothing in the catalog, but it is the gate every interpretation-affecting change must pass. The RAG-branch invariant holds: this task scores interpretations and never produces a user-facing one; it never writes <code>ban</code>/<code>cach_cuc</code>/<code>lich_phap</code>/<code>co_truong_phai</code>, and it measures - it does not weaken - the citation and AIDisclosure discipline.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// an EvalCase projected from a TASK-KB-002 pattern\n{ \"id\": \"qimen_thanh_long_hoi_dau\", \"version\": 2, \"system\": \"qimen\",\n  \"query\": \"co nen khoi su kinh doanh trong cua so nay khong\",\n  \"conditions\": { \"type\": \"and\", \"rules\": [ { \"field\": \"cach_cuc\", \"operator\": \"contains\",\n    \"value\": \"qimen_thanh_long_hoi_dau\" } ] },\n  \"expected_polarity\": \"cat\", \"meaning_classical\": \"丙加值符, cát khí tụ tập, lợi cho khởi sự.\",\n  \"expected_citations\": [\"yba_dieu_012\"] }</code></pre>\n<pre><code class=\"language-json\">// an EvalReport (abbreviated) that fails the gate on a citation-accuracy regression\n{ \"prompt_version\": \"sys-1.1.0\", \"judge_version\": null, \"n_cases\": 172,\n  \"faithfulness\": 0.94, \"relevance\": 0.86, \"citation_precision\": 0.79,\n  \"citation_recall\": 0.83, \"citation_f1\": 0.81,\n  \"regressions\": [\"qimen_phi_dieu_diet_huyet\", \"liuren_nguyen_thai\"],\n  \"baseline_ref\": \"sys-1.0.0\" }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>How much of faithfulness/relevance the deterministic checks can carry versus the judge. Default: the hard gate uses reproducible signals (citation-in-retrieved, expected-citation overlap, polarity conveyed); the semantic judge is advisory/marker-gated. Promote a judge signal to the hard gate only if it proves stable at temperature 0 with a tolerance.</li><li>Absolute thresholds and the regression delta. Default: faithfulness 0.90, relevance 0.80, citation_f1 0.85, max regression 0.03; set the first real numbers from the initial full-set run and ratchet upward, never down without review.</li><li>Whether per-case queries are authored or generated from the pattern. Default: author a probe query per flagship pattern; template the rest from <code>conditions</code> + <code>meaning_classical</code>; keep the probe queries versioned with the cases (TASK-KB-004).</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Silent quality regression</td><td>a prompt/retrieval change lowers grounding</td><td>gate fails on below-threshold or regression-vs-baseline; naming the metric + cases (RISK-9)</td></tr><tr><td>Flaky gate</td><td>eval depends on live-model stochasticity</td><td>default gate is deterministic (stub/pinned interpreter, reproducible signals); judge is advisory/marker-gated</td></tr><tr><td>Baseline gamed</td><td>baseline lowered to pass a bad change</td><td>baseline is a committed reviewed diff; an unreviewed drop fails; ratchet up, not down</td></tr><tr><td>Ruleset/test-set drift</td><td>cases diverge from the loaded patterns</td><td>cases derive from TASK-KB-002 <code>validation_cases()</code>, so they are the loaded rows by construction</td></tr><tr><td>Untraceable regression</td><td>a drop with no versioned inputs</td><td>every score stamps <code>(case_id, case_version)</code> + <code>prompt_version</code>; regressions point at exact inputs</td></tr><tr><td>Judge masquerades as gate</td><td>non-deterministic judge blocks CI</td><td>judge reports a band, never the hard pass; only reproducible metrics gate</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task is where TASK-RAG-003's two invariants become a measured number: no claim beyond the sources, no citation the retriever did not supply, scored every build against the same 150-200 cited cases the engine detects. Its credibility rests on determinism - keep the hard gate reproducible and the semantic judge advisory - and on the committed baseline, which turns \"still as good as last release\" into a diff a human signs. Wire it into CI as soon as TASK-RAG-003 and TASK-KB-002 exist; it is the difference between interpretation quality being managed and being hoped for (RISK-9). The package <code>tamthuc_rag</code> is shared with TASK-RAG-001/002/003/004/005/007; this task adds the <code>eval/</code> module, so the RAG branch stays one installable, mypy-clean unit.</p>";
