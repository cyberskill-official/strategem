window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLAT-010"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task is infrastructure as code: the runtime environment defined as versioned, reviewable Terraform and Kubernetes manifests, with horizontal autoscaling for the stateless calculation services and Celery workers for background tasks. It replaces the compose-based staging bootstrap TASK-PLAT-004 introduced with a declared, reproducible topology behind the same pipeline stages. It owns the infrastructure definitions and the autoscaling/worker topology; it does NOT own the pipeline that applies them (TASK-PLAT-004), the metrics that drive scaling decisions (TASK-PLAT-005), or the backup/DR policy on the data tier (TASK-PLAT-009), though it provisions the substrate all three run on.</p>\n<p>All infrastructure SHALL be declared as code: the network, the managed Postgres (TASK-PLAT-003), the Redis instance (TASK-PLAT-006), and the compute SHALL be provisioned by Terraform, and the three deployables (the Rust engine service, the Python API, the Next.js web app) SHALL run as Kubernetes workloads defined by checked-in manifests. No production resource SHALL be created or modified by hand outside the IaC; a change to the infrastructure is a change to a file, reviewed and applied through the TASK-PLAT-004 pipeline. The stateless calculation services (the engine service and the API) SHALL scale horizontally via a HorizontalPodAutoscaler keyed on CPU/latency, so load is met by adding replicas rather than by manual intervention; stateful components (Postgres, Redis) are managed services, not autoscaled pods.</p>\n<p>Background tasks SHALL run on Celery workers separate from the request-serving pods, so a long job (a batch report, a warming pass, a scheduled drill) never blocks the hot path. Batch report generation (TASK-REPORT-002) SHALL be a Celery task, not a synchronous request. The manifests SHALL be environment-parameterized (staging vs production) from the same source with per-environment values, SHALL set resource requests/limits, health/readiness probes, and SHALL source secrets from the secret manager (TASK-PLAT-007), never from a committed value. The IaC SHALL be idempotent and drift-detectable: a plan against the live state SHALL report no unexpected drift, and applying twice SHALL be a no-op the second time.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Hand-built infrastructure is the thing you cannot rebuild. The compose-based staging target TASK-PLAT-004 bootstrapped is fine for getting a pipeline real, but a production surface that real users and sensitive data depend on needs to be reproducible, reviewable, and rebuildable from source - which is exactly what Terraform + Kubernetes manifests provide (Grok-22, Grok-39). Declaring the whole topology as code means a disaster-recovery rebuild (TASK-PLAT-009) is <code>terraform apply</code> against a new region rather than an archaeology project, an audit of what is running is a <code>git log</code>, and an infrastructure change gets the same review a code change gets. The rule that no production resource is touched by hand is what keeps the code and the reality in sync; the moment someone clicks a console button, the IaC is a lie.</p>\n<p>Autoscaling the stateless calculation services is the right shape because those services are exactly stateless - a chart cast is a pure function of its input, so more load is simply more replicas, and the HPA turns a traffic spike into pods rather than a page. The stateful pieces stay managed services precisely because they are not horizontally trivial. Separating Celery workers from the request pods is the standard move that keeps a heavy batch report or a warming pass from stealing latency from a user waiting on a chart: background work belongs on background workers, and making batch report generation a Celery task (rather than a synchronous endpoint) is what lets the API stay responsive under a report load. Parameterizing staging and production from one source keeps the two environments honest - they differ by values, not by drift.</p>\n<h2>§3 - Contract (Terraform / K8s / autoscaling / workers)</h2>\n<h3>Terraform (<code>deploy/terraform/*</code>)</h3>\n<table><thead><tr><th>File</th><th>Provisions</th></tr></thead><tbody><tr><td><code>main.tf</code></td><td>providers, backend/state, module wiring</td></tr><tr><td><code>network.tf</code></td><td>VPC/subnets, security groups, the multi-AZ layout (TASK-PLAT-009 topology)</td></tr><tr><td><code>database.tf</code></td><td>managed Postgres (TASK-PLAT-003), automated backups + PITR hooks (TASK-PLAT-009)</td></tr><tr><td><code>redis.tf</code></td><td>managed Redis (TASK-PLAT-006 cache + TASK-API-003 counters)</td></tr><tr><td><code>variables.tf</code></td><td>per-environment parameters (staging vs production)</td></tr></tbody></table>\n<h3>Kubernetes workloads (<code>deploy/k8s/*</code>)</h3>\n<pre><code class=\"language-yaml\"># hpa.yaml (abridged) - autoscale the stateless calculation services\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata: { name: tamthuc-api }\nspec:\n  scaleTargetRef: { kind: Deployment, name: tamthuc-api }\n  minReplicas: 2\n  maxReplicas: 20\n  metrics:\n    - type: Resource\n      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }</code></pre>\n<ul><li><code>engine-deployment.yaml</code>, <code>api-deployment.yaml</code>: the stateless services, each with resource requests/limits, readiness/liveness probes, and an HPA (<code>hpa.yaml</code>).</li><li><code>web-deployment.yaml</code>: the Next.js app.</li><li><code>celery-worker.yaml</code>: background workers (batch reports, warming, scheduled jobs), separate from request pods, scaled independently.</li><li><code>ingress.yaml</code>: routing + TLS termination (TASK-PLAT-007 TLS 1.3).</li></ul>\n<h3>Background tasks (Celery)</h3>\n<p>Batch report generation (TASK-REPORT-002), cache warming (TASK-PLAT-006), and scheduled ops jobs run as Celery tasks on the worker deployment, brokered via Redis; a task never runs on a request-serving pod.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>terraform plan</code> against an empty target proposes the full topology (network, managed Postgres, managed Redis, compute) and <code>terraform apply</code> provisions it; a second <code>apply</code> is a no-op (idempotent).</li><li>The three deployables run as Kubernetes workloads from the checked-in manifests with resource requests/limits and readiness/liveness probes; a rollout is declarative.</li><li>The stateless calculation services (engine, API) scale out under load via the HPA (replicas increase past <code>minReplicas</code> toward <code>maxReplicas</code>) and scale back in when load drops; a load test demonstrates it.</li><li>Celery workers run on a separate deployment; a batch report (TASK-REPORT-002) executes as a background task and does not block or degrade the request-serving pods (a report load leaves chart-cast latency within SLO).</li><li>Staging and production are parameterized from one source (differ by values only); a drift check reports no unexpected drift between the manifests/state and the live environment.</li><li>No production resource exists outside the IaC, and no secret is a committed value (secrets sourced from the TASK-PLAT-007 manager); a scan confirms both.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li>A CI <code>terraform validate</code> + <code>terraform plan</code> (against a test/staging state) and a <code>kubeval</code>/<code>kubeconform</code> lint of the manifests run in the TASK-PLAT-004 pipeline; a <code>plan</code> showing unexpected drift fails.</li><li>A load test in staging drives the HPA to add replicas and confirms scale-in afterward; the p95 chart-cast latency (TASK-PLAT-005) stays within SLO during the spike.</li><li>A background-task test enqueues a batch report and confirms it runs on a Celery worker, not a request pod, and does not raise request-path latency.</li><li>A drift/secret check: <code>terraform plan</code> is clean on an unchanged tree; a scan confirms no committed secret and no hand-created production resource.</li><li>Gates: <code>terraform validate</code>, <code>tflint</code>, <code>kubeconform</code>, and the pipeline lint stages; the manifests deploy cleanly to staging behind the existing TASK-PLAT-004 stages.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>deploy/terraform/*</code>: providers + remote state; the network (multi-AZ), managed Postgres, and managed Redis modules; per-environment variables.</li><li><code>deploy/k8s/*-deployment.yaml</code>: the engine, API, and web workloads with probes and resource requests/limits; <code>ingress.yaml</code> with TLS termination.</li><li><code>deploy/k8s/hpa.yaml</code>: HPAs on the stateless engine + API services keyed on CPU/latency.</li><li><code>deploy/k8s/celery-worker.yaml</code>: the background-worker deployment (batch reports, warming, scheduled jobs), brokered via Redis, scaled independently.</li><li>Parameterize staging vs production from one source; source secrets from the TASK-PLAT-007 manager.</li><li>Wire the IaC apply behind the TASK-PLAT-004 deploy stages (replacing the compose bootstrap) and add the validate/plan/lint gates; document in <code>deploy/README-iac.md</code>.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-PLAT-004 (the pipeline that validates and applies this IaC; PLAT-010 swaps the compose staging target for the declared topology behind the same deploy stages). Provisions the substrate for TASK-PLAT-003 (managed Postgres), TASK-PLAT-006 (managed Redis, also the TASK-API-003 counter store and the Celery broker), and TASK-PLAT-009 (the multi-AZ + cross-region topology the backup/DR policy runs on). Feeds and is fed by TASK-PLAT-005 (autoscaling decisions read latency/CPU metrics; the HPA is observable there) and hosts the TASK-REPORT-002 batch reports as Celery tasks. Sources secrets from TASK-PLAT-007.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-yaml\"># celery-worker.yaml (abridged) - background tasks off the hot path\napiVersion: apps/v1\nkind: Deployment\nmetadata: { name: tamthuc-celery }\nspec:\n  replicas: 2\n  template:\n    spec:\n      containers:\n        - name: worker\n          image: ghcr.io/cyberskill/tamthuc-api:${SHA}\n          command: [\"celery\", \"-A\", \"tamthuc_api.tasks\", \"worker\", \"-Q\", \"reports,warming,ops\"]</code></pre>\n<pre><code># infra change flow - never by hand\nedit deploy/terraform/*.tf  -&gt;  PR  -&gt;  terraform plan (reviewed)  -&gt;  merge  -&gt;  pipeline apply</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Kubernetes distribution / managed vs self-hosted, and single vs multi-cloud. Default: a managed Kubernetes on the same cloud as the managed Postgres/Redis at MVP; the manifests stay portable so the cluster provider is a values change. Aligns with the strategy 4.1 \"any managed Postgres works\" posture.</li><li>HPA signal: CPU-only vs custom latency/queue-depth metrics. Default: CPU utilization first (simple, works with stock HPA), adding a custom latency or Celery-queue-depth metric via the TASK-PLAT-005 metrics once the scaling behavior is understood.</li><li>Whether the compose bootstrap is retired or kept for local dev. Default: keep a compose file for local development, but production/staging are Kubernetes from the IaC; the compose staging target TASK-PLAT-004 bootstrapped is retired once this lands.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Hand-built infra</td><td>a production resource created via a console</td><td>forbidden; all infra is Terraform/K8s from source; a drift check catches out-of-band resources</td></tr><tr><td>Config drift</td><td>manifests/state diverge from the live environment</td><td><code>terraform plan</code> reports drift and fails CI; apply-twice is a no-op</td></tr><tr><td>Hot path blocked by batch work</td><td>a batch report runs on a request pod</td><td>background tasks run on separate Celery workers; a report load leaves chart-cast latency within SLO</td></tr><tr><td>No autoscale under load</td><td>fixed replicas, manual scaling</td><td>the stateless services carry an HPA; a spike adds replicas, not a page</td></tr><tr><td>Secret in a manifest</td><td>a credential committed as a value</td><td>forbidden; secrets sourced from the TASK-PLAT-007 manager; a scan confirms none committed</td></tr><tr><td>Staging/prod divergence</td><td>environments defined separately</td><td>one parameterized source; environments differ by values, not by drift</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task turns the running system into code: Terraform for the substrate (network, managed Postgres, managed Redis, compute), Kubernetes manifests for the three deployables, an HPA on the stateless calculation services, and Celery workers for background tasks so batch reports never steal latency from a chart cast. The inviolable rule is that no production resource lives outside the IaC - an infra change is a reviewed file change applied through the TASK-PLAT-004 pipeline, which is what keeps the code and the reality in sync and makes a TASK-PLAT-009 DR rebuild an <code>apply</code> rather than an excavation. It is pure IaC, parameterized for staging and production from one source, sourcing secrets from the TASK-PLAT-007 manager.</p>";
