window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-RULE-002"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task defines the condition DSL - the small JSON language a pattern's <code>conditions</code> field is written in - and a deterministic evaluator that runs it against a la so envelope. This is step 4 of the nine-step query flow: the rule engine scans the chart and detects patterns (strategy 4.2). The module SHALL parse the DSL, SHALL evaluate it against a la so with no side effects and no panics, and SHALL emit each matching pattern as a <code>CachCuc</code> (the envelope type from TASK-PLAT-002) carrying the pattern id, name, palace, polarity, score, and citations.</p>\n<p>The DSL SHALL support boolean composition by <code>and</code>, <code>or</code>, and <code>not</code>, nestable to arbitrary depth, over leaf rules. A leaf rule SHALL be <code>{ \"field\": &lt;path&gt;, \"operator\": &lt;op&gt;, \"value\": &lt;json&gt; }</code> with operators <code>eq</code>, <code>in</code>, <code>gte</code>, <code>lte</code>, <code>exists</code>, and <code>contains</code>. <code>field</code> is a dotted path resolved against the la so (default root <code>ban</code>, escaping to other envelope sections by a reserved first segment, per section 3). The evaluator SHALL be a pure function of <code>(conditions, la_so)</code>: identical inputs SHALL always yield the identical boolean, and evaluation SHALL NOT mutate the chart (the AI/interpretation branch never writes <code>ban</code>, <code>cach_cuc</code>, <code>lich_phap</code>, or <code>co_truong_phai</code>, and neither does this deterministic detector - it only reads and appends detected patterns).</p>\n<p>A missing field path SHALL evaluate to a defined result, never an error or panic: for <code>eq</code>, <code>in</code>, <code>gte</code>, <code>lte</code>, and <code>contains</code> a missing or null path yields <code>false</code>; <code>exists</code> is the explicit presence test. Scoring: a matched pattern's score SHALL default to the pattern <code>confidence</code> (TASK-RULE-001); the DSL MAY carry an optional per-node <code>weight</code> reserved for TASK-RULE-004, ignored at MVP. This task provides the parser, the path resolver, the evaluator, and the scorer; TASK-RULE-003 wraps them with loading, caching, and per-system filtering.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>A general boolean-tree DSL with a handful of operators is exactly enough to express the cach cuc and khoa the conditions the Claude source lists (Claude-06 s2.3) and the pattern-as-data approach the Grok source specifies (Grok-31), while staying small enough to evaluate deterministically and audit by eye. QiMen cat/hung patterns are overwhelmingly conjunctions of positional facts (\"this door on this palace with this star\"), which is <code>and</code> over <code>in</code>/<code>eq</code> leaves; the rarer exclusions need <code>not</code>; alternatives need <code>or</code>. Six operators cover equality, set membership, ordered thresholds (wang-suy strength, scores), presence, and array/substring membership. Anything the DSL cannot express is a signal that the fact belongs in the engine output (<code>ban</code>) as a first-class field, not that the DSL needs more power - keeping the language small is what keeps it deterministic and reviewable.</p>\n<p>The defined-on-missing rule matters more than it looks. Charts across systems and school flags do not all carry the same fields; a QiMen-only path is absent from a LiuRen chart. If a missing path threw, a cross-system or <code>all</code> pattern would panic on half the charts; if it returned an ambiguous value, matching would be non-deterministic. Making a missing path cleanly <code>false</code> (except under the explicit <code>exists</code>) means a pattern simply does not fire where its inputs are absent, which is the correct behavior and keeps the evaluator total. Resolving paths against the whole envelope (not just <code>ban</code>) lets a QiMen pattern gate on <code>lich_phap.tiet_khi</code> or a flag in <code>co_truong_phai</code> without the engine having to copy those into <code>ban</code>.</p>\n<h2>§3 - Contract (DSL grammar + evaluator)</h2>\n<h3>JSON grammar</h3>\n<p>A condition node is one of four forms. Connective nodes are tagged by <code>type</code>; a leaf node has no <code>type</code> and carries <code>field</code>.</p>\n<pre><code class=\"language-json\">{ \"type\": \"and\", \"rules\": [ &lt;node&gt;, &lt;node&gt;, ... ] }\n{ \"type\": \"or\",  \"rules\": [ &lt;node&gt;, &lt;node&gt;, ... ] }\n{ \"type\": \"not\", \"rule\":  &lt;node&gt; }\n{ \"field\": \"&lt;dotted.path&gt;\", \"operator\": \"eq|in|gte|lte|exists|contains\", \"value\": &lt;json&gt; }</code></pre>\n<p>Worked example (the QiMen leaf pair from the design):</p>\n<pre><code class=\"language-json\">{ \"type\": \"and\", \"rules\": [\n  { \"field\": \"door\", \"operator\": \"in\", \"value\": [\"Sinh\", \"Khai\", \"Tu\"] },\n  { \"field\": \"star\", \"operator\": \"in\", \"value\": [\"At\", \"Binh\", \"Dinh\"] }\n] }</code></pre>\n<h3>Field paths (<code>crates/cyberos-rule/src/path.rs</code>)</h3>\n<p>Dotted segments with numeric indices for arrays, resolved over <code>serde_json::Value</code>. Root rule: if the first segment is a reserved envelope key (<code>lich_phap</code>, <code>cach_cuc</code>, <code>co_truong_phai</code>, <code>he</code>, <code>dau_vao</code>, <code>provenance</code>), the path resolves from the envelope root; otherwise it resolves from <code>ban</code> (so bare <code>door</code>, <code>star</code>, <code>cung.1.mon</code> address the plate). Examples:</p>\n<ul><li><code>door</code> -&gt; <code>ban.door</code></li><li><code>cung.1.thien_ban.can</code> -&gt; <code>ban.cung[1].thien_ban.can</code></li><li><code>lich_phap.tiet_khi.hien_hanh</code> -&gt; envelope root, the in-force tiet khi</li><li><code>co_truong_phai.pan_method</code> -&gt; the stamped school flag</li></ul>\n<pre><code class=\"language-rust\">pub fn resolve&lt;'a&gt;(path: &amp;str, laso: &amp;'a serde_json::Value) -&gt; Option&lt;&amp;'a serde_json::Value&gt;;</code></pre>\n<h3>Operators</h3>\n<table><thead><tr><th>Operator</th><th>Semantics</th><th>Missing/null path</th></tr></thead><tbody><tr><td><code>eq</code></td><td>field == value (JSON equality)</td><td>false</td></tr><tr><td><code>in</code></td><td>value is an array; field is a member</td><td>false</td></tr><tr><td><code>gte</code></td><td>field &gt;= value (numbers, or strings lexicographically)</td><td>false</td></tr><tr><td><code>lte</code></td><td>field &lt;= value</td><td>false</td></tr><tr><td><code>exists</code></td><td>path resolves to a non-null value</td><td>(this is the test)</td></tr><tr><td><code>contains</code></td><td>field is an array containing value, or a string containing the substring</td><td>false</td></tr></tbody></table>\n<h3>Evaluator (<code>crates/cyberos-rule/src/eval.rs</code>)</h3>\n<pre><code class=\"language-rust\">#[derive(Debug, Clone)]\npub enum Cond {\n    And(Vec&lt;Cond&gt;),\n    Or(Vec&lt;Cond&gt;),\n    Not(Box&lt;Cond&gt;),\n    Leaf { field: String, op: Op, value: serde_json::Value },\n}\n\n#[derive(Debug, Clone, Copy, PartialEq, Eq)]\npub enum Op { Eq, In, Gte, Lte, Exists, Contains }\n\nimpl Cond {\n    /// Dispatch on the JSON shape: `type` in {and,or,not} -&gt; connective; else leaf (`field`).\n    pub fn from_json(v: &amp;serde_json::Value) -&gt; Result&lt;Cond, DslError&gt;;\n}\n\npub fn evaluate(cond: &amp;Cond, laso: &amp;serde_json::Value) -&gt; bool {\n    match cond {\n        Cond::And(rs) =&gt; rs.iter().all(|r| evaluate(r, laso)),\n        Cond::Or(rs)  =&gt; rs.iter().any(|r| evaluate(r, laso)),\n        Cond::Not(r)  =&gt; !evaluate(r, laso),\n        Cond::Leaf { field, op, value } =&gt; eval_leaf(field, *op, value, laso),\n    }\n}\n\nfn eval_leaf(field: &amp;str, op: Op, value: &amp;serde_json::Value, laso: &amp;serde_json::Value) -&gt; bool {\n    let got = path::resolve(field, laso);          // Option&lt;&amp;Value&gt;\n    match op {\n        Op::Exists =&gt; got.map_or(false, |v| !v.is_null()),\n        _ =&gt; match got {\n            None =&gt; false,\n            Some(v) if v.is_null() =&gt; false,\n            Some(v) =&gt; match op {\n                Op::Eq       =&gt; v == value,\n                Op::In       =&gt; value.as_array().map_or(false, |a| a.contains(v)),\n                Op::Gte      =&gt; cmp(v, value).map_or(false, |o| o.is_ge()),\n                Op::Lte      =&gt; cmp(v, value).map_or(false, |o| o.is_le()),\n                Op::Contains =&gt; contains(v, value),\n                Op::Exists   =&gt; unreachable!(),\n            },\n        },\n    }\n}</code></pre>\n<p><code>from_json</code> is where the untagged leaf is disambiguated (a node with <code>type</code> is a connective; a node with <code>field</code> is a leaf; anything else is a <code>DslError</code>). An empty <code>and</code>/<code>or</code> is a <code>DslError</code> (a pattern must assert something). <code>cmp</code> returns <code>None</code> for non-orderable/mismatched JSON types, so <code>gte</code>/<code>lte</code> against a non-number are cleanly <code>false</code>.</p>\n<h3>Scoring and cach_cuc emission (<code>crates/cyberos-rule/src/score.rs</code>)</h3>\n<pre><code class=\"language-rust\">use laso_envelope::{LaSo, CachCuc};   // TASK-PLAT-002 types\n\n/// Evaluate one pattern against a chart; emit a CachCuc if it fires.\npub fn score_match(p: &amp;Pattern, laso: &amp;LaSo) -&gt; Option&lt;CachCuc&gt; {\n    let cond = Cond::from_json(&amp;p.conditions).ok()?;   // deep-validate here\n    if evaluate(&amp;cond, &amp;laso.as_addressing_root()) {\n        Some(CachCuc {\n            id: p.id.clone(),\n            name: p.name.clone(),\n            cung: resolve_cung(p, laso),               // Option&lt;u8&gt; from an optional `cung` hint/path\n            polarity: p.polarity.into(),               // RULE Polarity -&gt; envelope Polarity\n            score: Some(p.confidence),                 // base score = expert prior\n            citations: p.citations.clone(),\n        })\n    } else { None }\n}</code></pre>\n<p><code>as_addressing_root()</code> presents the envelope so paths resolve per the root rule above. The emitted <code>CachCuc</code> is exactly the envelope shape from TASK-PLAT-002 so the interpretation branch consumes detected patterns with no adapter.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>Cond::from_json</code> parses the worked example and arbitrarily nested <code>and</code>/<code>or</code>/<code>not</code> trees; it rejects an empty <code>and</code>, a leaf with an unknown operator, and a node with neither <code>type</code> nor <code>field</code>.</li><li><code>evaluate</code> returns the correct boolean for a truth table over all six operators, including a <code>not</code> of a nested <code>or</code>.</li><li>A missing field path yields <code>false</code> for <code>eq</code>/<code>in</code>/<code>gte</code>/<code>lte</code>/<code>contains</code> and the correct boolean for <code>exists</code>; no input panics the evaluator (property test over random paths and charts).</li><li><code>in</code> matches set membership; <code>contains</code> matches array membership and string substring; <code>gte</code>/<code>lte</code> order numbers and strings and return <code>false</code> on type mismatch.</li><li><code>score_match</code> emits a <code>CachCuc</code> with <code>score = confidence</code> and the pattern citations when the tree matches, and <code>None</code> when it does not; the emitted type is byte-identical to an TASK-PLAT-002 <code>CachCuc</code>.</li><li>The evaluator is deterministic: 1,000 repeat evaluations of the same <code>(pattern, chart)</code> return the identical result and identical <code>CachCuc</code> ordering of fields.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/dsl_eval.rs</code>: a truth-table suite over the six operators; nested-tree cases; the missing-path matrix; the QiMen worked example evaluated against <code>fixtures/laso_qimen_sample.json</code> (a real la so envelope emitted by the QMDG engine or hand-built to the TASK-PLAT-002 schema).</li><li>Property test: for 10,000 random <code>(field-path, operator, value, chart)</code> tuples, <code>evaluate</code> never panics and is total; <code>exists</code> agrees with a direct <code>resolve(path).is_some_and(non-null)</code>.</li><li>Schema: <code>docs/contracts/condition-dsl.schema.json</code> validates every <code>conditions</code> tree in the TASK-RULE-001 seed and rejects malformed trees; a test cross-checks that <code>Cond::from_json</code> accepts exactly what the schema accepts.</li><li>Cross-type: <code>score_match</code> output deserializes as a <code>laso_envelope::CachCuc</code> (contract with TASK-PLAT-002).</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-rule -- -D warnings</code>, <code>cargo test -p cyberos-rule</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>path.rs</code>: the dotted-path resolver with the reserved-root rule and numeric array indexing over <code>serde_json::Value</code>.</li><li><code>dsl.rs</code>: <code>Cond</code>, <code>Op</code>, <code>Cond::from_json</code>, <code>DslError</code>; author <code>docs/contracts/condition-dsl.schema.json</code> to match.</li><li><code>eval.rs</code>: <code>evaluate</code>, <code>eval_leaf</code>, <code>cmp</code>, <code>contains</code>; the total, panic-free leaf semantics.</li><li><code>score.rs</code>: <code>score_match</code>, <code>resolve_cung</code>, the RULE-to-envelope <code>Polarity</code> conversion; depend on the <code>laso-envelope</code> crate (TASK-PLAT-002).</li><li>Build <code>fixtures/laso_qimen_sample.json</code> and wire the truth-table, property, and schema-parity tests.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-RULE-001 (the <code>Pattern</code> type and the <code>conditions</code> field this task parses) and, for the emitted <code>CachCuc</code>, on TASK-PLAT-002 (the la so envelope crate). Blocks TASK-RULE-003 (the loader/match API wraps this evaluator), TASK-RULE-004 (cross-system nesting extends this grammar and the reserved <code>weight</code>), and TASK-QMDG-005, whose cach cuc detection runs this evaluator over the QiMen plate through the TASK-RULE-003 loader.</p>\n<h2>§8 - Example payloads</h2>\n<p>Input pattern <code>conditions</code> and the emitted cach_cuc:</p>\n<pre><code class=\"language-json\">// conditions (qimen_thanh_long_hoi_dau, abbreviated)\n{ \"type\": \"and\", \"rules\": [\n  { \"field\": \"cung.1.mon\", \"operator\": \"in\", \"value\": [\"Sinh\", \"Khai\"] },\n  { \"field\": \"cung.1.sao\", \"operator\": \"eq\", \"value\": \"Thien Nham\" },\n  { \"field\": \"co_truong_phai.pan_method\", \"operator\": \"eq\", \"value\": \"zhuan\" }\n] }</code></pre>\n<pre><code class=\"language-json\">// emitted CachCuc (TASK-PLAT-002 shape) when the tree matches\n{ \"id\": \"qimen_thanh_long_hoi_dau\", \"name\": \"Thanh Long Hoi Dau\", \"cung\": 1,\n  \"polarity\": \"cat\", \"score\": 0.9, \"citations\": [\"yba_thien_can_khac_ung_12\"] }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Do we need a <code>regex</code> or <code>range</code> operator for wang-suy strength bands, or is <code>gte</code>/<code>lte</code> enough? Default: the six operators; add only when a real seed pattern cannot be expressed, to keep the evaluator total and auditable.</li><li>Where does <code>cung</code> on a multi-palace pattern come from - a fixed hint field on the pattern, or resolved from a field path at match time? Default: an optional <code>cung</code> hint on the pattern row, else <code>None</code>; TASK-RULE-004 may compute it for multi-palace cross-system patterns.</li><li>Should <code>gte</code>/<code>lte</code> on strings be allowed at all, or numbers only? Default: allow lexicographic string order (some can/chi orderings use it) but document it; revisit if it causes surprise.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Panic on missing field</td><td>pattern path absent from this chart</td><td>resolver returns <code>None</code>; leaf is <code>false</code> (or the <code>exists</code> test); never panics</td></tr><tr><td>Non-deterministic match</td><td>reliance on map iteration order or float NaN</td><td>canonical JSON equality; <code>cmp</code> returns <code>None</code> on non-orderable, giving stable <code>false</code></td></tr><tr><td>Empty connective</td><td><code>{ \"type\": \"and\", \"rules\": [] }</code></td><td><code>from_json</code> is a <code>DslError</code>; the pattern fails TASK-RULE-001 review before load</td></tr><tr><td>Fabricated operator</td><td><code>\"operator\": \"matches\"</code></td><td><code>from_json</code> rejects; schema rejects; the seed never loads</td></tr><tr><td>Escaped write</td><td>detector tries to mutate <code>ban</code></td><td>impossible by type: evaluator takes <code>&amp;Value</code>, emits new <code>CachCuc</code>; no setter path exists</td></tr><tr><td>Cross-system field leak</td><td>a QiMen-only path used by an <code>all</code> pattern</td><td>resolves to <code>None</code> on non-QiMen charts -&gt; <code>false</code>; the <code>all</code> pattern simply does not fire there</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>The evaluator is the deterministic heart shared by all three systems: QiMen cach cuc, LiuRen khoa the, and TaiYi patterns are all rows whose <code>conditions</code> this one function runs. Keeping it total (no panics, defined-on-missing) is what lets a single ruleset span three chart shapes and every school-flag combination without special-casing. Do not add operators speculatively; each one widens the test surface and the review burden. This task plus TASK-RULE-001 make the ruleset fully data-driven; TASK-RULE-003 only adds the plumbing to load and cache it.</p>";
