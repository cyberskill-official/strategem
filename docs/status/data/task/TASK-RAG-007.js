window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-RAG-007"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task keeps interpretation available when the LLM is not: it wraps TASK-RAG-003's LLM caller with a timeout and a circuit breaker, and on failure it degrades to a rule-based interpretation assembled from the chart's detected cách cục and their cited pattern meanings - no free-form generation - always labeled degraded. It is the graceful-degradation half of the resilience story for the interpretation branch (strategy 4.4; TASK-RAG-003 §3 names this task as the wrapper), the RAG-specific application of the platform resilience patterns (TASK-PLAT-008).</p>\n<p>The LLM call SHALL carry a per-call timeout. A circuit breaker SHALL open after a configured number of consecutive failures or timeouts, fail fast to the fallback for a cooldown, and half-open a probe before closing; bounded retries with backoff MAY precede a counted failure. On any unrecoverable LLM outcome - timeout, open circuit, or invalid output after TASK-RAG-003's single repair retry - the interpreter SHALL NOT error to the user; it SHALL produce a rule-based interpretation. The rule-based interpretation SHALL be assembled from <code>la_so.cach_cuc</code> (the deterministic engine's detected patterns, read from the envelope) plus each pattern's <code>meaning_modern</code> / <code>meaning_classical</code> and citations (TASK-KB-002 via TASK-RULE-003), stitched through fixed templates; it SHALL contain no model-generated free text and SHALL make no claim that is not carried by a cited pattern meaning.</p>\n<p>Every degraded output SHALL be labeled degraded: this task SHALL add a <code>degraded: bool</code> field (default <code>false</code>) to the <code>AIDisclosure</code> contract - an additive, backward-compatible extension coordinated with TASK-RAG-003's schema - and SHALL set it <code>true</code>, set <code>AIDisclosure.model = \"rule-based-fallback\"</code>, lower <code>confidence</code>, and force <code>requires_human_review = true</code> for the degraded reading. The degraded interpretation SHALL still validate against TASK-RAG-003's <code>Interpretation</code> schema and SHALL carry citation cards for the pattern meanings it used. The fallback SHALL read the la so envelope read-only and SHALL NEVER write <code>ban</code>, <code>cach_cuc</code>, <code>lich_phap</code>, or <code>co_truong_phai</code> (strategy 4.3); it assembles cited pattern meanings only, and it preserves the citation and AIDisclosure discipline with the degraded marker set.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>An LLM is a network dependency that will be slow or down some of the time, and a divination-adjacent product that simply errors when the model is unavailable teaches users it cannot be relied on. But the naive fix - let the model answer from memory when retrieval or the service is flaky - is exactly the hallucination failure the whole branch is built to prevent (Claude-06 s4.3). The way out is that the chart is already deterministic: the engine has detected the cách cục, and each pattern already carries a cited, curator-reviewed meaning (TASK-KB-002). So when the LLM is unavailable, the product can still say something true and sourced - here are the patterns in your chart and what the classics, cited, say they mean - by stitching those cited meanings through fixed templates rather than generating prose. That is a real, honest, degraded reading, not a fabricated one, and it is only possible because the deterministic and cited layers exist beneath the LLM.</p>\n<p>Labeling it degraded and forcing human review is the honesty that makes the fallback safe to ship. A rule-based reading is thinner than a grounded LLM interpretation - it does not synthesize across patterns or tailor tone - so the user must be told this is a reduced reading, and the conservative posture (low confidence, review required) reflects that the richer path was unavailable. The circuit breaker is what keeps a struggling LLM from degrading the whole product: without it, every request waits out the timeout and the system stays slow under load; with it, after a few failures the interpreter fails fast to the cheap, available rule-based path and probes for recovery, so a model outage is a quality reduction, not an outage. Keeping the fallback strictly to cited pattern meanings, and strictly off the la so envelope, means the degraded path honors the same two invariants as the primary path - no claim without a citation, and the interpretation never writes the chart - so resilience is bought without any loss of the discipline the product's trust rests on.</p>\n<h2>§3 - Contract (resilience wrapper, rule-based fallback, disclosure)</h2>\n<h3>Resilience wrapper (<code>tamthuc_rag/resilience.py</code>)</h3>\n<pre><code class=\"language-python\">class CircuitState(str, Enum): closed = \"closed\"; open = \"open\"; half_open = \"half_open\"\n\nclass CircuitBreaker:\n    def __init__(self, fail_threshold: int = 5, cooldown_s: float = 30.0): ...\n    state: CircuitState\n    def call(self, fn: Callable[[], T]) -&gt; T: ...   # raises CircuitOpen when open\n\nclass ResilientLLM:                                  # implements TASK-RAG-003's LLM protocol\n    def __init__(self, inner: LLM, breaker: CircuitBreaker,\n                 timeout_s: float = 20.0, retries: int = 1, backoff_s: float = 0.5): ...\n    def complete(self, messages, schema):\n        # bounded retry+backoff -&gt; timeout-guarded inner.complete, via the breaker;\n        # on timeout / CircuitOpen / invalid-after-repair, raise LLMUnavailable</code></pre>\n<p><code>ResilientLLM</code> is a drop-in for the <code>LLM</code> TASK-RAG-003 already accepts; it reuses TASK-PLAT-008's breaker/retry primitives where present, else a local implementation.</p>\n<h3>Rule-based fallback (<code>tamthuc_rag/fallback.py</code>, <code>templates/</code>)</h3>\n<pre><code class=\"language-python\">def rule_based_interpretation(la_so: LaSo, persona: str,\n                              patterns: PatternIndex, retrieved: RetrievalResult | None) -&gt; Interpretation:\n    detected = la_so.cach_cuc                        # read-only from the envelope\n    lines, cites = [], []\n    for cc in detected:                              # ordered by score/polarity\n        p = patterns.get(cc.id)                      # TASK-RULE-003 / TASK-KB-002: meaning + citations\n        lines.append(render(cc, p, persona))         # fixed template; NO model text\n        cites += p.citations\n    cards = citation_cards(cites, retrieved)          # TASK-KB-003-resolved cards for the used patterns\n    return Interpretation(\n        persona_level=persona,\n        beginner_interpretation=assemble(\"degraded_beginner.md\", lines),\n        expert_interpretation=assemble(\"degraded_expert.md\", lines),\n        recommendations=recommendations_from_polarity(detected, patterns),\n        citations=cards,\n        confidence=degraded_confidence(detected),     # low\n        requires_human_review=True,                   # conservative in degraded mode\n        disclosure=AIDisclosure(is_ai_generated=False, model=\"rule-based-fallback\",\n            prompt_version=\"fallback-1.0.0\", degraded=True,\n            retrieved_citation_ids=[c.citation_id for c in cards], generated_at=now(),\n            review_status=\"pending\"))\n\ndef interpret_resilient(la_so, request, persona, retriever, llm, patterns,\n                        breaker: CircuitBreaker) -&gt; Interpretation:\n    try:\n        return interpret(la_so, request, persona, retriever, ResilientLLM(llm, breaker))  # TASK-RAG-003\n    except LLMUnavailable:\n        retrieved = safe_retrieve(retriever, request)   # best-effort; None on failure\n        return rule_based_interpretation(la_so, persona, patterns, retrieved)</code></pre>\n<p><code>interpret_resilient</code> wraps TASK-RAG-003's <code>interpret</code> unchanged: it injects the <code>ResilientLLM</code> and catches <code>LLMUnavailable</code> to produce the degraded reading. The degraded templates stitch only the cited pattern meanings; they contain no free-form slots the model would fill.</p>\n<h3>Disclosure extension (coordinated with TASK-RAG-003)</h3>\n<p>This task adds one field to the shared contract: <code>AIDisclosure.degraded: bool = False</code>. It is additive and defaulted, so every existing TASK-RAG-003 output is unaffected; the JSON Schema (<code>docs/contracts/interpretation.schema.json</code>) bumps a minor version. The degraded marker is the truthful signal TASK-WEB-003 renders as a reduced-reading banner.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A healthy LLM path returns TASK-RAG-003's normal interpretation unchanged, with <code>disclosure.degraded = false</code>.</li><li>On a timeout, the interpreter does not error to the user; it returns a rule-based interpretation assembled from <code>la_so.cach_cuc</code> and the cited pattern meanings, valid against the <code>Interpretation</code> schema.</li><li>The circuit breaker opens after the configured consecutive failures and fails fast to the fallback during cooldown, then half-opens a probe; a recovered LLM closes the circuit and the normal path resumes.</li><li>The degraded output is labeled: <code>disclosure.degraded = true</code>, <code>model = \"rule-based-fallback\"</code>, lowered <code>confidence</code>, <code>requires_human_review = true</code>; it carries citation cards for the pattern meanings used and contains no model-generated free text.</li><li>Every claim in the degraded reading maps to a cited pattern meaning; a detected cách cục with no cited meaning is omitted rather than asserted uncited (the anti-hallucination invariant holds in degraded mode).</li><li>The la so envelope is byte-identical before and after <code>interpret_resilient</code> (read-only invariant); no code path writes <code>ban</code>/<code>cach_cuc</code>/<code>lich_phap</code>/<code>co_truong_phai</code>.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_fallback.py</code>: the healthy path (non-degraded, delegates to TASK-RAG-003); the timeout path producing a schema-valid degraded reading; circuit-breaker open/half-open/close transitions with a flaky stub LLM; the degraded-label assertions (<code>degraded</code>, <code>model</code>, low confidence, <code>requires_human_review</code>); the no-free-text assertion (degraded text is exactly the template-stitched cited meanings); the uncited-cach-cuc omission; the read-only-envelope byte-equality check.</li><li>Resilience: assert bounded retries + backoff before a counted failure, fail-fast while open (no timeout wait), and recovery on a healthy probe; deterministic given the stub failure pattern.</li><li>Contract: the additive <code>AIDisclosure.degraded</code> field validates against the bumped <code>interpretation.schema.json</code> and defaults <code>false</code> for existing outputs (backward-compatible); Pydantic/JSON-Schema parity in CI.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_rag</code>, <code>pytest packages/tamthuc_rag</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>resilience.py</code>: <code>CircuitBreaker</code> (fail threshold, cooldown, half-open probe) and <code>ResilientLLM</code> (timeout + bounded retry/backoff + breaker), reusing TASK-PLAT-008 primitives where present; <code>LLMUnavailable</code>.</li><li><code>fallback.py</code>: <code>rule_based_interpretation</code> (assemble from <code>la_so.cach_cuc</code> + cited pattern meanings, read-only) and <code>interpret_resilient</code> (wrap TASK-RAG-003's <code>interpret</code>, catch <code>LLMUnavailable</code>).</li><li><code>templates/degraded_beginner.md</code>, <code>degraded_expert.md</code>: fixed templates that stitch cited pattern meanings with no free-form slots; version them (<code>fallback-1.0.0</code>).</li><li>Add <code>AIDisclosure.degraded: bool = False</code> to the shared TASK-RAG-003 contract; bump <code>docs/contracts/interpretation.schema.json</code> a minor version.</li><li>Wire <code>patterns</code> to TASK-RULE-003 (the pattern loader serving active TASK-KB-002 patterns with meanings + citations) and <code>citation_cards</code> to TASK-KB-003 resolution.</li><li>Commit <code>fixtures/fallback_cases.json</code> (a chart with detected cách cục, one with a cited meaning and one without) as the test exemplar.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-RAG-003 (it wraps that task's LLM caller and <code>interpret</code>, and extends its <code>AIDisclosure</code> with the additive <code>degraded</code> field). Uses TASK-PLAT-008 resilience primitives (circuit breaker, retry/backoff, graceful degradation) where available - this task is their interpretation-branch application. Reads the detected cách cục from the la so and their cited meanings via TASK-RULE-003 (serving active TASK-KB-002 patterns) and TASK-KB-003 (resolving the citation cards). Composes with TASK-RAG-004: a degraded reading is conservative and <code>requires_human_review = true</code>, so it also passes the HumanReviewGate; wiring the degraded reading in as the reject-time fallback is a natural follow-up. Blocks nothing in the catalog, but it is the resilience wrapper every production interpretation call SHOULD go through. The RAG-branch invariant is central here and enforced: the fallback reads the la so read-only, asserts only cited pattern meanings, and keeps the citation + AIDisclosure discipline with the degraded marker set.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// a degraded Interpretation (abbreviated) after an LLM timeout\n{ \"persona_level\": \"beginner\",\n  \"beginner_interpretation\": \"Reduced reading (AI unavailable). Your chart shows Thanh Long Hoi Dau, a favorable-timing pattern for initiating [yba_dieu_012].\",\n  \"expert_interpretation\": \"Detected cach cuc: qimen_thanh_long_hoi_dau (cat). Classical basis: 丙加值符 [yba_dieu_012].\",\n  \"recommendations\": [ { \"text\": \"Treat the window as supportive for beginning.\",\n      \"rationale\": \"Cat cach cuc on the acting palace.\", \"citations\": [\"yba_dieu_012\"] } ],\n  \"citations\": [ { \"citation_id\": \"yba_dieu_012\", \"source\": \"yen_ba_dieu_tau_ca\", \"unit_type\": \"dieu\",\n      \"han\": \"丙加值符...\", \"bach_thoai\": \"...\", \"dich\": \"Binh gia truc phu...\", \"locator\": \"dieu 12\" } ],\n  \"confidence\": 0.35, \"requires_human_review\": true,\n  \"disclosure\": { \"is_ai_generated\": false, \"model\": \"rule-based-fallback\", \"prompt_version\": \"fallback-1.0.0\",\n    \"degraded\": true, \"retrieved_citation_ids\": [\"yba_dieu_012\"], \"review_status\": \"pending\" } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether <code>is_ai_generated</code> should be <code>false</code> (the text is template-assembled, not model-generated) or <code>true</code> with <code>degraded=true</code>. Default: <code>is_ai_generated=false</code> plus <code>degraded=true</code> and <code>model=\"rule-based-fallback\"</code>, since no model wrote the text; the UI still shows a machine-assisted, reduced-reading banner. Confirm the exact labeling with TASK-LEGAL-003.</li><li>Circuit-breaker tuning (threshold, cooldown, timeout). Default: 5 consecutive failures, 30s cooldown, 20s per-call timeout, 1 retry; tune from TASK-PLAT-005 latency/error telemetry.</li><li>Whether the degraded reading synthesizes across multiple cách cục or lists them. Default: list each detected pattern with its cited meaning (no cross-pattern synthesis, which is the LLM's job); keep it strictly template-stitched so it can never drift into generation.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Errors to the user</td><td>LLM timeout/outage with no fallback</td><td>degrade to the rule-based reading; never surface a raw error as the interpretation</td></tr><tr><td>Free-memory degradation</td><td>fallback lets the model answer from memory</td><td>forbidden; degraded text is template-stitched cited pattern meanings only, no model free text</td></tr><tr><td>Unlabeled degraded output</td><td>a reduced reading shown as a full one</td><td><code>degraded=true</code> + <code>model=\"rule-based-fallback\"</code> + low confidence + review required; UI banner</td></tr><tr><td>Uncited claim in fallback</td><td>a cách cục with no cited meaning asserted</td><td>omitted, not asserted; every degraded claim maps to a cited pattern meaning</td></tr><tr><td>Slow-under-load LLM</td><td>every request waits out the timeout</td><td>circuit breaker opens, fails fast to fallback, half-open probe recovers</td></tr><tr><td>Envelope write</td><td>fallback mutates the chart</td><td>impossible: la so read-only; byte-equality test; assembles from <code>cach_cuc</code>, never writes it</td></tr><tr><td>Schema break from the new field</td><td><code>degraded</code> field breaks existing outputs</td><td>additive + defaulted <code>false</code>; minor schema bump; existing TASK-RAG-003 outputs unaffected</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task is what lets the interpretation branch stay both available and honest under an LLM outage: the deterministic, cited layers beneath the model make a true degraded reading possible, so resilience never becomes an excuse to hallucinate. Two invariants carry over from TASK-RAG-003 unchanged - the fallback reads the la so and never writes it, and it makes no claim without a citation - and one label is added: degraded output is always marked degraded, low-confidence, and review-required, because a reduced reading must announce itself. Keep the degraded templates free of any model-fillable slot so the path can never drift into generation, and coordinate the single additive <code>AIDisclosure.degraded</code> field with TASK-RAG-003 as a reviewed schema bump. The package <code>tamthuc_rag</code> is shared with TASK-RAG-001/002/003/004/005/006; this task adds <code>fallback.py</code>, <code>resilience.py</code>, and the degraded templates, so the RAG branch stays one installable, mypy-clean unit.</p>";
