window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CORE-007"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task provides the can chi (干支) primitives and their relations - the atomic vocabulary that every engine speaks. It is pure lookup and modular arithmetic over the ten thien can, the twelve dia chi, and the sixty-term giap ty cycle, plus the ngu hanh (five phases) of each and the classical branch relations (hinh, xung, pha, hai, hop). It is deliberately dependency-light so it can be built in parallel with the astronomy layer and heavily unit-tested (Claude-01, Claude-06 s3.2).</p>\n<p>The module SHALL define the ten thien can, the twelve dia chi, and the sixty giap ty pairs, with total conversion between a can/chi/giap-ty value and its integer index and back. It SHALL define the ngu hanh of each can and each chi, the sinh (generating) cycle and the khac (controlling) cycle over the five phases. It SHALL define the dia chi relations: luc hop and tam hop (harmonies), luc xung (clashes), luc hai (harms), luc pha (breaks), and the three groups of hinh (punishments) including the tu hinh (self-punishment). Every relation SHALL be exposed both as data (the tables) and as a queryable predicate (does chi A clash chi B). All lookups MUST round-trip: <code>index -&gt; value -&gt; index</code> is identity across the whole domain.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>The three engines diverge in how they build and read a chart, but they all reduce to statements about can and chi and the phase relations between them (Claude-01). QiMen cach cuc detection asks whether the ten stems overcome one another (thap can khac ung); LiuRen tu khoa and luc than ask whether the day stem generates or controls a branch and whether branches harmonize or clash; TaiYi and the derived states ask for the phase of a stem or the seasonal strength of a phase. If each engine re-encoded \"Moc controls Tho\" or \"Ty clashes Ngo,\" the tables would drift and a fix in one would miss the others. One primitive layer, exhaustively tested, is the cheapest way to keep them consistent.</p>\n<p>Keeping every relation as both data and predicate serves two readers: the rule engine (RULE-002) and the knowledge-graph edge taxonomy (Claude-06 s3.2) consume the tables as data, while the engines call the predicates in casting code. Because the domain is small and closed (10 x 12 x 60 and a handful of relation sets), the whole surface can be enumerated in tests, which is exactly the kind of hard, cheap correctness the platform wants at its base. The classical Han is kept in every table because these relations are read and checked by domain experts against texts, and the glyph is the unambiguous identity.</p>\n<h2>§3 - Contract (types, tables, relations)</h2>\n<h3>Primitives</h3>\n<pre><code class=\"language-rust\">pub enum Can { Giap, At, Binh, Dinh, Mau, Ky, Canh, Tan, Nham, Quy }   // 甲乙丙丁戊己庚辛壬癸, index 0..10\npub enum Chi { Ty, Suu, Dan, Mao, Thin, Ty2, Ngo, Mui, Than, Dau, Tuat, Hoi } // 子丑寅卯辰巳午未申酉戌亥, 0..12\npub enum NguHanh { Moc, Hoa, Tho, Kim, Thuy }                          // 木火土金水\npub struct GiapTy(pub u8);                                            // 0..60, 0 = 甲子\n// Chi::Ty2 is 巳 (snake); Chi::Ty is 子 (rat). Distinct glyphs, kept apart in code.\npub fn giap_ty_from_can_chi(c: Can, z: Chi) -&gt; GiapTy;                 // valid only for the 60 legal pairs\npub fn can_chi_of(g: GiapTy) -&gt; (Can, Chi);</code></pre>\n<h3>Ngu hanh of can and chi</h3>\n<table><thead><tr><th>Can</th><th>Ngu hanh</th><th>Chi</th><th>Ngu hanh</th></tr></thead><tbody><tr><td>甲 乙</td><td>木 Mộc</td><td>寅 卯</td><td>木 Mộc</td></tr><tr><td>丙 丁</td><td>火 Hỏa</td><td>巳 午</td><td>火 Hỏa</td></tr><tr><td>戊 己</td><td>土 Thổ</td><td>辰 戌 丑 未</td><td>土 Thổ</td></tr><tr><td>庚 辛</td><td>金 Kim</td><td>申 酉</td><td>金 Kim</td></tr><tr><td>壬 癸</td><td>水 Thủy</td><td>子 亥</td><td>水 Thủy</td></tr></tbody></table>\n<h3>Sinh and khac (Claude-01)</h3>\n<pre><code>sinh (sinh ra, generating):   木 -&gt; 火 -&gt; 土 -&gt; 金 -&gt; 水 -&gt; 木\nkhac (khac che, controlling): 木 -&gt; 土 -&gt; 水 -&gt; 火 -&gt; 金 -&gt; 木</code></pre>\n<p>Predicates: <code>sinh(a, b)</code> iff b is the phase a generates; <code>khac(a, b)</code> iff b is the phase a controls. Their inverses (<code>duoc_sinh</code>, <code>bi_khac</code>) follow.</p>\n<h3>Dia chi relations</h3>\n<p>Luc hop (六合, six harmonies):</p>\n<pre><code>子丑  寅亥  卯戌  辰酉  巳申  午未</code></pre>\n<p>Tam hop (三合, three harmonies) and the phase each trine forms:</p>\n<pre><code>申子辰 -&gt; 水 Thủy    亥卯未 -&gt; 木 Mộc    寅午戌 -&gt; 火 Hỏa    巳酉丑 -&gt; 金 Kim</code></pre>\n<p>Luc xung (六冲, six clashes):</p>\n<pre><code>子午  丑未  寅申  卯酉  辰戌  巳亥</code></pre>\n<p>Luc hai (六害, six harms):</p>\n<pre><code>子未  丑午  寅巳  卯辰  申亥  酉戌</code></pre>\n<p>Luc pha (六破, six breaks):</p>\n<pre><code>子酉  午卯  申巳  寅亥  辰丑  戌未</code></pre>\n<p>Hinh (刑, punishments):</p>\n<pre><code>Vo an chi hinh:   寅巳申   (the tri-punishment)\nTri the chi hinh: 丑戌未\nVo le chi hinh:   子卯\nTu hinh (self):   辰辰  午午  酉酉  亥亥</code></pre>\n<h3>Relation API</h3>\n<pre><code class=\"language-rust\">pub enum ChiQuanHe { LucHop, TamHop, LucXung, LucHai, LucPha, Hinh, TuHinh }\npub fn quan_he(a: Chi, b: Chi) -&gt; Vec&lt;ChiQuanHe&gt;;   // all relations that hold for the (unordered) pair\npub fn tam_hop_cua(z: Chi) -&gt; (Chi, Chi, NguHanh);  // the other two of z's trine + its phase</code></pre>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>Round-trip identity: for all 10 can, 12 chi, and 60 giap ty, <code>index -&gt; value -&gt; index</code> is identity; <code>giap_ty_from_can_chi</code> accepts exactly the 60 legal pairs and <code>can_chi_of</code> inverts it.</li><li>Ngu hanh assignment is correct for all 10 can and all 12 chi against the tables above (辰 戌 丑 未 all Thổ; 子 亥 both Thủy).</li><li>Sinh and khac cycles are correct for all 5 phases each way; <code>khac(Moc, Tho)</code> and <code>sinh(Moc, Hoa)</code> hold, and non-relations are false.</li><li>Every branch relation set is exhaustively correct: 6 luc hop, 4 tam hop trines (with phase), 6 luc xung, 6 luc hai, 6 luc pha, the three hinh groups and 4 tu hinh; <code>quan_he</code> returns all and only the relations that hold for a pair.</li><li><code>子</code> (Ty, rat) and <code>巳</code> (Ty2, snake) are never conflated; a test asserts the two enum variants map to distinct glyphs and distinct relations.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/ganzhi_relations.rs</code> enumerates the full domains: 60 giap-ty round-trips, 10+12 ngu-hanh rows, 5+5 sinh/khac rows, and every relation pair, all against the §3 tables.</li><li>Symmetry test: all listed relations are symmetric (<code>quan_he(a,b) == quan_he(b,a)</code>), and the trine <code>tam_hop_cua</code> is consistent (each member returns the same trine and phase).</li><li>Negative tests: pairs with no relation return an empty <code>quan_he</code>; illegal can-chi pairs (e.g. 甲丑) are rejected by <code>giap_ty_from_can_chi</code>.</li><li>Gates: <code>cargo fmt --check</code>, <code>cargo clippy -p cyberos-lichphap -- -D warnings</code>, <code>cargo test -p cyberos-lichphap</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>ganzhi.rs</code>: the <code>Can</code> / <code>Chi</code> / <code>NguHanh</code> / <code>GiapTy</code> types, their glyph and index mappings, the 60-pair legality check, and <code>giap_ty_from_can_chi</code> / <code>can_chi_of</code>.</li><li><code>relations.rs</code>: the ngu-hanh-of tables, the sinh/khac cycles as <code>const</code> maps, the branch-relation sets as <code>const</code> data, and the <code>quan_he</code> / <code>tam_hop_cua</code> predicates over them.</li><li>Serialize <code>Can</code> / <code>Chi</code> / <code>GiapTy</code> to their Han glyph (so the calendar output and envelope carry 甲子, not indices), with <code>Deserialize</code> accepting the glyph.</li><li>Add the exhaustive enumerated tests and the symmetry / negative tests.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-PLAT-001 (workspace) only - deliberately independent of the astronomy layer so it can proceed in parallel and be joined at integration. Consumed by TASK-CORE-003 (pillars use <code>Can</code>/<code>Chi</code>) and TASK-CORE-004 (derived states use phase + sinh/khac). Blocks the relation-consuming engine slices: TASK-QMDG-005 (thap can khac ung / cat-hung uses stem overcoming), TASK-LN-002 (tu khoa thuong/ha khac and later luc than use sinh/khac and branch relations), TASK-TAT-003 (cac toan). NOTE: the master catalog does not list CORE-007 in those tasks' <code>depends_on</code> because relations are a shared primitive many slices touch; the dependency is real but soft (build CORE-007 early, wire as engines land) - flag for catalog reconciliation if hard edges are wanted.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-rust\">quan_he(Chi::Ty, Chi::Ngo)     // =&gt; [LucXung]          (子午 clash)\nquan_he(Chi::Dan, Chi::Hoi)    // =&gt; [LucHop, LucPha]   (寅亥 harmonize and break)\ntam_hop_cua(Chi::Than)         // =&gt; (Chi::Ty, Chi::Thin, NguHanh::Thuy)   (申子辰 -&gt; 水)\nkhac(NguHanh::Moc, NguHanh::Tho) // =&gt; true             (木 khac 土)</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Do we model the ban-hop (half-harmony) and the hoa (transformation of a hop into its phase), or only the full relations? Decision for MVP: model the full luc hop / tam hop / xung / hai / pha / hinh sets; ban-hop and hoa-cuc are engine-level refinements deferred to whichever engine first needs them (likely LiuRen luc than), added as predicates over this base.</li><li>Should the sixty na-am (nap am, the sound-phase of each giap ty pair) live here? Decision: not in this task; nap am is a separate 60-entry table added when an engine (TaiYi / QiMen) needs it, layered on <code>GiapTy</code> without changing this API.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>子 / 巳 conflated</td><td>Ty (rat) and Ty2 (snake) share a variant</td><td>distinct-glyph test fails; keep two enum variants</td></tr><tr><td>Illegal pair accepted</td><td>甲丑 (odd/even parity broken) passed</td><td><code>giap_ty_from_can_chi</code> rejects; only 60 legal pairs</td></tr><tr><td>khac cycle reversed</td><td>木-&gt;金 instead of 木-&gt;土</td><td>sinh/khac enumeration fails</td></tr><tr><td>Relation set incomplete</td><td>a xung or hai pair dropped</td><td>exhaustive relation test fails</td></tr><tr><td>Relation asymmetric</td><td><code>quan_he(a,b) != quan_he(b,a)</code></td><td>symmetry test fails</td></tr><tr><td>Index drift</td><td>glyph order changed without index update</td><td>round-trip identity test fails</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This is the smallest task in the calendar core and the most re-used - treat its test suite as a specification of the domain, enumerated rather than sampled. Keep the classical Han in every table; the 子/巳 (Ty/Ty2) and 辰戌丑未-all-Thổ cases are exactly where ASCII normalization would introduce a silent error. Same crate <code>cyberos-lichphap</code> - this task adds <code>ganzhi.rs</code> and <code>relations.rs</code>, the vocabulary the pillars, derived states, and all three engines are written in.</p>";
