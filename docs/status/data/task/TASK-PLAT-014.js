window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-PLAT-014"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task ships the <strong>user web</strong> surface to <strong>Vercel</strong>. The Next.js app under <code>apps/web</code> SHALL build on Vercel. Runtime configuration SHALL include:</p>\n<ul><li><code>NEXT_PUBLIC_API_BASE</code> — browser origin for the VPS API (e.g. <code>https://api.example.com</code>), empty string only when same-origin reverse proxy is used.</li><li>Optional server-only <code>API_URL</code> for SSR/rewrites to the API (never a Supabase service role).</li></ul>\n<p>The browser SHALL NOT embed Supabase service-role keys. CORS on the API (TASK-PLAT-013) SHALL allow the Vercel production and preview origins.</p>\n<p><code>vercel.json</code> SHALL set install/build rooted at the monorepo as needed (<code>pnpm --filter web build</code> or <code>cd apps/web</code>). Output remains Next default / standalone as compatible with Vercel.</p>\n<h2>§2 - Why this design</h2>\n<p>Vercel optimizes the user-facing SPA/SSR path; CyberOS already uses Vercel for docs-style surfaces. Keeping the web off the VPS reduces Node load from the API host and gives PR preview URLs for design review.</p>\n<h2>§3 - Contract</h2>\n<table><thead><tr><th>Env</th><th>Where</th><th>Purpose</th></tr></thead><tbody><tr><td><code>NEXT_PUBLIC_API_BASE</code></td><td>Vercel</td><td>Browser API calls</td></tr><tr><td><code>API_URL</code></td><td>Vercel (optional)</td><td>Server rewrite target</td></tr></tbody></table>\n<p>Production web MUST call HTTPS API. Local dev continues to rewrite <code>/api</code> → <code>127.0.0.1:8000</code>.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li><code>apps/web/vercel.json</code> present and documents monorepo build.</li><li><code>.env.example</code> lists public API base without secrets.</li><li><code>docs/deploy/vercel-web.md</code> has project link steps and env checklist.</li><li>Cast client works when <code>NEXT_PUBLIC_API_BASE</code> is the VPS API origin (no <code>/api</code> rewrite required in prod).</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>pnpm --filter web build</code> still green.</li><li>Grep: no <code>SERVICE_ROLE</code> in <code>apps/web</code>.</li></ul>";
