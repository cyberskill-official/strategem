window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-CHART-004"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task completes the chart export seam TASK-CHART-001 exposed and delivers the full accessibility pass for every chart type. It is the shared, cross-cutting slice: it takes the SVG serialization hook each chart provides (QiMen TASK-CHART-001, LiuRen TASK-CHART-002, TaiYi TASK-CHART-003) and turns it into PNG, SVG, and print export, and it makes every chart pass the Vietnamese stacked-diacritics (dau chong) clip test and be fully screen-reader navigable. It owns export and accessibility for charts; it does NOT render the chart internals (each chart task does) and does NOT re-cast or read the envelope beyond what the chart already read.</p>\n<p>The module SHALL provide, for all chart types via their common export seam: (a) SVG export - direct serialization of the rendered chart, crisp at any zoom; (b) PNG export - a raster of the same at a specified scale; and (c) print - a print stylesheet (<code>chart-print.css</code>) that lays the chart to a page, collapses any opt-in glass to a solid surface (TASK-WEB-001), and preserves every marker. The clip test SHALL run over each chart's palace/lesson/than labels (Han plus stacked Vietnamese diacritics) at 100% / 200% / 400% zoom on both light and dark themes, and no diacritic or descender SHALL be clipped (TASK-WEB-001). Every chart component - each palace, lesson, transmission, than, and general - SHALL carry a screen-reader label naming its content and role (<code>a11y-labels.ts</code>), so the chart is navigable and announced without sight of the visual. cat/hung / trung polarity SHALL never be encoded by color alone - every polarity marker SHALL pair color with an icon and text - and this SHALL hold in every export: a PNG, an SVG, and a print output SHALL each carry the icon and text, not color only, so the marker survives grayscale, print, and low vision.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Export and accessibility are done once, as a shared slice, because they are the same job across all three charts: each chart already exposes an SVG seam and already renders cat/hung as icon-plus-text, so the export and the a11y pass belong in one place rather than re-implemented three times. Putting them together is deliberate - export and accessibility are the same underlying requirement seen twice. A chart that is truly accessible (each element labeled, nothing color-only) is also a chart that exports faithfully (the labels and the icon-plus-text markers survive into the PNG, the SVG, and the printed page). Building them apart would let one drift from the other.</p>\n<p>The dau chong clip test is a first-class gate here for the same reason it is in TASK-WEB-001: the product's primary language is Vietnamese, its charts are dense with Han plus stacked tone-and-vowel marks, and those marks are the first thing a tight line-height clips - most visibly at the 200% and 400% zoom a low-vision user relies on (Claude-07 s5.1). Screen-reader labels for palaces and components make the signature visual usable by a blind practitioner, which a purely visual chart never could be. And the never-color-alone rule matters most in export: a chart printed in grayscale or read by a screen reader must still convey cat from hung, so the icon and word travel with the color into every output - the accessibility floor and the export fidelity are one requirement (strategy 4.4).</p>\n<h2>§3 - Contract (export / print / accessibility)</h2>\n<h3>Export (<code>lib/chart/export-svg.ts</code>, <code>lib/chart/export-png.ts</code>)</h3>\n<pre><code class=\"language-ts\">// consumes the TASK-CHART-001 export seam each chart type exposes (QiMen / LiuRen / TaiYi)\nfunction exportSvg(chartEl: SVGSVGElement): string;                 // serialized SVG (fonts + markers inlined)\nasync function exportPng(chartEl: SVGSVGElement, scale?: number): Promise&lt;Blob&gt;;   // raster at scale (default 2x)\n// both preserve every cat/hung marker as color + icon + text; neither re-reads or re-casts the envelope.</code></pre>\n<p>Export works for all chart types because each renders through the common seam; a new chart type is exportable the moment it exposes the seam, with no change here.</p>\n<h3>Print (<code>styles/chart-print.css</code>)</h3>\n<p>A <code>@media print</code> stylesheet that fits the chart to the page, collapses opt-in glass to a solid surface (TASK-WEB-001), preserves the icon+text polarity markers, and keeps Han plus diacritics unclipped at print scale.</p>\n<h3>Accessibility (<code>lib/chart/a11y-labels.ts</code>)</h3>\n<table><thead><tr><th>Requirement</th><th>Rule</th></tr></thead><tbody><tr><td>labeled components</td><td>every palace / lesson / transmission / than / general has a screen-reader label naming its content and role</td></tr><tr><td>keyboard</td><td>every component is focusable and its detail is keyboard-reachable (baseline from each chart task; completed here)</td></tr><tr><td>dau chong clip test</td><td>palace/label text (Han + stacked diacritics) unclipped at 100/200/400% on light + dark</td></tr><tr><td>never color alone</td><td>cat/hung/trung is color + icon + text, in the UI and in every export (PNG/SVG/print)</td></tr></tbody></table>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>SVG, PNG, and print export are available for all chart types (QiMen/LiuRen/TaiYi) through the common TASK-CHART-001 export seam; a chart type is exportable via the seam with no per-type export code.</li><li>The dau chong stacked-diacritics clip test passes over each chart's palace/label text (Han + diacritics) at 100% / 200% / 400% zoom on light and dark themes; no diacritic or descender is clipped.</li><li>Every chart component (palace / lesson / transmission / than / general) carries a screen-reader label naming its content and role; the chart is keyboard-navigable and announced.</li><li>cat/hung/trung is never color alone - every polarity marker pairs color with an icon and text - in the UI and in the PNG, SVG, and print outputs (a grayscale export still distinguishes polarity).</li><li>Print collapses opt-in glass to a solid surface and keeps the chart and its markers unclipped at print scale.</li><li>Export does not re-cast or re-read the envelope beyond what the chart already rendered; it serializes the rendered chart only.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/chart-export.test.tsx</code>: exports a rendered QiMen, LiuRen, and TaiYi chart to SVG and PNG through the common seam; asserts the serialized output contains the icon+text polarity markers (not color-only) and the labels; asserts a forced-grayscale render still distinguishes cat from hung; asserts print collapses glass to solid.</li><li><code>tests/chart-a11y.test.tsx</code>: <code>jest-axe</code> clean on each chart; asserts every palace/lesson/than/general has a screen-reader label and role; asserts keyboard traversal reaches every component and its detail; runs the dau chong clip test at 100/200/400% on light + dark over Han + diacritic labels.</li><li>Cross-type: the export and a11y tests run over all three chart types via the shared seam, so a new chart type inherits the gate.</li><li>Gates: <code>pnpm --filter web lint</code>, <code>pnpm --filter web test</code>, <code>next build</code>.</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>lib/chart/export-svg.ts</code>: serialize the chart's SVG through the TASK-CHART-001 seam, inlining fonts and the icon+text markers.</li><li><code>lib/chart/export-png.ts</code>: raster the serialized SVG at a scale (default 2x); preserve markers.</li><li><code>styles/chart-print.css</code>: the <code>@media print</code> layout - fit to page, glass to solid, markers and diacritics preserved.</li><li><code>lib/chart/a11y-labels.ts</code>: the label/role builder for every chart component (palace/lesson/transmission/than/general).</li><li><code>components/chart/export-controls.tsx</code>: the export menu (SVG / PNG / print) wired to the seam.</li><li><code>tests/chart-export.test.tsx</code> + <code>tests/chart-a11y.test.tsx</code> run across QiMen/LiuRen/TaiYi.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-CHART-001 (the export seam and the baseline accessibility it exposes; this task completes both). Applies to TASK-CHART-002 (LiuRen) and TASK-CHART-003 (TaiYi) through the same seam, so all three chart types export and pass the a11y gate uniformly. Uses TASK-WEB-001 (the tokens, the glass-collapses-on-print rule, the cat/hung color+icon+text convention, and the shared clip-test harness). The export it completes is what the report view (TASK-WEB-005), the PDF export (TASK-REPORT-002), and the management-flow export (TASK-WEB-007) build on for chart images. It re-reads no envelope and re-casts nothing (strategy 4.3) - it serializes the rendered chart.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-ts\">// export any chart type through the common TASK-CHART-001 seam\nconst svg = exportSvg(chartEl);              // \"&lt;svg ...&gt;...&lt;/svg&gt;\" with icon+text markers inlined\nconst png = await exportPng(chartEl, 2);     // 2x PNG Blob; cat/hung still icon + text (survives grayscale)\n\n// a11y label for a QiMen palace carrying a cat cach cuc (icon + text, not color-only)\nlabelFor(palace);\n// -&gt; \"Cung 1: dia ban 戊, thien ban 天蓬, cua 休門, than 值符. Cach 青龍返首, tinh chat: cat.\"</code></pre>\n<pre><code class=\"language-css\">/* chart-print.css - glass collapses to solid, markers preserved, nothing clipped */\n@media print {\n  .cs-surface-glass { backdrop-filter: none; background: var(--surface-solid); }\n  .chart { break-inside: avoid; }\n}</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>PNG rasterization approach: canvas <code>drawImage</code> of the serialized SVG vs a headless render. Default: serialize the SVG (fonts and markers inlined) and raster via canvas client-side at MVP; a server-side headless render is an option only if font/diacritic fidelity needs it, and it must reuse the same serialization so the icon+text markers are identical.</li><li>Whether print is per-chart or whole-report. Default: this task owns the chart print stylesheet; the whole-report PDF is TASK-REPORT-002, which embeds the chart export - one chart-export source of truth, two consumers.</li><li>How verbose the screen-reader labels are. Default: name the component, its key contents, its role, and its polarity as icon+text-equivalent words; keep it a concise, ordered reading of the palace/lesson rather than a dump, so a screen-reader user can scan the chart.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Color-only in export</td><td>a PNG/SVG/print marker conveys polarity by color alone</td><td>forbidden; every export carries the icon + text; a grayscale-export test asserts cat vs hung</td></tr><tr><td>Diacritic clipped</td><td>a label clips a stacked mark at 100/200/400%</td><td>forbidden; the dau chong clip test fails on light+dark; fix line/height before ship</td></tr><tr><td>Unlabeled component</td><td>a palace/than/general has no screen-reader label</td><td>forbidden; every component has a label + role; axe + a label test assert it</td></tr><tr><td>Glass survives print</td><td>opt-in glass renders on the printed page</td><td>forbidden; print collapses glass to a solid surface (TASK-WEB-001)</td></tr><tr><td>Export re-casts</td><td>the exporter re-reads / recomputes the chart</td><td>forbidden; it serializes the rendered chart only; no envelope re-read</td></tr><tr><td>Per-type export drift</td><td>one chart type exports differently</td><td>forbidden; all types export through the common seam; the cross-type tests assert parity</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>TASK-CHART-004 completes the export seam TASK-CHART-001 opened and delivers the accessibility pass for all three chart types at once, because export and accessibility are one requirement seen twice: a chart whose every component is labeled and whose polarity is icon-plus-text is also a chart that exports faithfully to PNG, SVG, and print, grayscale and screen-reader included. The dau chong clip test at 100/200/400% on light and dark is a gate, not a nicety (Claude-07 s5.1), and the never-color-alone rule holds into every export (strategy 4.4). It re-reads no envelope and re-casts nothing - it serializes the rendered chart - and the export it finishes is what TASK-WEB-005, TASK-REPORT-002, and TASK-WEB-007 build on for chart images.</p>";
