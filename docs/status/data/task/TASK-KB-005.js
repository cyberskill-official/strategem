window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-KB-005"]="<h2>§1 - Description (BCP-14 normative)</h2>\n<p>This task exposes the knowledge graph (TASK-KB-001) as a hybrid-retrieval query API: given a la so's components, it traverses the graph and returns the related entities, the relations that connect them, and the citations attached along the way. It is the graph arm that TASK-RAG-002 was built to accept - the second retrieval source that TASK-RAG-002 fuses with its vector arm (Claude-06 s3.3). It does NOT own the graph shape or store (TASK-KB-001) nor the classical text (TASK-KB-003); it is a read-only query layer over both.</p>\n<p>The API SHALL accept a <code>GraphQuery</code> carrying the <code>system</code>, a set of seed tokens (the salient chart entities: can, chi, cung, sao, mon, than, and the detected cách cục ids), and traversal bounds. It SHALL resolve each seed token to a graph node id, traverse the graph over the Tam Thuc relations - ngũ hành sinh/khắc (生/剋); địa chi hình/xung/phá/hại/hợp (刑/沖/破/害/合); vị trí ký cung/lạc cung/lâm (寄宮/落宮/臨); trạng thái thừa and vượng-tướng-hưu-tù-tử (乘/旺-相-休-囚-死) (Claude-06 s3.2) - to a bounded hop depth, and SHALL return ranked <code>GraphHit</code> records. Each hit SHALL carry the related node, the relation path that reached it from a seed, and the citation ids collected along that path, filtered to <code>system in {system, all}</code>.</p>\n<p>A seed token that resolves to no node SHALL be surfaced in <code>seeds_unresolved</code>, never silently dropped. Citations SHALL be collected from the cách cục / thần sát nodes on the path (which carry the TASK-KB-002 pattern citations) and from any citation on a traversed edge, de-duplicated, and each SHALL resolve into the TASK-KB-003 corpus. A traversed relation that carries no citation SHALL still be returned as structural context, but with an empty <code>citations</code> list, so the consumer (TASK-RAG-003, via TASK-RAG-002) can only turn a graph result into a cited claim when a citation actually exists. The query SHALL be deterministic given the graph state and the query, and SHALL be read-only over the graph and the corpus.</p>\n<h2>§2 - Why this design (rationale for humans)</h2>\n<p>Vector retrieval finds passages semantically near a query, but Tam Thuc interpretation runs on relational structure the vectors do not see: this door on this palace is generated by that stem, which clashes with that branch, and the star sitting here is vượng in this season (Claude-06 s3.3). Those facts live in the graph as edges, and the only way to pull the exact rules that bear on this particular chart is to start from the chart's own entities and traverse. That is why TASK-RAG-002 has two arms and why the graph arm is not optional to the design even though it is optional to the P0 runtime: without it, an interpretation is grounded in what the classics say near the words of the question, but not in which relations connect the parts of this chart. This task is the traversal that supplies the second kind of grounding.</p>\n<p>Returning the citations along the path, not just the related nodes, is what keeps the graph arm inside the anti-hallucination contract. A relation on its own (\"Mộc sinh Hỏa\") is structural context; it becomes a citable claim only when a cách cục node on the path carries a TASK-KB-002 pattern citation that resolves into the TASK-KB-003 corpus. By collecting exactly those citations and returning an empty list where none exist, this task hands TASK-RAG-002 a result that TASK-RAG-003's guard can treat correctly: relations with citations can ground a claim, relations without them are context that cannot. Surfacing unresolved seeds rather than dropping them matters for the same honesty reason - a chart entity the graph does not know about is a coverage gap to see, not a silent omission that makes the graph arm look more complete than it is.</p>\n<h2>§3 - Contract (query, traversal, citations)</h2>\n<h3>Query and result (<code>tamthuc_kb/graph/query.py</code>)</h3>\n<pre><code class=\"language-python\">class GraphQuery(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    system: System\n    seeds: list[str]                 # entity tokens or node ids: can/chi/cung/sao/mon/than + cach_cuc ids\n    max_hops: int = 2                # bounded traversal depth\n    rels: list[EdgeRel] | None = None    # restrict to certain relations; None = all Tam Thuc relations\n    k: int = 12\n\nclass GraphRelation(BaseModel):\n    src: str; rel: EdgeRel; dst: str\n    src_label: str; dst_label: str\n    attrs: dict = {}                 # e.g. {\"state\": \"vuong\"} for a trang_thai edge\n    citations: list[str] = []        # citation_ids on this edge, if any\n\nclass GraphHit(BaseModel):\n    node_id: str                     # the related entity reached\n    label: str\n    label_han: str | None\n    kind: NodeKind\n    system: System\n    path: list[GraphRelation]        # the relations traversed from a seed to this node\n    citations: list[str]             # de-duped union of citations along the path + on the node\n    score: float                     # closeness rank (fewer hops + more seed support -&gt; higher)\n\nclass GraphQueryResult(BaseModel):\n    hits: list[GraphHit]             # length &lt;= k, ranked\n    seeds_resolved: list[str]        # seed tokens that resolved to node ids\n    seeds_unresolved: list[str]      # tokens with no node (surfaced, not dropped)\n    filter: dict                     # the applied system filter, for audit</code></pre>\n<h3>Seed resolution (<code>tamthuc_kb/graph/resolve.py</code>)</h3>\n<pre><code class=\"language-python\">def resolve_seeds(tokens: list[str], system: System, store: GraphStore) -&gt; tuple[list[str], list[str]]:\n    # map a token to a node id, accepting Han (丙), romanized (Binh), and slug (thien_can_binh) forms,\n    # and cach_cuc ids directly; filter candidates by system in {system, all};\n    # return (resolved_node_ids, unresolved_tokens). Never guess a node for an unknown token.</code></pre>\n<h3>Traversal and citation collection (<code>tamthuc_kb/graph/query.py</code>, <code>citations.py</code>)</h3>\n<pre><code class=\"language-python\">class GraphQueryService:\n    def __init__(self, graph: GraphStore, corpus: ClassicalStore): ...   # TASK-KB-001 + TASK-KB-003\n    def query(self, q: GraphQuery) -&gt; GraphQueryResult:\n        # 1. resolve seeds -&gt; node ids (surface unresolved)\n        # 2. BFS to max_hops over q.rels (default all), via GraphStore.neighbors, filtered system in {system, all}\n        # 3. for each reached node, build its GraphRelation path and collect citations (citations.py):\n        #      - citations on cach_cuc / than_sat nodes on the path (the KB-002 pattern citations)\n        #      - citations on traversed edges (edge.attrs), if any\n        #    de-dup; keep an empty list where none exist (structural-only context)\n        # 4. score by (hop distance asc, seed-support count desc), rank, cut to k, deterministic tie-break</code></pre>\n<p><code>citations.py</code> also offers <code>resolvable(citation_ids)</code> which checks each collected id against <code>ClassicalStore.resolve_citation</code>, so a citation the graph carries but the corpus cannot resolve is caught (a dangling citation shared with the TASK-KB-003 check).</p>\n<h3>The TASK-RAG-002 seam</h3>\n<p>TASK-RAG-002 defines a <code>GraphRetriever</code> protocol (<code>available()</code>, <code>traverse(ctx, k)</code>) with a <code>NullGraphRetriever</code> default until this task lands. This task is the server this protocol's real client calls: <code>traverse</code> maps TASK-RAG-002's <code>ChartContext</code> (<code>system</code>, <code>entities</code>, <code>cach_cuc_ids</code>) onto a <code>GraphQuery</code> and maps <code>GraphHit</code>s back to TASK-RAG-002's <code>GraphHit</code>s. When this task is present, <code>available()</code> returns true and TASK-RAG-002's graph arm activates with no change above it.</p>\n<h2>§4 - Acceptance criteria</h2>\n<ol><li>A <code>GraphQuery</code> with seeds <code>[\"丙\", \"cung_1\", \"Sinh_mon\"]</code> and <code>system=qimen</code> resolves the known tokens to node ids and returns ranked <code>GraphHit</code>s; an unknown token appears in <code>seeds_unresolved</code>, not silently dropped.</li><li>Traversal honors <code>max_hops</code> and <code>rels</code>: with <code>rels=[sinh]</code> only sinh edges are followed; a two-hop query reaches a node two relations away and records the full <code>path</code>.</li><li>The per-system filter is enforced: a <code>qimen</code> query never returns a <code>liuren</code>/<code>taiyi</code>-only node; <code>system=all</code> primitives (can, chi, ngũ hành) are included.</li><li>Citations are collected along the path and de-duped; a hit through a cách cục node carries that node's TASK-KB-002 citations, and every returned <code>citation_id</code> resolves via TASK-KB-003 (<code>resolve_citation</code>), with dangling ids reported.</li><li>A traversed relation with no citation returns as a <code>GraphHit</code>/<code>GraphRelation</code> with an empty <code>citations</code> list (structural context), never fabricated citations.</li><li>The query is deterministic: identical graph state and query yield an identical ranked result across runs (stable tie-break on score, hop distance, node id); the TASK-RAG-002 <code>GraphRetriever</code> client maps context and hits without loss on the fixture.</li></ol>\n<h2>§5 - Verification</h2>\n<ul><li><code>tests/test_graph_query.py</code>: seed resolution across Han/romanized/slug forms with unresolved surfacing; bounded traversal with <code>rels</code> and <code>max_hops</code>; the per-system filter (isolation) case; citation collection and de-dup through a cách cục node; the structural-only (empty citations) case; the dangling-citation report against a corpus fixture missing an id; determinism (repeat runs equal).</li><li>Seam test: a stub of TASK-RAG-002's <code>GraphRetriever</code> client maps a <code>ChartContext</code> to a <code>GraphQuery</code> and back, asserting the arm produces TASK-RAG-002-shaped <code>GraphHit</code>s.</li><li>Backend parity: run the query over the TASK-KB-001 <code>RelationalGraphStore</code> (test Postgres or fake) and the <code>InMemoryGraphStore</code>; assert identical hits on the fixture.</li><li>Schema: <code>docs/contracts/graph-query.schema.json</code> validates the request/result; Pydantic/JSON-Schema parity checked in CI.</li><li>Gates: <code>ruff check</code>, <code>ruff format --check</code>, <code>mypy tamthuc_kb</code>, <code>pytest packages/tamthuc_kb</code> (default in-memory; Postgres path behind a marker).</li></ul>\n<h2>§6 - Implementation skeleton</h2>\n<ol><li><code>graph/resolve.py</code>: <code>resolve_seeds</code> accepting Han/romanized/slug + cách cục ids, system-filtered, unresolved surfaced.</li><li><code>graph/query.py</code>: <code>GraphQuery</code>, <code>GraphRelation</code>, <code>GraphHit</code>, <code>GraphQueryResult</code>, and <code>GraphQueryService.query</code> (BFS to <code>max_hops</code> over <code>GraphStore.neighbors</code>, system filter, scoring, deterministic rank).</li><li><code>graph/citations.py</code>: collect + de-dup citations along a path (cách cục / thần sát nodes and edge attrs), <code>resolvable()</code> check against <code>ClassicalStore</code>.</li><li>Author <code>docs/contracts/graph-query.schema.json</code> (request/result source of truth).</li><li>Wire the TASK-RAG-002 <code>GraphRetriever</code> mapping (<code>ChartContext</code> &lt;-&gt; <code>GraphQuery</code>, <code>GraphHit</code> &lt;-&gt; <code>GraphHit</code>) and the <code>available()</code> capability check.</li><li>Commit <code>fixtures/graph_query_cases.json</code> (a QiMen seed set over the TASK-KB-001 seeded taxonomy) as the test exemplar.</li></ol>\n<h2>§7 - Dependencies</h2>\n<p>Depends on TASK-KB-001 (the node/edge graph and the <code>GraphStore</code> protocol this task traverses; the relations ngũ hành sinh/khắc, chi hình/xung/phá/hại/hợp, ký cung/lạc cung/lâm, thừa/trạng thái are its <code>EdgeRel</code> set) and TASK-KB-003 (the corpus every collected citation resolves into). Read at runtime by TASK-RAG-002 as the graph arm of hybrid retrieval; consumes cách cục citations authored in TASK-KB-002. Does not hard-block TASK-RAG-002: TASK-RAG-002's <code>depends_on</code> is TASK-RAG-001 only, and its graph arm degrades to vector-only via <code>NullGraphRetriever</code> until this task ships, at which point <code>available()</code> returns true and the arm activates with no change above it (the phase-inversion TASK-RAG-002 documents). <code>blocks: []</code> accordingly - this task activates rather than blocks.</p>\n<h2>§8 - Example payloads</h2>\n<pre><code class=\"language-json\">// GraphQuery from a QiMen la so's components\n{ \"system\": \"qimen\", \"seeds\": [\"丙\", \"值符\", \"cung_1\", \"Sinh_mon\", \"qimen_thanh_long_hoi_dau\"],\n  \"max_hops\": 2, \"rels\": null, \"k\": 8 }</code></pre>\n<pre><code class=\"language-json\">// GraphQueryResult (abbreviated) - a related entity with its relation path and cited grounding\n{ \"hits\": [\n    { \"node_id\": \"ngu_hanh_hoa\", \"label\": \"Hỏa\", \"label_han\": \"火\", \"kind\": \"ngu_hanh\",\n      \"system\": \"all\", \"score\": 0.82,\n      \"path\": [ { \"src\": \"thien_can_binh\", \"rel\": \"thua\", \"dst\": \"ngu_hanh_hoa\",\n                  \"src_label\": \"Bính\", \"dst_label\": \"Hỏa\", \"attrs\": {}, \"citations\": [] } ],\n      \"citations\": [] },\n    { \"node_id\": \"qimen_thanh_long_hoi_dau\", \"label\": \"Thanh Long Hồi Đầu\", \"label_han\": \"青龍返首\",\n      \"kind\": \"cach_cuc\", \"system\": \"qimen\", \"score\": 0.95, \"path\": [],\n      \"citations\": [\"yba_dieu_012\"] } ],\n  \"seeds_resolved\": [\"thien_can_binh\", \"cuu_cung_1\", \"bat_mon_sinh\", \"qimen_thanh_long_hoi_dau\"],\n  \"seeds_unresolved\": [\"值符\"], \"filter\": { \"system\": [\"qimen\", \"all\"] } }</code></pre>\n<h2>§9 - Open questions</h2>\n<ul><li>Whether primitive relations (Mộc sinh Hỏa) should carry their own citations or stay structural-only. Default: structural-only at MVP; the cited grounding rides on the cách cục / thần sát nodes (TASK-KB-002); add edge citations only where a specific classical line documents a specific relation. Revisit from TASK-RAG-006 retrieval quality.</li><li>Hop bound. Default: <code>max_hops=2</code> (one- and two-hop neighborhoods, the shapes TASK-KB-001 kept relational and cheap in SQL); deeper traversal is a reason to reconsider a graph DB, not to raise the default blindly.</li><li>Scoring of graph hits before fusion. Default: hop distance ascending plus seed-support count, deterministic tie-break; TASK-RAG-002 applies reciprocal-rank fusion across arms, so this task only needs a stable within-arm rank. Tune with TASK-RAG-006 evidence.</li></ul>\n<h2>§10 - Failure modes inventory</h2>\n<table><thead><tr><th>Mode</th><th>Trigger</th><th>Required behavior</th></tr></thead><tbody><tr><td>Silent seed drop</td><td>an unknown chart token is ignored</td><td>surfaced in <code>seeds_unresolved</code>; never guessed into a node</td></tr><tr><td>Fabricated citation</td><td>a structural relation reported as cited</td><td>empty <code>citations</code> where none exist; only real, resolvable ids are attached</td></tr><tr><td>Dangling citation</td><td>a graph citation the corpus cannot resolve</td><td><code>resolvable()</code> reports it (shared with the TASK-KB-003 check); not returned as valid grounding</td></tr><tr><td>Cross-system leak</td><td>traversal returns foreign-system nodes</td><td><code>system in {system, all}</code> filter on every hop; isolation test</td></tr><tr><td>Unbounded traversal</td><td>traversal without a hop cap</td><td><code>max_hops</code> enforced; the query is bounded and cheap</td></tr><tr><td>Non-deterministic rank</td><td>unstable sort of hits</td><td>stable tie-break on (score desc, hops asc, node_id); determinism test</td></tr><tr><td>Hard-required by RAG-002 in P0</td><td>code assumes this task exists early</td><td><code>GraphRetriever.available()</code> gate; TASK-RAG-002 degrades to vector-only until this lands</td></tr></tbody></table>\n<h2>§11 - Notes</h2>\n<p>This task is the second half of the hybrid retrieval the architecture calls for, and its whole value is relational grounding that vector search cannot give. Two things keep it honest: it returns citations only where they truly exist (empty lists for structural-only relations, so TASK-RAG-003's guard never sees a fabricated ground), and it surfaces unresolved seeds so the graph arm's coverage is visible. Keep it read-only over both the graph and the corpus - it queries TASK-KB-001 and TASK-KB-003, it never writes them. Because TASK-RAG-002 already ships the <code>GraphRetriever</code> protocol and the degraded path, landing this task is a clean activation, not a rewire above it. The package <code>tamthuc_kb</code> is shared with TASK-KB-001/002/003/004; this task adds <code>graph/query.py</code>, <code>resolve.py</code>, and <code>citations.py</code> to the existing <code>graph/</code> module, so the knowledge base stays one installable, mypy-clean unit.</p>";
