# COV-017 implementation notes

## Landed

- `apps/web/src/components/chart/palace-detail-sidebar.tsx`
- Wired into `results-panel.tsx` beside KM board (LN/TA show seat index panel)
- Keyboard: palace buttons already `role=gridcell` + sidebar `aria-label`

## Status

`ready_to_review` — HITL required. Agent will not set `done`.
