# COV-012 implementation notes

## Landed

- API: `POST /api/v1/cross-system/validate` using STRAT-004 `validate`
- Web: `apps/web/app/cross-system/page.tsx` three columns + soft consensus
- No invented consensus numbers — engine stances only

## Status

`ready_to_review` — **HITL required**. Agent will not set `done`.
