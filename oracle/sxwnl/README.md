# sxwnl / external calendar dumps

- `sample/tietkhi.csv` — published equinox/solstice UTC (USNO/almanac class),
  2018–2022 × 4 cardinal terms (20 rows) — the same published set as the W3
  `tietkhi_audit_ac.csv` AC audit under lichphap fixtures. Harness proof
  against independent published times (not engine self-fixtures).
- `full/tietkhi.csv` — drop a real **sxwnl** (or tyme4py) multi-decade 24×N dump
  for TASK-CORE-006. Absent → full sxwnl certification SKIP.

The W3 `tietkhi_audit_ac.csv` under lichphap fixtures is the same *class* of
published almanac samples; the `full/` dump here must be a true multi-decade
24×N sxwnl export, not a copy of the four-cardinal-term AC set.
