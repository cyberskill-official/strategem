[project]
name = "tamthuc-kb"
version = "0.1.0"
description = "FR-KB-003 classical-text three-layer store"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tamthuc_kb"]
