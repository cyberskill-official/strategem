[project]
name = "tamthuc-api"
version = "0.1.0"
description = "API surface + observability (FR-PLAT-005 bootstrap)"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tamthuc_api"]
