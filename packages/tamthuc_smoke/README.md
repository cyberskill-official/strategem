# tamthuc-smoke

Placeholder smoke package for PLAT-001 to exercise the Python CI lane (ruff / mypy / pytest).

Replaced by real packages owned by their FRs (tamthuc_api, tamthuc_rag, etc.).

See root pyproject.toml for workspace config.