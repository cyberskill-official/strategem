[project]
name = "tamthuc-strat"
version = "0.1.0"
description = "Strategic tools — Timing Optimizer (FR-STRAT-001)"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tamthuc_strat"]
