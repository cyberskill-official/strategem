[project]
name = "tamthuc-compliance"
version = "0.1.0"
description = "FR-LEGAL-002 PDPD/GDPR DSAR + retention contracts"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tamthuc_compliance"]
