[project]
name = "tamthuc-edu"
version = "0.1.0"
description = "Education curriculum + practice (TASK-EDU-001..004)"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tamthuc_edu"]
