# Supabase (optional local)

Production DB is **hosted Supabase**. Schema is applied from `db/migrations/` via `deploy/vps/migrate.sh`.

```bash
# Local CLI (optional)
# supabase start
# Then point DATABASE_URL at local and run migrate.sh
```
