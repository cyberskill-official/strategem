# Rust
target/
**/*.rs.bk
# Cargo.lock is committed for reproducible engine Docker builds

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Python packaging `lib/` only at repo root — NOT apps/web/src/lib (app source)
/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.venv/
# uv.lock is committed for reproducible api Docker builds

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/
*.log

# Secrets / env (never commit real ones)
.env
.env.local
.env.*.local
*.pem
secrets/

# Test / coverage
coverage/
*.lcov
.nyc_output/

tmp/

# External oracle full dumps (W4) — samples + READMEs stay committed
oracle/*/full/*.csv
oracle/*/full/*.json
!oracle/*/full/README.md

# pytest coverage data
.coverage
.coverage.*

# >>> cyberos (managed block - regenerated by cyberos install; edit outside the markers) >>>
# vendored machine + local BRAIN store + render intermediates (regenerable via install; tenant data)
.cyberos/
# skill symlinks -> .cyberos/plugin/skills (regenerable via install)
.claude/skills/ship-tasks
.claude/skills/task-author
.claude/skills/task-audit
.grok/skills/ship-tasks
.commandcode/skills/ship-tasks
.codex/skills/ship-tasks
.opencode/skill/ship-tasks
# shared .agents/skills entries (Agent Skills open standard) chain via .claude/skills (TASK-IMP-094)
.agents/skills/ship-tasks
.agents/skills/task-author
.agents/skills/task-audit
# <<< cyberos <<<
