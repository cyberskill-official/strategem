# Rust
target/
**/*.rs.bk
# Cargo.lock is committed for reproducible engine Docker builds

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.venv/
# uv.lock is committed for reproducible api Docker builds

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/
*.log

# Secrets / env (never commit real ones)
.env
.env.local
.env.*.local
*.pem
secrets/

# Test / coverage
coverage/
*.lcov
.nyc_output/

# >>> cyberos (managed block - regenerated by cyberos init; edit outside the markers) >>>
# vendored machine + local BRAIN store + render intermediates (regenerable via init; tenant data)
.cyberos/
# skill symlinks -> .cyberos/plugin/skills (regenerable via init)
.claude/skills/ship-feature-requests
.grok/skills/ship-feature-requests
.commandcode/skills/ship-feature-requests
.codex/skills/ship-feature-requests
.opencode/skill/ship-feature-requests
# <<< cyberos <<<
tmp/
