# Production (Vercel): set public API origin (VPS)
NEXT_PUBLIC_API_BASE=https://api.example.com

# Optional server-side rewrite target (same origin as above)
# API_URL=https://api.example.com

# Local: leave NEXT_PUBLIC_API_BASE empty so next.config rewrites /api → 127.0.0.1:8000
